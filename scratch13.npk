func:main = int32() {
    int64:x = 0i64;
    ptr uint8:p = @cast_unchecked<ptr uint8>(x);
    uint8:b1 = p[0];
    exit 0i32;
};
func:failsafe = int32(tbb32:err) { exit @cast_unchecked<int32>(err); };
