pub func:main = int32() {
    byte[1]:one;
    *byte[]:p = @cast_unchecked<*byte[]>(one);
    int64:p2 = @cast_unchecked<int64>(p);
    exit 0i32;
};

pub func:failsafe = int32(tbb32:err) {
    exit @cast_unchecked<int32>(err);
};
