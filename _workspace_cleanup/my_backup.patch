diff --git a/build.abc b/build.abc
index 62f592a..4989edd 100644
--- a/build.abc
+++ b/build.abc
@@ -88,18 +88,18 @@ project:libn = {
     test:unit = {
         sources: [
             // v0.1.x tests (TODO: write these)
-            // "tests/unit/test_syscall.npk",
-            // "tests/unit/test_errno.npk",
+            "tests/unit/test_syscall.npk",
+            "tests/unit/test_errno.npk",
 
             // v0.2.x tests (TODO: write these)
-            // "tests/unit/test_mem_mmap.npk",
+            "tests/unit/test_mem_mmap.npk",
             // "tests/unit/test_mem_memset.npk",
             // "tests/unit/test_mem_memcpy.npk",
             // "tests/unit/test_mem_memutil.npk",
             // "tests/unit/test_mem_slab.npk",
 
             // v0.3.x tests (TODO: write these)
-            // "tests/unit/test_str_strlen.npk",
+            "tests/unit/test_str_strlen.npk",
             // "tests/unit/test_str_strcpy.npk",
             // "tests/unit/test_str_strcmp.npk",
             // "tests/unit/test_str_strlcpy.npk",
@@ -111,7 +111,8 @@ project:libn = {
             // v0.4.x tests (TODO: write these)
             // "tests/unit/test_io_write.npk",
             // "tests/unit/test_io_read.npk",
-            // "tests/unit/test_io_printf.npk",
+            "tests/unit/test_io_printf.npk",
+            "tests/unit/test_time_clock.npk",
             // "tests/unit/test_io_open.npk",
             // "tests/unit/test_io_seek.npk",
             // "tests/unit/test_io_dup.npk",
diff --git a/src/fs/path.npk b/src/fs/path.npk
index 476008a..cefc9a5 100644
--- a/src/fs/path.npk
+++ b/src/fs/path.npk
@@ -1,17 +1,16 @@
 // path.npk — Path string manipulation utilities (v0.5.1)
 //
 // Pure string-level path operations. No syscalls, no fd access. All functions
-// work on NUL-terminated byte strings passed as int64 pointers, matching the
+// work on NUL-terminated byte strings @cast_unchecked<int64>(passed) pointers, matching the
 // rest of libn's ABI convention. All outputs are written into caller-supplied
 // buffers to avoid allocation.
 //
 // Path semantics:
-//   - Paths are POSIX byte strings. "/" is the root. "." is the cwd.
+//   - Paths are POSIX byte strings. "/" (the root. "." is the cwd.
 //   - Trailing slashes are normalized away (except for the root "/").
 //   - Empty string and NULL pointer both return an empty-path error (ERR_BADARG).
 //
-// Functions:
-//   path_is_abs(path)              → 1 if absolute (starts with '/'), else 0
+// Functions) ? //   path_is_abs(path)              → 1 if absolute (starts with '/'), else 0
 //   path_join(a, b, out, out_sz)   → join two path segments with '/'
 //   path_dirname(path, out, out_sz)→ parent directory component
 //   path_basename(path, out, out_sz)→ final component (filename)
@@ -28,68 +27,66 @@ use "src/syscall/errno.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 
 // path_strlen — Inline strlen for path strings.
-func:path_strlen = int64(int64:s) {
-    if s == 0i64 {
-        pass 0i64;
+func : path_strlen = int64(int64:s) {
+    if (s == 0i64) {
+        pass(0i64);
     }
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:n = 0i64;
-    while p[n] != 0u8 {
+    while (p[n] != 0u8) {
         n = n + 1i64;
     }
-    pass n;
-}
+    pass(n);
+};
 
 // path_copy_n — Copy at most n bytes from src to dst, always NUL-terminates.
 // Returns number of bytes copied (not counting NUL).
 func:path_copy_n = int64(int64:dst, int64:src, int64:n) {
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
     int64:i = 0i64;
-    while i < n && s[i] != 0u8 {
+    while (i < n && s[i] != 0u8) {
         d[i] = s[i];
         i = i + 1i64;
     }
     d[i] = 0u8;
-    pass i;
-}
+    pass(i);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// path_is_abs — Test whether a path is absolute (starts with '/').
+// path_is_abs — Test whether a path (absolute (starts with '/').
 //
-// Returns:
-//   Ok(1) — path is absolute
+// Returns) ? //   Ok(1) — path is absolute
 //   Ok(0) — path is relative
 //   Err(ERR_BADARG) — path is NULL or empty
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_is_abs = Result<int64>(int64:path) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func : path_is_abs = int64(int64:path) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    *byte:p = path as *byte;
-    if p[0] == 0u8 {
-        pass 0i64;   // empty string is relative
+    uint8->:p = @cast_unchecked<uint8->>(path);
+    if (p[0] == 0u8) {
+        pass(0i64);   // empty string (relative
     }
-    if p[0] == 47u8 {  // '/'
-        pass 1i64;
+    if (p[0] == 47u8) {  // '/'
+        pass(1i64);
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_join — Join two path segments, inserting exactly one '/' between them.
 //
-// Rules:
-//   - If b is absolute (starts with '/'), b is returned as-is (mirrors Python
-//     os.path.join semantics: absolute b discards a).
+// Rules) ? //   - If b is absolute (starts with '/'), b is returned as-is (mirrors Python
+//     os.path.join semantics : absolute b discards a).
 //   - If a is empty, b is returned as-is.
-//   - Otherwise: strip trailing '/' from a, prepend '/' if a doesn't end in
+//   - Otherwise: strip trailing '/' from a, prepend '/' if a doesn't end_pos in
 //     one, then append b.
 //
 // Parameters:
-//   a:      first path segment (NUL-terminated, pointer as int64)
-//   b:      second path segment (NUL-terminated, pointer as int64)
+//   a:      first path segment (NUL-terminated, @cast_unchecked<int64>(pointer))
+//   b:      second path segment (NUL-terminated, @cast_unchecked<int64>(pointer))
 //   out:    destination buffer
 //   out_sz: size of out (including NUL slot)
 //
@@ -99,77 +96,81 @@ pub func:path_is_abs = Result<int64>(int64:path) {
 //   Err(ENAMETOOLONG) — result would exceed out_sz-1 bytes
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_join = Result<int64>(int64:a, int64:b, int64:out, int64:out_sz) {
-    if a == 0i64 || b == 0i64 || out == 0i64 || out_sz < 1i64 {
-        fail ERR_BADARG as tbb8;
+pub func:path_join = int64(int64:a, int64:b, int64:out, int64:out_sz) {
+    if (a == 0i64 || b == 0i64 || out == 0i64 || out_sz < 1i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:ob = out as *byte;
-    *byte:ab = a as *byte;
-    *byte:bb = b as *byte;
+    uint8->:ob = @cast_unchecked<uint8->>(out);
+    uint8->:ab = @cast_unchecked<uint8->>(a);
+    uint8->:bb = @cast_unchecked<uint8->>(b);
 
-    // If b is absolute, return b directly.
-    if bb[0] == 47u8 {  // '/'
-        int64:blen = path_strlen(b);
-        if blen >= out_sz {
-            fail ENAMETOOLONG as tbb8;
+    // If b (absolute, return b directly.
+    if (bb[0] == 47u8) {  // '/'
+        int64) ? blen = _!path_strlen(b);
+        if (blen >= out_sz) {
+            fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
         }
-        drop path_copy_n(out, b, blen);
-        pass blen;
+        _?path_copy_n(out, b, blen);
+        pass(blen);
     }
 
-    int64:alen = path_strlen(a);
-    int64:blen = path_strlen(b);
+    int64 : alen = _!path_strlen(a);
+    int64:blen = _!path_strlen(b);
 
-    // If a is empty, return b as-is.
-    if alen == 0i64 {
-        if blen >= out_sz {
-            fail ENAMETOOLONG as tbb8;
+    // If a (empty, return b as-is.
+    if (alen == 0i64) {
+        if (blen >= out_sz) {
+            fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
         }
-        drop path_copy_n(out, b, blen);
-        pass blen;
+        _?path_copy_n(out, b, blen);
+        pass(blen);
     }
 
     // Strip trailing slashes from a.
-    int64:a_end = alen;
-    while a_end > 1i64 && ab[a_end - 1i64] == 47u8 {  // not root
+    int64) ? a_end = alen;
+    while (a_end > 1i64 && ab[a_end - 1i64] == 47u8) {  // not root
         a_end = a_end - 1i64;
     }
 
     // If b is empty, just copy a (stripped).
-    if blen == 0i64 {
-        if a_end >= out_sz {
-            fail ENAMETOOLONG as tbb8;
+    if (blen == 0i64) {
+        if (a_end >= out_sz) {
+            fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
         }
-        drop path_copy_n(out, a, a_end);
+        _?path_copy_n(out, a, a_end);
         ob[a_end] = 0u8;
-        pass a_end;
+        pass(a_end);
     }
 
-    // Total: a_end + 1 (slash) + blen
-    int64:total = a_end + 1i64 + blen;
-    if total >= out_sz {
-        fail ENAMETOOLONG as tbb8;
+    // [FIX A18-5.3] Don't add a separator slash when a is "/" (root) to avoid "//b".
+    bool : add_slash = !(a_end == 1i64 && ab[0] == 47u8);  // don't add slash if a ("/"
+
+    // Total) ? a_end + 0or1 (slash) + blen
+    int64 : total = a_end + (((add_slash)) ? 1i64 : 0i64) + blen;
+    if (total >= out_sz) {
+        fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
     }
 
-    // Write a (stripped), then '/', then b.
+    // Write a (stripped), then optional '/', then b.
     int64:i = 0i64;
-    while i < a_end {
+    while (i < a_end) {
         ob[i] = ab[i];
         i = i + 1i64;
     }
-    ob[i] = 47u8;  // '/'
-    i = i + 1i64;
+    if (add_slash) {
+        ob[i] = 47u8;  // '/'
+        i = i + 1i64;
+    }
     int64:j = 0i64;
-    while j < blen {
+    while (j < blen) {
         ob[i] = bb[j];
         i = i + 1i64;
         j = j + 1i64;
     }
     ob[i] = 0u8;
-    pass total;
-}
-
+    pass(total);
+};
 // ════════════════════════════════════════════════════════════════════════════
 // path_dirname — Return the directory component of path (like POSIX dirname).
 //
@@ -189,75 +190,74 @@ pub func:path_join = Result<int64>(int64:a, int64:b, int64:out, int64:out_sz) {
 // Returns Ok(n) bytes written (not counting NUL), or Err.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_dirname = Result<int64>(int64:path, int64:out, int64:out_sz) {
-    if path == 0i64 || out == 0i64 || out_sz < 2i64 {
-        fail ERR_BADARG as tbb8;
+pub func:path_dirname = int64(int64:path, int64:out, int64:out_sz) {
+    if (path == 0i64 || out == 0i64 || out_sz < 2i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p  = path as *byte;
-    *byte:ob = out  as *byte;
+    uint8->:p  = @cast_unchecked<uint8->>(path);
+    uint8->:ob = @cast_unchecked<uint8->>(out);
 
-    int64:len = path_strlen(path);
+    int64:len = _!path_strlen(path);
 
     // Empty path → "."
-    if len == 0i64 {
+    if (len == 0i64) {
         ob[0] = 46u8;  // '.'
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     // Find the rightmost non-trailing slash.
-    // Step 1: strip trailing slashes (POSIX says: treat as if not there)
-    int64:end = len;
-    while end > 1i64 && p[end - 1i64] == 47u8 {
-        end = end - 1i64;
+    // Step 1: strip trailing slashes (POSIX says: @cast_unchecked<if>(treat) not there)
+    int64:end_pos = len;
+    while (end_pos > 1i64 && p[end_pos - 1i64] == 47u8) {
+        end_pos = end_pos - 1i64;
     }
 
     // Step 2: scan backwards for the last slash
     int64:last_slash = -1i64;
-    int64:i = end - 1i64;
-    while i >= 0i64 {
-        if p[i] == 47u8 {
+    int64:i = end_pos - 1i64;
+    while (i >= 0i64) {
+        if (p[i] == 47u8) {
             last_slash = i;
             break;
         }
         i = i - 1i64;
     }
 
-    if last_slash < 0i64 {
-        // No slash found — dirname is "."
+    if (last_slash < 0i64) {
+        // No slash found — dirname ("."
         ob[0] = 46u8;  // '.'
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
-    if last_slash == 0i64 {
+    if (last_slash == 0i64) {
         // The only slash is the root
         ob[0] = 47u8;  // '/'
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     // dirname is path[0..last_slash], stripping any trailing slashes from that too
-    int64:dir_end = last_slash;
-    while dir_end > 1i64 && p[dir_end - 1i64] == 47u8 {
+    int64) ? dir_end = last_slash;
+    while (dir_end > 1i64 && p[dir_end - 1i64] == 47u8) {
         dir_end = dir_end - 1i64;
     }
 
-    if dir_end >= out_sz {
-        fail ENAMETOOLONG as tbb8;
+    if (dir_end >= out_sz) {
+        fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
     }
 
-    drop path_copy_n(out, path, dir_end);
+    _?path_copy_n(out, path, dir_end);
     ob[dir_end] = 0u8;
-    pass dir_end;
-}
+    pass(dir_end);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_basename — Return the final component of a path (like POSIX basename).
 //
-// Examples:
-//   "/usr/lib/libc.so" → "libc.so"
+// Examples : //   "/usr/lib/libc.so" → "libc.so"
 //   "/usr/lib/"        → "lib"
 //   "/usr"             → "usr"
 //   "/"                → "/"
@@ -266,41 +266,41 @@ pub func:path_dirname = Result<int64>(int64:path, int64:out, int64:out_sz) {
 // Returns Ok(n) bytes written (not counting NUL), or Err.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_basename = Result<int64>(int64:path, int64:out, int64:out_sz) {
-    if path == 0i64 || out == 0i64 || out_sz < 2i64 {
-        fail ERR_BADARG as tbb8;
+pub func:path_basename = int64(int64:path, int64:out, int64:out_sz) {
+    if (path == 0i64 || out == 0i64 || out_sz < 2i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p  = path as *byte;
-    *byte:ob = out  as *byte;
+    uint8->:p  = @cast_unchecked<uint8->>(path);
+    uint8->:ob = @cast_unchecked<uint8->>(out);
 
-    int64:len = path_strlen(path);
+    int64:len = _!path_strlen(path);
 
     // Empty → "."
-    if len == 0i64 {
+    if (len == 0i64) {
         ob[0] = 46u8;  // '.'
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     // "/" → "/"
-    if len == 1i64 && p[0] == 47u8 {
+    if (len == 1i64 && p[0] == 47u8) {
         ob[0] = 47u8;
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     // Strip trailing slashes
-    int64:end = len;
-    while end > 1i64 && p[end - 1i64] == 47u8 {
-        end = end - 1i64;
+    int64:end_pos = len;
+    while (end_pos > 1i64 && p[end_pos - 1i64] == 47u8) {
+        end_pos = end_pos - 1i64;
     }
 
-    // Find last slash before end
+    // Find last slash before end_pos
     int64:last_slash = -1i64;
-    int64:i = end - 1i64;
-    while i >= 0i64 {
-        if p[i] == 47u8 {
+    int64:i = end_pos - 1i64;
+    while (i >= 0i64) {
+        if (p[i] == 47u8) {
             last_slash = i;
             break;
         }
@@ -308,63 +308,63 @@ pub func:path_basename = Result<int64>(int64:path, int64:out, int64:out_sz) {
     }
 
     int64:start = 0i64;
-    if last_slash >= 0i64 {
+    if (last_slash >= 0i64) {
         start = last_slash + 1i64;
     }
 
-    int64:blen = end - start;
-    if blen <= 0i64 {
+    int64:blen = end_pos - start;
+    if (blen <= 0i64) {
         // Should not happen after the checks above, but be safe
         ob[0] = 47u8;
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
-    if blen >= out_sz {
-        fail ENAMETOOLONG as tbb8;
+    if (blen >= out_sz) {
+        fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
     }
 
     int64:j = 0i64;
-    while j < blen {
+    while (j < blen) {
         ob[j] = p[start + j];
         j = j + 1i64;
     }
     ob[j] = 0u8;
-    pass blen;
-}
+    pass(blen);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_ext — Return pointer to the start of the file extension (the dot).
 //
 // Returns the int64 address of the LAST '.' in the basename component,
-// or 0 (NULL) if there is no extension.
+// or 0 (NULL) if there (no extension.
 //
-// The caller can do: if ext != 0 { *byte:ep = ext as *byte; ... }
+// The caller can do) ? if (ext != 0) { uint8-> : ep = @cast_unchecked<uint8->>(ext); ... }
 //
 // Examples:
 //   "foo.tar.gz" → pointer to ".gz"
-//   ".hidden"    → 0 (leading dot is not an extension)
+//   ".hidden"    → 0 (leading dot (not an extension)
 //   "no_ext"     → 0
 //   "/path/to/a.b" → pointer to ".b"
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_ext = Result<int64>(int64:path) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? path_ext = int64(int64 : path) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p = path as *byte;
-    int64:len = path_strlen(path);
+    uint8->:p = @cast_unchecked<uint8->>(path);
+    int64:len = _!path_strlen(path);
 
-    if len == 0i64 {
-        pass 0i64;
+    if (len == 0i64) {
+        pass(0i64);
     }
 
     // Find start of basename (skip directory prefix)
     int64:base_start = 0i64;
     int64:i = len - 1i64;
-    while i >= 0i64 {
-        if p[i] == 47u8 {  // '/'
+    while (i >= 0i64) {
+        if (p[i] == 47u8) {  // '/'
             base_start = i + 1i64;
             break;
         }
@@ -372,22 +372,22 @@ pub func:path_ext = Result<int64>(int64:path) {
     }
 
     // Look for last '.' in the basename, excluding a leading '.'
-    // (i.e., ".hidden" is not considered to have an extension)
-    int64:last_dot = -1i64;
-    int64:j = base_start + 1i64;   // +1 to skip leading dot of hidden files
-    while j < len {
-        if p[j] == 46u8 {  // '.'
+    // (i.e., ".hidden" (not considered to have an extension)
+    int64) ? last_dot = -1i64;
+    int64 : j = base_start + 1i64;   // +1 to skip leading dot of hidden files
+    while (j < len) {
+        if (p[j] == 46u8) {  // '.'
             last_dot = j;
         }
         j = j + 1i64;
     }
 
-    if last_dot < 0i64 {
-        pass 0i64;   // no extension
+    if (last_dot < 0i64) {
+        pass(0i64);   // no extension
     }
 
-    pass path + last_dot;
-}
+    pass(path + last_dot);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_depth — Count the number of path components (separated by '/').
@@ -401,28 +401,28 @@ pub func:path_ext = Result<int64>(int64:path) {
 //   ""                          → 0
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_depth = Result<int64>(int64:path) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:path_depth = int64(int64:path) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p = path as *byte;
-    int64:len = path_strlen(path);
+    uint8->:p = @cast_unchecked<uint8->>(path);
+    int64:len = _!path_strlen(path);
 
-    if len == 0i64 {
-        pass 0i64;
+    if (len == 0i64) {
+        pass(0i64);
     }
 
     int64:depth = 0i64;
     int64:in_component = 0i64;
     int64:i = 0i64;
 
-    while i < len {
-        if p[i] == 47u8 {  // '/'
-            if in_component != 0i64 {
+    while (i < len) {
+        if (p[i] == 47u8) {  // '/'
+            if (in_component != 0i64) {
                 depth = depth + 1i64;
                 in_component = 0i64;
-            } else if i == 0i64 {
+            } else if (i == 0i64) {
                 // Leading slash = root component
                 depth = depth + 1i64;
             }
@@ -433,76 +433,76 @@ pub func:path_depth = Result<int64>(int64:path) {
     }
 
     // Flush last component
-    if in_component != 0i64 {
+    if (in_component != 0i64) {
         depth = depth + 1i64;
     }
 
-    pass depth;
-}
+    pass(depth);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_has_trailing_slash — Test whether path ends with '/' (and len > 1).
 //
 // Returns:
-//   Ok(1) — ends with '/' (and is not just "/")
-//   Ok(0) — does not end with '/', or is "/" or empty
+//   Ok(1) — ends with '/' (and (not just "/")
+//   Ok(0) — does not end_pos with '/', or is "/" or empty
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_has_trailing_slash = Result<int64>(int64:path) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? path_has_trailing_slash = int64(int64 : path) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p = path as *byte;
-    int64:len = path_strlen(path);
+    uint8->:p = @cast_unchecked<uint8->>(path);
+    int64:len = _!path_strlen(path);
 
-    if len <= 1i64 {
-        pass 0i64;
+    if (len <= 1i64) {
+        pass(0i64);
     }
 
-    if p[len - 1i64] == 47u8 {  // '/'
-        pass 1i64;
+    if (p[len - 1i64] == 47u8) {  // '/'
+        pass(1i64);
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_strip_trailing_slash — Copy path with trailing '/' removed.
 //
-// If path is "/" or has no trailing slash, copies unchanged.
+// If path ("/" or has no trailing slash, copies unchanged.
 // Always NUL-terminates out.
 //
 // Returns Ok(n) bytes written (not counting NUL), or Err.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_strip_trailing_slash = Result<int64>(int64:path, int64:out, int64:out_sz) {
-    if path == 0i64 || out == 0i64 || out_sz < 2i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? path_strip_trailing_slash = int64(int64 : path, int64:out, int64:out_sz) {
+    if (path == 0i64 || out == 0i64 || out_sz < 2i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p  = path as *byte;
-    *byte:ob = out  as *byte;
+    uint8->:p  = @cast_unchecked<uint8->>(path);
+    uint8->:ob = @cast_unchecked<uint8->>(out);
 
-    int64:len = path_strlen(path);
+    int64:len = _!path_strlen(path);
 
-    if len == 0i64 {
+    if (len == 0i64) {
         ob[0] = 0u8;
-        pass 0i64;
+        pass(0i64);
     }
 
-    int64:end = len;
-    while end > 1i64 && p[end - 1i64] == 47u8 {
-        end = end - 1i64;
+    int64:end_pos = len;
+    while (end_pos > 1i64 && p[end_pos - 1i64] == 47u8) {
+        end_pos = end_pos - 1i64;
     }
 
-    if end >= out_sz {
-        fail ENAMETOOLONG as tbb8;
+    if (end_pos >= out_sz) {
+        fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
     }
 
-    drop path_copy_n(out, path, end);
-    ob[end] = 0u8;
-    pass end;
-}
+    _?path_copy_n(out, path, end_pos);
+    ob[end_pos] = 0u8;
+    pass(end_pos);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // path_normalize — Collapse redundant slashes and "/./" sequences.
@@ -512,31 +512,30 @@ pub func:path_strip_trailing_slash = Result<int64>(int64:path, int64:out, int64:
 //   - "/./" → "/"  (current-directory segments are removed)
 //   - Leading '/' preserved for absolute paths
 //   - Trailing slash preserved only for root
-//   - Does NOT resolve ".." (that requires stat() and is done by path_realpath
+//   - Does NOT resolve ".." (that requires stat() and (done by path_realpath
 //     in a future module)
 //
-// Examples:
-//   "//usr//lib/"  → "/usr/lib"
+// Examples) ? //   "//usr//lib/"  → "/usr/lib"
 //   "./foo/./bar"  → "foo/bar"
 //   "/a/./b//c/"  → "/a/b/c"
 //
 // Returns Ok(n) bytes written (not counting NUL), or Err.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:path_normalize = Result<int64>(int64:path, int64:out, int64:out_sz) {
-    if path == 0i64 || out == 0i64 || out_sz < 2i64 {
-        fail ERR_BADARG as tbb8;
+pub func : path_normalize = int64(int64:path, int64:out, int64:out_sz) {
+    if (path == 0i64 || out == 0i64 || out_sz < 2i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:p  = path as *byte;
-    *byte:ob = out  as *byte;
+    uint8->:p  = @cast_unchecked<uint8->>(path);
+    uint8->:ob = @cast_unchecked<uint8->>(out);
 
-    int64:len = path_strlen(path);
+    int64:len = _!path_strlen(path);
 
-    if len == 0i64 {
+    if (len == 0i64) {
         ob[0] = 46u8;  // '.'
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     int64:out_pos  = 0i64;
@@ -544,49 +543,49 @@ pub func:path_normalize = Result<int64>(int64:path, int64:out, int64:out_sz) {
     int64:is_abs   = 0i64;
 
     // Handle leading '/'
-    if p[0] == 47u8 {
+    if (p[0] == 47u8) {
         is_abs = 1i64;
-        if out_pos >= out_sz - 1i64 {
-            fail ENAMETOOLONG as tbb8;
+        if (out_pos >= out_sz - 1i64) {
+            fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
         }
         ob[out_pos] = 47u8;
         out_pos = out_pos + 1i64;
         // Skip all leading slashes
-        while in_pos < len && p[in_pos] == 47u8 {
+        while (in_pos < len && p[in_pos] == 47u8) {
             in_pos = in_pos + 1i64;
         }
     }
 
     int64:component_start = in_pos;
 
-    while in_pos <= len {
-        byte:ch = 0u8;
-        if in_pos < len {
+    while (in_pos <= len) {
+        uint8:ch = 0u8;
+        if (in_pos < len) {
             ch = p[in_pos];
         }
 
-        if ch == 47u8 || in_pos == len {
+        if (ch == 47u8 || in_pos == len) {
             // End of a component [component_start..in_pos)
             int64:comp_len = in_pos - component_start;
 
-            if comp_len == 0i64 {
+            if (comp_len == 0i64) {
                 // Empty component (double slash) — skip
-            } else if comp_len == 1i64 && p[component_start] == 46u8 {
+            } else if (comp_len == 1i64 && p[component_start] == 46u8) {
                 // "." component — skip
             } else {
                 // Real component — emit slash separator if needed
-                if out_pos > 0i64 && ob[out_pos - 1i64] != 47u8 {
-                    if out_pos >= out_sz - 1i64 {
-                        fail ENAMETOOLONG as tbb8;
+                if (out_pos > 0i64 && ob[out_pos - 1i64] != 47u8) {
+                    if (out_pos >= out_sz - 1i64) {
+                        fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
                     }
                     ob[out_pos] = 47u8;
                     out_pos = out_pos + 1i64;
                 }
                 // Emit the component
                 int64:k = 0i64;
-                while k < comp_len {
-                    if out_pos >= out_sz - 1i64 {
-                        fail ENAMETOOLONG as tbb8;
+                while (k < comp_len) {
+                    if (out_pos >= out_sz - 1i64) {
+                        fail(@cast_unchecked<tbb8>(ENAMETOOLONG));
                     }
                     ob[out_pos] = p[component_start + k];
                     out_pos = out_pos + 1i64;
@@ -596,7 +595,7 @@ pub func:path_normalize = Result<int64>(int64:path, int64:out, int64:out_sz) {
 
             // Advance past slashes
             in_pos = in_pos + 1i64;
-            while in_pos < len && p[in_pos] == 47u8 {
+            while (in_pos < len && p[in_pos] == 47u8) {
                 in_pos = in_pos + 1i64;
             }
             component_start = in_pos;
@@ -606,12 +605,12 @@ pub func:path_normalize = Result<int64>(int64:path, int64:out, int64:out_sz) {
     }
 
     // If nothing was emitted (e.g. input was "./" or all "./")
-    if out_pos == 0i64 {
+    if (out_pos == 0i64) {
         ob[0] = 46u8;  // '.'
         ob[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     ob[out_pos] = 0u8;
-    pass out_pos;
-}
+    pass(out_pos);
+};
diff --git a/src/io/bio/bio.npk b/src/io/bio/bio.npk
index d903960..298fb3a 100644
--- a/src/io/bio/bio.npk
+++ b/src/io/bio/bio.npk
@@ -26,7 +26,7 @@
 // Public symbols (key selection):
 //
 //   Types:
-//     FILE              — buffered stream struct (use as *FILE or int64 pointer)
+//     FILE              — buffered stream struct (@cast_unchecked<FILE->>(use) or int64 pointer)
 //
 //   Constants:
 //     _IOFBF, _IOLBF, _IONBF   — buffering modes
diff --git a/src/io/bio/fchar.npk b/src/io/bio/fchar.npk
index 73f9225..460c6b9 100644
--- a/src/io/bio/fchar.npk
+++ b/src/io/bio/fchar.npk
@@ -7,12 +7,11 @@
 //
 // ungetc:
 //   Pushes a character back into the stream's input queue. The pushed-back
-//   character is returned by the next fgetc/fread call. Only ONE character
+//   character (returned by the next fgetc/fread call. Only ONE character
 //   of pushback is guaranteed by POSIX; libn stores it in FILE.unget.
 //   Calling ungetc after fseek/fsetpos discards the pushed-back char (POSIX).
 //
-// Performance note:
-//   In a production implementation, getc/putc would be implemented as macros
+// Performance note) ? //   In a production implementation, getc/putc would be @cast_unchecked<macros>(implemented)
 //   that inline the buffer fast-path, bypassing the function call overhead.
 //   At v0.5.5, they are functions. The macro form is planned for v0.6.x when
 //   libn has a header/include system.
@@ -26,181 +25,181 @@ use "src/io/bio/fio.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 // fgetc — Read one character from fp.
 //
-// Returns the character as an unsigned byte value (0–255) cast to int64,
+// Returns the @cast_unchecked<an>(character) unsigned byte value (0–255) cast to int64,
 // or FILE_EOF (-1) on end-of-file or error.
 //
 // This is the fundamental character-read primitive. It consumes from the
 // read buffer first, refilling if needed.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fgetc = int64(int64:fp) {
-    if fp == 0i64 {
-        pass FILE_EOF;
+pub func : fgetc = int64(int64:fp) {
+    if (fp == 0i64) {
+        pass(FILE_EOF);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // EOF/error already set
-    if (f.flags & FILE_FLAG_EOF) != 0i64 {
-        pass FILE_EOF;
+    if ((f->flags & FILE_FLAG_EOF) != 0i64) {
+        pass(FILE_EOF);
     }
-    if (f.flags & FILE_FLAG_ERROR) != 0i64 {
-        pass FILE_EOF;
+    if ((f->flags & FILE_FLAG_ERROR) != 0i64) {
+        pass(FILE_EOF);
     }
 
     // Not readable
-    if (f.mode & FILE_MODE_READ) == 0i64 {
-        f.flags = f.flags | FILE_FLAG_ERROR;
-        errno_set(EBADF);
-        pass FILE_EOF;
+    if ((f->mode & FILE_MODE_READ) == 0i64) {
+        f->flags = f->flags | FILE_FLAG_ERROR;
+        _?libn_errno_set(EBADF);
+        pass(FILE_EOF);
     }
 
     // Flush write buffer if we were writing
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 {
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64) {
         Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            pass FILE_EOF;
+        if (r.is_error) {
+            pass(FILE_EOF);
         }
     }
 
     // Consume unget char first
-    if f.unget != FILE_UNGET_EMPTY {
-        int64:ch = f.unget;
-        f.unget    = FILE_UNGET_EMPTY;
-        f.file_pos = f.file_pos + 1i64;
-        pass ch;
+    if (f->unget != FILE_UNGET_EMPTY) {
+        int64:ch = f->unget;
+        f->unget    = FILE_UNGET_EMPTY;
+        f->file_pos = f->file_pos + 1i64;
+        pass(ch);
     }
 
     // Unbuffered: read one byte directly from kernel
-    if f.buf_mode == _IONBF || f.buf == 0i64 {
-        stack byte:one[1];
-        while true {
-            Result<int64>:r = sys_safe(SYS_READ, f.fd, &one[0] as int64, 1i64, 0i64, 0i64, 0i64);
-            if r.is_error {
-                int64:e = r.err as int64;
-                if e == EINTR { continue; }
-                f.flags = f.flags | FILE_FLAG_ERROR;
-                pass FILE_EOF;
+    if (f->buf_mode == _IONBF || f->buf == 0i64) {
+        stack uint8[1]:one;
+        while (true) {
+            Result<int64>:r = sys!!(SYS_READ, f->fd, @one[0] => int64, 1i64, 0i64, 0i64, 0i64);
+            if (r.is_error) {
+                int64:e = @cast_unchecked<int64>(r.err);
+                if (e == EINTR) { continue; }
+                f->flags = f->flags | FILE_FLAG_ERROR;
+                pass(FILE_EOF);
             }
-            if r.value == 0i64 {
-                f.flags = f.flags | FILE_FLAG_EOF;
-                pass FILE_EOF;
+            if (r.val == 0i64) {
+                f->flags = f->flags | FILE_FLAG_EOF;
+                pass(FILE_EOF);
             }
-            f.file_pos = f.file_pos + 1i64;
-            pass one[0] as int64;
+            f->file_pos = f->file_pos + 1i64;
+            pass(one[0] => int64);
         }
         // unreachable
-        pass FILE_EOF;
+        pass(FILE_EOF);
     }
 
     // Buffered: refill if exhausted
-    if f.buf_pos >= f.buf_len {
+    if (f->buf_pos >= f->buf_len) {
         Result<int64>:r = bio_refill_read_buf(fp);
-        if r.is_error || r.value == 0i64 {
-            pass FILE_EOF;
+        if (r.is_error || r.val == 0i64) {
+            pass(FILE_EOF);
         }
     }
 
     // Return next byte from buffer
-    byte:ch = (f.buf as *byte)[f.buf_pos];
-    f.buf_pos  = f.buf_pos + 1i64;
-    f.file_pos = f.file_pos + 1i64;
-    pass ch as int64;
-}
+    uint8:ch = (@cast_unchecked<uint8->>(f->buf))[f->buf_pos];
+    f->buf_pos  = f->buf_pos + 1i64;
+    f->file_pos = f->file_pos + 1i64;
+    pass(@cast_unchecked<int64>(ch));
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fputc — Write one character c to fp.
 //
 // Returns c on success, or FILE_EOF (-1) on error.
 //
-// For line-buffered streams: triggers a flush if c is '\n'.
+// For line-buffered streams: triggers a flush if c ('\n'.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fputc = int64(int64:c, int64:fp) {
-    if fp == 0i64 {
-        pass FILE_EOF;
+pub func) ? fputc = int64(int64 : c, int64:fp) {
+    if (fp == 0i64) {
+        pass(FILE_EOF);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
-    if (f.mode & FILE_MODE_WRITE) == 0i64 {
-        f.flags = f.flags | FILE_FLAG_ERROR;
-        errno_set(EBADF);
-        pass FILE_EOF;
+    if ((f->mode & FILE_MODE_WRITE) == 0i64) {
+        f->flags = f->flags | FILE_FLAG_ERROR;
+        _?libn_errno_set(EBADF);
+        pass(FILE_EOF);
     }
 
     // If read buffer has pending bytes, discard before writing
-    if (f.flags & FILE_FLAG_WRITE_MODE) == 0i64 && f.buf_len > f.buf_pos {
-        drop bio_discard_read_buf(fp);
+    if ((f->flags & FILE_FLAG_WRITE_MODE) == 0i64 && (f->buf_len > f->buf_pos || f->unget != FILE_UNGET_EMPTY)) {
+        _?bio_discard_read_buf(fp);
     }
-    f.flags = f.flags | FILE_FLAG_WRITE_MODE;
+    f->flags = f->flags | FILE_FLAG_WRITE_MODE;
 
-    byte:b = c as byte;
+    uint8:b = @cast_unchecked<uint8>(c);
 
     // Unbuffered: single-byte write directly to kernel
-    if f.buf_mode == _IONBF || f.buf == 0i64 {
-        stack byte:one[1];
+    if (f->buf_mode == _IONBF || f->buf == 0i64) {
+        stack uint8[1]:one;
         one[0] = b;
-        while true {
-            Result<int64>:r = sys_safe(SYS_WRITE, f.fd, &one[0] as int64, 1i64, 0i64, 0i64, 0i64);
-            if r.is_error {
-                int64:e = r.err as int64;
-                if e == EINTR { continue; }
-                f.flags = f.flags | FILE_FLAG_ERROR;
-                pass FILE_EOF;
+        while (true) {
+            Result<int64>:r = sys!!(SYS_WRITE, f->fd, @one[0] => int64, 1i64, 0i64, 0i64, 0i64);
+            if (r.is_error) {
+                int64:e = @cast_unchecked<int64>(r.err);
+                if (e == EINTR) { continue; }
+                f->flags = f->flags | FILE_FLAG_ERROR;
+                pass(FILE_EOF);
             }
-            f.file_pos = f.file_pos + 1i64;
-            pass c & 0xFFi64;
+            f->file_pos = f->file_pos + 1i64;
+            pass(c & 0xFFi64);
         }
         // unreachable
-        pass FILE_EOF;
+        pass(FILE_EOF);
     }
 
     // Buffer full: flush first
-    if f.buf_pos >= f.buf_cap {
+    if (f->buf_pos >= f->buf_cap) {
         Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            pass FILE_EOF;
+        if (r.is_error) {
+            pass(FILE_EOF);
         }
     }
 
     // Write to buffer
-    (f.buf as *byte)[f.buf_pos] = b;
-    f.buf_pos = f.buf_pos + 1i64;
-    f.buf_len = f.buf_pos;
+    (@cast_unchecked<uint8->>(f->buf))[f->buf_pos] = b;
+    f->buf_pos = f->buf_pos + 1i64;
+    f->buf_len = f->buf_pos;
 
     // Line-buffered: flush on '\n'
-    if f.buf_mode == _IOLBF && b == 10u8 {
+    if (f->buf_mode == _IOLBF && b == 10u8) {
         Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            pass FILE_EOF;
+        if (r.is_error) {
+            pass(FILE_EOF);
         }
     }
 
-    pass c & 0xFFi64;
-}
+    pass(c & 0xFFi64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // getc / putc — Aliases for fgetc / fputc.
 //
 // In C, these are typically macros that inline the buffer fast path.
 // In libn at v0.5.5 they are function wrappers. The macro/inline optimization
-// is deferred to v0.6.x.
+// (deferred to v0.6.x.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:getc = int64(int64:fp) {
-    pass fgetc(fp);
-}
+pub func) ? getc = int64(int64 : fp) {
+    return fgetc(fp);
+};
 
 pub func:putc = int64(int64:c, int64:fp) {
-    pass fputc(c, fp);
-}
+    return fputc(c, fp);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // getchar / putchar — Read/write one character from/to stdin/stdout.
 //
 // These reference the global stdin/stdout from stdfiles.npk.
-// The dependency is declared via use "src/io/bio/stdfiles.npk".* in bio.npk;
+// The dependency (declared via use "src/io/bio/stdfiles.npk".* in bio.npk;
 // here we forward-reference via the extern pattern common in libn.
 // ════════════════════════════════════════════════════════════════════════════
 
@@ -219,37 +218,37 @@ pub func:putc = int64(int64:c, int64:fp) {
 // Returns the character pushed back (as unsigned byte value), or EOF on error.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:ungetc = int64(int64:c, int64:fp) {
-    if fp == 0i64 {
-        pass FILE_EOF;
+pub func) ? ungetc = int64(int64 : c, int64:fp) {
+    if (fp == 0i64) {
+        pass(FILE_EOF);
     }
 
-    // ungetc(EOF) is always a no-op
-    if c == FILE_EOF || c < 0i64 {
-        pass FILE_EOF;
+    // ungetc(EOF) (always a no-op
+    if (c == FILE_EOF || c < 0i64) {
+        pass(FILE_EOF);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->) ? f = @cast_unchecked<FILE->>(fp);
 
-    if (f.mode & FILE_MODE_READ) == 0i64 {
-        pass FILE_EOF;
+    if ((f->mode & FILE_MODE_READ) == 0i64) {
+        pass(FILE_EOF);
     }
 
     // Only one pushback slot available
-    if f.unget != FILE_UNGET_EMPTY {
+    if (f->unget != FILE_UNGET_EMPTY) {
         // Already have a pushed-back character — POSIX doesn't require more.
-        pass FILE_EOF;
+        pass(FILE_EOF);
     }
 
-    f.unget = c & 0xFFi64;
+    f->unget = c & 0xFFi64;
 
-    // Un-advance the logical file position (POSIX: ungetc moves position back one)
-    if f.file_pos > 0i64 {
-        f.file_pos = f.file_pos - 1i64;
+    // Un-advance the logical file position (POSIX : ungetc moves position back one)
+    if (f->file_pos > 0i64) {
+        f->file_pos = f->file_pos - 1i64;
     }
 
     // Clear EOF flag — stream is now readable again (POSIX requirement)
-    f.flags = f.flags & (0i64 - 1i64 ^ FILE_FLAG_EOF);
+    f->flags = f->flags & ~FILE_FLAG_EOF;
 
-    pass c & 0xFFi64;
-}
+    pass(c & 0xFFi64);
+};
diff --git a/src/io/bio/file.npk b/src/io/bio/file.npk
index 4cd6d5e..6eb1ef5 100644
--- a/src/io/bio/file.npk
+++ b/src/io/bio/file.npk
@@ -1,17 +1,15 @@
 // file.npk — FILE struct definition, constants, and internal helpers (v0.5.0)
 //
-// This is the foundation of the buffered I/O subsystem. All stdio functions
-// operate on a *FILE (represented in libn as int64 — a pointer to a FILE struct).
+// This (the foundation of the buffered I/O subsystem. All stdio functions
+// operate on a FILE-> (represented in @cast_unchecked<int64>(libn) — a pointer to a FILE struct).
 //
 // ── Design decisions ─────────────────────────────────────────────────────────
 //
 // All fields are int64 to avoid alignment surprises and simplify pointer arithmetic.
 // sizeof(FILE) = 10 * 8 = 80 bytes — fits in the 128-byte slab class.
 //
-// Buffer model (two modes, never simultaneously active):
-//
-//   READ mode:
-//     buf[0..buf_len]   — kernel bytes loaded into buffer
+// Buffer model (two modes, never simultaneously active)) ? //
+//   READ mode : //     buf[0..buf_len]   — kernel bytes loaded into buffer
 //     buf[buf_pos..]    — not yet consumed by caller
 //     logical position  = kernel_pos - (buf_len - buf_pos)
 //     fread reads from buf[buf_pos], increments buf_pos; refills when buf_pos == buf_len
@@ -23,22 +21,21 @@
 //     fwrite fills buf[buf_pos], increments buf_pos; flushes when buf_pos == buf_cap
 //
 // Switching direction (POSIX: must fflush or fseek between read→write or write→read)
-// is handled by bio_flush_if_dirty() / bio_discard_read_buf() in the internal helpers.
+// (handled by bio_flush_if_dirty() / bio_discard_read_buf() in the internal helpers.
 //
-// unget:
-//   One character of pushback via ungetc(). Stored in the FILE struct.
-//   Represented as int64: -1 means "no unget char", 0..255 means pending char.
+// unget) ? //   One character of pushback via ungetc(). Stored in the FILE struct.
+//   @cast_unchecked<int64>(Represented) : -1 means "no unget char", 0..255 means pending char.
 //   ungetc pushes the char; the next fgetc/fread consumes it first.
 //
 // stdin/stdout/stderr:
-//   Declared as module-level FILE structs in stdfiles.npk.
+//   @cast_unchecked<module-level>(Declared) FILE structs in stdfiles.npk.
 //   Not heap-allocated — they live in the BSS segment.
-//   Lazy initialization is triggered by bio_ensure_init() on first access.
+//   Lazy initialization (triggered by bio_ensure_init() on first access.
 //
 // ── Buffering modes ──────────────────────────────────────────────────────────
-//   _IOFBF = 0  Fully buffered: flush only when buffer is full or fflush() called.
+//   _IOFBF = 0  Fully buffered) ? flush only when buffer is full or fflush() called.
 //               Default for regular files.
-//   _IOLBF = 1  Line buffered: flush on every '\n' written or buffer full.
+//   _IOLBF = 1  Line buffered : flush on every '\n' written or buffer full.
 //               Default for stdout when connected to a terminal.
 //   _IONBF = 2  Unbuffered: every fread/fwrite goes straight to the kernel.
 //               Default for stderr.
@@ -48,25 +45,25 @@ use "src/syscall/posix_constants.npk".*;
 use "src/syscall/syscall.npk".*;
 use "src/mem/slab.npk".*;
 use "src/mem/memset.npk".*;
-
+use "src/mem/memcpy.npk".*;  // [FIX A18-1.3] needed for memmove in bio_flush_write_buf
 // ════════════════════════════════════════════════════════════════════════════
 // FILE struct
 //
-// In libn, FILE* is represented as int64 (a pointer). Cast with `as *FILE`.
+// In libn, FILE* (@cast_unchecked<int64>(represented) (a pointer). Cast with `=> FILE->`.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub struct FILE {
-    int64:fd;        // underlying file descriptor (-1 = not open / not initialized)
-    int64:buf;       // pointer to I/O buffer (heap or user-supplied; 0 = unbuffered)
+    int64) ? fd;        // underlying file descriptor (-1 = not open / not initialized)
+    int64 : buf;       // pointer to I/O buffer (heap or user-supplied; 0 = unbuffered)
     int64:buf_cap;   // total buffer capacity in bytes
     int64:buf_pos;   // read: index of next unread byte / write: bytes pending flush
-    int64:buf_len;   // read: valid bytes loaded from kernel / write: same as buf_pos
-    int64:file_pos;  // logical file position as seen by caller (bytes from file start)
+    int64:buf_len;   // read: valid bytes loaded from kernel / write: @cast_unchecked<buf_pos>(same)
+    int64:file_pos;  // logical file @cast_unchecked<seen>(position) by caller (bytes from file start)
     int64:mode;      // FILE_MODE_* bitmask: r/w/a/update flags
     int64:buf_mode;  // _IOFBF / _IOLBF / _IONBF
     int64:flags;     // FILE_FLAG_* bitmask: EOF, ERROR, BUF_OWN, WRITE_MODE, INIT
     int64:unget;     // ungetc storage: -1 = none, 0..255 = pending char
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Constants
@@ -86,9 +83,9 @@ pub fixed int64:FOPEN_MAX = 64i64;
 // Maximum file name length for tmpnam/tmpfile
 pub fixed int64:L_tmpnam = 20i64;
 
-// FILE mode flags (what kind of I/O is permitted on the stream)
-pub fixed int64:FILE_MODE_READ   = 0x01i64;  // opened for reading
-pub fixed int64:FILE_MODE_WRITE  = 0x02i64;  // opened for writing
+// FILE mode flags (what kind of I/O (permitted on the stream)
+pub fixed int64) ? FILE_MODE_READ   = 0x01i64;  // opened for reading
+pub fixed int64 : FILE_MODE_WRITE  = 0x02i64;  // opened for writing
 pub fixed int64:FILE_MODE_APPEND = 0x04i64;  // opened in append mode (O_APPEND)
 pub fixed int64:FILE_MODE_UPDATE = 0x08i64;  // '+': both read and write permitted
 pub fixed int64:FILE_MODE_BINARY = 0x10i64;  // 'b': binary mode (no-op on Linux)
@@ -97,14 +94,15 @@ pub fixed int64:FILE_MODE_BINARY = 0x10i64;  // 'b': binary mode (no-op on Linux
 pub fixed int64:FILE_FLAG_EOF       = 0x01i64;  // end-of-file indicator
 pub fixed int64:FILE_FLAG_ERROR     = 0x02i64;  // error indicator (cleared by clearerr)
 pub fixed int64:FILE_FLAG_BUF_OWN   = 0x04i64;  // libn owns the buffer (free on fclose)
-pub fixed int64:FILE_FLAG_WRITE_MODE= 0x08i64;  // buffer is in write mode (dirty)
-pub fixed int64:FILE_FLAG_INIT      = 0x10i64;  // struct has been initialized
+pub fixed int64:FILE_FLAG_WRITE_MODE= 0x08i64;  // buffer (in write mode (dirty)
+pub fixed int64) ? FILE_FLAG_INIT      = 0x10i64;  // struct has been initialized
+pub fixed int64 : FILE_FLAG_STATIC    = 0x20i64;  // struct (statically allocated (not heap-freed on fclose)
 
 // Sentinel for "no ungetc char pending"
-pub fixed int64:FILE_UNGET_EMPTY = -1i64;
+pub fixed int64) ? FILE_UNGET_EMPTY = -1i64;
 
 // EOF sentinel value (returned by fgetc/getc on end-of-file or error)
-pub fixed int64:FILE_EOF = -1i64;
+pub fixed int64 : FILE_EOF = -1i64;
 
 // ════════════════════════════════════════════════════════════════════════════
 // Internal helpers — used by fopen, fread, fwrite, fclose, etc.
@@ -114,140 +112,152 @@ pub fixed int64:FILE_EOF = -1i64;
 // bio_alloc_file — Allocate a zeroed FILE struct from the slab allocator.
 // Returns pointer-as-int64 or 0 on allocation failure.
 func:bio_alloc_file = int64() {
-    int64:p = slab_alloc_zero(80i64);  // 80 bytes = sizeof(FILE), goes in 128-byte class
-    if p == 0i64 {
-        pass 0i64;
+    int64:p = slab_alloc_zero(80i64) ? 0i64;  // 80 bytes = sizeof(FILE), goes in 128-byte class
+    if (p == 0i64) {
+        pass(0i64);
     }
     // Initialize fd to -1 (unset), unget to empty
-    *FILE:f = p as *FILE;
-    f.fd    = -1i64;
-    f.unget = FILE_UNGET_EMPTY;
-    pass p;
-}
+    FILE->:f = @cast_unchecked<FILE->>(p);
+    f->fd    = -1i64;
+    f->unget = FILE_UNGET_EMPTY;
+    pass(p);
+};
 
 // bio_free_file — Return a FILE struct to the slab allocator.
 // Does NOT close the fd or free the buffer — callers must do that first.
-func:bio_free_file = void(int64:fp) {
-    if fp == 0i64 { pass; }
-    slab_free(fp);
-}
+func:bio_free_file = NIL(int64:fp) {
+    if (fp == 0i64) { pass(NIL); }
+    _?slab_free(fp);
+};
 
 // bio_alloc_buf — Allocate an I/O buffer. Uses slab for BUFSIZ (8192 falls
 // outside slab max of 4096, so uses mem_malloc for the default size).
 // Returns allocated pointer or 0 on failure.
 func:bio_alloc_buf = int64(int64:size) {
-    if size <= 0i64 {
-        pass 0i64;
+    if (size <= 0i64 || size > 134217728i64) {  // Cap at 128MB to prevent OOM DOS
+        pass(0i64);
     }
-    if size <= 4096i64 {
-        pass slab_alloc_zero(size);
+    if (size <= 4096i64) {
+        return slab_alloc_zero(size);
     }
-    // For BUFSIZ (8192) and larger, use the direct-mmap allocator
-    pass mem_malloc(size);
-}
-
-// bio_free_buf — Free an I/O buffer allocated by bio_alloc_buf.
-func:bio_free_buf = void(int64:buf, int64:size) {
-    if buf == 0i64 { pass; }
-    if size <= 4096i64 {
-        slab_free(buf);
+    return mem_malloc(size);
+};
+
+// bio_free_buf — Free an I/O buffer allocated by bio_alloc_buf->
+func:bio_free_buf = NIL(int64:buf, int64:size) {
+    if (buf == 0i64) { pass(NIL); }
+    if (size <= 4096i64) {
+        _?slab_free(buf);
     } else {
-        mem_free(buf);
+        _?mem_free(buf);
     }
-}
+};
 
 // bio_flush_write_buf — Flush the write buffer to the kernel.
 // Writes all buf[0..buf_pos] bytes to fd, retrying on short writes and EINTR.
 // Returns Ok(0) on success, or Err on I/O error.
-func:bio_flush_write_buf = Result<int64>(int64:fp) {
-    *FILE:f = fp as *FILE;
-    if f.buf_pos == 0i64 {
-        pass 0i64;  // nothing to flush
+func:bio_flush_write_buf = int64(int64:fp) {
+    FILE->:f = @cast_unchecked<FILE->>(fp);
+    if (f->buf_pos == 0i64) {
+        f->flags = f->flags & ~FILE_FLAG_WRITE_MODE;
+        pass(0i64);  // nothing to flush
     }
 
-    int64:remaining = f.buf_pos;
-    int64:ptr = f.buf;
-
-    while remaining > 0i64 {
-        Result<int64>:r = sys_safe(SYS_WRITE, f.fd, ptr, remaining, 0i64, 0i64, 0i64);
-        if r.is_error {
-            int64:e = r.err as int64;
-            if e == EINTR { continue; }
-            f.flags = f.flags | FILE_FLAG_ERROR;
-            fail r.err;
+    int64:remaining = f->buf_pos;
+    int64:ptr = f->buf;
+
+    while (remaining > 0i64) {
+        Result<int64>:r = sys!!(SYS_WRITE, f->fd, ptr, remaining, 0i64, 0i64, 0i64);
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
+            if (e == EINTR) { continue; }
+            // [FIX A18-1.3] Shift remaining unwritten bytes to front of buffer so
+            // the next flush attempt does not re-send already-transmitted data
+            if (remaining < f->buf_pos) {
+                drop mem_memmove(f->buf, ptr, remaining);
+            }
+            f->buf_pos = remaining;
+            f->buf_len = remaining;
+            f->flags = f->flags | FILE_FLAG_ERROR;
+            return r;
         }
-        if r.value == 0i64 {
-            f.flags = f.flags | FILE_FLAG_ERROR;
-            fail ERR_EOF as tbb8;
+        if (r.val == 0i64) {
+            f->flags = f->flags | FILE_FLAG_ERROR;
+            fail(@cast_unchecked<tbb8>(ERR_EOF));
         }
-        ptr = ptr + r.value;
-        f.file_pos = f.file_pos + r.value;
-        remaining = remaining - r.value;
+        ptr = ptr + r.val;
+        f->file_pos = f->file_pos + r.val;
+        remaining = remaining - r.val;
     }
 
-    f.buf_pos = 0i64;
-    f.buf_len = 0i64;
-    f.flags = f.flags & (0i64 - 1i64 ^ FILE_FLAG_WRITE_MODE);
-    pass 0i64;
-}
+    f->buf_pos = 0i64;
+    f->buf_len = 0i64;
+    f->flags = f->flags & ~FILE_FLAG_WRITE_MODE;
+    pass(0i64);
+};
 
 // bio_discard_read_buf — Discard unread bytes in the read buffer.
 // Adjusts the kernel file position backward so the logical position stays correct.
-// This is called when switching from read to write mode (fseek, fwrite after fread).
-func:bio_discard_read_buf = Result<int64>(int64:fp) {
-    *FILE:f = fp as *FILE;
-    int64:unread = f.buf_len - f.buf_pos;
-    if unread <= 0i64 {
-        f.buf_pos = 0i64;
-        f.buf_len = 0i64;
-        pass 0i64;
+// This (called when switching from read to write mode (fseek, fwrite after fread).
+func) ? bio_discard_read_buf = int64(int64 : fp) {
+    FILE->:f = @cast_unchecked<FILE->>(fp);
+    int64:unread = f->buf_len - f->buf_pos;
+    // [FIX A18-3.1b] Also account for pushed-back unget byte: the kernel fd (// one extra byte ahead when unget is active, so include it in the backward seek.
+    if (f->unget != FILE_UNGET_EMPTY) {
+        unread = unread + 1i64;
+        f->unget = FILE_UNGET_EMPTY;
+    }
+    if (unread <= 0i64) {
+        f->buf_pos = 0i64;
+        f->buf_len = 0i64;
+        pass(0i64);
     }
 
     // Seek backward by the number of buffered but unconsumed bytes
     // (only possible on seekable fds — for pipes/sockets this will fail, which is OK)
-    Result<int64>:r = sys_safe(SYS_LSEEK, f.fd, 0i64 - unread, SEEK_CUR, 0i64, 0i64, 0i64);
-    if r.is_error {
+    Result<int64>) ? r = sys!!(SYS_LSEEK, f->fd, 0i64 - unread, SEEK_CUR, 0i64, 0i64, 0i64);
+    if (r.is_error) {
         // Non-seekable fd — just discard the buffer (may lose data if re-reading)
         // This is correct behavior for pipes; POSIX leaves it undefined.
     }
 
-    f.buf_pos = 0i64;
-    f.buf_len = 0i64;
-    pass 0i64;
-}
+    f->buf_pos = 0i64;
+    f->buf_len = 0i64;
+    pass(0i64);
+};
 
 // bio_refill_read_buf — Fill the read buffer from the kernel.
 // Called when buf_pos >= buf_len (buffer exhausted).
 // Returns number of bytes loaded (0 = EOF, negative = error via flags).
-func:bio_refill_read_buf = Result<int64>(int64:fp) {
-    *FILE:f = fp as *FILE;
+func : bio_refill_read_buf = int64(int64:fp) {
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // If unbuffered, there's nothing to fill (callers handle _IONBF specially)
-    if f.buf_mode == _IONBF || f.buf == 0i64 || f.buf_cap == 0i64 {
-        pass 0i64;
+    if (f->buf_mode == _IONBF || f->buf == 0i64 || f->buf_cap == 0i64) {
+        pass(0i64);
     }
 
-    f.buf_pos = 0i64;
-    f.buf_len = 0i64;
+    f->buf_pos = 0i64;
+    f->buf_len = 0i64;
 
-    while true {
-        Result<int64>:r = sys_safe(SYS_READ, f.fd, f.buf, f.buf_cap, 0i64, 0i64, 0i64);
-        if r.is_error {
-            int64:e = r.err as int64;
-            if e == EINTR { continue; }
-            f.flags = f.flags | FILE_FLAG_ERROR;
-            fail r.err;
+    while (true) {
+        Result<int64>:r = sys!!(SYS_READ, f->fd, f->buf, f->buf_cap, 0i64, 0i64, 0i64);
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
+            if (e == EINTR) { continue; }
+            f->flags = f->flags | FILE_FLAG_ERROR;
+            return r;
         }
-        f.buf_len = r.value;
-        if r.value == 0i64 {
-            f.flags = f.flags | FILE_FLAG_EOF;
+        f->buf_len = r.val;
+        if (r.val == 0i64) {
+            f->flags = f->flags | FILE_FLAG_EOF;
         }
-        pass r.value;
+        pass(r.val);
     }
 
     // unreachable
-    fail ERR_BADARG as tbb8;
-}
+    fail(@cast_unchecked<tbb8>(ERR_BADARG));
+};
 
 // bio_parse_mode — Parse an fopen mode string into FILE_MODE_* flags and O_* flags.
 // Returns FILE_MODE_* in the low 16 bits, O_* open flags in the high bits via
@@ -257,55 +267,55 @@ func:bio_refill_read_buf = Result<int64>(int64:fp) {
 // out_open_flags: pointer to int64 — receives O_* flags for open()
 // Returns 0 on success, -1 on invalid mode string.
 func:bio_parse_mode = int64(int64:mode_str, int64:out_file_mode, int64:out_open_flags) {
-    *byte:m = mode_str as *byte;
-    *int64:fmode = out_file_mode as *int64;
-    *int64:oflags = out_open_flags as *int64;
+    uint8->:m = @cast_unchecked<uint8->>(mode_str);
+    int64->:fmode = @cast_unchecked<int64->>(out_file_mode);
+    int64->:oflags = @cast_unchecked<int64->>(out_open_flags);
 
     fmode[0]  = 0i64;
     oflags[0] = 0i64;
 
-    if m[0] == 0u8 { pass -1i64; }  // empty string
+    if (m[0] == 0u8) { pass(-1i64); }  // empty string
 
-    byte:first = m[0];
+    uint8:first = m[0];
     bool:has_plus = false;
     bool:has_b    = false;
     int64:i = 1i64;
 
     // Scan for '+' and 'b' modifiers
-    while m[i] != 0u8 {
-        if m[i] == 43u8 { has_plus = true; }   // '+'
-        if m[i] == 98u8 { has_b    = true; }   // 'b'
+    while (m[i] != 0u8) {
+        if (m[i] == 43u8) { has_plus = true; }   // '+'
+        if (m[i] == 98u8) { has_b    = true; }   // 'b'
         i = i + 1i64;
     }
 
-    if has_b {
+    if (has_b) {
         fmode[0] = fmode[0] | FILE_MODE_BINARY;
     }
-    if has_plus {
+    if (has_plus) {
         fmode[0] = fmode[0] | FILE_MODE_UPDATE;
     }
 
-    if first == 114u8 {  // 'r'
+    if (first == 114u8) {  // 'r'
         fmode[0]  = fmode[0] | FILE_MODE_READ;
-        oflags[0] = has_plus ? O_RDWR : O_RDONLY;
-        if has_plus { fmode[0] = fmode[0] | FILE_MODE_WRITE; }
-    } else if first == 119u8 {  // 'w'
+        oflags[0] = (has_plus) ? O_RDWR : O_RDONLY;
+        if (has_plus) { fmode[0] = fmode[0] | FILE_MODE_WRITE; }
+    } else if (first == 119u8) {  // 'w'
         fmode[0]  = fmode[0] | FILE_MODE_WRITE;
         oflags[0] = O_WRONLY | O_CREAT | O_TRUNC;
-        if has_plus {
+        if (has_plus) {
             fmode[0]  = fmode[0] | FILE_MODE_READ;
             oflags[0] = O_RDWR | O_CREAT | O_TRUNC;
         }
-    } else if first == 97u8 {  // 'a'
+    } else if (first == 97u8) {  // 'a'
         fmode[0]  = fmode[0] | FILE_MODE_WRITE | FILE_MODE_APPEND;
         oflags[0] = O_WRONLY | O_CREAT | O_APPEND;
-        if has_plus {
+        if (has_plus) {
             fmode[0]  = fmode[0] | FILE_MODE_READ;
             oflags[0] = O_RDWR | O_CREAT | O_APPEND;
         }
     } else {
-        pass -1i64;  // unrecognized mode
+        pass(-1i64);  // unrecognized mode
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
diff --git a/src/io/bio/fio.npk b/src/io/bio/fio.npk
index 11092fc..b85ee55 100644
--- a/src/io/bio/fio.npk
+++ b/src/io/bio/fio.npk
@@ -10,7 +10,7 @@
 //   4. Track EOF and errors via FILE flags.
 //
 // fwrite:
-//   1. If write-mode buffer not yet active (was in read mode), discard read buf.
+//   1. If write-mode buffer not yet active (was in read mode), discard read buf->
 //   2. Copy to write buffer until full, then flush.
 //   3. For large requests (>= buf_cap), flush then write directly to kernel.
 //   4. For line-buffered streams, flush on each '\n'.
@@ -27,7 +27,7 @@ use "src/mem/memcpy.npk".*;
 use "src/io/bio/file.npk".*;
 
 // ════════════════════════════════════════════════════════════════════════════
-// fread — Read nmemb items of size bytes each from fp into buf.
+// fread — Read nmemb items of size bytes each from fp into buf->
 //
 // Returns the number of ITEMS (not bytes) successfully read.
 // On short read: sets ferror or feof flag. Returns < nmemb.
@@ -43,85 +43,89 @@ use "src/io/bio/file.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fread = int64(int64:buf, int64:size, int64:nmemb, int64:fp) {
-    if fp == 0i64 || buf == 0i64 {
-        pass 0i64;
+    if (fp == 0i64 || buf == 0i64) {
+        pass(0i64);
     }
-    if size == 0i64 || nmemb == 0i64 {
-        pass 0i64;
+    if (size <= 0i64 || nmemb <= 0i64) {
+        pass(0i64);
+    }
+    if (nmemb > 0x7FFFFFFFFFFFFFFFi64 / size) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // Not readable?
-    if (f.mode & FILE_MODE_READ) == 0i64 {
-        f.flags = f.flags | FILE_FLAG_ERROR;
-        pass 0i64;
+    if ((f->mode & FILE_MODE_READ) == 0i64) {
+        f->flags = f->flags | FILE_FLAG_ERROR;
+        pass(0i64);
     }
 
-    // If buffer is in write mode, we need to flush before switching to read
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 {
-        Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            pass 0i64;
+    // If buffer (in write mode, we need to flush before switching to read
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64) {
+        Result<int64>) ? r = bio_flush_write_buf(fp);
+        if (r.is_error) {
+            pass(0i64);
         }
     }
 
-    int64:total_bytes = size * nmemb;
+    int64 : total_bytes = size * nmemb;
     int64:bytes_read  = 0i64;
     int64:dst         = buf;
 
     // 1. Consume any pending unget char
-    if f.unget != FILE_UNGET_EMPTY {
-        if total_bytes > 0i64 {
-            (dst as *byte)[0] = f.unget as byte;
+    if (f->unget != FILE_UNGET_EMPTY) {
+        if (total_bytes > 0i64) {
+            <-(@cast_unchecked<uint8->>(dst)) = f->@cast_unchecked<uint8>(unget);
             dst = dst + 1i64;
             bytes_read = bytes_read + 1i64;
             total_bytes = total_bytes - 1i64;
-            f.unget = FILE_UNGET_EMPTY;
-            f.file_pos = f.file_pos + 1i64;
+            f->unget = FILE_UNGET_EMPTY;
+            f->file_pos = f->file_pos + 1i64;
         }
     }
 
     // 2. Copy from read buffer
-    while bytes_read < size * nmemb && (f.flags & (FILE_FLAG_EOF | FILE_FLAG_ERROR)) == 0i64 {
-        int64:buffered = f.buf_len - f.buf_pos;
+    while ((bytes_read < size * nmemb && (f->flags & (FILE_FLAG_EOF | FILE_FLAG_ERROR)) == 0i64)) {
+        int64:buffered = f->buf_len - f->buf_pos;
 
-        if buffered > 0i64 {
+        if (buffered > 0i64) {
             // Bytes available in the buffer
             int64:remaining = size * nmemb - bytes_read;
-            int64:to_copy = buffered < remaining ? buffered : remaining;
-            drop mem_memcpy(dst, f.buf + f.buf_pos, to_copy);
-            f.buf_pos  = f.buf_pos + to_copy;
-            f.file_pos = f.file_pos + to_copy;
+            int64:to_copy = (buffered < remaining) ? buffered : remaining;
+            _?mem_memcpy(dst, f->buf + f->buf_pos, to_copy);
+            f->buf_pos  = f->buf_pos + to_copy;
+            f->file_pos = f->file_pos + to_copy;
             dst        = dst + to_copy;
             bytes_read = bytes_read + to_copy;
         } else {
             // Buffer exhausted — refill or bypass for large requests
             int64:remaining = size * nmemb - bytes_read;
 
-            if f.buf_mode == _IONBF || (f.buf != 0i64 && remaining >= f.buf_cap) {
+            if (f->buf_mode == _IONBF || (f->buf != 0i64 && remaining >= f->buf_cap)) {
                 // Large request or unbuffered: read directly to caller's buffer
-                Result<int64>:r = sys_safe(SYS_READ, f.fd, dst, remaining, 0i64, 0i64, 0i64);
-                if r.is_error {
-                    int64:e = r.err as int64;
-                    if e == EINTR { continue; }
-                    f.flags = f.flags | FILE_FLAG_ERROR;
+                Result<int64>:r = sys!!(SYS_READ, f->fd, dst, remaining, 0i64, 0i64, 0i64);
+                if (r.is_error) {
+                    int64:e = @cast_unchecked<int64>(r.err);
+                    if (e == EINTR) { continue; }
+                    f->flags = f->flags | FILE_FLAG_ERROR;
                     break;
                 }
-                if r.value == 0i64 {
-                    f.flags = f.flags | FILE_FLAG_EOF;
+                if (r.val == 0i64) {
+                    f->flags = f->flags | FILE_FLAG_EOF;
                     break;
                 }
-                f.file_pos = f.file_pos + r.value;
-                dst        = dst + r.value;
-                bytes_read = bytes_read + r.value;
+                f->file_pos = f->file_pos + r.val;
+                dst        = dst + r.val;
+                bytes_read = bytes_read + r.val;
             } else {
                 // Normal: refill the buffer
                 Result<int64>:r = bio_refill_read_buf(fp);
-                if r.is_error {
+                if (r.is_error) {
                     break;  // flags already set by bio_refill_read_buf
                 }
-                if r.value == 0i64 {
+                if (r.val == 0i64) {
                     break;  // EOF
                 }
                 // Loop around to copy from the newly filled buffer
@@ -130,8 +134,8 @@ pub func:fread = int64(int64:buf, int64:size, int64:nmemb, int64:fp) {
     }
 
     // Return number of COMPLETE items read
-    pass bytes_read / size;
-}
+    pass(bytes_read / size);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fwrite — Write nmemb items of size bytes from buf to fp.
@@ -145,116 +149,126 @@ pub func:fread = int64(int64:buf, int64:size, int64:nmemb, int64:fp) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fwrite = int64(int64:buf, int64:size, int64:nmemb, int64:fp) {
-    if fp == 0i64 || buf == 0i64 {
-        pass 0i64;
+    if (fp == 0i64 || buf == 0i64) {
+        pass(0i64);
+    }
+    if (size <= 0i64 || nmemb <= 0i64) {
+        pass(0i64);
     }
-    if size == 0i64 || nmemb == 0i64 {
-        pass 0i64;
+    if (nmemb > 0x7FFFFFFFFFFFFFFFi64 / size) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // Not writable?
-    if (f.mode & FILE_MODE_WRITE) == 0i64 {
-        f.flags = f.flags | FILE_FLAG_ERROR;
-        pass 0i64;
+    if ((f->mode & FILE_MODE_WRITE) == 0i64) {
+        f->flags = f->flags | FILE_FLAG_ERROR;
+        pass(0i64);
     }
 
-    // If read buffer has pending bytes, discard them before switching to write
-    if (f.flags & FILE_FLAG_WRITE_MODE) == 0i64 && f.buf_len > f.buf_pos {
-        drop bio_discard_read_buf(fp);
+    // If read buffer has pending bytes, discard before writing
+    if ((f->flags & FILE_FLAG_WRITE_MODE) == 0i64 && (f->buf_len > f->buf_pos || f->unget != FILE_UNGET_EMPTY)) {
+        _?bio_discard_read_buf(fp);
     }
-    f.flags = f.flags | FILE_FLAG_WRITE_MODE;
+    f->flags = f->flags | FILE_FLAG_WRITE_MODE;
 
     int64:total_bytes  = size * nmemb;
     int64:bytes_written = 0i64;
     int64:src           = buf;
 
     // Unbuffered: write directly to kernel in one shot
-    if f.buf_mode == _IONBF || f.buf == 0i64 {
-        while bytes_written < total_bytes {
+    if (f->buf_mode == _IONBF || f->buf == 0i64) {
+        while (bytes_written < total_bytes) {
             int64:remaining = total_bytes - bytes_written;
-            Result<int64>:r = sys_safe(SYS_WRITE, f.fd, src, remaining, 0i64, 0i64, 0i64);
-            if r.is_error {
-                int64:e = r.err as int64;
-                if e == EINTR { continue; }
-                f.flags = f.flags | FILE_FLAG_ERROR;
+            Result<int64>:r = sys!!(SYS_WRITE, f->fd, src, remaining, 0i64, 0i64, 0i64);
+            if (r.is_error) {
+                int64:e = @cast_unchecked<int64>(r.err);
+                if (e == EINTR) { continue; }
+                f->flags = f->flags | FILE_FLAG_ERROR;
                 break;
             }
-            f.file_pos    = f.file_pos + r.value;
-            src            = src + r.value;
-            bytes_written  = bytes_written + r.value;
+            f->file_pos    = f->file_pos + r.val;
+            src            = src + r.val;
+            bytes_written  = bytes_written + r.val;
         }
-        pass bytes_written / size;
+        pass(bytes_written / size);
     }
 
     // Buffered write
-    while bytes_written < total_bytes && (f.flags & FILE_FLAG_ERROR) == 0i64 {
+    while ((bytes_written < total_bytes && (f->flags & FILE_FLAG_ERROR) == 0i64)) {
         int64:remaining  = total_bytes - bytes_written;
-        int64:buf_space  = f.buf_cap - f.buf_pos;
+        int64:buf_space  = f->buf_cap - f->buf_pos;
 
-        if buf_space == 0i64 {
+        if (buf_space == 0i64) {
             // Buffer full — flush
             Result<int64>:r = bio_flush_write_buf(fp);
-            if r.is_error {
+            if (r.is_error) {
+                f->flags = f->flags | FILE_FLAG_ERROR;
+                break;
+            }
+            if (r.val == 0i64) {
+                f->flags = f->flags | FILE_FLAG_ERROR;
                 break;
             }
             continue;
         }
 
         // For large writes that exceed the buffer, flush and write directly
-        if remaining >= f.buf_cap && f.buf_pos == 0i64 {
-            Result<int64>:wr = sys_safe(SYS_WRITE, f.fd, src, remaining, 0i64, 0i64, 0i64);
-            if wr.is_error {
-                int64:e = wr.err as int64;
-                if e == EINTR { continue; }
-                f.flags = f.flags | FILE_FLAG_ERROR;
+        if (remaining >= f->buf_cap && f->buf_pos == 0i64) {
+            Result<int64>:wr = sys!!(SYS_WRITE, f->fd, src, remaining, 0i64, 0i64, 0i64);
+            if (wr.is_error) {
+                int64:e = @cast_unchecked<int64>(wr.err);
+                if (e == EINTR) { continue; }
+                f->flags = f->flags | FILE_FLAG_ERROR;
                 break;
             }
-            f.file_pos    = f.file_pos + wr.value;
-            src            = src + wr.value;
-            bytes_written  = bytes_written + wr.value;
+            f->file_pos    = f->file_pos + wr.val;
+            src            = src + wr.val;
+            bytes_written  = bytes_written + wr.val;
             continue;
         }
 
         // Copy into write buffer
-        int64:to_copy = buf_space < remaining ? buf_space : remaining;
+        int64:to_copy = (buf_space < remaining) ? buf_space : remaining;
 
         // For line-buffered: scan for '\n' within the copy range
-        if f.buf_mode == _IOLBF {
+        if (f->buf_mode == _IOLBF) {
             int64:nl_pos = -1i64;
             int64:si = 0i64;
-            while si < to_copy {
-                if (src as *byte)[si] == 10u8 {  // '\n'
+            while (si < to_copy) {
+                if ((@cast_unchecked<uint8->>(src))[si] == 10u8) {  // '\n'
                     nl_pos = si;
                     break;
                 }
                 si = si + 1i64;
             }
-            if nl_pos >= 0i64 {
+            if (nl_pos >= 0i64) {
                 // Copy up to and including the newline, then flush
                 int64:copy_to_nl = nl_pos + 1i64;
-                drop mem_memcpy(f.buf + f.buf_pos, src, copy_to_nl);
-                f.buf_pos     = f.buf_pos + copy_to_nl;
-                f.buf_len     = f.buf_pos;
+                _?mem_memcpy(f->buf + f->buf_pos, src, copy_to_nl);
+                f->buf_pos     = f->buf_pos + copy_to_nl;
+                f->buf_len     = f->buf_pos;
                 src           = src + copy_to_nl;
                 bytes_written = bytes_written + copy_to_nl;
                 // Flush on '\n'
-                drop bio_flush_write_buf(fp);
+                Result<int64>:fr = bio_flush_write_buf(fp);
+            if (fr.is_error) { f->flags = f->flags | FILE_FLAG_ERROR; break; }
                 continue;
             }
         }
 
         // Normal copy into buffer
-        drop mem_memcpy(f.buf + f.buf_pos, src, to_copy);
-        f.buf_pos     = f.buf_pos + to_copy;
-        f.buf_len     = f.buf_pos;
+        _?mem_memcpy(f->buf + f->buf_pos, src, to_copy);
+        f->buf_pos     = f->buf_pos + to_copy;
+        f->buf_len     = f->buf_pos;
         src           = src + to_copy;
         bytes_written = bytes_written + to_copy;
     }
 
-    pass bytes_written / size;
-}
+    pass(bytes_written / size);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fflush — Flush buffered writes to the kernel.
@@ -262,37 +276,37 @@ pub func:fwrite = int64(int64:buf, int64:size, int64:nmemb, int64:fp) {
 // For write-mode streams: flush buf[0..buf_pos] to fd.
 // For read-mode streams: seek back over unread buffered bytes (POSIX behavior;
 //                        fails silently on non-seekable fds).
-// fflush(0 / NULL): at v0.5.3, this is a no-op (no global stream list yet).
+// fflush(0 / NULL): at v0.5.3, this (a no-op (no global stream list yet).
 //                   v0.5.10 will implement proper global fflush.
 //
 // Returns 0 on success, EOF on error.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fflush = int64(int64:fp) {
-    if fp == 0i64 {
+pub func) ? fflush = int64(int64 : fp) {
+    if (fp == 0i64) {
         // fflush(NULL): flush all open streams.
         // Not implemented until stream table is in place (v0.5.10).
-        pass 0i64;
+        pass(0i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
-    if f.fd < 0i64 {
-        errno_set(EBADF);
-        pass FILE_EOF;
+    if (f->fd < 0i64) {
+        _?libn_errno_set(EBADF);
+        pass(FILE_EOF);
     }
 
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 {
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64) {
         // Flush write buffer
         Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            errno_set(r.err as int64);
-            pass FILE_EOF;
+        if (r.is_error) {
+            _?libn_errno_set(@cast_unchecked<int64>(r.err));
+            pass(FILE_EOF);
         }
     } else {
         // Read mode: seek back over unconsumed read buffer
-        drop bio_discard_read_buf(fp);
+        _?bio_discard_read_buf(fp);
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
diff --git a/src/io/bio/fopen.npk b/src/io/bio/fopen.npk
index 5085d4a..6631b04 100644
--- a/src/io/bio/fopen.npk
+++ b/src/io/bio/fopen.npk
@@ -1,15 +1,14 @@
 // fopen.npk — fopen, fclose, fdopen, freopen, fileno (v0.5.1)
 //
-// Stream lifecycle management. Every FILE* in libn is backed by an fd.
-// The lifecycle is:
-//
+// Stream lifecycle management. Every FILE* in libn (backed by an fd.
+// The lifecycle is) ? //
 //   fopen(path, mode)   → allocates FILE struct + I/O buffer, opens the fd
 //   fdopen(fd, mode)    → wraps an existing fd in a FILE struct
 //   freopen(path, mode, fp) → re-opens a stream in-place (path change or mode change)
 //   fileno(fp)          → returns the underlying fd
 //   fclose(fp)          → flushes, closes fd, frees buffer and FILE struct
 //
-// stdin/stdout/stderr are special-cased: their FILE structs are statically
+// stdin/stdout/stderr are special-cased : their FILE structs are statically
 // allocated in stdfiles.npk. fclose() on them flushes but does NOT close fd 0/1/2
 // and does NOT free their structs (they're not heap-allocated).
 //
@@ -35,55 +34,55 @@ fixed int64:FOPEN_CREAT_MODE = 0o666i64;  // masked by umask at open() time
 // Sets up the buffer, mode flags, and buffer mode. Called by fopen/fdopen.
 // Assumes the FILE struct fields are zeroed (out of bio_alloc_file or BSS).
 //
-// is_static: true if this FILE struct is NOT heap-allocated (stdin/stdout/stderr)
+// is_static: true if this FILE struct (NOT heap-allocated (stdin/stdout/stderr)
 //            so fclose() knows not to call slab_free() on it.
 // ════════════════════════════════════════════════════════════════════════════
 
-func:bio_init_stream = Result<int64>(int64:fp, int64:fd, int64:file_mode, bool:is_static) {
-    *FILE:f = fp as *FILE;
+func) ? bio_init_stream = int64(int64 : fp, int64:fd, int64:file_mode, bool:is_static) {
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
-    f.fd       = fd;
-    f.mode     = file_mode;
-    f.unget    = FILE_UNGET_EMPTY;
-    f.flags    = FILE_FLAG_INIT;
-    f.file_pos = 0i64;
-    f.buf_pos  = 0i64;
-    f.buf_len  = 0i64;
+    f->fd       = fd;
+    f->mode     = file_mode;
+    f->unget    = FILE_UNGET_EMPTY;
+    f->flags    = FILE_FLAG_INIT;
+    Result<int64>:pos_r = sys!!(SYS_LSEEK, fd, 0i64, SEEK_CUR, 0i64, 0i64, 0i64);
+    f->file_pos = pos_r ? 0i64;
+    f->buf_pos  = 0i64;
+    f->buf_len  = 0i64;
 
-    if is_static {
-        // Don't set BUF_OWN — the struct is in BSS, not slab
+    if (is_static) {
+        // Don't set BUF_OWN — the struct (in BSS, not slab
         // (buf ownership is separate from struct ownership)
     }
 
-    // Choose default buffering mode:
-    //   stderr (fd == 2): _IONBF — unbuffered for immediate error output
+    // Choose default buffering mode) ? //   stderr (fd == 2) : _IONBF — unbuffered for immediate error output
     //   stdout (fd == 1): _IOLBF — line-buffered (we can't detect tty here yet;
     //                              v0.30.x will upgrade to isatty() check)
     //   everything else:  _IOFBF — fully buffered
-    if fd == 2i64 {
-        f.buf_mode = _IONBF;
-        f.buf      = 0i64;
-        f.buf_cap  = 0i64;
-        pass 0i64;
+    if (fd == 2i64) {
+        f->buf_mode = _IONBF;
+        f->buf      = 0i64;
+        f->buf_cap  = 0i64;
+        pass(0i64);
     }
 
-    f.buf_mode = (fd == 1i64) ? _IOLBF : _IOFBF;
+    f->buf_mode = ((fd == 1i64)) ? _IOLBF : _IOFBF;
 
     // Allocate buffer
-    int64:buf = bio_alloc_buf(BUFSIZ);
-    if buf == 0i64 {
-        fail ENOMEM as tbb8;
+    int64:buf = bio_alloc_buf(BUFSIZ) ? 0i64;
+    if (buf == 0i64) {
+        fail(@cast_unchecked<tbb8>(ENOMEM));
     }
 
-    f.buf     = buf;
-    f.buf_cap = BUFSIZ;
-    f.flags   = f.flags | FILE_FLAG_BUF_OWN;
+    f->buf     = buf;
+    f->buf_cap = BUFSIZ;
+    f->flags   = f->flags | FILE_FLAG_BUF_OWN;
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// fopen — Open a file as a buffered stdio stream.
+// fopen — Open a @cast_unchecked<a>(file) buffered stdio stream.
 //
 // Parameters:
 //   path: NUL-terminated file path (as int64 pointer)
@@ -91,22 +90,22 @@ func:bio_init_stream = Result<int64>(int64:fp, int64:fd, int64:file_mode, bool:i
 //
 // Returns: FILE* (as int64) on success, or 0 (NULL) on error.
 //
-// On error: errno is set (ENOENT, EACCES, ENOMEM, etc.)
+// On error: errno (set (ENOENT, EACCES, ENOMEM, etc.)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fopen = int64(int64:path, int64:mode) {
-    if path == 0i64 || mode == 0i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+pub func) ? fopen = int64(int64 : path, int64:mode) {
+    if (path == 0i64 || mode == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
     // Parse mode string
-    stack int64:file_mode_out[1];
-    stack int64:open_flags_out[1];
-    int64:parse_ok = bio_parse_mode(mode, &file_mode_out[0] as int64, &open_flags_out[0] as int64);
-    if parse_ok != 0i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+    stack int64[1]:file_mode_out;
+    stack int64[1]:open_flags_out;
+    int64:parse_ok = _!bio_parse_mode(mode, @file_mode_out[0] => int64, @open_flags_out[0] => int64);
+    if (parse_ok != 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
     int64:file_mode  = file_mode_out[0];
@@ -114,31 +113,31 @@ pub func:fopen = int64(int64:path, int64:mode) {
 
     // Open the file (O_CLOEXEC added automatically by libn_open)
     Result<int64>:r = libn_open(path, open_flags, FOPEN_CREAT_MODE);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass 0i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(0i64);
     }
-    int64:fd = r.value;
+    int64:fd = r.val;
 
     // Allocate FILE struct
-    int64:fp = bio_alloc_file();
-    if fp == 0i64 {
-        drop libn_close(fd);
-        errno_set(ENOMEM);
-        pass 0i64;
+    int64:fp = bio_alloc_file() ? 0i64;
+    if (fp == 0i64) {
+        _?libn_close(fd);
+        _?libn_errno_set(ENOMEM);
+        pass(0i64);
     }
 
     // Initialize the stream
     Result<int64>:init_r = bio_init_stream(fp, fd, file_mode, false);
-    if init_r.is_error {
-        drop libn_close(fd);
-        bio_free_file(fp);
-        errno_set(ENOMEM);
-        pass 0i64;
+    if (init_r.is_error) {
+        _?libn_close(fd);
+        _?bio_free_file(fp);
+        _?libn_errno_set(ENOMEM);
+        pass(0i64);
     }
 
-    pass fp;
-}
+    pass(fp);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fdopen — Create a FILE stream from an existing file descriptor.
@@ -153,98 +152,103 @@ pub func:fopen = int64(int64:path, int64:mode) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fdopen = int64(int64:fd, int64:mode) {
-    if fd < 0i64 || mode == 0i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+    if (fd < 0i64 || mode == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
-    stack int64:file_mode_out[1];
-    stack int64:open_flags_out[1];
-    int64:parse_ok = bio_parse_mode(mode, &file_mode_out[0] as int64, &open_flags_out[0] as int64);
-    if parse_ok != 0i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+    stack int64[1]:file_mode_out;
+    stack int64[1]:open_flags_out;
+    int64:parse_ok = _!bio_parse_mode(mode, @file_mode_out[0] => int64, @open_flags_out[0] => int64);
+    if (parse_ok != 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
-    int64:fp = bio_alloc_file();
-    if fp == 0i64 {
-        errno_set(ENOMEM);
-        pass 0i64;
+    int64:fp = bio_alloc_file() ? 0i64;
+    if (fp == 0i64) {
+        _?libn_errno_set(ENOMEM);
+        pass(0i64);
     }
 
     Result<int64>:init_r = bio_init_stream(fp, fd, file_mode_out[0], false);
-    if init_r.is_error {
-        bio_free_file(fp);
-        errno_set(ENOMEM);
-        pass 0i64;
+    if (init_r.is_error) {
+        _?bio_free_file(fp);
+        _?libn_errno_set(ENOMEM);
+        pass(0i64);
     }
 
-    pass fp;
-}
+    pass(fp);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // freopen — Re-open a stream with a new path or mode.
 //
-// If path is non-NULL: flush and close old stream, open new path, reuse struct.
-// If path is NULL: flush, change mode flags on the same fd (limited support).
+// If path (non-NULL) ? flush and close old stream, open new path, reuse struct.
+// If path is NULL : flush, change mode flags on the same fd (limited support).
 //
 // Returns fp on success (same pointer), or 0 on error (fp is NOT valid after error).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:freopen = int64(int64:path, int64:mode, int64:fp) {
-    if mode == 0i64 || fp == 0i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+    if (mode == 0i64 || fp == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // Parse new mode
-    stack int64:file_mode_out[1];
-    stack int64:open_flags_out[1];
-    int64:parse_ok = bio_parse_mode(mode, &file_mode_out[0] as int64, &open_flags_out[0] as int64);
-    if parse_ok != 0i64 {
-        fclose(fp);
-        errno_set(EINVAL);
-        pass 0i64;
+    stack int64[1]:file_mode_out;
+    stack int64[1]:open_flags_out;
+    int64:parse_ok = _!bio_parse_mode(mode, @file_mode_out[0] => int64, @open_flags_out[0] => int64);
+    if (parse_ok != 0i64) {
+        _?fclose(fp);
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
     // Flush existing write buffer
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 {
-        drop bio_flush_write_buf(fp);
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64) {
+        _?bio_flush_write_buf(fp);
     }
 
-    int64:old_fd = f.fd;
-    bool:buf_owned = (f.flags & FILE_FLAG_BUF_OWN) != 0i64;
+    int64:old_fd = f->fd;
+    bool:buf_owned = (f->flags & FILE_FLAG_BUF_OWN) != 0i64;
 
-    if path != 0i64 {
+    if (path != 0i64) {
         // Open new path
         Result<int64>:r = libn_open(path, open_flags_out[0], FOPEN_CREAT_MODE);
-        if r.is_error {
+        if (r.is_error) {
             // POSIX: freopen closes fp even on error
-            drop libn_close(old_fd);
-            if buf_owned { bio_free_buf(f.buf, f.buf_cap); }
-            f.fd    = -1i64;
-            f.flags = FILE_FLAG_ERROR;
-            errno_set(r.err as int64);
-            pass 0i64;
+            _?libn_close(old_fd);
+            if (buf_owned) { _?bio_free_buf(f->buf, f->buf_cap); }
+            if ((f->flags & FILE_FLAG_STATIC) == 0i64) {
+                _?bio_free_file(fp);
+            } else {
+                f->fd    = -1i64;
+                f->flags = FILE_FLAG_ERROR;
+            }
+            _?libn_errno_set(@cast_unchecked<int64>(r.err));
+            pass(0i64);
         }
 
         // Close old fd, install new one
-        drop libn_close(old_fd);
-        f.fd = r.value;
+        _?libn_close(old_fd);
+        f->fd = r.val;
     }
 
     // Reset stream state
-    f.mode     = file_mode_out[0];
-    f.buf_pos  = 0i64;
-    f.buf_len  = 0i64;
-    f.file_pos = 0i64;
-    f.unget    = FILE_UNGET_EMPTY;
-    f.flags    = (f.flags & FILE_FLAG_BUF_OWN) | FILE_FLAG_INIT;  // preserve buf ownership
+    f->mode     = file_mode_out[0];
+    f->buf_pos  = 0i64;
+    f->buf_len  = 0i64;
+    f->file_pos = 0i64;
+    f->unget    = FILE_UNGET_EMPTY;
+    // [FIX A19-1.1a] Preserve both buffer ownership and static allocation flags
+    f->flags    = (f->flags & (FILE_FLAG_BUF_OWN | FILE_FLAG_STATIC)) | FILE_FLAG_INIT;
 
-    pass fp;
-}
+    pass(fp);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fclose — Flush and close a FILE stream.
@@ -254,80 +258,80 @@ pub func:freopen = int64(int64:path, int64:mode, int64:fp) {
 //
 // Returns 0 on success, EOF on error (POSIX: fclose sets errno on error).
 //
-// After fclose(), fp is invalid — do not use it.
-// Closing stdin/stdout/stderr: flushes but does NOT close fd 0/1/2, and does
+// After fclose(), fp (invalid — do not use it.
+// Closing stdin/stdout/stderr) ? flushes but does NOT close fd 0/1/2, and does
 // NOT free the statically allocated FILE structs.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fclose = int64(int64:fp) {
-    if fp == 0i64 {
-        errno_set(EINVAL);
-        pass FILE_EOF;
+pub func : fclose = int64(int64:fp) {
+    if (fp == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(FILE_EOF);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
     int64:ret = 0i64;
 
     // Flush write buffer
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 && f.buf_pos > 0i64 {
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64 && f->buf_pos > 0i64) {
         Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            errno_set(r.err as int64);
+        if (r.is_error) {
+            _?libn_errno_set(@cast_unchecked<int64>(r.err));
             ret = FILE_EOF;
             // Continue with close even on flush error
         }
     }
 
     // Close the fd (skip for stdin/stdout/stderr)
-    int64:fd = f.fd;
-    bool:is_std = (fd == 0i64 || fd == 1i64 || fd == 2i64);
-    if !is_std && fd >= 0i64 {
+    int64:fd = f->fd;
+    bool:is_std = (f->flags & FILE_FLAG_STATIC) != 0i64;  // [FIX A18-1.2] check static flag, not fd number
+    if (!is_std && fd >= 0i64) {
         Result<int64>:r = libn_close(fd);
-        if r.is_error && ret == 0i64 {
-            errno_set(r.err as int64);
+        if (r.is_error && ret == 0i64) {
+            _?libn_errno_set(@cast_unchecked<int64>(r.err));
             ret = FILE_EOF;
         }
     }
 
-    f.fd = -1i64;
+    f->fd = -1i64;
 
     // Free I/O buffer if we own it
-    if (f.flags & FILE_FLAG_BUF_OWN) != 0i64 && f.buf != 0i64 {
-        bio_free_buf(f.buf, f.buf_cap);
-        f.buf     = 0i64;
-        f.buf_cap = 0i64;
+    if ((f->flags & FILE_FLAG_BUF_OWN) != 0i64 && f->buf != 0i64) {
+        _?bio_free_buf(f->buf, f->buf_cap);
+        f->buf     = 0i64;
+        f->buf_cap = 0i64;
     }
 
     // Free FILE struct if heap-allocated (not stdin/stdout/stderr)
-    if !is_std {
-        bio_free_file(fp);
+    if (!is_std) {
+        _?bio_free_file(fp);
     } else {
         // Reset std stream to allow re-init if needed
-        f.flags    = 0i64;
-        f.buf_pos  = 0i64;
-        f.buf_len  = 0i64;
-        f.file_pos = 0i64;
-        f.unget    = FILE_UNGET_EMPTY;
+        f->flags    = FILE_FLAG_STATIC;  // [FIX A19-1.1b] Preserve the static flag!
+        f->buf_pos  = 0i64;
+        f->buf_len  = 0i64;
+        f->file_pos = 0i64;
+        f->unget    = FILE_UNGET_EMPTY;
     }
 
-    pass ret;
-}
+    pass(ret);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fileno — Return the underlying file descriptor for a stream.
 //
-// Returns the fd on success, or -1 with errno=EBADF if fp is invalid.
+// Returns the fd on success, or -1 with errno=EBADF if fp (invalid.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fileno = int64(int64:fp) {
-    if fp == 0i64 {
-        errno_set(EBADF);
-        pass -1i64;
+pub func) ? fileno = int64(int64 : fp) {
+    if (fp == 0i64) {
+        _?libn_errno_set(EBADF);
+        pass(-1i64);
     }
-    *FILE:f = fp as *FILE;
-    if f.fd < 0i64 {
-        errno_set(EBADF);
-        pass -1i64;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
+    if (f->fd < 0i64) {
+        _?libn_errno_set(EBADF);
+        pass(-1i64);
     }
-    pass f.fd;
-}
+    pass(f->fd);
+};
diff --git a/src/io/bio/fprintf.npk b/src/io/bio/fprintf.npk
index 0836037..58b1f03 100644
--- a/src/io/bio/fprintf.npk
+++ b/src/io/bio/fprintf.npk
@@ -10,33 +10,34 @@
 //
 // Naming:
 //   fprintf0..8  — FILE* versions of io_fprintf (fp + fmt + N args)
-//   printf0..8   — to stdout (std stream, not raw fd)
-//   eprintf0..8  — to stderr (std stream, not raw fd)
+//   printf0..8   — to stdout (std stream, not _!fd)
+//   eprintf0..8  — to stderr (std stream, not _!fd)
 //
 // vs. the existing io_printf* family:
-//   io_printf* in printf.npk targets raw fds (int64:fd).
+//   io_printf* in printf.npk targets _!fds (int64:fd).
 //   fprintf* here targets FILE* streams (int64:fp), going through the
 //   FILE buffer and eventually the bio/ write path.
 //   Use fprintf* for code that already uses FILE* streams; io_fprintf* for
-//   code that works at the raw fd level.
+//   code that works at the _!fd level.
 //
 // asprintf:
 //   Renders into a heap-allocated buffer sized to exactly fit the output.
-//   The caller is responsible for freeing the returned buffer via mem_free().
+//   The caller (responsible for freeing the returned buffer via mem_free().
 //   Returns the buffer pointer (as int64) and writes the length to *out_len.
 //
-// Thread safety: inherits FILE*'s non-thread-safe status at v0.6.x.
+// Thread safety) ? inherits FILE*'s non-thread-safe status at v0.6.x.
 
 use "src/syscall/errno.npk".*;
 use "src/str/strfmt.npk".*;
+use "src/str/strbuf.npk".*;
 use "src/str/strlen.npk".*;
 use "src/mem/slab.npk".*;
 use "src/io/bio/file.npk".*;
 use "src/io/bio/fio.npk".*;
 use "src/io/bio/stdfiles.npk".*;
 
-// Internal render buffer — same as printf.npk
-fixed int64:FPRINTF_BUF_SIZE = 4096i64;
+// Internal render buffer — @cast_unchecked<printf>(same).npk
+fixed int64 : FPRINTF_BUF_SIZE = 4096i64;
 
 // ════════════════════════════════════════════════════════════════════════════
 // Internal helper: render into stack buffer, emit via fwrite.
@@ -46,16 +47,16 @@ fixed int64:FPRINTF_BUF_SIZE = 4096i64;
 // ════════════════════════════════════════════════════════════════════════════
 
 func:bio_fprint_rendered = int64(int64:fp, int64:rendered_buf) {
-    int64:len = str_strlen(rendered_buf);
-    if len == 0i64 {
-        pass 0i64;
+    int64:len = _!str_strlen(rendered_buf);
+    if (len == 0i64) {
+        pass(0i64);
     }
-    int64:written = fwrite(rendered_buf, 1i64, len, fp);
-    if written < len {
-        pass -1i64;
+    int64:written = _!fwrite(rendered_buf, 1i64, len, fp);
+    if (written < len) {
+        pass(-1i64);
     }
-    pass written;
-}
+    pass(written);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fprintf — Write a formatted string to a FILE* stream.
@@ -64,88 +65,112 @@ func:bio_fprint_rendered = int64(int64:fp, int64:rendered_buf) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fprintf0 = int64(int64:fp, int64:fmt) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf0(buf_ptr, FPRINTF_BUF_SIZE, fmt);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf0(buf_ptr, FPRINTF_BUF_SIZE, fmt);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf0 = int64(int64:fmt) {
-    bio_ensure_std_init();
-    pass fprintf0(stdout_fp, fmt);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf0(stdout_fp, fmt);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf0 = int64(int64:fmt) {
-    bio_ensure_std_init();
-    pass fprintf0(stderr_fp, fmt);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf0(stderr_fp, fmt);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 1 format argument
 // ────────────────────────────────────────────────────────────────────────────
 
 pub func:fprintf1 = int64(int64:fp, int64:fmt, int64:a0) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf1(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf1(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf1 = int64(int64:fmt, int64:a0) {
-    bio_ensure_std_init();
-    pass fprintf1(stdout_fp, fmt, a0);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf1(stdout_fp, fmt, a0);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf1 = int64(int64:fmt, int64:a0) {
-    bio_ensure_std_init();
-    pass fprintf1(stderr_fp, fmt, a0);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf1(stderr_fp, fmt, a0);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 2 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
 pub func:fprintf2 = int64(int64:fp, int64:fmt, int64:a0, int64:a1) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf2(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf2(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf2 = int64(int64:fmt, int64:a0, int64:a1) {
-    bio_ensure_std_init();
-    pass fprintf2(stdout_fp, fmt, a0, a1);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf2(stdout_fp, fmt, a0, a1);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf2 = int64(int64:fmt, int64:a0, int64:a1) {
-    bio_ensure_std_init();
-    pass fprintf2(stderr_fp, fmt, a0, a1);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf2(stderr_fp, fmt, a0, a1);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 3 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
 pub func:fprintf3 = int64(int64:fp, int64:fmt, int64:a0, int64:a1, int64:a2) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf3(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf3(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf3 = int64(int64:fmt, int64:a0, int64:a1, int64:a2) {
-    bio_ensure_std_init();
-    pass fprintf3(stdout_fp, fmt, a0, a1, a2);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf3(stdout_fp, fmt, a0, a1, a2);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf3 = int64(int64:fmt, int64:a0, int64:a1, int64:a2) {
-    bio_ensure_std_init();
-    pass fprintf3(stderr_fp, fmt, a0, a1, a2);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf3(stderr_fp, fmt, a0, a1, a2);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 4 format arguments
@@ -153,22 +178,28 @@ pub func:eprintf3 = int64(int64:fmt, int64:a0, int64:a1, int64:a2) {
 
 pub func:fprintf4 = int64(int64:fp, int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf4(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf4(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf4 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
-    bio_ensure_std_init();
-    pass fprintf4(stdout_fp, fmt, a0, a1, a2, a3);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf4(stdout_fp, fmt, a0, a1, a2, a3);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf4 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
-    bio_ensure_std_init();
-    pass fprintf4(stderr_fp, fmt, a0, a1, a2, a3);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf4(stderr_fp, fmt, a0, a1, a2, a3);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 5 format arguments
@@ -176,24 +207,30 @@ pub func:eprintf4 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
 
 pub func:fprintf5 = int64(int64:fp, int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf5(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf5(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf5 = int64(int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    bio_ensure_std_init();
-    pass fprintf5(stdout_fp, fmt, a0, a1, a2, a3, a4);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf5(stdout_fp, fmt, a0, a1, a2, a3, a4);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf5 = int64(int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    bio_ensure_std_init();
-    pass fprintf5(stderr_fp, fmt, a0, a1, a2, a3, a4);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf5(stderr_fp, fmt, a0, a1, a2, a3, a4);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 6 format arguments
@@ -202,26 +239,32 @@ pub func:eprintf5 = int64(int64:fmt,
 pub func:fprintf6 = int64(int64:fp, int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3,
                            int64:a4, int64:a5) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf6(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf6(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf6 = int64(int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5) {
-    bio_ensure_std_init();
-    pass fprintf6(stdout_fp, fmt, a0, a1, a2, a3, a4, a5);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf6(stdout_fp, fmt, a0, a1, a2, a3, a4, a5);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf6 = int64(int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3,
                            int64:a4, int64:a5) {
-    bio_ensure_std_init();
-    pass fprintf6(stderr_fp, fmt, a0, a1, a2, a3, a4, a5);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf6(stderr_fp, fmt, a0, a1, a2, a3, a4, a5);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 7 format arguments
@@ -230,26 +273,32 @@ pub func:eprintf6 = int64(int64:fmt,
 pub func:fprintf7 = int64(int64:fp, int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3,
                            int64:a4, int64:a5, int64:a6) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf7(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf7(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf7 = int64(int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5, int64:a6) {
-    bio_ensure_std_init();
-    pass fprintf7(stdout_fp, fmt, a0, a1, a2, a3, a4, a5, a6);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf7(stdout_fp, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf7 = int64(int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3,
                            int64:a4, int64:a5, int64:a6) {
-    bio_ensure_std_init();
-    pass fprintf7(stderr_fp, fmt, a0, a1, a2, a3, a4, a5, a6);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf7(stderr_fp, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 8 format arguments
@@ -258,26 +307,32 @@ pub func:eprintf7 = int64(int64:fmt,
 pub func:fprintf8 = int64(int64:fp, int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3,
                            int64:a4, int64:a5, int64:a6, int64:a7) {
-    bio_ensure_std_init();
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf8(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-    pass bio_fprint_rendered(fp, buf_ptr);
-}
+    _?bio_ensure_std_init();
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf8(buf_ptr, FPRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    Result<int64>:r = bio_fprint_rendered(fp, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:printf8 = int64(int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5, int64:a6, int64:a7) {
-    bio_ensure_std_init();
-    pass fprintf8(stdout_fp, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf8(stdout_fp, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:eprintf8 = int64(int64:fmt,
                            int64:a0, int64:a1, int64:a2, int64:a3,
                            int64:a4, int64:a5, int64:a6, int64:a7) {
-    bio_ensure_std_init();
-    pass fprintf8(stderr_fp, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fprintf8(stderr_fp, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // asprintf — Render into a heap-allocated buffer, sized to fit the output.
@@ -285,7 +340,7 @@ pub func:eprintf8 = int64(int64:fmt,
 // Unlike sprintf/snprintf, asprintf does not require the caller to pre-size a
 // buffer. It renders into the 4096-byte stack buffer first; if the output fits,
 // it allocates exactly (len+1) bytes and copies. If the format output exceeds
-// 4095 bytes, the result is truncated (same as snprintf — a known limitation
+// 4095 bytes, the result (truncated (@cast_unchecked<snprintf>(same) — a known limitation
 // lifted in v0.7.x when the growable buffer is introduced).
 //
 // Returns a heap pointer (as int64) that the caller must free with mem_free().
@@ -293,67 +348,104 @@ pub func:eprintf8 = int64(int64:fmt,
 // Returns 0 on allocation failure.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:asprintf0 = int64(int64:out_len, int64:fmt) {
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf0(tmp_ptr, 4096i64, fmt);
-    int64:len = str_strlen(tmp_ptr);
-    int64:p = mem_malloc(len + 1i64);
-    if p == 0i64 { pass 0i64; }
-    drop mem_memcpy(p, tmp_ptr, len + 1i64);
-    if out_len != 0i64 { (out_len as *int64)[0] = len; }
-    pass p;
-}
+pub func) ? asprintf0 = int64(int64 : out_len, int64:fmt) {
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf0(sb, fmt);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
 
 pub func:asprintf1 = int64(int64:out_len, int64:fmt, int64:a0) {
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf1(tmp_ptr, 4096i64, fmt, a0);
-    int64:len = str_strlen(tmp_ptr);
-    int64:p = mem_malloc(len + 1i64);
-    if p == 0i64 { pass 0i64; }
-    drop mem_memcpy(p, tmp_ptr, len + 1i64);
-    if out_len != 0i64 { (out_len as *int64)[0] = len; }
-    pass p;
-}
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf1(sb, fmt, a0);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
 
 pub func:asprintf2 = int64(int64:out_len, int64:fmt, int64:a0, int64:a1) {
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf2(tmp_ptr, 4096i64, fmt, a0, a1);
-    int64:len = str_strlen(tmp_ptr);
-    int64:p = mem_malloc(len + 1i64);
-    if p == 0i64 { pass 0i64; }
-    drop mem_memcpy(p, tmp_ptr, len + 1i64);
-    if out_len != 0i64 { (out_len as *int64)[0] = len; }
-    pass p;
-}
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf2(sb, fmt, a0, a1);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
 
 pub func:asprintf3 = int64(int64:out_len, int64:fmt,
                             int64:a0, int64:a1, int64:a2) {
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf3(tmp_ptr, 4096i64, fmt, a0, a1, a2);
-    int64:len = str_strlen(tmp_ptr);
-    int64:p = mem_malloc(len + 1i64);
-    if p == 0i64 { pass 0i64; }
-    drop mem_memcpy(p, tmp_ptr, len + 1i64);
-    if out_len != 0i64 { (out_len as *int64)[0] = len; }
-    pass p;
-}
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf3(sb, fmt, a0, a1, a2);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
 
 pub func:asprintf4 = int64(int64:out_len, int64:fmt,
                             int64:a0, int64:a1, int64:a2, int64:a3) {
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf4(tmp_ptr, 4096i64, fmt, a0, a1, a2, a3);
-    int64:len = str_strlen(tmp_ptr);
-    int64:p = mem_malloc(len + 1i64);
-    if p == 0i64 { pass 0i64; }
-    drop mem_memcpy(p, tmp_ptr, len + 1i64);
-    if out_len != 0i64 { (out_len as *int64)[0] = len; }
-    pass p;
-}
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf4(sb, fmt, a0, a1, a2, a3);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
+
+pub func:asprintf5 = int64(int64:out_len, int64:fmt,
+                            int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf5(sb, fmt, a0, a1, a2, a3, a4);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
+
+pub func:asprintf6 = int64(int64:out_len, int64:fmt,
+                            int64:a0, int64:a1, int64:a2, int64:a3,
+                            int64:a4, int64:a5) {
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf6(sb, fmt, a0, a1, a2, a3, a4, a5);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
+
+pub func:asprintf7 = int64(int64:out_len, int64:fmt,
+                            int64:a0, int64:a1, int64:a2, int64:a3,
+                            int64:a4, int64:a5, int64:a6) {
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf7(sb, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
+
+pub func:asprintf8 = int64(int64:out_len, int64:fmt,
+                            int64:a0, int64:a1, int64:a2, int64:a3,
+                            int64:a4, int64:a5, int64:a6, int64:a7) {
+    int64:sb = _!strbuf_new();
+    int64:r = _!strbuf_appendf8(sb, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (r < 0i64) { _?strbuf_free(sb); pass(0i64); }
+    int64:p = _!strbuf_finish(sb);
+    if (p == 0i64) { pass(0i64); }
+    if (out_len != 0i64) { <-(@cast_unchecked<int64->>(out_len)) = _!str_strlen(p); }
+    pass(p);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // perror — Write "prefix: errno description\n" to stderr FILE* stream.
diff --git a/src/io/bio/fscanf.npk b/src/io/bio/fscanf.npk
index 8e2db94..41420d7 100644
--- a/src/io/bio/fscanf.npk
+++ b/src/io/bio/fscanf.npk
@@ -6,7 +6,7 @@
 // ── Supported conversions ──────────────────────────────────────────────────
 //
 //   %d / %i    — signed decimal integer → int64* output
-//   %u         — unsigned decimal integer → int64* (stored as unsigned bits)
+//   %u         — unsigned decimal integer → int64* (@cast_unchecked<unsigned>(stored) bits)
 //   %o         — unsigned octal integer → int64*
 //   %x / %X   — unsigned hex integer → int64*
 //   %s         — whitespace-delimited string → byte* output (NUL-terminated)
@@ -29,16 +29,14 @@
 //   Returns FILE_EOF (-1) if end-of-file was reached before any conversion.
 //
 // ── Architecture ──────────────────────────────────────────────────────────
-//   The core is bio_scan_engine(), which works on a generic "source" abstraction:
-//   either a FILE* stream (via fgetc) or a string (via pointer + index).
+//   The core (bio_scan_engine(), which works on a generic "source" abstraction) ? //   either a FILE* stream (via fgetc) or a string (via pointer + index).
 //   This avoids duplicating the parsing logic for fscanf vs sscanf.
 //
 //   fscanfN(fp, fmt, ...) — calls bio_scan_engine with FILE* source
 //   sscanfN(str, fmt, ...) — calls bio_scan_engine with string source
 //   scanfN(fmt, ...)  — calls fscanfN(stdin_fp, ...)
 //
-//   The source state is a 3-element int64 array:
-//     src[0] = source type: 0=FILE*, 1=string
+//   The source state is a 3-element int64 array : //     src[0] = source type: 0=FILE*, 1=string
 //     src[1] = FILE* pointer OR string pointer
 //     src[2] = current position (for strings) OR unused for FILE*
 //
@@ -61,33 +59,33 @@ fixed int64:SCAN_SRC_STRING = 1i64;   // src[1] = string pointer, src[2] = index
 
 // bio_scan_getc — Read one character from the source. Returns -1 on EOF.
 func:bio_scan_getc = int64(int64:src) {
-    *int64:s = src as *int64;
-    if s[0] == SCAN_SRC_FILE {
-        pass fgetc(s[1]);
+    int64->:s = @cast_unchecked<int64->>(src);
+    if (s[0] == SCAN_SRC_FILE) {
+        return fgetc(s[1]);
     } else {
         // String source
-        *byte:str = s[1] as *byte;
-        byte:c = str[s[2]];
-        if c == 0u8 {
-            pass -1i64;  // EOF = end of string
+        uint8->:str = @cast_unchecked<uint8->>(s[1]);
+        uint8:c = str[s[2]];
+        if (c == 0u8) {
+            pass(-1i64);  // EOF = end of string
         }
         s[2] = s[2] + 1i64;
-        pass c as int64;
+        pass(@cast_unchecked<int64>(c));
     }
-}
+};
 
 // bio_scan_ungetc — Push a character back into the source.
-func:bio_scan_ungetc = void(int64:c, int64:src) {
-    if c < 0i64 { pass; }  // don't push back EOF
-    *int64:s = src as *int64;
-    if s[0] == SCAN_SRC_FILE {
-        drop ungetc(c, s[1]);
+func:bio_scan_ungetc = NIL(int64:c, int64:src) {
+    if (c < 0i64) { pass(NIL); }  // don't push back EOF
+    int64->:s = @cast_unchecked<int64->>(src);
+    if (s[0] == SCAN_SRC_FILE) {
+        _?ungetc(c, s[1]);
     } else {
-        if s[2] > 0i64 {
+        if (s[2] > 0i64) {
             s[2] = s[2] - 1i64;
         }
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Parsing helpers
@@ -96,18 +94,18 @@ func:bio_scan_ungetc = void(int64:c, int64:src) {
 // bio_scan_skip_ws — Skip whitespace from source. Returns the first
 // non-whitespace character (without consuming it), or -1 on EOF.
 func:bio_scan_skip_ws = int64(int64:src) {
-    while true {
-        int64:c = bio_scan_getc(src);
-        if c < 0i64 { pass -1i64; }
+    while (true) {
+        int64:c = _!bio_scan_getc(src);
+        if (c < 0i64) { pass(-1i64); }
         // whitespace: space(32), tab(9), newline(10), CR(13), FF(12), VT(11)
         if c != 32i64 && c != 9i64 && c != 10i64 &&
            c != 13i64 && c != 12i64 && c != 11i64 {
-            bio_scan_ungetc(c, src);
-            pass c;
+            _?bio_scan_ungetc(c, src);
+            pass(c);
         }
     }
-    pass -1i64;  // unreachable
-}
+    pass(-1i64);  // unreachable
+};
 
 // bio_scan_int — Parse a signed decimal/octal/hex integer from source.
 // base: 10 (decimal), 8 (octal), 16 (hex), 0 (auto-detect 0x/0)
@@ -117,36 +115,38 @@ func:bio_scan_skip_ws = int64(int64:src) {
 // Returns 1 if a number was parsed, 0 otherwise.
 func:bio_scan_int = int64(int64:src, int64:base, int64:width,
                            int64:out, int64:consumed) {
-    int64:c = bio_scan_getc(src);
-    if c < 0i64 { pass 0i64; }
+    int64:c = _!bio_scan_getc(src);
+    if (c < 0i64) { pass(0i64); }
 
     bool:negative = false;
     int64:chars = 0i64;
-    int64:max = width > 0i64 ? width : 0x7FFFFFFFFFFFFFFFi64;
+    int64:max = (width > 0i64) ? width : 0x7FFFFFFFFFFFFFFFi64;
 
     // Optional sign
-    if c == 45i64 && chars < max {  // '-'
+    if (c == 45i64 && chars < max) {  // '-'
         negative = true;
         chars = chars + 1i64;
-        c = bio_scan_getc(src);
-    } else if c == 43i64 && chars < max {  // '+'
+        c = _!bio_scan_getc(src);
+    } else if (c == 43i64 && chars < max) {  // '+'
         chars = chars + 1i64;
-        c = bio_scan_getc(src);
+        c = _!bio_scan_getc(src);
     }
 
     // Auto-detect base if base == 0
-    if base == 0i64 {
-        if c == 48i64 && chars < max {  // '0'
+    int64:value = 0i64;       // [FIX A18-2.3b] moved before auto-detect so got_digit can be set there
+    bool:got_digit = false;   // [FIX A18-2.3b] moved before auto-detect
+    if (base == 0i64) {
+        if (c == 48i64 && chars < max) {  // '0'
             chars = chars + 1i64;
-            int64:next = bio_scan_getc(src);
-            if (next == 120i64 || next == 88i64) && chars < max {  // 'x' or 'X'
+            int64:next = _!bio_scan_getc(src);
+            if ((next == 120i64 || next == 88i64) && chars < max) {  // 'x' or 'X'
                 chars = chars + 1i64;
                 base = 16i64;
-                c = bio_scan_getc(src);
+                c = _!bio_scan_getc(src);
             } else {
-                bio_scan_ungetc(next, src);
                 base = 8i64;
-                // c is already '0', will contribute 0 to result
+                c = next;           // [FIX A18-2.3b] consume '0', advance to next char
+                got_digit = true;   // [FIX A18-2.3b] the '0' was a valid octal digit
             }
         } else {
             base = 10i64;
@@ -154,92 +154,88 @@ func:bio_scan_int = int64(int64:src, int64:base, int64:width,
     }
 
     // Require at least one digit
-    if c < 0i64 {
-        pass 0i64;
+    if (c < 0i64) {
+        if (!got_digit) { pass(0i64); }  // no digit at all (e.g. just a sign)
     }
 
-    int64:value = 0i64;
-    bool:got_digit = false;
-
-    while chars < max && c >= 0i64 {
+    while (chars < max && c >= 0i64) {
         int64:digit = -1i64;
 
-        if c >= 48i64 && c <= 57i64 {   // '0'..'9'
+        if (c >= 48i64 && c <= 57i64) {  // '0'..'9'
             digit = c - 48i64;
-        } else if base == 16i64 {
-            if c >= 65i64 && c <= 70i64 {  // 'A'..'F'
+        } else if (base == 16i64) {
+            if (c >= 65i64 && c <= 70i64) {  // 'A'..'F'
                 digit = c - 55i64;
-            } else if c >= 97i64 && c <= 102i64 {  // 'a'..'f'
+            } else if (c >= 97i64 && c <= 102i64) {  // 'a'..'f'
                 digit = c - 87i64;
             }
         }
 
-        if digit < 0i64 || digit >= base {
-            bio_scan_ungetc(c, src);
-            break;
+        if (digit < 0i64 || digit >= base) {
+            break;  // [FIX A18-2.3a] do NOT unget here; post-loop block handles it
         }
 
         value = value * base + digit;
         got_digit = true;
         chars = chars + 1i64;
-        c = bio_scan_getc(src);
+        c = _!bio_scan_getc(src);
     }
 
-    if !got_digit {
-        // Undo sign character if no digits followed
-        pass 0i64;
+    if (c >= 0i64) {
+        _?bio_scan_ungetc(c, src);
     }
 
-    if c >= 0i64 {
-        bio_scan_ungetc(c, src);
+    if (!got_digit) {
+        // Undo sign character if no digits followed
+        pass(0i64);
     }
 
-    if negative { value = 0i64 - value; }
+    if (negative) { value = 0i64 - value; }
 
-    if out != 0i64 {
-        (out as *int64)[0] = value;
+    if (out != 0i64) {
+        (@cast_unchecked<int64->>(out))[0] = value;
     }
 
-    if consumed != 0i64 {
-        (consumed as *int64)[0] = (consumed as *int64)[0] + chars;
+    if (consumed != 0i64) {
+        (@cast_unchecked<int64->>(consumed))[0] = (@cast_unchecked<int64->>(consumed))[0] + chars;
     }
 
-    pass 1i64;
-}
+    pass(1i64);
+};
 
 // bio_scan_str — Parse a whitespace-delimited string from source into buf.
 // width: max characters (0 = unlimited, but ALWAYS NUL-terminates)
 // Returns 1 on success, 0 on EOF before any character.
 func:bio_scan_str = int64(int64:src, int64:width, int64:buf, int64:consumed) {
-    int64:c = bio_scan_skip_ws(src);
-    if c < 0i64 { pass 0i64; }
+    int64:c = _!bio_scan_skip_ws(src);
+    if (c < 0i64) { pass(0i64); }
 
-    int64:max = width > 0i64 ? width : 0x7FFFFFFFFFFFFFFFi64;
+    int64:max = (width > 0i64) ? width : 0x7FFFFFFFFFFFFFFFi64;
     int64:count = 0i64;
 
-    while count < max {
-        c = bio_scan_getc(src);
-        if c < 0i64 { break; }
+    while (count < max) {
+        c = _!bio_scan_getc(src);
+        if (c < 0i64) { break; }
         // Stop at whitespace
         if c == 32i64 || c == 9i64 || c == 10i64 ||
            c == 13i64 || c == 12i64 || c == 11i64 {
-            bio_scan_ungetc(c, src);
+            _?bio_scan_ungetc(c, src);
             break;
         }
-        if buf != 0i64 {
-            (buf as *byte)[count] = c as byte;
+        if (buf != 0i64) {
+            (@cast_unchecked<uint8->>(buf))[count] = @cast_unchecked<uint8>(c);
         }
         count = count + 1i64;
     }
 
-    if count == 0i64 { pass 0i64; }
-    if buf != 0i64 { (buf as *byte)[count] = 0u8; }
+    if (count == 0i64) { pass(0i64); }
+    if (buf != 0i64) { (@cast_unchecked<uint8->>(buf))[count] = 0u8; }
 
-    if consumed != 0i64 {
-        (consumed as *int64)[0] = (consumed as *int64)[0] + count;
+    if (consumed != 0i64) {
+        (@cast_unchecked<int64->>(consumed))[0] = (@cast_unchecked<int64->>(consumed))[0] + count;
     }
-    pass 1i64;
-}
+    pass(1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // bio_scan_engine — Core scanner: process a format string against a source.
@@ -247,41 +243,41 @@ func:bio_scan_str = int64(int64:src, int64:width, int64:buf, int64:consumed) {
 // args: pointer to int64 array of output pointer arguments
 // nargs: number of elements in args
 //
-// Returns number of items matched (stored), or FILE_EOF if first char is EOF.
+// Returns number of items matched (stored), or FILE_EOF if first char (EOF.
 // ════════════════════════════════════════════════════════════════════════════
 
-func:bio_scan_engine = int64(int64:src, int64:fmt, int64:args, int64:nargs) {
-    *byte:f      = fmt as *byte;
-    *int64:argv  = args as *int64;
+func) ? bio_scan_engine = int64(int64 : src, int64:fmt, int64:args, int64:nargs) {
+    uint8->:f      = @cast_unchecked<uint8->>(fmt);
+    int64->:argv  = @cast_unchecked<int64->>(args);
     int64:fi     = 0i64;    // format string index
     int64:ai     = 0i64;    // argument index
     int64:matched = 0i64;   // items successfully stored
     int64:consumed = 0i64;  // total bytes consumed from source
     bool:first   = true;    // for EOF-before-any-conversion detection
 
-    while f[fi] != 0u8 {
-        byte:ch = f[fi];
+    while (f[fi] != 0u8) {
+        uint8:ch = f[fi];
         fi = fi + 1i64;
 
-        if ch != 37u8 {  // not '%' — literal character match
+        if (ch != 37u8) {  // not '%' — literal character match
             // Whitespace in format matches zero or more whitespace in input
-            if ch == 32u8 || ch == 9u8 || ch == 10u8 {
-                int64:c = bio_scan_skip_ws(src);
-                // Don't fail on EOF here — whitespace is optional
-                if c >= 0i64 {
+            if (ch == 32u8 || ch == 9u8 || ch == 10u8) {
+                int64:c = _!bio_scan_skip_ws(src);
+                // Don't fail on EOF here — whitespace (optional
+                if (c >= 0i64) {
                     first = false;
                 }
             } else {
-                // Literal: must match exactly
-                int64:c = bio_scan_getc(src);
-                if c < 0i64 {
-                    if first { pass FILE_EOF; }
-                    pass matched;
+                // Literal) ? must match exactly
+                int64 : c = _!bio_scan_getc(src);
+                if (c < 0i64) {
+                    if (first) { pass(FILE_EOF); }
+                    pass(matched);
                 }
                 first = false;
-                if c != ch as int64 {
-                    bio_scan_ungetc(c, src);
-                    pass matched;  // mismatch
+                if (c != @cast_unchecked<int64>(ch)) {
+                    _?bio_scan_ungetc(c, src);
+                    pass(matched);  // mismatch
                 }
                 consumed = consumed + 1i64;
             }
@@ -291,15 +287,15 @@ func:bio_scan_engine = int64(int64:src, int64:fmt, int64:args, int64:nargs) {
         // '%' — parse conversion specifier
         // Check for '*' suppressor
         bool:suppress = false;
-        if f[fi] == 42u8 {  // '*'
+        if (f[fi] == 42u8) {  // '*'
             suppress = true;
             fi = fi + 1i64;
         }
 
         // Parse optional width
         int64:width = 0i64;
-        while f[fi] >= 48u8 && f[fi] <= 57u8 {  // '0'..'9'
-            width = width * 10i64 + (f[fi] as int64 - 48i64);
+        while (f[fi] >= 48u8 && f[fi] <= 57u8) {  // '0'..'9'
+            width = width * 10i64 + (f[fi] => int64 - 48i64);
             fi = fi + 1i64;
         }
 
@@ -307,94 +303,93 @@ func:bio_scan_engine = int64(int64:src, int64:fmt, int64:args, int64:nargs) {
         if f[fi] == 108u8 || f[fi] == 104u8 || f[fi] == 122u8 ||
            f[fi] == 106u8 || f[fi] == 116u8 {
             fi = fi + 1i64;
-            if f[fi] == 108u8 || f[fi] == 104u8 {  // ll / hh
+            if (f[fi] == 108u8 || f[fi] == 104u8) {  // ll / hh
                 fi = fi + 1i64;
             }
         }
 
-        byte:spec = f[fi];
+        uint8:spec = f[fi];
         fi = fi + 1i64;
 
+        if (spec == 37u8) {  // '%%' — literal percent
+            int64:c = _!bio_scan_getc(src);
+            if (c < 0i64) { if (first) { pass(FILE_EOF); } pass(matched); }
+            first = false;
+            if (c != 37i64) {
+                _?bio_scan_ungetc(c, src);
+                pass(matched);
+            }
+            consumed = consumed + 1i64;
+            continue;
+        }
+
         // Get the output pointer (or 0 if suppressing)
         int64:out = 0i64;
-        if !suppress && ai < nargs {
+        if (!suppress && ai < nargs) {
             out = argv[ai];
             ai = ai + 1i64;
-        } else if !suppress {
+        } else if (!suppress) {
             // Ran out of arguments — stop
-            pass matched;
-        }
-
-        if spec == 37u8 {  // '%%' — literal percent
-            int64:c = bio_scan_getc(src);
-            if c < 0i64 { if first { pass FILE_EOF; } pass matched; }
-            first = false;
-            if c != 37i64 {
-                bio_scan_ungetc(c, src);
-                pass matched;
-            }
-            consumed = consumed + 1i64;
-            // No assignment for %%
-            if !suppress { ai = ai - 1i64; }  // undo arg advance
-            continue;
+            pass(matched);
         }
 
-        if spec == 110u8 {  // '%n' — store consumed count
-            if out != 0i64 {
-                (out as *int64)[0] = consumed;
+        if (spec == 110u8) {  // '%n' — store consumed count
+            if (out != 0i64) {
+                (@cast_unchecked<int64->>(out))[0] = consumed;
             }
             // %n does NOT increment matched count (POSIX)
             continue;
         }
 
-        if spec == 99u8 {  // '%c' — single character, no whitespace skip
-            int64:c = bio_scan_getc(src);
-            if c < 0i64 { if first { pass FILE_EOF; } pass matched; }
+        if (spec == 99u8) {  // '%c' — single character, no whitespace skip
+            int64:c = _!bio_scan_getc(src);
+            if (c < 0i64) { if (first) { pass(FILE_EOF); } pass(matched); }
             first = false;
             consumed = consumed + 1i64;
-            if out != 0i64 {
-                (out as *byte)[0] = c as byte;
+            if (out != 0i64) {
+                (@cast_unchecked<uint8->>(out))[0] = @cast_unchecked<uint8>(c);
             }
             matched = matched + 1i64;
             continue;
         }
 
-        if spec == 115u8 {  // '%s' — whitespace-delimited string
-            int64:c = bio_scan_skip_ws(src);
-            if c < 0i64 { if first { pass FILE_EOF; } pass matched; }
+        if (spec == 115u8) {
+            if (width == 0i64 && out != 0i64) { fail(@cast_unchecked<tbb8>(ERR_BADARG)); }  // '%s' — whitespace-delimited string
+            int64:c = _!bio_scan_skip_ws(src);
+            if (c < 0i64) { if (first) { pass(FILE_EOF); } pass(matched); }
             first = false;
-            int64:r = bio_scan_str(src, width, out, &consumed);
-            if r == 0i64 { pass matched; }
+            int64:r = _!bio_scan_str(src, width, out, @consumed);
+            if (r == 0i64) { pass(matched); }
             matched = matched + 1i64;
             continue;
         }
 
         // Integer conversions — all skip leading whitespace
-        int64:c = bio_scan_skip_ws(src);
-        if c < 0i64 { if first { pass FILE_EOF; } pass matched; }
+        int64:c = _!bio_scan_skip_ws(src);
+        if (c < 0i64) { if (first) { pass(FILE_EOF); } pass(matched); }
         first = false;
 
         int64:base = 10i64;
-        if spec == 100u8 || spec == 105u8 {  // %d, %i
-            base = (spec == 105u8) ? 0i64 : 10i64;  // %i auto-detects base
-        } else if spec == 117u8 {  // %u
+        if (spec == 100u8 || spec == 105u8) {  // %d, %i
+            base = ((spec == 105u8)) ? 0i64 : 10i64;  // %i auto-detects base
+        } else if (spec == 117u8) {  // %u
             base = 10i64;
-        } else if spec == 111u8 {  // %o
+        } else if (spec == 111u8) {  // %o
             base = 8i64;
-        } else if spec == 120u8 || spec == 88u8 {  // %x, %X
+        } else if (spec == 120u8 || spec == 88u8) {  // %x, %X
             base = 16i64;
         } else {
             // Unknown specifier — skip
             continue;
         }
 
-        int64:r = bio_scan_int(src, base, width, out, &consumed);
-        if r == 0i64 { pass matched; }
+        int64:r = _!bio_scan_int(src, base, width, out, @consumed);
+        if (r == 0i64) { pass(matched); }
         matched = matched + 1i64;
     }
 
-    pass matched;
-}
+    pass(matched);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fscanf — Read from FILE* stream, parse according to format.
@@ -405,230 +400,248 @@ func:bio_scan_engine = int64(int64:src, int64:fmt, int64:args, int64:nargs) {
 // Each fscanfN allocates a 3-element int64 src array.
 
 pub func:fscanf0 = int64(int64:fp, int64:fmt) {
-    bio_ensure_std_init();
-    stack int64:src[3];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, 0i64, 0i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, 0i64, 0i64);
+};
 
 pub func:fscanf1 = int64(int64:fp, int64:fmt, int64:a0) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[1];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[1]:argv;
     argv[0] = a0;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 1i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 1i64);
+};
 
 pub func:fscanf2 = int64(int64:fp, int64:fmt, int64:a0, int64:a1) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[2];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[2]:argv;
     argv[0] = a0; argv[1] = a1;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 2i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 2i64);
+};
 
 pub func:fscanf3 = int64(int64:fp, int64:fmt,
                           int64:a0, int64:a1, int64:a2) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[3];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[3]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 3i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 3i64);
+};
 
 pub func:fscanf4 = int64(int64:fp, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[4];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[4]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 4i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 4i64);
+};
 
 pub func:fscanf5 = int64(int64:fp, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[5];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[5]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3; argv[4] = a4;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 5i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 5i64);
+};
 
 pub func:fscanf6 = int64(int64:fp, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[6];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[6]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 6i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 6i64);
+};
 
 pub func:fscanf7 = int64(int64:fp, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5, int64:a6) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[7];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[7]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5; argv[6] = a6;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 7i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 7i64);
+};
 
 pub func:fscanf8 = int64(int64:fp, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5, int64:a6, int64:a7) {
-    bio_ensure_std_init();
-    stack int64:src[3];
-    stack int64:argv[8];
+    _?bio_ensure_std_init();
+    stack int64[3]:src;
+    stack int64[8]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5; argv[6] = a6; argv[7] = a7;
     src[0] = SCAN_SRC_FILE; src[1] = fp; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 8i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 8i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // sscanf — Parse from a NUL-terminated string instead of a FILE* stream.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:sscanf0 = int64(int64:str, int64:fmt) {
-    stack int64:src[3];
+    stack int64[3]:src;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, 0i64, 0i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, 0i64, 0i64);
+};
 
 pub func:sscanf1 = int64(int64:str, int64:fmt, int64:a0) {
-    stack int64:src[3];
-    stack int64:argv[1];
+    stack int64[3]:src;
+    stack int64[1]:argv;
     argv[0] = a0;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 1i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 1i64);
+};
 
 pub func:sscanf2 = int64(int64:str, int64:fmt, int64:a0, int64:a1) {
-    stack int64:src[3];
-    stack int64:argv[2];
+    stack int64[3]:src;
+    stack int64[2]:argv;
     argv[0] = a0; argv[1] = a1;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 2i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 2i64);
+};
 
 pub func:sscanf3 = int64(int64:str, int64:fmt,
                           int64:a0, int64:a1, int64:a2) {
-    stack int64:src[3];
-    stack int64:argv[3];
+    stack int64[3]:src;
+    stack int64[3]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 3i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 3i64);
+};
 
 pub func:sscanf4 = int64(int64:str, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3) {
-    stack int64:src[3];
-    stack int64:argv[4];
+    stack int64[3]:src;
+    stack int64[4]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 4i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 4i64);
+};
 
 pub func:sscanf5 = int64(int64:str, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    stack int64:src[3];
-    stack int64:argv[5];
+    stack int64[3]:src;
+    stack int64[5]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3; argv[4] = a4;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 5i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 5i64);
+};
 
 pub func:sscanf6 = int64(int64:str, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5) {
-    stack int64:src[3];
-    stack int64:argv[6];
+    stack int64[3]:src;
+    stack int64[6]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 6i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 6i64);
+};
 
 pub func:sscanf7 = int64(int64:str, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5, int64:a6) {
-    stack int64:src[3];
-    stack int64:argv[7];
+    stack int64[3]:src;
+    stack int64[7]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5; argv[6] = a6;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 7i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 7i64);
+};
 
 pub func:sscanf8 = int64(int64:str, int64:fmt,
                           int64:a0, int64:a1, int64:a2, int64:a3,
                           int64:a4, int64:a5, int64:a6, int64:a7) {
-    stack int64:src[3];
-    stack int64:argv[8];
+    stack int64[3]:src;
+    stack int64[8]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5; argv[6] = a6; argv[7] = a7;
     src[0] = SCAN_SRC_STRING; src[1] = str; src[2] = 0i64;
-    pass bio_scan_engine(&src[0] as int64, fmt, &argv[0] as int64, 8i64);
-}
+    return bio_scan_engine(@src[0] => int64, fmt, @argv[0] => int64, 8i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // scanf — Read from stdin.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:scanf0 = int64(int64:fmt) {
-    bio_ensure_std_init();
-    pass fscanf0(stdin_fp, fmt);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf0(stdin_fp, fmt);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf1 = int64(int64:fmt, int64:a0) {
-    bio_ensure_std_init();
-    pass fscanf1(stdin_fp, fmt, a0);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf1(stdin_fp, fmt, a0);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf2 = int64(int64:fmt, int64:a0, int64:a1) {
-    bio_ensure_std_init();
-    pass fscanf2(stdin_fp, fmt, a0, a1);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf2(stdin_fp, fmt, a0, a1);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf3 = int64(int64:fmt, int64:a0, int64:a1, int64:a2) {
-    bio_ensure_std_init();
-    pass fscanf3(stdin_fp, fmt, a0, a1, a2);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf3(stdin_fp, fmt, a0, a1, a2);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf4 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
-    bio_ensure_std_init();
-    pass fscanf4(stdin_fp, fmt, a0, a1, a2, a3);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf4(stdin_fp, fmt, a0, a1, a2, a3);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf5 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    bio_ensure_std_init();
-    pass fscanf5(stdin_fp, fmt, a0, a1, a2, a3, a4);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf5(stdin_fp, fmt, a0, a1, a2, a3, a4);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf6 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3,
                          int64:a4, int64:a5) {
-    bio_ensure_std_init();
-    pass fscanf6(stdin_fp, fmt, a0, a1, a2, a3, a4, a5);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf6(stdin_fp, fmt, a0, a1, a2, a3, a4, a5);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf7 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3,
                          int64:a4, int64:a5, int64:a6) {
-    bio_ensure_std_init();
-    pass fscanf7(stdin_fp, fmt, a0, a1, a2, a3, a4, a5, a6);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf7(stdin_fp, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 pub func:scanf8 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3,
                          int64:a4, int64:a5, int64:a6, int64:a7) {
-    bio_ensure_std_init();
-    pass fscanf8(stdin_fp, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-}
+    _?bio_ensure_std_init();
+    Result<int64>:r = fscanf8(stdin_fp, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
diff --git a/src/io/bio/fseek.npk b/src/io/bio/fseek.npk
index 5cb13c9..3afcf28 100644
--- a/src/io/bio/fseek.npk
+++ b/src/io/bio/fseek.npk
@@ -5,16 +5,14 @@
 // fseek:
 //   Moves the file position indicator for fp. Discards the ungetc pushback char.
 //   Clears the EOF flag (seeking to a valid position makes the stream readable again).
-//   If fp is in write mode, flushes the write buffer before seeking.
+//   If fp (in write mode, flushes the write buffer before seeking.
 //   If fp is in read mode, discards the unread bytes in the read buffer.
 //
-// ftell:
-//   Returns the current logical file position (bytes from start of file).
+// ftell) ? //   Returns the current logical file position (bytes from start of file).
 //   Accounts for buffered but unread bytes (subtracts them from kernel position).
 //   Accounts for pending unget char.
 //
-// rewind:
-//   Equivalent to fseek(fp, 0, SEEK_SET) + clearerr(fp).
+// rewind : //   Equivalent to fseek(fp, 0, SEEK_SET) + clearerr(fp).
 //   Unlike fseek, rewind does not return an error value.
 //
 // fgetpos / fsetpos:
@@ -31,7 +29,7 @@ use "src/io/bio/file.npk".*;
 // fseek — Move the file position indicator.
 //
 // Parameters:
-//   fp:     FILE* (as int64)
+//   fp:     FILE*
 //   offset: signed byte offset (interpreted relative to whence)
 //   whence: SEEK_SET (from start), SEEK_CUR (from current), SEEK_END (from end)
 //
@@ -39,106 +37,108 @@ use "src/io/bio/file.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fseek = int64(int64:fp, int64:offset, int64:whence) {
-    if fp == 0i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+    if (fp == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
-    if whence < SEEK_SET || whence > SEEK_END {
-        errno_set(EINVAL);
-        pass -1i64;
+    if (whence < SEEK_SET || whence > SEEK_END) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
-    if f.fd < 0i64 {
-        errno_set(EBADF);
-        pass -1i64;
+    if (f->fd < 0i64) {
+        _?libn_errno_set(EBADF);
+        pass(-1i64);
     }
 
     // Flush write buffer if dirty
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 && f.buf_pos > 0i64 {
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64 && f->buf_pos > 0i64) {
         Result<int64>:r = bio_flush_write_buf(fp);
-        if r.is_error {
-            errno_set(r.err as int64);
-            pass -1i64;
+        if (r.is_error) {
+            _?libn_errno_set(@cast_unchecked<int64>(r.err));
+            pass(-1i64);
         }
     }
 
     // For SEEK_CUR: adjust for unread buffered bytes to get the true logical offset
     int64:real_offset = offset;
-    if whence == SEEK_CUR {
-        // The kernel's position is ahead by (buf_len - buf_pos) unread bytes
-        int64:buffered_ahead = f.buf_len - f.buf_pos;
-        if f.unget != FILE_UNGET_EMPTY {
+    if (whence == SEEK_CUR) {
+        // The kernel's position (ahead by (buf_len - buf_pos) unread bytes
+        int64) ? buffered_ahead = f->buf_len - f->buf_pos;
+        if (f->unget != FILE_UNGET_EMPTY) {
             buffered_ahead = buffered_ahead + 1i64;
         }
         real_offset = offset - buffered_ahead;
     }
 
     // Discard read buffer (kernel will be moved to new position)
-    f.buf_pos = 0i64;
-    f.buf_len = 0i64;
+    f->buf_pos = 0i64;
+    f->buf_len = 0i64;
 
-    // Discard ungetc pushback (POSIX: fseek cancels ungetc)
-    f.unget = FILE_UNGET_EMPTY;
+    // Discard ungetc pushback (POSIX : fseek cancels ungetc)
+    f->unget = FILE_UNGET_EMPTY;
 
     // Issue the actual lseek
-    Result<int64>:r = sys_safe(SYS_LSEEK, f.fd, real_offset, whence, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    Result<int64>:r = sys!!(SYS_LSEEK, f->fd, real_offset, whence, 0i64, 0i64, 0i64);
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
 
     // Update logical file position
-    f.file_pos = r.value;
+    f->file_pos = r.val;
 
     // Clear EOF flag — a successful seek makes the stream readable
-    f.flags = f.flags & (0i64 - 1i64 ^ FILE_FLAG_EOF);
-    // Clear write-mode flag (position is now clean)
-    f.flags = f.flags & (0i64 - 1i64 ^ FILE_FLAG_WRITE_MODE);
+    f->flags = f->flags & ~FILE_FLAG_EOF;
+    // Clear write-mode flag (position (now clean)
+    f->flags = f->flags & ~FILE_FLAG_WRITE_MODE;
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// ftell — Return the current file position as a byte offset from the start.
+// ftell — Return the current file @cast_unchecked<a>(position) byte offset from the start.
 //
-// Returns the position as int64 (>= 0), or -1 on error (errno set).
+// Returns the @cast_unchecked<int64>(position) (>= 0), or -1 on error (errno set).
 //
-// Accounts for:
-//   - Buffered unread data (subtracts it from the kernel position)
+// Accounts for) ? //   - Buffered unread data (subtracts it from the kernel position)
 //   - Pending write buffer (adds buf_pos to kernel position)
 //   - Pushed-back unget char (subtracts 1 from position)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:ftell = int64(int64:fp) {
-    if fp == 0i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+pub func : ftell = int64(int64:fp) {
+    if (fp == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
-    if f.fd < 0i64 {
-        errno_set(EBADF);
-        pass -1i64;
+    if (f->fd < 0i64) {
+        _?libn_errno_set(EBADF);
+        pass(-1i64);
     }
 
     // Use the logical position we track — this avoids a syscall for the common case
-    // and is always accurate as long as our buffer tracking is correct.
-    int64:pos = f.file_pos;
+    // and (always @cast_unchecked<long>(accurate) as our buffer tracking is correct.
+    int64) ? pos = f->file_pos;
 
-    // Unget char moves the position back by 1
-    if f.unget != FILE_UNGET_EMPTY {
-        pos = pos - 1i64;
+    if (((f->flags & FILE_FLAG_WRITE_MODE) != 0i64)) {
+        pos = pos + f->buf_pos;
     }
 
-    if pos < 0i64 {
+    // [FIX A18-3.1a] Do NOT subtract 1 for unget here.
+    // ungetc() already decrements f->file_pos when it pushes back a char.
+    // Subtracting again would give pos - 2 instead of the correct pos - 1.
+
+    if (pos < 0i64) {
         pos = 0i64;
     }
 
-    pass pos;
-}
+    pass(pos);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // rewind — Seek to the beginning of the file and clear error/EOF flags.
@@ -147,51 +147,51 @@ pub func:ftell = int64(int64:fp) {
 // on non-seekable streams (POSIX). Use fseek() if you need error propagation.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:rewind = void(int64:fp) {
-    if fp == 0i64 { pass; }
-    *FILE:f = fp as *FILE;
+pub func : rewind = NIL(int64:fp) {
+    if (fp == 0i64) { pass(NIL); }
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // Flush write buffer
-    if (f.flags & FILE_FLAG_WRITE_MODE) != 0i64 {
-        drop bio_flush_write_buf(fp);
+    if ((f->flags & FILE_FLAG_WRITE_MODE) != 0i64) {
+        _?bio_flush_write_buf(fp);
     }
 
     // Discard read buffer and unget
-    f.buf_pos = 0i64;
-    f.buf_len = 0i64;
-    f.unget   = FILE_UNGET_EMPTY;
+    f->buf_pos = 0i64;
+    f->buf_len = 0i64;
+    f->unget   = FILE_UNGET_EMPTY;
 
     // Seek to start (ignore error)
-    drop sys_safe(SYS_LSEEK, f.fd, 0i64, SEEK_SET, 0i64, 0i64, 0i64);
-    f.file_pos = 0i64;
+    _?sys!!(SYS_LSEEK, f->fd, 0i64, SEEK_SET, 0i64, 0i64, 0i64);
+    f->file_pos = 0i64;
 
-    // Clear all error and EOF flags (this is what distinguishes rewind from fseek)
-    f.flags = f.flags & (0i64 - 1i64 ^ FILE_FLAG_EOF ^ FILE_FLAG_ERROR ^ FILE_FLAG_WRITE_MODE);
-}
+    // Clear all error and EOF flags (this (what distinguishes rewind from fseek)
+    f->flags = f->flags & ~(FILE_FLAG_EOF | FILE_FLAG_ERROR | FILE_FLAG_WRITE_MODE);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fgetpos — Store the current position in *pos.
 //
 // In libn at v0.5.4, fpos_t is simply int64 (a byte offset).
-// pos: pointer to int64 — receives the current position.
+// pos) ? pointer to int64 — receives the current position.
 //
 // Returns 0 on success, -1 on error.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fgetpos = int64(int64:fp, int64:pos) {
-    if fp == 0i64 || pos == 0i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+pub func : fgetpos = int64(int64:fp, int64:pos) {
+    if (fp == 0i64 || pos == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    int64:p = ftell(fp);
-    if p < 0i64 {
-        pass -1i64;
+    int64:p = _!ftell(fp);
+    if (p < 0i64) {
+        pass(-1i64);
     }
 
-    (pos as *int64)[0] = p;
-    pass 0i64;
-}
+    <-(@cast_unchecked<int64->>(pos)) = p;
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fsetpos — Restore a position stored by fgetpos.
@@ -201,11 +201,11 @@ pub func:fgetpos = int64(int64:fp, int64:pos) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fsetpos = int64(int64:fp, int64:pos) {
-    if fp == 0i64 || pos == 0i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+    if (fp == 0i64 || pos == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    int64:target = (pos as *int64)[0];
-    pass fseek(fp, target, SEEK_SET);
-}
+    int64:target = <-(@cast_unchecked<int64->>(pos));
+    return fseek(fp, target, SEEK_SET);
+};
diff --git a/src/io/bio/fstate.npk b/src/io/bio/fstate.npk
index 57d3eae..7c57384 100644
--- a/src/io/bio/fstate.npk
+++ b/src/io/bio/fstate.npk
@@ -2,15 +2,15 @@
 //
 // Stream state query and buffer configuration.
 //
-// feof(fp):    Returns non-zero if the end-of-file indicator is set.
-// ferror(fp):  Returns non-zero if the error indicator is set.
-// clearerr(fp): Clears both the EOF and error indicators.
+// feof(fp):    Returns non-zero if the end-of-file indicator (set.
+// ferror(fp)) ? Returns non-zero if the error indicator is set.
+// clearerr(fp) : Clears both the EOF and error indicators.
 //
 // Note: feof() returns non-zero only AFTER a read attempt has encountered EOF.
 // It is NOT a look-ahead (unlike feof in some educational contexts). The correct
 // idiom is:
-//   while fgetc(fp) != FILE_EOF { ... }
-//   if ferror(fp) { /* error */ } else { /* normal EOF */ }
+//   while (fgetc(fp) != FILE_EOF) { ... }
+//   if (ferror(fp)) { /* error */ } else { /* normal EOF */ }
 //
 // setvbuf — set buffering mode and optionally supply a user buffer.
 // setbuf  — simplified interface (buf=NULL for unbuffered, else BUFSIZ-buffered).
@@ -22,36 +22,36 @@ use "src/io/bio/file.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 // feof — Test the end-of-file indicator.
 //
-// Returns non-zero if FILE_FLAG_EOF is set, zero otherwise.
+// Returns non-zero if FILE_FLAG_EOF (set, zero otherwise.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:feof = int64(int64:fp) {
-    if fp == 0i64 {
-        pass 0i64;
+pub func) ? feof = int64(int64 : fp) {
+    if (fp == 0i64) {
+        pass(0i64);
     }
-    *FILE:f = fp as *FILE;
-    if (f.flags & FILE_FLAG_EOF) != 0i64 {
-        pass 1i64;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
+    if ((f->flags & FILE_FLAG_EOF) != 0i64) {
+        pass(1i64);
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // ferror — Test the error indicator.
 //
-// Returns non-zero if FILE_FLAG_ERROR is set, zero otherwise.
+// Returns non-zero if FILE_FLAG_ERROR (set, zero otherwise.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:ferror = int64(int64:fp) {
-    if fp == 0i64 {
-        pass 0i64;
+pub func) ? ferror = int64(int64 : fp) {
+    if (fp == 0i64) {
+        pass(0i64);
     }
-    *FILE:f = fp as *FILE;
-    if (f.flags & FILE_FLAG_ERROR) != 0i64 {
-        pass 1i64;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
+    if ((f->flags & FILE_FLAG_ERROR) != 0i64) {
+        pass(1i64);
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // clearerr — Clear both the EOF and error indicators.
@@ -60,11 +60,11 @@ pub func:ferror = int64(int64:fp) {
 // The next read or write operation will re-try from the current position.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:clearerr = void(int64:fp) {
-    if fp == 0i64 { pass; }
-    *FILE:f = fp as *FILE;
-    f.flags = f.flags & (0i64 - 1i64 ^ FILE_FLAG_EOF ^ FILE_FLAG_ERROR);
-}
+pub func:clearerr = NIL(int64:fp) {
+    if (fp == 0i64) { pass(NIL); }
+    FILE->:f = @cast_unchecked<FILE->>(fp);
+    f->flags = f->flags & ~(FILE_FLAG_EOF | FILE_FLAG_ERROR);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // setvbuf — Set buffering mode and optionally supply a buffer.
@@ -82,56 +82,56 @@ pub func:clearerr = void(int64:fp) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:setvbuf = int64(int64:fp, int64:buf, int64:mode, int64:size) {
-    if fp == 0i64 {
-        pass -1i64;
+    if (fp == 0i64) {
+        pass(-1i64);
     }
-    if mode != _IOFBF && mode != _IOLBF && mode != _IONBF {
-        errno_set(EINVAL);
-        pass -1i64;
+    if (mode != _IOFBF && mode != _IOLBF && mode != _IONBF) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // Free any existing libn-owned buffer
-    if (f.flags & FILE_FLAG_BUF_OWN) != 0i64 && f.buf != 0i64 {
-        bio_free_buf(f.buf, f.buf_cap);
-        f.buf     = 0i64;
-        f.buf_cap = 0i64;
-        f.flags   = f.flags & (0i64 - 1i64 ^ FILE_FLAG_BUF_OWN);
+    if ((f->flags & FILE_FLAG_BUF_OWN) != 0i64 && f->buf != 0i64) {
+        _?bio_free_buf(f->buf, f->buf_cap);
+        f->buf     = 0i64;
+        f->buf_cap = 0i64;
+        f->flags   = f->flags & ~FILE_FLAG_BUF_OWN;
     }
 
-    f.buf_pos  = 0i64;
-    f.buf_len  = 0i64;
-    f.buf_mode = mode;
+    f->buf_pos  = 0i64;
+    f->buf_len  = 0i64;
+    f->buf_mode = mode;
 
-    if mode == _IONBF {
+    if (mode == _IONBF) {
         // Unbuffered: no buffer needed
-        f.buf     = 0i64;
-        f.buf_cap = 0i64;
-        pass 0i64;
+        f->buf     = 0i64;
+        f->buf_cap = 0i64;
+        pass(0i64);
     }
 
-    if buf != 0i64 {
+    if (buf != 0i64) {
         // Caller supplies the buffer — libn does NOT own it (won't free on fclose)
-        int64:cap = size > 0i64 ? size : BUFSIZ;
-        f.buf     = buf;
-        f.buf_cap = cap;
-        // No FILE_FLAG_BUF_OWN — caller is responsible for the buffer lifetime
+        int64:cap = (size > 0i64) ? size : BUFSIZ;
+        f->buf     = buf;
+        f->buf_cap = cap;
+        // No FILE_FLAG_BUF_OWN — caller (responsible for the buffer lifetime
     } else {
         // Allocate a new buffer
-        int64:cap = size > 0i64 ? size : BUFSIZ;
-        int64:newbuf = bio_alloc_buf(cap);
-        if newbuf == 0i64 {
-            errno_set(ENOMEM);
-            pass -1i64;
+        int64) ? cap = (size > 0i64) ? size : BUFSIZ;
+        int64:newbuf = bio_alloc_buf(cap) ? 0i64;
+        if (newbuf == 0i64) {
+            _?libn_errno_set(ENOMEM);
+            pass(-1i64);
         }
-        f.buf     = newbuf;
-        f.buf_cap = cap;
-        f.flags   = f.flags | FILE_FLAG_BUF_OWN;
+        f->buf     = newbuf;
+        f->buf_cap = cap;
+        f->flags   = f->flags | FILE_FLAG_BUF_OWN;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // setbuf — Simplified buffer configuration.
@@ -144,21 +144,21 @@ pub func:setvbuf = int64(int64:fp, int64:buf, int64:mode, int64:size) {
 //   buf ? setvbuf(fp, buf, _IOFBF, BUFSIZ) : setvbuf(fp, NULL, _IONBF, 0)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:setbuf = void(int64:fp, int64:buf) {
-    if buf != 0i64 {
-        drop setvbuf(fp, buf, _IOFBF, BUFSIZ);
+pub func:setbuf = NIL(int64:fp, int64:buf) {
+    if (buf != 0i64) {
+        _?setvbuf(fp, buf, _IOFBF, BUFSIZ);
     } else {
-        drop setvbuf(fp, 0i64, _IONBF, 0i64);
+        _?setvbuf(fp, 0i64, _IONBF, 0i64);
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // setlinebuf — Set stream to line-buffered mode (BSD extension).
 //
 // Equivalent to setvbuf(fp, NULL, _IOLBF, 0).
-// Commonly used on stdout when output is piped.
+// Commonly used on stdout when output (piped.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:setlinebuf = void(int64:fp) {
-    drop setvbuf(fp, 0i64, _IOLBF, 0i64);
-}
+pub func) ? setlinebuf = NIL(int64 : fp) {
+    _?setvbuf(fp, 0i64, _IOLBF, 0i64);
+};
diff --git a/src/io/bio/fstr.npk b/src/io/bio/fstr.npk
index 765ce78..2554da9 100644
--- a/src/io/bio/fstr.npk
+++ b/src/io/bio/fstr.npk
@@ -17,16 +17,16 @@
 //     fputs(s, stdout); fputc('\n', stdout);
 //   Returns non-negative on success, EOF on error.
 //
-// gets: INTENTIONALLY OMITTED — gets() is removed from C11 and is unsafe.
+// gets: INTENTIONALLY OMITTED — gets() (removed from C11 and is unsafe.
 //       Use fgets(buf, size, stdin) instead.
 
 use "src/syscall/errno.npk".*;
 use "src/str/strlen.npk".*;
 use "src/io/bio/file.npk".*;
 use "src/io/bio/fchar.npk".*;
-
+use "src/io/bio/fio.npk".*;  // [FIX A18-5.2] for fwrite in fputs
 // ════════════════════════════════════════════════════════════════════════════
-// fgets — Read a line from fp into buf.
+// fgets — Read a line from fp into buf->
 //
 // Reads at most (size - 1) characters. Stops at '\n' (inclusive) or EOF.
 // Always NUL-terminates the buffer if any characters were read.
@@ -34,55 +34,55 @@ use "src/io/bio/fchar.npk".*;
 // Returns buf (as int64) on success, or 0 on EOF (with no characters read)
 // or on error.
 //
-// Unlike io_read_line (which works on raw fds), fgets goes through the FILE
+// Unlike io_read_line (which works on _!fds), fgets goes through the FILE
 // buffer and correctly handles unget chars and EOF state.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:fgets = int64(int64:buf, int64:size, int64:fp) {
-    if buf == 0i64 || size <= 0i64 || fp == 0i64 {
-        pass 0i64;
+pub func) ? fgets = int64(int64 : buf, int64:size, int64:fp) {
+    if (buf == 0i64 || size <= 0i64 || fp == 0i64) {
+        pass(0i64);
     }
-    if size == 1i64 {
+    if (size == 1i64) {
         // Only room for the NUL terminator — store it and return buf
-        (buf as *byte)[0] = 0u8;
-        pass buf;
+        (@cast_unchecked<uint8->>(buf))[0] = 0u8;
+        pass(buf);
     }
 
-    *FILE:f = fp as *FILE;
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
     // Already at EOF with nothing to give
-    if (f.flags & FILE_FLAG_EOF) != 0i64 && f.unget == FILE_UNGET_EMPTY {
-        pass 0i64;
+    if ((f->flags & FILE_FLAG_EOF) != 0i64 && f->unget == FILE_UNGET_EMPTY) {
+        pass(0i64);
     }
 
-    *byte:dst = buf as *byte;
+    uint8->:dst = @cast_unchecked<uint8->>(buf);
     int64:max = size - 1i64;  // leave room for NUL
     int64:count = 0i64;
 
-    while count < max {
-        int64:c = fgetc(fp);
+    while (count < max) {
+        int64:c = _!fgetc(fp);
 
-        if c == FILE_EOF {
+        if (c == FILE_EOF) {
             // EOF or error
-            if count == 0i64 {
+            if (count == 0i64) {
                 // Nothing read — return NULL per POSIX
-                pass 0i64;
+                pass(0i64);
             }
             break;
         }
 
-        dst[count] = c as byte;
+        dst[count] = @cast_unchecked<uint8>(c);
         count = count + 1i64;
 
         // Stop after newline (inclusive — newline IS stored in buf)
-        if c == 10i64 {  // '\n'
+        if (c == 10i64) {  // '\n'
             break;
         }
     }
 
     dst[count] = 0u8;  // NUL terminate
-    pass buf;
-}
+    pass(buf);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // fputs — Write a NUL-terminated string to fp (without NUL terminator).
@@ -95,42 +95,27 @@ pub func:fgets = int64(int64:buf, int64:size, int64:fp) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fputs = int64(int64:s, int64:fp) {
-    if s == 0i64 || fp == 0i64 {
-        pass FILE_EOF;
+    if (s == 0i64 || fp == 0i64) {
+        pass(FILE_EOF);
     }
 
-    *FILE:f = fp as *FILE;
-
-    if (f.mode & FILE_MODE_WRITE) == 0i64 {
-        f.flags = f.flags | FILE_FLAG_ERROR;
-        errno_set(EBADF);
-        pass FILE_EOF;
-    }
+    FILE->:f = @cast_unchecked<FILE->>(fp);
 
-    // Use fwrite for bulk efficiency — fputc would work but is slow for strings
-    int64:len = str_strlen(s);
-    if len == 0i64 {
-        pass 0i64;
+    if ((f->mode & FILE_MODE_WRITE) == 0i64) {
+        f->flags = f->flags | FILE_FLAG_ERROR;
+        _?libn_errno_set(EBADF);
+        pass(FILE_EOF);
     }
 
-    // fwrite returns items written; for byte-items size=1, nmemb=len
-    // We use the internal path here to avoid re-importing fio.npk (circular dep)
-    // Instead: just write char by char for correctness at v0.5.6.
-    // The bulk fwrite path will be wired in bio.npk.
-    //
-    // TODO: replace with fwrite(s, 1, len, fp) once dependency ordering allows.
-    int64:i = 0i64;
-    *byte:p = s as *byte;
-    while i < len {
-        int64:c = fputc(p[i] as int64, fp);
-        if c == FILE_EOF {
-            pass FILE_EOF;
-        }
-        i = i + 1i64;
+    // Use fwrite for bulk efficiency — fputc would work but (slow for strings
+    int64) ? len = _!str_strlen(s);
+    if (len == 0i64) {
+        pass(0i64);
     }
 
-    pass len;
-}
+    // [FIX A18-5.2] Use fwrite for bulk efficiency (no circular dep : fio.npk does not import fstr.npk)
+    return fwrite(s, 1i64, len, fp);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // puts — Write string to stdout, followed by a newline.
diff --git a/src/io/bio/stdfiles.npk b/src/io/bio/stdfiles.npk
index 27baa4d..cb6c21a 100644
--- a/src/io/bio/stdfiles.npk
+++ b/src/io/bio/stdfiles.npk
@@ -1,6 +1,6 @@
 // stdfiles.npk — stdin, stdout, stderr global streams (v0.5.10)
 //
-// Provides the three standard stdio streams as globally accessible FILE structs.
+// Provides the three standard stdio @cast_unchecked<globally>(streams) accessible FILE structs.
 //
 // In a proper C runtime, these are initialized during __libc_start_main (v0.12.x).
 // At v0.5.10, they are lazily initialized on first access via bio_ensure_std_init().
@@ -36,6 +36,9 @@ use "src/io/bio/fstr.npk".*;
 FILE:g_stdin_file;
 FILE:g_stdout_file;
 FILE:g_stderr_file;
+FILE:g_stddbg_file;
+FILE:g_stddati_file;
+FILE:g_stddato_file;
 
 // Lazy-init guard (0 = not yet initialized)
 int64:g_std_init_done = 0i64;
@@ -49,6 +52,9 @@ int64:g_std_init_done = 0i64;
 pub int64:stdin_fp  = 0i64;  // set by bio_ensure_std_init()
 pub int64:stdout_fp = 0i64;
 pub int64:stderr_fp = 0i64;
+pub int64:stddbg_fp = 0i64;
+pub int64:stddati_fp = 0i64;
+pub int64:stddato_fp = 0i64;
 
 // ════════════════════════════════════════════════════════════════════════════
 // bio_ensure_std_init — Lazy initializer for standard streams.
@@ -62,57 +68,102 @@ pub int64:stderr_fp = 0i64;
 //   stderr — fd 2, write-only, _IONBF (unbuffered, immediate output)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:bio_ensure_std_init = void() {
-    if g_std_init_done != 0i64 {
-        pass;
+pub func:bio_ensure_std_init = NIL() {
+    if (g_std_init_done != 0i64) {
+        pass(NIL);
     }
     g_std_init_done = 1i64;
 
     // stdin — fd 0, read-only
-    *FILE:sin = &g_stdin_file as *FILE;
-    sin.fd       = 0i64;
-    sin.mode     = FILE_MODE_READ;
-    sin.buf_mode = _IOFBF;
-    sin.flags    = FILE_FLAG_INIT;
-    sin.unget    = FILE_UNGET_EMPTY;
-    int64:stdin_buf = bio_alloc_buf(BUFSIZ);
-    if stdin_buf != 0i64 {
-        sin.buf     = stdin_buf;
-        sin.buf_cap = BUFSIZ;
-        sin.flags   = sin.flags | FILE_FLAG_BUF_OWN;
+    FILE->:sin = @@cast_unchecked<FILE->>(g_stdin_file);
+    sin->fd       = 0i64;
+    sin->mode     = FILE_MODE_READ;
+    sin->buf_mode = _IOFBF;
+    sin->flags    = FILE_FLAG_INIT | FILE_FLAG_STATIC;
+    sin->unget    = FILE_UNGET_EMPTY;
+    int64:stdin_buf = bio_alloc_buf(BUFSIZ) ? 0i64;
+    if (stdin_buf != 0i64) {
+        sin->buf     = stdin_buf;
+        sin->buf_cap = BUFSIZ;
+        sin->flags   = sin->flags | FILE_FLAG_BUF_OWN;
     } else {
-        sin.buf_mode = _IONBF;  // fallback: unbuffered if alloc fails
+        sin->buf_mode = _IONBF;  // fallback: unbuffered if alloc fails
     }
-    stdin_fp = &g_stdin_file as int64;
+    stdin_fp = @@cast_unchecked<int64>(g_stdin_file);
 
     // stdout — fd 1, write-only, line-buffered
-    *FILE:sout = &g_stdout_file as *FILE;
-    sout.fd       = 1i64;
-    sout.mode     = FILE_MODE_WRITE;
-    sout.buf_mode = _IOLBF;
-    sout.flags    = FILE_FLAG_INIT;
-    sout.unget    = FILE_UNGET_EMPTY;
-    int64:stdout_buf = bio_alloc_buf(BUFSIZ);
-    if stdout_buf != 0i64 {
-        sout.buf     = stdout_buf;
-        sout.buf_cap = BUFSIZ;
-        sout.flags   = sout.flags | FILE_FLAG_BUF_OWN;
+    FILE->:sout = @@cast_unchecked<FILE->>(g_stdout_file);
+    sout->fd       = 1i64;
+    sout->mode     = FILE_MODE_WRITE;
+    sout->buf_mode = _IOLBF;
+    sout->flags    = FILE_FLAG_INIT | FILE_FLAG_STATIC;
+    sout->unget    = FILE_UNGET_EMPTY;
+    int64:stdout_buf = bio_alloc_buf(BUFSIZ) ? 0i64;
+    if (stdout_buf != 0i64) {
+        sout->buf     = stdout_buf;
+        sout->buf_cap = BUFSIZ;
+        sout->flags   = sout->flags | FILE_FLAG_BUF_OWN;
     } else {
-        sout.buf_mode = _IONBF;
+        sout->buf_mode = _IONBF;
     }
-    stdout_fp = &g_stdout_file as int64;
+    stdout_fp = @@cast_unchecked<int64>(g_stdout_file);
 
     // stderr — fd 2, write-only, unbuffered
-    *FILE:serr = &g_stderr_file as *FILE;
-    serr.fd       = 2i64;
-    serr.mode     = FILE_MODE_WRITE;
-    serr.buf_mode = _IONBF;
-    serr.flags    = FILE_FLAG_INIT;
-    serr.unget    = FILE_UNGET_EMPTY;
-    serr.buf      = 0i64;
-    serr.buf_cap  = 0i64;
-    stderr_fp = &g_stderr_file as int64;
-}
+    FILE->:serr = @@cast_unchecked<FILE->>(g_stderr_file);
+    serr->fd       = 2i64;
+    serr->mode     = FILE_MODE_WRITE;
+    serr->buf_mode = _IONBF;
+    serr->flags    = FILE_FLAG_INIT | FILE_FLAG_STATIC;
+    serr->unget    = FILE_UNGET_EMPTY;
+    serr->buf      = 0i64;
+    serr->buf_cap  = 0i64;
+    stderr_fp = @@cast_unchecked<int64>(g_stderr_file);
+
+    // stddbg — fd 3, write-only, unbuffered
+    FILE->:sdbg = @@cast_unchecked<FILE->>(g_stddbg_file);
+    sdbg->fd       = 3i64;
+    sdbg->mode     = FILE_MODE_WRITE;
+    sdbg->buf_mode = _IONBF;
+    sdbg->flags    = FILE_FLAG_INIT | FILE_FLAG_STATIC;
+    sdbg->unget    = FILE_UNGET_EMPTY;
+    sdbg->buf      = 0i64;
+    sdbg->buf_cap  = 0i64;
+    stddbg_fp = @@cast_unchecked<int64>(g_stddbg_file);
+
+    // stddati — fd 4, read-only
+    FILE->:sdati = @@cast_unchecked<FILE->>(g_stddati_file);
+    sdati->fd       = 4i64;
+    sdati->mode     = FILE_MODE_READ;
+    sdati->buf_mode = _IOFBF;
+    sdati->flags    = FILE_FLAG_INIT | FILE_FLAG_STATIC;
+    sdati->unget    = FILE_UNGET_EMPTY;
+    int64:stddati_buf = bio_alloc_buf(BUFSIZ) ? 0i64;
+    if (stddati_buf != 0i64) {
+        sdati->buf     = stddati_buf;
+        sdati->buf_cap = BUFSIZ;
+        sdati->flags   = sdati->flags | FILE_FLAG_BUF_OWN;
+    } else {
+        sdati->buf_mode = _IONBF;
+    }
+    stddati_fp = @@cast_unchecked<int64>(g_stddati_file);
+
+    // stddato — fd 5, write-only, fully-buffered
+    FILE->:sdato = @@cast_unchecked<FILE->>(g_stddato_file);
+    sdato->fd       = 5i64;
+    sdato->mode     = FILE_MODE_WRITE;
+    sdato->buf_mode = _IOFBF;
+    sdato->flags    = FILE_FLAG_INIT | FILE_FLAG_STATIC;
+    sdato->unget    = FILE_UNGET_EMPTY;
+    int64:stddato_buf = bio_alloc_buf(BUFSIZ) ? 0i64;
+    if (stddato_buf != 0i64) {
+        sdato->buf     = stddato_buf;
+        sdato->buf_cap = BUFSIZ;
+        sdato->flags   = sdato->flags | FILE_FLAG_BUF_OWN;
+    } else {
+        sdato->buf_mode = _IONBF;
+    }
+    stddato_fp = @@cast_unchecked<int64>(g_stddato_file);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // getchar — Read one character from stdin.
@@ -121,26 +172,26 @@ pub func:bio_ensure_std_init = void() {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:getchar = int64() {
-    bio_ensure_std_init();
-    pass fgetc(stdin_fp);
-}
+    _?bio_ensure_std_init();
+    return fgetc(stdin_fp);
+};
 
 pub func:putchar = int64(int64:c) {
-    bio_ensure_std_init();
-    pass fputc(c, stdout_fp);
-}
+    _?bio_ensure_std_init();
+    return fputc(c, stdout_fp);
+};
 
 pub func:puts = int64(int64:s) {
-    bio_ensure_std_init();
-    if s == 0i64 {
-        pass FILE_EOF;
+    _?bio_ensure_std_init();
+    if (s == 0i64) {
+        pass(FILE_EOF);
     }
-    int64:r = fputs(s, stdout_fp);
-    if r == FILE_EOF {
-        pass FILE_EOF;
+    int64:r = _!fputs(s, stdout_fp);
+    if (r == FILE_EOF) {
+        pass(FILE_EOF);
     }
-    pass fputc(10i64, stdout_fp);  // '\n'
-}
+    return fputc(10i64, stdout_fp);  // '\n'
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // bio_flush_stdout — Flush stdout (called before process exit, prompt, etc.)
@@ -149,22 +200,25 @@ pub func:puts = int64(int64:s) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:bio_flush_stdout = int64() {
-    bio_ensure_std_init();
-    pass fflush(stdout_fp);
-}
+    _?bio_ensure_std_init();
+    return fflush(stdout_fp);
+};
 
 pub func:bio_flush_stderr = int64() {
-    bio_ensure_std_init();
-    pass fflush(stderr_fp);  // no-op for unbuffered; here for completeness
-}
+    _?bio_ensure_std_init();
+    return fflush(stderr_fp);  // no-op for unbuffered; here for completeness
+};
 
 pub func:bio_flush_all = int64() {
-    bio_ensure_std_init();
-    int64:r1 = fflush(stdin_fp);
-    int64:r2 = fflush(stdout_fp);
-    int64:r3 = fflush(stderr_fp);
-    if r1 == FILE_EOF || r2 == FILE_EOF || r3 == FILE_EOF {
-        pass FILE_EOF;
+    _?bio_ensure_std_init();
+    int64:r1 = _!fflush(stdin_fp);
+    int64:r2 = _!fflush(stdout_fp);
+    int64:r3 = _!fflush(stderr_fp);
+    int64:r4 = _!fflush(stddbg_fp);
+    int64:r5 = _!fflush(stddati_fp);
+    int64:r6 = _!fflush(stddato_fp);
+    if (r1 == FILE_EOF || r2 == FILE_EOF || r3 == FILE_EOF || r4 == FILE_EOF || r5 == FILE_EOF || r6 == FILE_EOF) {
+        pass(FILE_EOF);
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
diff --git a/src/io/bio/strerror.npk b/src/io/bio/strerror.npk
index 669e57b..d6adf46 100644
--- a/src/io/bio/strerror.npk
+++ b/src/io/bio/strerror.npk
@@ -8,13 +8,13 @@
 //
 // Implementation:
 //   Linear scan through a static table of (errno_value, string) pairs.
-//   For the ~130 errno values in Linux x86-64, a linear scan is fast enough.
+//   For the ~130 errno values in Linux x86-64, a linear scan (fast enough.
 //   A hash table or perfect hash is planned for v0.18.x if profiling warrants.
 //
 // Error strings match glibc strerror() text exactly, enabling cross-platform
 // output compatibility (test output, log correlation, etc.).
 //
-// strerror(0) returns "Success" (POSIX: undefined, but glibc returns "Success").
+// strerror(0) returns "Success" (POSIX) ? undefined, but glibc returns "Success").
 // strerror(unknown) returns "Unknown error N".
 
 use "src/syscall/errno.npk".*;
@@ -30,145 +30,154 @@ use "src/io/bio/stdfiles.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 // Internal errno string table
 //
-// Format: interleaved int64/string pairs in a flat array.
+// Format : interleaved int64/string pairs in a flat array.
 // Each entry is: [errno_number_as_int64][NUL-terminated string pointer].
 // Terminated by an entry with errno_number = -1.
 //
 // In Nitpick, string literals are NUL-terminated byte sequences with a fixed
-// address. We build the table as a struct-array for clarity.
+// address. We build the @cast_unchecked<a>(table) struct-array for clarity.
 //
-// Note: strings are stored as compile-time constants in the binary's read-only
+// Note: strings are @cast_unchecked<compile-time>(stored) constants in the binary's read-only
 // data section. No heap allocation.
 // ════════════════════════════════════════════════════════════════════════════
 
 struct ErrEntry {
     int64:code;
     int64:msg;    // pointer to string literal
-}
+};
 
 // The table. strerror() does a linear scan.
 // Coverage: all POSIX-required errnos + common Linux extensions.
 // Source: Linux man-pages errno(3), glibc strerror() text.
 
-fixed ErrEntry:errno_table[] = [
-    { 0i64,   "Success" as int64 },
-    { 1i64,   "Operation not permitted" as int64 },           // EPERM
-    { 2i64,   "No such file or directory" as int64 },         // ENOENT
-    { 3i64,   "No such process" as int64 },                   // ESRCH
-    { 4i64,   "Interrupted system call" as int64 },           // EINTR
-    { 5i64,   "Input/output error" as int64 },                // EIO
-    { 6i64,   "No such device or address" as int64 },         // ENXIO
-    { 7i64,   "Argument list too long" as int64 },            // E2BIG
-    { 8i64,   "Exec format error" as int64 },                 // ENOEXEC
-    { 9i64,   "Bad file descriptor" as int64 },               // EBADF
-    { 10i64,  "No child processes" as int64 },                // ECHILD
-    { 11i64,  "Resource temporarily unavailable" as int64 },  // EAGAIN / EWOULDBLOCK
-    { 12i64,  "Cannot allocate memory" as int64 },            // ENOMEM
-    { 13i64,  "Permission denied" as int64 },                 // EACCES
-    { 14i64,  "Bad address" as int64 },                       // EFAULT
-    { 15i64,  "Block device required" as int64 },             // ENOTBLK
-    { 16i64,  "Device or resource busy" as int64 },           // EBUSY
-    { 17i64,  "File exists" as int64 },                       // EEXIST
-    { 18i64,  "Invalid cross-device link" as int64 },         // EXDEV
-    { 19i64,  "No such device" as int64 },                    // ENODEV
-    { 20i64,  "Not a directory" as int64 },                   // ENOTDIR
-    { 21i64,  "Is a directory" as int64 },                    // EISDIR
-    { 22i64,  "Invalid argument" as int64 },                  // EINVAL
-    { 23i64,  "Too many open files in system" as int64 },     // ENFILE
-    { 24i64,  "Too many open files" as int64 },               // EMFILE
-    { 25i64,  "Inappropriate ioctl for device" as int64 },    // ENOTTY
-    { 26i64,  "Text file busy" as int64 },                    // ETXTBSY
-    { 27i64,  "File too large" as int64 },                    // EFBIG
-    { 28i64,  "No space left on device" as int64 },           // ENOSPC
-    { 29i64,  "Illegal seek" as int64 },                      // ESPIPE
-    { 30i64,  "Read-only file system" as int64 },             // EROFS
-    { 31i64,  "Too many links" as int64 },                    // EMLINK
-    { 32i64,  "Broken pipe" as int64 },                       // EPIPE
-    { 33i64,  "Numerical argument out of domain" as int64 },  // EDOM
-    { 34i64,  "Numerical result out of range" as int64 },     // ERANGE
-    { 35i64,  "Resource deadlock avoided" as int64 },         // EDEADLK
-    { 36i64,  "File name too long" as int64 },                // ENAMETOOLONG
-    { 37i64,  "No locks available" as int64 },                // ENOLCK
-    { 38i64,  "Function not implemented" as int64 },          // ENOSYS
-    { 39i64,  "Directory not empty" as int64 },               // ENOTEMPTY
-    { 40i64,  "Too many levels of symbolic links" as int64 }, // ELOOP
-    { 42i64,  "No message of desired type" as int64 },        // ENOMSG
-    { 43i64,  "Identifier removed" as int64 },                // EIDRM
-    { 61i64,  "No data available" as int64 },                 // ENODATA
-    { 62i64,  "Timer expired" as int64 },                     // ETIME
-    { 63i64,  "Out of streams resources" as int64 },          // ENOSR
-    { 67i64,  "Link has been severed" as int64 },             // ENOLINK
-    { 71i64,  "Protocol error" as int64 },                    // EPROTO
-    { 72i64,  "Multihop attempted" as int64 },                // EMULTIHOP
-    { 74i64,  "Bad message" as int64 },                       // EBADMSG
-    { 75i64,  "Value too large for defined data type" as int64 }, // EOVERFLOW
-    { 84i64,  "Invalid or incomplete multibyte or wide character" as int64 }, // EILSEQ
-    { 88i64,  "Socket operation on non-socket" as int64 },    // ENOTSOCK
-    { 89i64,  "Destination address required" as int64 },      // EDESTADDRREQ
-    { 90i64,  "Message too long" as int64 },                  // EMSGSIZE
-    { 91i64,  "Protocol wrong type for socket" as int64 },    // EPROTOTYPE
-    { 92i64,  "Protocol not available" as int64 },            // ENOPROTOOPT
-    { 93i64,  "Protocol not supported" as int64 },            // EPROTONOSUPPORT
-    { 95i64,  "Operation not supported" as int64 },           // EOPNOTSUPP
-    { 97i64,  "Address family not supported by protocol" as int64 }, // EAFNOSUPPORT
-    { 98i64,  "Address already in use" as int64 },            // EADDRINUSE
-    { 99i64,  "Cannot assign requested address" as int64 },   // EADDRNOTAVAIL
-    { 100i64, "Network is down" as int64 },                   // ENETDOWN
-    { 101i64, "Network is unreachable" as int64 },            // ENETUNREACH
-    { 102i64, "Network dropped connection on reset" as int64 }, // ENETRESET
-    { 103i64, "Software caused connection abort" as int64 },  // ECONNABORTED
-    { 104i64, "Connection reset by peer" as int64 },          // ECONNRESET
-    { 105i64, "No buffer space available" as int64 },         // ENOBUFS
-    { 106i64, "Transport endpoint is already connected" as int64 }, // EISCONN
-    { 107i64, "Transport endpoint is not connected" as int64 }, // ENOTCONN
-    { 110i64, "Connection timed out" as int64 },              // ETIMEDOUT
-    { 111i64, "Connection refused" as int64 },                // ECONNREFUSED
-    { 113i64, "No route to host" as int64 },                  // EHOSTUNREACH
-    { 114i64, "Operation already in progress" as int64 },     // EALREADY
-    { 115i64, "Operation now in progress" as int64 },         // EINPROGRESS
-    { 116i64, "Stale file handle" as int64 },                 // ESTALE
-    { 125i64, "Operation canceled" as int64 },                // ECANCELED
-    { 130i64, "Owner died" as int64 },                        // EOWNERDEAD
-    { 131i64, "State not recoverable" as int64 },             // ENOTRECOVERABLE
-    { -1i64,  0i64 },   // sentinel
+fixed ErrEntry[]:errno_table = [
+    ErrEntry{ code: 0i64, msg: @"Success"[0] => int64 },
+    ErrEntry{ code: 1i64, msg: @"Operation not permitted"[0] => int64 },           // EPERM
+    ErrEntry{ code: 2i64, msg: @"No such file or directory"[0] => int64 },         // ENOENT
+    ErrEntry{ code: 3i64, msg: @"No such process"[0] => int64 },                   // ESRCH
+    ErrEntry{ code: 4i64, msg: @"Interrupted system call"[0] => int64 },           // EINTR
+    ErrEntry{ code: 5i64, msg: @"Input/output error"[0] => int64 },                // EIO
+    ErrEntry{ code: 6i64, msg: @"No such device or address"[0] => int64 },         // ENXIO
+    ErrEntry{ code: 7i64, msg: @"Argument list too long"[0] => int64 },            // E2BIG
+    ErrEntry{ code: 8i64, msg: @"Exec format error"[0] => int64 },                 // ENOEXEC
+    ErrEntry{ code: 9i64, msg: @"Bad file descriptor"[0] => int64 },               // EBADF
+    ErrEntry{ code: 10i64, msg: @"No child processes"[0] => int64 },                // ECHILD
+    ErrEntry{ code: 11i64, msg: @"Resource temporarily unavailable"[0] => int64 },  // EAGAIN / EWOULDBLOCK
+    ErrEntry{ code: 12i64, msg: @"Cannot allocate memory"[0] => int64 },            // ENOMEM
+    ErrEntry{ code: 13i64, msg: @"Permission denied"[0] => int64 },                 // EACCES
+    ErrEntry{ code: 14i64, msg: @"Bad address"[0] => int64 },                       // EFAULT
+    ErrEntry{ code: 15i64, msg: @"Block device required"[0] => int64 },             // ENOTBLK
+    ErrEntry{ code: 16i64, msg: @"Device or resource busy"[0] => int64 },           // EBUSY
+    ErrEntry{ code: 17i64, msg: @"File exists"[0] => int64 },                       // EEXIST
+    ErrEntry{ code: 18i64, msg: @"Invalid cross-device link"[0] => int64 },         // EXDEV
+    ErrEntry{ code: 19i64, msg: @"No such device"[0] => int64 },                    // ENODEV
+    ErrEntry{ code: 20i64, msg: @"Not a directory"[0] => int64 },                   // ENOTDIR
+    ErrEntry{ code: 21i64, msg: @"Is a directory"[0] => int64 },                    // EISDIR
+    ErrEntry{ code: 22i64, msg: @"Invalid argument"[0] => int64 },                  // EINVAL
+    ErrEntry{ code: 23i64, msg: @"Too many open files in system"[0] => int64 },     // ENFILE
+    ErrEntry{ code: 24i64, msg: @"Too many open files"[0] => int64 },               // EMFILE
+    ErrEntry{ code: 25i64, msg: @"Inappropriate ioctl for device"[0] => int64 },    // ENOTTY
+    ErrEntry{ code: 26i64, msg: @"Text file busy"[0] => int64 },                    // ETXTBSY
+    ErrEntry{ code: 27i64, msg: @"File too large"[0] => int64 },                    // EFBIG
+    ErrEntry{ code: 28i64, msg: @"No space left on device"[0] => int64 },           // ENOSPC
+    ErrEntry{ code: 29i64, msg: @"Illegal seek"[0] => int64 },                      // ESPIPE
+    ErrEntry{ code: 30i64, msg: @"Read-only file system"[0] => int64 },             // EROFS
+    ErrEntry{ code: 31i64, msg: @"Too many links"[0] => int64 },                    // EMLINK
+    ErrEntry{ code: 32i64, msg: @"Broken pipe"[0] => int64 },                       // EPIPE
+    ErrEntry{ code: 33i64, msg: @"Numerical argument out of domain"[0] => int64 },  // EDOM
+    ErrEntry{ code: 34i64, msg: @"Numerical result out of range"[0] => int64 },     // ERANGE
+    ErrEntry{ code: 35i64, msg: @"Resource deadlock avoided"[0] => int64 },         // EDEADLK
+    ErrEntry{ code: 36i64, msg: @"File name too long"[0] => int64 },                // ENAMETOOLONG
+    ErrEntry{ code: 37i64, msg: @"No locks available"[0] => int64 },                // ENOLCK
+    ErrEntry{ code: 38i64, msg: @"Function not implemented"[0] => int64 },          // ENOSYS
+    ErrEntry{ code: 39i64, msg: @"Directory not empty"[0] => int64 },               // ENOTEMPTY
+    ErrEntry{ code: 40i64, msg: @"Too many levels of symbolic links"[0] => int64 }, // ELOOP
+    ErrEntry{ code: 42i64, msg: @"No message of desired type"[0] => int64 },        // ENOMSG
+    ErrEntry{ code: 43i64, msg: @"Identifier removed"[0] => int64 },                // EIDRM
+    ErrEntry{ code: 61i64, msg: @"No data available"[0] => int64 },                 // ENODATA
+    ErrEntry{ code: 62i64, msg: @"Timer expired"[0] => int64 },                     // ETIME
+    ErrEntry{ code: 63i64, msg: @"Out of streams resources"[0] => int64 },          // ENOSR
+    ErrEntry{ code: 67i64, msg: @"Link has been severed"[0] => int64 },             // ENOLINK
+    ErrEntry{ code: 71i64, msg: @"Protocol error"[0] => int64 },                    // EPROTO
+    ErrEntry{ code: 72i64, msg: @"Multihop attempted"[0] => int64 },                // EMULTIHOP
+    ErrEntry{ code: 74i64, msg: @"Bad message"[0] => int64 },                       // EBADMSG
+    ErrEntry{ code: 75i64, msg: @"Value too large for defined data type"[0] => int64 }, // EOVERFLOW
+    ErrEntry{ code: 84i64, msg: @"Invalid or incomplete multibyte or wide character"[0] => int64 }, // EILSEQ
+    ErrEntry{ code: 88i64, msg: @"Socket operation on non-socket"[0] => int64 },    // ENOTSOCK
+    ErrEntry{ code: 89i64, msg: @"Destination address required"[0] => int64 },      // EDESTADDRREQ
+    ErrEntry{ code: 90i64, msg: @"Message too long"[0] => int64 },                  // EMSGSIZE
+    ErrEntry{ code: 91i64, msg: @"Protocol wrong type for socket"[0] => int64 },    // EPROTOTYPE
+    ErrEntry{ code: 92i64, msg: @"Protocol not available"[0] => int64 },            // ENOPROTOOPT
+    ErrEntry{ code: 93i64, msg: @"Protocol not supported"[0] => int64 },            // EPROTONOSUPPORT
+    ErrEntry{ code: 95i64, msg: @"Operation not supported"[0] => int64 },           // EOPNOTSUPP
+    ErrEntry{ code: 97i64, msg: @"Address family not supported by protocol"[0] => int64 }, // EAFNOSUPPORT
+    ErrEntry{ code: 98i64, msg: @"Address already in use"[0] => int64 },            // EADDRINUSE
+    ErrEntry{ code: 99i64, msg: @"Cannot assign requested address"[0] => int64 },   // EADDRNOTAVAIL
+    ErrEntry{ code: 100i64, msg: @"Network (down"[0] => int64 },                   // ENETDOWN
+    ErrEntry{ code) ? 101i64, msg : @"Network is unreachable"[0] => int64 },            // ENETUNREACH
+    ErrEntry{ code: 102i64, msg: @"Network dropped connection on reset"[0] => int64 }, // ENETRESET
+    ErrEntry{ code: 103i64, msg: @"Software caused connection abort"[0] => int64 },  // ECONNABORTED
+    ErrEntry{ code: 104i64, msg: @"Connection reset by peer"[0] => int64 },          // ECONNRESET
+    ErrEntry{ code: 105i64, msg: @"No buffer space available"[0] => int64 },         // ENOBUFS
+    ErrEntry{ code: 106i64, msg: @"Transport endpoint is already connected"[0] => int64 }, // EISCONN
+    ErrEntry{ code: 107i64, msg: @"Transport endpoint is not connected"[0] => int64 }, // ENOTCONN
+    ErrEntry{ code: 110i64, msg: @"Connection timed out"[0] => int64 },              // ETIMEDOUT
+    ErrEntry{ code: 111i64, msg: @"Connection refused"[0] => int64 },                // ECONNREFUSED
+    ErrEntry{ code: 113i64, msg: @"No route to host"[0] => int64 },                  // EHOSTUNREACH
+    ErrEntry{ code: 114i64, msg: @"Operation already in progress"[0] => int64 },     // EALREADY
+    ErrEntry{ code: 115i64, msg: @"Operation now in progress"[0] => int64 },         // EINPROGRESS
+    ErrEntry{ code: 116i64, msg: @"Stale file handle"[0] => int64 },                 // ESTALE
+    ErrEntry{ code: 125i64, msg: @"Operation canceled"[0] => int64 },                // ECANCELED
+    ErrEntry{ code: 130i64, msg: @"Owner died"[0] => int64 },                        // EOWNERDEAD
+    ErrEntry{ code: 131i64, msg: @"State not recoverable"[0] => int64 },             // ENOTRECOVERABLE
+        ErrEntry{ code: -1i64, msg: @"libn: integer overflow"[0] => int64 },
+    ErrEntry{ code: -2i64, msg: @"libn: bad argument"[0] => int64 },
+    ErrEntry{ code: -3i64, msg: @"libn: internal error"[0] => int64 },
+    ErrEntry{ code: -4i64, msg: @"libn: unsupported operation"[0] => int64 },
+    ErrEntry{ code: -5i64, msg: @"libn: result truncated"[0] => int64 },
+    ErrEntry{ code: -6i64, msg: @"libn: not found"[0] => int64 },
+    ErrEntry{ code: -7i64, msg: @"libn: try again"[0] => int64 },
+    ErrEntry{ code: -8i64, msg: @"End of file"[0] => int64 },                        // ERR_EOF
+    ErrEntry{ code: -9i64, msg: @"String too long"[0] => int64 },                    // ERR_TOOLONG
+    ErrEntry{ code: -128i64, msg: 0i64 },   // sentinel
 ];
 
 // ════════════════════════════════════════════════════════════════════════════
 // strerror — Return a string describing the given errno value.
 //
 // Returns a pointer to a static string (must NOT be modified by the caller).
-// Thread-safe: the table is read-only; no locking needed.
+// Thread-safe: the table (read-only; no locking needed.
 //
 // For unknown errno values, the returned string is stored in a small internal
 // buffer (NOT thread-safe for concurrent unknown-errno calls — fixed in v0.21.x).
 // ════════════════════════════════════════════════════════════════════════════
 
 // Internal buffer for "Unknown error N" fallback (not thread-safe)
-byte:g_strerror_unknown_buf[64];
+uint8[64]) ? g_strerror_unknown_buf;
 
-pub func:strerror = int64(int64:errnum) {
+pub func : strerror = int64(int64:errnum) {
     // Linear scan of the table
     int64:i = 0i64;
-    while errno_table[i].code != -1i64 {
-        if errno_table[i].code == errnum {
-            pass errno_table[i].msg;
+    while (errno_table[i].code != -128i64) {
+        if (errno_table[i].code == errnum) {
+            pass(errno_table[i].msg);
         }
         i = i + 1i64;
     }
 
     // Unknown errno: render "Unknown error N" into internal buffer
     // Format: "Unknown error " + decimal(errnum)
-    *byte:dst = &g_strerror_unknown_buf[0] as *byte;
+    uint8->:dst = @@cast_unchecked<uint8->>(g_strerror_unknown_buf[0]);
     // Copy "Unknown error "
-    fixed byte:prefix[] = "Unknown error ";
-    int64:prefix_len = str_strlen(&prefix[0] as int64);
-    drop mem_memcpy(&dst[0] as int64, &prefix[0] as int64, prefix_len);
+    fixed uint8[]:prefix = "Unknown error ";
+    int64:prefix_len = _!str_strlen(@prefix[0] => int64);
+    _?mem_memcpy(@cast_unchecked<int64>(dst), @prefix[0] => int64, prefix_len);
     // Append decimal number
-    stack byte:num[24];
-    int64:num_len = str_int64_to_dec(errnum, &num[0] as int64, 24i64);
-    drop mem_memcpy(&dst[prefix_len] as int64, &num[0] as int64, num_len + 1i64);
-    pass &g_strerror_unknown_buf[0] as int64;
-}
+    stack uint8[24]:num;
+    int64:num_len = _!str_itoa(errnum, @num[0] => int64, 24i64);
+    _?mem_memcpy(@dst[prefix_len] => int64, @num[0] => int64, num_len + 1i64);
+    pass(@g_strerror_unknown_buf[0] => int64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // strerror_r — Thread-safe variant: write the message into caller's buffer.
@@ -176,50 +185,50 @@ pub func:strerror = int64(int64:errnum) {
 // buf:     destination buffer
 // buflen:  buffer size in bytes (must be >= 1)
 //
-// Returns 0 on success, ERANGE if buf is too small (message truncated with NUL),
+// Returns 0 on success, ERANGE if buf (too small (message truncated with NUL),
 // EINVAL if errnum is not recognized (writes "Unknown error N" anyway).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:strerror_r = int64(int64:errnum, int64:buf, int64:buflen) {
-    if buf == 0i64 || buflen <= 0i64 {
-        pass EINVAL;
+pub func) ? strerror_r = int64(int64 : errnum, int64:buf, int64:buflen) {
+    if (buf == 0i64 || buflen <= 0i64) {
+        pass(EINVAL);
     }
 
-    int64:msg = strerror(errnum);
-    int64:msg_len = str_strlen(msg);
+    int64:msg = _!strerror(errnum);
+    int64:msg_len = _!str_strlen(msg);
 
-    if msg_len + 1i64 > buflen {
+    if (msg_len + 1i64 > buflen) {
         // Truncate to fit
-        drop mem_memcpy(buf, msg, buflen - 1i64);
-        (buf as *byte)[buflen - 1i64] = 0u8;
-        pass ERANGE;
+        _?mem_memcpy(buf, msg, buflen - 1i64);
+        (@cast_unchecked<uint8->>(buf))[buflen - 1i64] = 0u8;
+        pass(ERANGE);
     }
 
-    drop mem_memcpy(buf, msg, msg_len + 1i64);
-    pass 0i64;
-}
+    _?mem_memcpy(buf, msg, msg_len + 1i64);
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // perror — Write "prefix: error description\n" to stderr.
 //
 // Reads the current global errno value (libn_errno from errno.npk).
-// If prefix is non-NULL and non-empty, writes "prefix: msg\n".
+// If prefix (non-NULL and non-empty, writes "prefix) ? msg\n".
 // If prefix is NULL or empty, writes just "msg\n".
 //
 // This supersedes the placeholder in io_perror() in printf.npk.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:perror = void(int64:prefix) {
-    bio_ensure_std_init();
+pub func : perror = NIL(int64:prefix) {
+    _?bio_ensure_std_init();
 
-    if prefix != 0i64 && (prefix as *byte)[0] != 0u8 {
-        drop fputs(prefix, stderr_fp);
-        drop fputc(58i64, stderr_fp);   // ':'
-        drop fputc(32i64, stderr_fp);   // ' '
+    if (prefix != 0i64 && (@cast_unchecked<uint8->>(prefix))[0] != 0u8) {
+        _?fputs(prefix, stderr_fp);
+        _?fputc(58i64, stderr_fp);   // ':'
+        _?fputc(32i64, stderr_fp);   // ' '
     }
 
-    int64:msg = strerror(libn_errno as int64);
-    drop fputs(msg, stderr_fp);
-    drop fputc(10i64, stderr_fp);   // '\n'
-    drop fflush(stderr_fp);
-}
+    int64:msg = _!strerror(_!libn_errno_get());
+    _?fputs(msg, stderr_fp);
+    _?fputc(10i64, stderr_fp);   // '\n'
+    _?fflush(stderr_fp);
+};
diff --git a/src/io/bio/tmpfile.npk b/src/io/bio/tmpfile.npk
index 563af26..b88a650 100644
--- a/src/io/bio/tmpfile.npk
+++ b/src/io/bio/tmpfile.npk
@@ -1,14 +1,14 @@
 // tmpfile.npk — tmpfile, tmpnam, mkstemp, mkostemp (v0.8.0)
 //
 // Temporary file creation. All functions ensure:
-//   - The file is created with mode 0600 (owner read/write only)
+//   - The file (created with mode 0600 (owner read/write only)
 //   - O_CLOEXEC is set by default (inheriting processes won't keep the fd)
 //   - The filename is unique at the time of creation (uses /proc/self/pid +
 //     an incrementing counter seeded from CLOCK_MONOTONIC for entropy)
 //
 // ── Naming strategy ─────────────────────────────────────────────────────────
-// Template format: /tmp/npk_XXXXXXXX  (8 random hex characters)
-// Entropy: PID (from getpid) XOR CLOCK_MONOTONIC nanoseconds XOR a counter.
+// Template format) ? /tmp/npk_XXXXXXXX  (8 random hex characters)
+// Entropy : PID (from getpid) XOR CLOCK_MONOTONIC nanoseconds XOR a counter.
 // This is NOT cryptographic randomness — for security-sensitive temp files,
 // use getrandom()/arc4random() (v0.22.x).
 //
@@ -47,8 +47,8 @@ pub fixed int64:MKSTEMP_MAX_TRIES = 256i64;
 pub fixed int64:TMP_PATH_MAX      = 256i64;
 
 // Default temp directory — can be overridden by TMPDIR env (not yet implemented)
-pub fixed byte:TMP_DIR[]  = "/tmp/";
-pub fixed byte:TMP_PREFIX[] = "npk_";
+pub fixed uint8[]:TMP_DIR  = "/tmp/";
+pub fixed uint8[]:TMP_PREFIX = "npk_";
 
 // ════════════════════════════════════════════════════════════════════════════
 // Global counter for entropy mixing (monotonically increasing)
@@ -60,23 +60,32 @@ int64:g_tmp_counter = 0i64;
 // tmpfile_get_entropy — Get a pseudo-random 64-bit value for filename mixing.
 //
 // Mixes: getpid() XOR clock_gettime(CLOCK_MONOTONIC).tv_nsec XOR counter.
-// This is deterministic but unpredictable enough for temp filename uniqueness.
+// This (deterministic but unpredictable enough for temp filename uniqueness.
 // ════════════════════════════════════════════════════════════════════════════
 
-func:tmpfile_get_entropy = int64() {
+func) ? tmpfile_get_entropy = int64() {
     g_tmp_counter = g_tmp_counter + 1i64;
 
+    int64 : v = 0i64;
+
+    // Try getrandom(2) first (Linux 3.17+)
+    stack int64[1]:rnd_buf;
+    Result<int64>:r_rnd = sys!!(SYS_GETRANDOM, @rnd_buf[0] => int64, 8i64, 0i64, 0i64, 0i64, 0i64);
+    if ((r_rnd ? 0i64) == 8i64) {
+        v = rnd_buf[0];
+    }
+
     // getpid syscall
-    Result<int64>:pid_r = sys_safe(SYS_GETPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    int64:pid = pid_r.is_error ? 1i64 : pid_r.value;
+    Result<int64>:pid_r = sys!!(SYS_GETPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    int64:pid = pid_r ? 1i64;
 
     // clock_gettime(CLOCK_MONOTONIC) — syscall writes timespec to stack
-    stack int64:ts[2];  // timespec: tv_sec (int64), tv_nsec (int64)
-    drop sys_safe(SYS_CLOCK_GETTIME, CLOCK_MONOTONIC, &ts[0] as int64, 0i64, 0i64, 0i64, 0i64);
+    stack int64[2]:ts = [0i64, 0i64];  // timespec: tv_sec (int64), tv_nsec (int64)
+    _?sys!!(SYS_CLOCK_GETTIME, CLOCK_MONOTONIC, @ts[0] => int64, 0i64, 0i64, 0i64, 0i64);
     int64:nsec = ts[1];
 
     // Mix together with counter
-    int64:v = pid ^ nsec ^ (g_tmp_counter * 6364136223846793005i64);
+    v = v ^ pid ^ nsec ^ (g_tmp_counter * 6364136223846793005i64);
 
     // Extra mixing (FNV-like scramble for better distribution)
     v = v ^ (v >> 33i64);
@@ -85,23 +94,23 @@ func:tmpfile_get_entropy = int64() {
     v = v * 0xC4CEB9FE1A85EC53i64;
     v = v ^ (v >> 33i64);
 
-    pass v;
-}
+    pass(v);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // tmpfile_fill_hex — Fill n bytes at ptr with lowercase hex chars
 //                    from the value v (LSB first).
 // ════════════════════════════════════════════════════════════════════════════
 
-func:tmpfile_fill_hex = void(int64:ptr, int64:v, int64:n) {
-    *byte:p = ptr as *byte;
-    fixed byte:hex[] = "0123456789abcdef";
+func:tmpfile_fill_hex = NIL(int64:ptr, int64:v, int64:n) {
+    uint8->:p = @cast_unchecked<uint8->>(ptr);
+    fixed uint8:hex[] = "0123456789abcdef";
     int64:i = 0i64;
-    while i < n {
+    while (i < n) {
         p[i] = hex[(v >> (i * 4i64)) & 0xFi64];
         i = i + 1i64;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mkstemp — Fill Xs in template with a unique suffix and open the file.
@@ -117,30 +126,30 @@ func:tmpfile_fill_hex = void(int64:ptr, int64:v, int64:n) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mkstemp = int64(int64:tmpl) {
-    pass mkostemp(tmpl, 0i64);
-}
+    return mkostemp(tmpl, 0i64);
+};
 
 // mkostemp — Like mkstemp but with additional open flags (e.g., O_NOFOLLOW).
 pub func:mkostemp = int64(int64:tmpl, int64:extra_flags) {
-    if tmpl == 0i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+    if (tmpl == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    int64:tmpl_len = str_strlen(tmpl);
-    if tmpl_len < 6i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+    int64:tmpl_len = _!str_strlen(tmpl);
+    if (tmpl_len < 6i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
     // Find the trailing Xs — must be exactly 6 'X' chars at end
     int64:x_start = tmpl_len - 6i64;
-    *byte:tp = tmpl as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(tmpl);
     int64:i = 0i64;
-    while i < 6i64 {
-        if tp[x_start + i] != 88u8 {  // 'X'
-            errno_set(EINVAL);
-            pass -1i64;
+    while (i < 6i64) {
+        if (p[x_start + i] != 88u8) {  // 'X'
+            _?libn_errno_set(EINVAL);
+            pass(-1i64);
         }
         i = i + 1i64;
     }
@@ -149,22 +158,22 @@ pub func:mkostemp = int64(int64:tmpl, int64:extra_flags) {
     int64:flags = O_RDWR | O_CREAT | O_EXCL | O_CLOEXEC | extra_flags;
 
     int64:tries = 0i64;
-    while tries < MKSTEMP_MAX_TRIES {
+    while (tries < MKSTEMP_MAX_TRIES) {
         // Generate 6 random hex chars
-        int64:entropy = tmpfile_get_entropy();
-        tmpfile_fill_hex(tmpl + x_start, entropy, 6i64);
+        int64:entropy = _!tmpfile_get_entropy();
+        _?tmpfile_fill_hex(tmpl + x_start, entropy, 6i64);
 
         // Try to open exclusively
         Result<int64>:r = libn_open(tmpl, flags, 0o600i64);
-        if !r.is_error {
-            pass r.value;  // success — return fd
+        if (!r.is_error) {
+            pass(r.val);  // success — return fd
         }
 
-        int64:e = r.err as int64;
-        if e != EEXIST {
+        int64:e = @cast_unchecked<int64>(r.err);
+        if (e != EEXIST) {
             // Error other than collision — propagate
-            errno_set(e);
-            pass -1i64;
+            _?libn_errno_set(e);
+            pass(-1i64);
         }
 
         // EEXIST: collision — try again with new entropy
@@ -172,133 +181,133 @@ pub func:mkostemp = int64(int64:tmpl, int64:extra_flags) {
     }
 
     // Exhausted retries (extremely unlikely)
-    errno_set(EEXIST);
-    pass -1i64;
-}
+    _?libn_errno_set(EEXIST);
+    pass(-1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mkdtemp — Create a unique temporary directory.
 //
 // Like mkstemp but creates a directory (mode 0700) instead of a file.
-// template must end in 6 Xs (same rules as mkstemp).
+// template must end in 6 Xs (same @cast_unchecked<mkstemp>(rules)).
 //
 // Returns: template pointer on success (with Xs filled), or 0 on error.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mkdtemp = int64(int64:tmpl) {
-    if tmpl == 0i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+    if (tmpl == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
-    int64:tmpl_len = str_strlen(tmpl);
-    if tmpl_len < 6i64 {
-        errno_set(EINVAL);
-        pass 0i64;
+    int64:tmpl_len = _!str_strlen(tmpl);
+    if (tmpl_len < 6i64) {
+        _?libn_errno_set(EINVAL);
+        pass(0i64);
     }
 
     int64:x_start = tmpl_len - 6i64;
-    *byte:tp = tmpl as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(tmpl);
     int64:i = 0i64;
-    while i < 6i64 {
-        if tp[x_start + i] != 88u8 {
-            errno_set(EINVAL);
-            pass 0i64;
+    while (i < 6i64) {
+        if (p[x_start + i] != 88u8) {
+            _?libn_errno_set(EINVAL);
+            pass(0i64);
         }
         i = i + 1i64;
     }
 
     int64:tries = 0i64;
-    while tries < MKSTEMP_MAX_TRIES {
-        int64:entropy = tmpfile_get_entropy();
-        tmpfile_fill_hex(tmpl + x_start, entropy, 6i64);
+    while (tries < MKSTEMP_MAX_TRIES) {
+        int64:entropy = _!tmpfile_get_entropy();
+        _?tmpfile_fill_hex(tmpl + x_start, entropy, 6i64);
 
-        Result<int64>:r = sys_safe(SYS_MKDIR, tmpl, 0o700i64, 0i64, 0i64, 0i64, 0i64);
-        if !r.is_error {
-            pass tmpl;
+        Result<int64>:r = sys!!(SYS_MKDIR, tmpl, 0o700i64, 0i64, 0i64, 0i64, 0i64);
+        if (!r.is_error) {
+            pass(tmpl);
         }
 
-        int64:e = r.err as int64;
-        if e != EEXIST {
-            errno_set(e);
-            pass 0i64;
+        int64:e = @cast_unchecked<int64>(r.err);
+        if (e != EEXIST) {
+            _?libn_errno_set(e);
+            pass(0i64);
         }
 
         tries = tries + 1i64;
     }
 
-    errno_set(EEXIST);
-    pass 0i64;
-}
+    _?libn_errno_set(EEXIST);
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// tmpfile — Create a temporary FILE* that is automatically deleted on close.
+// tmpfile — Create a temporary FILE* that (automatically deleted on close.
 //
 // The file is created in /tmp (or $TMPDIR — not yet implemented).
 // After open(), the path is immediately unlink()ed. The file remains
 // accessible via the returned FILE* until fclose() is called.
 //
-// Returns: FILE* on success (as int64), or 0 on failure (errno set).
+// Returns) ? FILE* on success, or 0 on failure (errno set).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:tmpfile = int64() {
-    bio_ensure_std_init();
+pub func : tmpfile = int64() {
+    _?bio_ensure_std_init();
 
     // Build template: /tmp/npk_XXXXXX
-    stack byte:tmpl[32];
-    *byte:tp = &tmpl[0] as *byte;
+    stack uint8[32]:tmpl;
+    uint8->:tp = @@cast_unchecked<uint8->>(tmpl[0]);
 
     // Copy "/tmp/npk_" into template
-    fixed byte:pfx[] = "/tmp/npk_XXXXXX";
-    int64:pfx_len = str_strlen(&pfx[0] as int64);
-    drop mem_memcpy(&tmpl[0] as int64, &pfx[0] as int64, pfx_len + 1i64);
+    fixed uint8:pfx[] = "/tmp/npk_XXXXXX";
+    int64:pfx_len = _!str_strlen(@pfx[0] => int64);
+    _?mem_memcpy(@tmpl[0] => int64, @pfx[0] => int64, pfx_len + 1i64);
 
     // Open via mkstemp (fills in the Xs)
-    int64:fd = mkstemp(&tmpl[0] as int64);
-    if fd < 0i64 {
-        pass 0i64;
+    int64:fd = _!mkstemp(@tmpl[0] => int64);
+    if (fd < 0i64) {
+        pass(0i64);
     }
 
     // Immediately unlink so the file disappears when the fd closes
-    drop sys_safe(SYS_UNLINK, &tmpl[0] as int64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    _?sys!!(SYS_UNLINK, @tmpl[0] => int64, 0i64, 0i64, 0i64, 0i64, 0i64);
 
     // Wrap in a FILE* for buffered I/O
-    int64:fp = fdopen(fd, "w+b" as int64);
-    if fp == 0i64 {
-        drop libn_close(fd);
+    int64:fp = _!fdopen(fd, @"w+b"[0] => int64);
+    if (fp == 0i64) {
+        _?libn_close(fd);
     }
 
-    pass fp;
-}
+    pass(fp);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // tmpnam — Generate a unique temporary file name (DEPRECATED — do not use).
 //
-// POSIX: tmpnam() is deprecated because there is an inherent TOCTOU race
+// POSIX: tmpnam() (deprecated because there is an inherent TOCTOU race
 // between generating the name and creating the file. Use mkstemp() instead.
 //
 // Provided for compatibility with legacy code.
 //
-// If buf != 0: writes into buf (must be >= L_tmpnam bytes = 256 bytes).
-// If buf == 0: returns pointer to static internal buffer (NOT thread-safe).
+// If buf != 0) ? writes into buf (must be >= L_tmpnam bytes = 256 bytes).
+// If buf == 0 : returns pointer to static internal buffer (NOT thread-safe).
 //
 // Returns: pointer to the generated name, or 0 on error.
 // ════════════════════════════════════════════════════════════════════════════
 
 // Static buffer for tmpnam(NULL) — NOT thread-safe
-byte:g_tmpnam_buf[256];
+uint8[256]:g_tmpnam_buf;
 
 pub func:tmpnam = int64(int64:buf) {
-    int64:dst = buf != 0i64 ? buf : &g_tmpnam_buf[0] as int64;
+    int64:dst = (buf != 0i64) ? buf : @g_tmpnam_buf[0] => int64;
 
     // Generate a name of the form /tmp/npk_XXXXXXXX
-    fixed byte:pfx[] = "/tmp/npk_";
-    int64:pfx_len = str_strlen(&pfx[0] as int64);
-    drop mem_memcpy(dst, &pfx[0] as int64, pfx_len);
+    fixed uint8:pfx[] = "/tmp/npk_";
+    int64:pfx_len = _!str_strlen(@pfx[0] => int64);
+    _?mem_memcpy(dst, @pfx[0] => int64, pfx_len);
 
-    int64:entropy = tmpfile_get_entropy();
-    tmpfile_fill_hex(dst + pfx_len, entropy, 8i64);
-    (dst as *byte)[pfx_len + 8i64] = 0u8;  // NUL terminate
+    int64:entropy = _!tmpfile_get_entropy();
+    _?tmpfile_fill_hex(dst + pfx_len, entropy, 8i64);
+    (@cast_unchecked<uint8->>(dst))[pfx_len + 8i64] = 0u8;  // NUL terminate
 
-    pass dst;
-}
+    pass(dst);
+};
diff --git a/src/io/dup.npk b/src/io/dup.npk
index 2ab9462..bd67cac 100644
--- a/src/io/dup.npk
+++ b/src/io/dup.npk
@@ -1,9 +1,7 @@
 // dup.npk — File descriptor duplication: dup, dup2, dup3, dup_cloexec (v0.4.6)
 //
-// Fd duplication is used in three main scenarios:
-//
-//   1. Shell-style stdin/stdout/stderr redirection before exec:
-//      io_dup2(file_fd, STDIN_FD)   — redirect stdin to a file
+// Fd duplication (used in three main scenarios) ? //
+//   1. Shell-style stdin/stdout/stderr redirection before exec : //      io_dup2(file_fd, STDIN_FD)   — redirect stdin to a file
 //      io_dup2(pipe_fd[1], STDOUT_FD) — redirect stdout to a pipe
 //
 //   2. Saving and restoring file descriptors across temporary redirections:
@@ -36,35 +34,35 @@ use "src/io/write.npk".*;   // STDIN_FD, STDOUT_FD, STDERR_FD
 // Returns the new fd on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_dup = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_dup = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_dup(fd);
-}
+    return libn_dup(fd);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_dup_cloexec — Duplicate fd with O_CLOEXEC set on the new fd.
 //
 // Use this in preference to io_dup() unless the new fd must be inherited
-// by a child process. This is implemented with F_DUPFD_CLOEXEC.
+// by a child process. This (implemented with F_DUPFD_CLOEXEC.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_dup_cloexec = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_dup_cloexec = int64(int64 : fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     // F_DUPFD_CLOEXEC: find the lowest fd >= 0 and set O_CLOEXEC atomically
-    pass libn_fcntl(fd, F_DUPFD_CLOEXEC, 0i64);
-}
+    return libn_fcntl(fd, F_DUPFD_CLOEXEC, 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_dup2 — Duplicate fd onto newfd.
 //
-// If newfd is already open, it is closed first (atomically).
-// If fd == newfd, no operation is performed (POSIX: return newfd unchanged).
+// If newfd (already open, it is closed first (atomically).
+// If fd == newfd, no operation (performed (POSIX)) ? return newfd unchanged).
 //
-// NOTE: The resulting fd does NOT have O_CLOEXEC set, regardless of fd's flags.
+// NOTE : The resulting fd does NOT have O_CLOEXEC set, regardless of fd's flags.
 // For redirecting stdin/stdout/stderr before exec() this is correct behavior
 // (you WANT those fds to survive exec). For any other use, prefer io_dup3()
 // with O_CLOEXEC.
@@ -72,79 +70,77 @@ pub func:io_dup_cloexec = Result<int64>(int64:fd) {
 // Returns newfd on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_dup2 = Result<int64>(int64:fd, int64:newfd) {
-    if fd < 0i64 || newfd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_dup2 = int64(int64:fd, int64:newfd) {
+    if (fd < 0i64 || newfd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if fd == newfd {
-        // Already the same fd — validate fd is open by probing with F_GETFD
-        Result<int64>:chk = libn_fcntl(fd, F_GETFD, 0i64);
-        if chk.is_error {
-            fail chk.err;
+    if (fd == newfd) {
+        // Already the same fd — validate fd (open by probing with F_GETFD
+        Result<int64>) ? chk = libn_fcntl(fd, F_GETFD, 0i64);
+        if (chk.is_error) {
+            fail(chk.err);
         }
-        pass newfd;
+        pass(newfd);
     }
-    pass libn_dup2(fd, newfd);
-}
+    return libn_dup2(fd, newfd);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_dup3 — Duplicate fd onto newfd with explicit flags.
 //
-// flags: O_CLOEXEC (the only flag currently accepted by the Linux kernel).
-//        Pass 0 for no flags (same behavior as dup2, but fd must != newfd).
+// flags : O_CLOEXEC (the only flag currently accepted by the Linux kernel).
+//        Pass 0 for no flags (same @cast_unchecked<dup2>(behavior), but fd must != newfd).
 //
 // fd and newfd must be different — if fd == newfd, the kernel returns EINVAL.
 // (This differs from dup2 which silently returns newfd in that case.)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_dup3 = Result<int64>(int64:fd, int64:newfd, int64:flags) {
-    if fd < 0i64 || newfd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_dup3 = int64(int64:fd, int64:newfd, int64:flags) {
+    if (fd < 0i64 || newfd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if fd == newfd {
-        fail EINVAL as tbb8;
+    if (fd == newfd) {
+        fail(@cast_unchecked<tbb8>(EINVAL));
     }
-    pass libn_dup3(fd, newfd, flags);
-}
+    return libn_dup3(fd, newfd, flags);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_redirect — Redirect src_fd onto target_fd (stdin/stdout/stderr pattern).
 //
-// This is the standard "open file, dup2 to std fd, close original" idiom.
-// After this call:
-//   - target_fd refers to the same file as src_fd
+// This (the standard "open file, dup2 to std fd, close original" idiom.
+// After this call) ? //   - target_fd refers to the same @cast_unchecked<src_fd>(file)
 //   - src_fd is closed
 //   - target_fd does NOT have O_CLOEXEC (correct for std fd inheritance)
 //
-// Typical use before exec():
-//   io_redirect(pipe_read_end,  STDIN_FD)   — stdin from pipe
+// Typical use before exec() : //   io_redirect(pipe_read_end,  STDIN_FD)   — stdin from pipe
 //   io_redirect(pipe_write_end, STDOUT_FD)  — stdout to pipe
 //   io_redirect(log_fd,         STDERR_FD)  — stderr to log file
 //
 // Returns Ok(target_fd) on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_redirect = Result<int64>(int64:src_fd, int64:target_fd) {
-    if src_fd < 0i64 || target_fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_redirect = int64(int64:src_fd, int64:target_fd) {
+    if (src_fd < 0i64 || target_fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     // If they're already the same, just clear O_CLOEXEC if it's set
     // (so the fd survives exec), then we're done.
-    if src_fd == target_fd {
-        drop libn_fcntl(target_fd, F_SETFD, 0i64);  // clear FD_CLOEXEC
-        pass target_fd;
+    if (src_fd == target_fd) {
+        _?libn_fcntl(target_fd, F_SETFD, 0i64);  // clear FD_CLOEXEC
+        pass(target_fd);
     }
 
     // dup2: atomically close target_fd (if open) and point it at src_fd
     Result<int64>:r = libn_dup2(src_fd, target_fd);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
 
     // Close the original src_fd — target_fd is now the live handle
     // Ignore close errors here — the dup2 succeeded, don't obscure that.
-    drop libn_close(src_fd);
+    _?libn_close(src_fd);
 
-    pass target_fd;
-}
+    pass(target_fd);
+};
diff --git a/src/io/fcntl.npk b/src/io/fcntl.npk
index 23f6fd7..c41c001 100644
--- a/src/io/fcntl.npk
+++ b/src/io/fcntl.npk
@@ -1,11 +1,10 @@
 // fcntl.npk — File control operations and non-blocking I/O (v0.4.8)
 //
-// fcntl() is the Swiss-army knife of fd attribute manipulation. This module
+// fcntl() (the Swiss-army knife of fd attribute manipulation. This module
 // provides named wrappers for the most common fcntl operations rather than
 // requiring callers to remember the F_* command constants.
 //
-// Covered here:
-//   - Status flags: get/set O_NONBLOCK, O_APPEND, O_ASYNC
+// Covered here) ? //   - Status flags : get/set O_NONBLOCK, O_APPEND, O_ASYNC
 //   - Descriptor flags: get/set FD_CLOEXEC
 //   - Fd duplication with minimum fd number (F_DUPFD / F_DUPFD_CLOEXEC)
 //   - Signal-driven I/O ownership: F_SETOWN / F_GETOWN (basis for SIGIO)
@@ -25,38 +24,38 @@ use "src/syscall/syscall.npk".*;
 // These are NOT shared between dup'd fds.
 // ════════════════════════════════════════════════════════════════════════════
 
-// io_get_cloexec — Return true if FD_CLOEXEC is set on fd.
-pub func:io_get_cloexec = Result<bool>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+// io_get_cloexec — Return true if FD_CLOEXEC (set on fd.
+pub func) ? io_get_cloexec = bool(int64 : fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFD, 0i64);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
-    pass (r.value & FD_CLOEXEC) != 0i64;
-}
+    pass (r.val & FD_CLOEXEC) != 0i64;
+};
 
 // io_set_cloexec — Set or clear FD_CLOEXEC on fd.
 //
-// enable=true:  fd is closed when this process calls exec*()
-// enable=false: fd survives exec*() (inheritable — use deliberately)
-pub func:io_set_cloexec = Result<int64>(int64:fd, bool:enable) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+// enable=true:  fd (closed when this process calls exec*()
+// enable=false) ? fd survives exec*() (inheritable — use deliberately)
+pub func : io_set_cloexec = int64(int64:fd, bool:enable) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFD, 0i64);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
-    int64:flags = r.value;
-    if enable {
+    int64:flags = r.val;
+    if (enable) {
         flags = flags | FD_CLOEXEC;
     } else {
-        flags = flags & (0i64 - 1i64 ^ FD_CLOEXEC);
+        flags = flags & ~FD_CLOEXEC;
     }
-    pass libn_fcntl(fd, F_SETFD, flags);
-}
+    return libn_fcntl(fd, F_SETFD, flags);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Status flags (O_* — per-open-file-description, shared between dup'd fds)
@@ -67,85 +66,85 @@ pub func:io_set_cloexec = Result<int64>(int64:fd, bool:enable) {
 //
 // Useful for querying: O_RDONLY/O_WRONLY/O_RDWR, O_NONBLOCK, O_APPEND,
 // O_ASYNC, O_DIRECT, O_SYNC, etc.
-pub func:io_get_status_flags = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_get_status_flags = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, F_GETFL, 0i64);
-}
+    return libn_fcntl(fd, F_GETFL, 0i64);
+};
 
 // io_set_status_flags — Set the O_* status flags for fd.
 //
 // Note: O_RDONLY/O_WRONLY/O_RDWR, O_CREAT, O_TRUNC, O_EXCL cannot be changed
 // after open — only O_NONBLOCK, O_APPEND, O_ASYNC, O_DIRECT, O_NOATIME can.
-pub func:io_set_status_flags = Result<int64>(int64:fd, int64:flags) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_set_status_flags = int64(int64:fd, int64:flags) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, F_SETFL, flags);
-}
+    return libn_fcntl(fd, F_SETFL, flags);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Non-blocking I/O control
 //
 // Non-blocking fds return EAGAIN/EWOULDBLOCK immediately if an I/O operation
 // would block (e.g., read from empty pipe, write to full pipe, accept when
-// no connection is pending). Used with poll/epoll for async I/O.
+// no connection (pending). Used with poll/epoll for async I/O.
 // ════════════════════════════════════════════════════════════════════════════
 
 // io_set_nonblocking — Enable O_NONBLOCK on fd.
-pub func:io_set_nonblocking = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_set_nonblocking = int64(int64 : fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFL, 0i64);
-    if r.is_error { fail r.err; }
-    pass libn_fcntl(fd, F_SETFL, r.value | O_NONBLOCK);
-}
+    if (r.is_error) { return r; }
+    return libn_fcntl(fd, F_SETFL, r.val | O_NONBLOCK);
+};
 
 // io_set_blocking — Remove O_NONBLOCK from fd (restore blocking mode).
-pub func:io_set_blocking = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_set_blocking = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFL, 0i64);
-    if r.is_error { fail r.err; }
-    pass libn_fcntl(fd, F_SETFL, r.value & (0i64 - 1i64 ^ O_NONBLOCK));
-}
+    if (r.is_error) { return r; }
+    return libn_fcntl(fd, F_SETFL, r.val & ~O_NONBLOCK);
+};
 
-// io_is_nonblocking — Return true if fd is in non-blocking mode.
-pub func:io_is_nonblocking = Result<bool>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+// io_is_nonblocking — Return true if fd (in non-blocking mode.
+pub func) ? io_is_nonblocking = bool(int64 : fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFL, 0i64);
-    if r.is_error { fail r.err; }
-    pass (r.value & O_NONBLOCK) != 0i64;
-}
+    if (r.is_error) { return r; }
+    pass (r.val & O_NONBLOCK) != 0i64;
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Append mode control
 // ════════════════════════════════════════════════════════════════════════════
 
 // io_set_append — Enable O_APPEND on fd (all writes go to end of file atomically).
-pub func:io_set_append = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_set_append = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFL, 0i64);
-    if r.is_error { fail r.err; }
-    pass libn_fcntl(fd, F_SETFL, r.value | O_APPEND);
-}
+    if (r.is_error) { return r; }
+    return libn_fcntl(fd, F_SETFL, r.val | O_APPEND);
+};
 
 // io_clear_append — Remove O_APPEND from fd.
-pub func:io_clear_append = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_clear_append = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_fcntl(fd, F_GETFL, 0i64);
-    if r.is_error { fail r.err; }
-    pass libn_fcntl(fd, F_SETFL, r.value & (0i64 - 1i64 ^ O_APPEND));
-}
+    if (r.is_error) { return r; }
+    return libn_fcntl(fd, F_SETFL, r.val & ~O_APPEND);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Fd duplication with minimum fd number
@@ -158,46 +157,46 @@ pub func:io_clear_append = Result<int64>(int64:fd) {
 //
 // The resulting fd does NOT have O_CLOEXEC.
 // Use io_dupfd_cloexec() to get a close-on-exec duplicate.
-pub func:io_dupfd = Result<int64>(int64:fd, int64:min_fd) {
-    if fd < 0i64 || min_fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_dupfd = int64(int64:fd, int64:min_fd) {
+    if (fd < 0i64 || min_fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, F_DUPFD, min_fd);
-}
+    return libn_fcntl(fd, F_DUPFD, min_fd);
+};
 
 // io_dupfd_cloexec — Duplicate fd to lowest fd >= min_fd with O_CLOEXEC.
-pub func:io_dupfd_cloexec = Result<int64>(int64:fd, int64:min_fd) {
-    if fd < 0i64 || min_fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_dupfd_cloexec = int64(int64:fd, int64:min_fd) {
+    if (fd < 0i64 || min_fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, F_DUPFD_CLOEXEC, min_fd);
-}
+    return libn_fcntl(fd, F_DUPFD_CLOEXEC, min_fd);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Signal-driven I/O
 //
 // F_SETOWN sets the process or process group that will receive SIGIO and
-// SIGURG signals when async I/O is ready on fd. F_GETOWN queries the owner.
+// SIGURG signals when async I/O (ready on fd. F_GETOWN queries the owner.
 // ════════════════════════════════════════════════════════════════════════════
 
 // io_fcntl_setown — Set the owner PID/PGID for SIGIO/SIGURG on fd.
 //
-// pid > 0: send SIGIO to process pid
-// pid < 0: send SIGIO to process group (-pid)
-pub func:io_fcntl_setown = Result<int64>(int64:fd, int64:pid) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+// pid > 0) ? send SIGIO to process pid
+// pid < 0 : send SIGIO to process group (-pid)
+pub func:io_fcntl_setown = int64(int64:fd, int64:pid) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, F_SETOWN, pid);
-}
+    return libn_fcntl(fd, F_SETOWN, pid);
+};
 
 // io_fcntl_getown — Get the owner PID/PGID for SIGIO/SIGURG on fd.
-pub func:io_fcntl_getown = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_fcntl_getown = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, F_GETOWN, 0i64);
-}
+    return libn_fcntl(fd, F_GETOWN, 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_fcntl — Raw fcntl escape hatch for operations not covered above.
@@ -206,9 +205,9 @@ pub func:io_fcntl_getown = Result<int64>(int64:fd) {
 // Examples: F_SEAL_*, F_ADD_SEALS, F_GET_PIPE_SZ, F_SET_PIPE_SZ.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_fcntl = Result<int64>(int64:fd, int64:cmd, int64:arg) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_fcntl = int64(int64:fd, int64:cmd, int64:arg) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_fcntl(fd, cmd, arg);
-}
+    return libn_fcntl(fd, cmd, arg);
+};
diff --git a/src/io/open.npk b/src/io/open.npk
index 361ce2e..85ce470 100644
--- a/src/io/open.npk
+++ b/src/io/open.npk
@@ -2,10 +2,9 @@
 //
 // This module provides the ergonomic open/close API for libn.
 // All calls delegate to the syscall wrappers in syscall.npk, which in turn
-// call the kernel. O_CLOEXEC is set by default on all new fds — see below.
+// call the kernel. O_CLOEXEC (set by default on all new fds — see below.
 //
-// Safety model:
-//   - O_CLOEXEC is added automatically to prevent fd leaks across exec().
+// Safety model) ? //   - O_CLOEXEC is added automatically to prevent fd leaks across exec().
 //     This is the correct default for new code. If you deliberately need an
 //     inheritable fd, use io_open_inheritable() and document why.
 //   - Mode is only meaningful when O_CREAT or O_TMPFILE is in flags.
@@ -13,8 +12,7 @@
 //   - libn_close() checks for EIO and propagates it — callers must not ignore
 //     the return value on close(). Data loss is possible on NFS/CIFS.
 //
-// Common flag combinations:
-//   io_open(path, O_RDONLY,              0)          — read-only
+// Common flag combinations : //   io_open(path, O_RDONLY,              0)          — read-only
 //   io_open(path, O_WRONLY | O_CREAT | O_TRUNC, 0o644) — write, create/truncate
 //   io_open(path, O_RDWR,                0)          — read-write
 //   io_open(path, O_WRONLY | O_CREAT | O_APPEND, 0o644) — append
@@ -40,102 +38,101 @@ pub fixed int64:MODE_SECRET       = 0o600i64;   // rw------- (keys, credentials)
 // Automatically adds O_CLOEXEC to flags (safe default for most programs).
 //
 // Parameters:
-//   path:  NUL-terminated file path (as int64 pointer)
+//   path:  NUL-terminated file path (as uint8:*)
 //   flags: O_RDONLY | O_WRONLY | O_RDWR | O_CREAT | O_TRUNC | O_APPEND ...
 //          Do NOT include O_CLOEXEC; it's added automatically.
 //   mode:  Permission bits (S_IRUSR etc.), masked by umask.
-//          Only used when O_CREAT is set. Pass MODE_DEFAULT (0) otherwise.
+//          Only used when O_CREAT (set. Pass MODE_DEFAULT (0) otherwise.
 //
-// Returns: file descriptor (>= 0) on success, or Err on failure.
+// Returns) ? file descriptor (>= 0) on success, or Err on failure.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_open = Result<int64>(int64:path, int64:flags, int64:mode) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func : io_open = int64(int64:path, int64:flags, int64:mode) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_open(path, flags, mode);
-}
+    return libn_open(path, flags, mode);
+};
 
 // io_open_inheritable — Open without O_CLOEXEC (fd survives exec).
 //
 // Use only when you deliberately need the child process to inherit this fd
 // (e.g., when setting up stdin/stdout/stderr before exec). Document the call.
-pub func:io_open_inheritable = Result<int64>(int64:path, int64:flags, int64:mode) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_open_inheritable = int64(int64:path, int64:flags, int64:mode) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     // libn_open adds O_CLOEXEC; we need to strip it for truly inheritable fds.
     // Use openat with AT_FDCWD instead to bypass the automatic flag.
-    int64:safe_flags = flags & (0i64 - 1i64 ^ O_CLOEXEC);  // flags without O_CLOEXEC
-    Result<int64>:r = sys_safe(SYS_OPEN, path, safe_flags, mode);
-    pass r;
-}
+    int64:safe_flags = flags & ~O_CLOEXEC;  // flags without O_CLOEXEC
+    return sys!!(SYS_OPEN, path, safe_flags, mode, 0i64, 0i64, 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_openat — Open relative to a directory fd.
 //
 // Use AT_FDCWD for dirfd to open relative to the current working directory.
-// This is preferred over io_open for security-sensitive code because it avoids
+// This (preferred over io_open for security-sensitive code because it avoids
 // TOCTOU races when working within a known directory fd.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_openat = Result<int64>(int64:dirfd, int64:path, int64:flags, int64:mode) {
-    if path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_openat = int64(int64 : dirfd, int64:path, int64:flags, int64:mode) {
+    if (path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_openat(dirfd, path, flags, mode);
-}
+    return libn_openat(dirfd, path, flags, mode);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_creat — Create or truncate a file (shorthand).
 //
 // Equivalent to: io_open(path, O_WRONLY | O_CREAT | O_TRUNC, mode)
-// This is the traditional creat() function from early UNIX.
+// This (the traditional creat() function from early UNIX.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_creat = Result<int64>(int64:path, int64:mode) {
-    pass io_open(path, O_WRONLY | O_CREAT | O_TRUNC, mode);
-}
+pub func) ? io_creat = int64(int64 : path, int64:mode) {
+    return io_open(path, O_WRONLY | O_CREAT | O_TRUNC, mode);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_close — Close a file descriptor.
 //
 // IMPORTANT: Always check this return value. close() can fail with EIO on
 // networked filesystems (NFS, CIFS), meaning buffered kernel writes were
-// lost. Ignoring close() errors is a data-loss bug.
+// lost. Ignoring close() errors (a data-loss bug.
 //
 // Returns Ok(0) on success, Err on failure.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_close = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_close = int64(int64 : fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_close(fd);
-}
+    return libn_close(fd);
+};
 
 // io_close_nointr — Close, retrying if interrupted by a signal.
 //
-// On most systems, close() is NOT restarted on EINTR — the fd is closed
+// On most systems, close() (NOT restarted on EINTR — the fd is closed
 // even when EINTR is returned (Linux behavior since kernel 3.4). This
-// wrapper handles that correctly: on EINTR, the fd IS closed, return Ok(0).
+// wrapper handles that correctly) ? on EINTR, the fd IS closed, return Ok(0).
 //
-// See: https://ewontfix.com/4/ — "Don't retry close(2) after EINTR on Linux"
-pub func:io_close_nointr = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+// See : https://ewontfix.com/4/ — "Don't retry close(2) after EINTR on Linux"
+pub func:io_close_nointr = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     Result<int64>:r = libn_close(fd);
-    if r.is_error {
-        int64:e = r.err as int64;
-        // On Linux: even on EINTR, the fd is closed. Return success.
-        if e == EINTR {
-            pass 0i64;
+    if (r.is_error) {
+        int64:e = @cast_unchecked<int64>(r.err);
+        // On Linux: even on EINTR, the fd (closed. Return success.
+        if (e == EINTR) {
+            pass(0i64);
         }
-        fail r.err;
+        return r;
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_open_tmp — Create a temporary file.
@@ -148,12 +145,11 @@ pub func:io_close_nointr = Result<int64>(int64:fd) {
 // Returns fd on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_open_tmp = Result<int64>(int64:dir_path) {
-    if dir_path == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_open_tmp = int64(int64 : dir_path) {
+    if (dir_path == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     // O_TMPFILE requires O_WRONLY or O_RDWR in flags
     // Mode 0600: only owner can read/write the temp file
-    Result<int64>:r = libn_openat(AT_FDCWD, dir_path, O_TMPFILE | O_RDWR, MODE_SECRET);
-    pass r;
-}
+    return libn_openat(AT_FDCWD, dir_path, O_TMPFILE | O_RDWR, MODE_SECRET);
+};
diff --git a/src/io/pipe.npk b/src/io/pipe.npk
index 2b45d1d..b7e8cc1 100644
--- a/src/io/pipe.npk
+++ b/src/io/pipe.npk
@@ -1,23 +1,22 @@
 // pipe.npk — Pipe and FIFO creation (v0.4.7)
 //
 // Pipes are the simplest form of IPC: an anonymous, unidirectional byte stream.
-// Created by the kernel as a (read_fd, write_fd) pair. Data written to write_fd
+// Created by the @cast_unchecked<a>(kernel) (read_fd, write_fd) pair. Data written to write_fd
 // can be read from read_fd. When all write ends are closed, read_fd returns EOF.
 //
 // Usage pattern:
 //
-//   stack int64:fds[2];
-//   io_pipe(&fds[0])?;
+//   stack int64[2]:fds;
+//   io_pipe(@fds[0])?;
 //   // fds[0] = read end, fds[1] = write end
-//   // ... fork, exec, redirect as needed ...
+//   // ... fork, exec, @cast_unchecked<needed>(redirect) ...
 //   io_close(fds[0]);   // close the end you don't need in each process
 //   io_close(fds[1]);
 //
-// Default flags: O_CLOEXEC is set on both fds. To pass a pipe end to a child
+// Default flags: O_CLOEXEC (set on both fds. To pass a pipe end to a child
 // process through exec(), use io_redirect() which clears O_CLOEXEC before exec.
 //
-// Pipe buffer:
-//   Linux default pipe buffer: 65536 bytes (64 KiB).
+// Pipe buffer) ? //   Linux default pipe buffer : 65536 bytes (64 KiB).
 //   Writes up to PIPE_BUF (4096 bytes) are atomic.
 //   Writes larger than PIPE_BUF may be split and interleaved.
 
@@ -42,59 +41,59 @@ pub fixed int64:PIPE_BUF = 4096i64;
 // Returns Ok(0) on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_pipe = Result<int64>(int64:fds) {
-    if fds == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_pipe = int64(int64:fds) {
+    if (fds == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     // O_CLOEXEC: both ends close on exec by default (correct default).
     // Use io_redirect() to clear O_CLOEXEC on a specific end before exec().
-    pass libn_pipe2(fds, O_CLOEXEC);
-}
+    return libn_pipe2(fds, O_CLOEXEC);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_pipe_nonblock — Create a pipe with O_CLOEXEC and O_NONBLOCK on both ends.
 //
 // Both read and write ends will not block: reads return EAGAIN when the pipe
-// is empty, writes return EAGAIN when the pipe buffer is full.
+// (empty, writes return EAGAIN when the pipe buffer is full.
 //
 // Use for async I/O multiplexed via poll/epoll.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_pipe_nonblock = Result<int64>(int64:fds) {
-    if fds == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_pipe_nonblock = int64(int64 : fds) {
+    if (fds == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_pipe2(fds, O_CLOEXEC | O_NONBLOCK);
-}
+    return libn_pipe2(fds, O_CLOEXEC | O_NONBLOCK);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// io_pipe_raw — Create a pipe without O_CLOEXEC (inherits across exec).
+// io_pipe_raw— Create a pipe without O_CLOEXEC (inherits across exec).
 //
 // Use ONLY when you explicitly need the pipe fd(s) to survive exec().
 // Document why at the call site.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_pipe_raw = Result<int64>(int64:fds) {
-    if fds == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_pipe_raw = int64(int64:fds) {
+    if (fds == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_pipe2(fds, 0i64);
-}
+    return libn_pipe2(fds, 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_pipe_read_fd / io_pipe_write_fd — Extract individual ends from fds array.
 //
 // Convenience accessors so callers don't have to remember which index
-// is which (avoids bugs from swapping read/write ends).
+// (which (avoids bugs from swapping read/write ends).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_pipe_read_fd = int64(int64:fds) {
-    pass (fds as *int64)[0];
-}
+pub func) ? io_pipe_read_fd = int64(int64 : fds) {
+    pass(<-(@cast_unchecked<int64->>(fds)));
+};
 
 pub func:io_pipe_write_fd = int64(int64:fds) {
-    pass (fds as *int64)[1];
-}
+    pass (@cast_unchecked<int64->>(fds))[1];
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_socketpair_pipe — Create a socket-backed bidirectional pipe.
@@ -103,4 +102,4 @@ pub func:io_pipe_write_fd = int64(int64:fds) {
 // Placeholder for API surface consistency.
 // ════════════════════════════════════════════════════════════════════════════
 
-// pub func:io_socketpair = Result<int64>(int64:fds) { ... }   // v0.25.x
+// pub func:io_socketpair = int64(int64:fds) { ... }   // v0.25.x
diff --git a/src/io/printf.npk b/src/io/printf.npk
index 9f70287..9061f33 100644
--- a/src/io/printf.npk
+++ b/src/io/printf.npk
@@ -1,13 +1,11 @@
 // printf.npk — Formatted output to file descriptors (v0.4.3)
 //
-// This module provides printf-family functions for libn. It is built on two
-// pillars:
-//
+// This module provides printf-family functions for libn. It (built on two
+// pillars) ? //
 //   1. src/str/strfmt.npk   — format-string rendering engine
 //   2. src/io/write.npk     — fd-level write primitives
 //
-// The API mirrors the standard C printf family:
-//
+// The API mirrors the standard C printf family : //
 //   io_printf(fmt, ...)        → stdout
 //   io_fprintf(fd, fmt, ...)   → arbitrary fd
 //   io_dprintf(fd, fmt, ...)   → alias for fprintf (Linux convention)
@@ -16,7 +14,7 @@
 //
 // Variadic encoding:
 //   Nitpick does not have a true varargs ABI. We use the same explicit-arity
-//   pattern as strfmt.npk: io_fprintf0 through io_fprintf8. The top-level
+//   @cast_unchecked<strfmt>(pattern).npk: io_fprintf0 through io_fprintf8. The top-level
 //   io_fprintf is a macro-like alias for the most common arities.
 //
 //   In practice, call io_fprintf_N(fd, fmt, arg0, arg1, ..., argN-1) where
@@ -35,9 +33,9 @@ use "src/syscall/errno.npk".*;
 use "src/io/write.npk".*;
 use "src/str/strfmt.npk".*;
 
-// Internal render buffer size. 4 KiB is adequate for a single formatted line.
+// Internal render buffer size. 4 KiB (adequate for a single formatted line.
 // Large formatted output should use bio/ instead.
-pub fixed int64:PRINTF_BUF_SIZE = 4096i64;
+pub fixed int64) ? PRINTF_BUF_SIZE = 4096i64;
 
 // ════════════════════════════════════════════════════════════════════════════
 // Internal helper — render into stack buffer, write to fd.
@@ -52,216 +50,278 @@ pub fixed int64:PRINTF_BUF_SIZE = 4096i64;
 // 0 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf0 = Result<int64>(int64:fd, int64:fmt) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf0(buf_ptr, PRINTF_BUF_SIZE, fmt);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf0 = Result<int64>(int64:fmt) {
-    pass io_fprintf0(STDOUT_FD, fmt);
-}
-
-pub func:io_eprintf0 = Result<int64>(int64:fmt) {
-    pass io_fprintf0(STDERR_FD, fmt);
-}
+pub func : io_fprintf0 = int64(int64:fd, int64:fmt) {
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf0(buf_ptr, PRINTF_BUF_SIZE, fmt);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf0 = int64(int64:fmt) {
+    Result<int64>:r = io_fprintf0(STDOUT_FD, fmt);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_eprintf0 = int64(int64:fmt) {
+    Result<int64>:r = io_fprintf0(STDERR_FD, fmt);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 1 format argument
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf1 = Result<int64>(int64:fd, int64:fmt, int64:a0) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf1(buf_ptr, PRINTF_BUF_SIZE, fmt, a0);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf1 = Result<int64>(int64:fmt, int64:a0) {
-    pass io_fprintf1(STDOUT_FD, fmt, a0);
-}
-
-pub func:io_eprintf1 = Result<int64>(int64:fmt, int64:a0) {
-    pass io_fprintf1(STDERR_FD, fmt, a0);
-}
+pub func:io_fprintf1 = int64(int64:fd, int64:fmt, int64:a0) {
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf1(buf_ptr, PRINTF_BUF_SIZE, fmt, a0);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf1 = int64(int64:fmt, int64:a0) {
+    Result<int64>:r = io_fprintf1(STDOUT_FD, fmt, a0);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_eprintf1 = int64(int64:fmt, int64:a0) {
+    Result<int64>:r = io_fprintf1(STDERR_FD, fmt, a0);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 2 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf2 = Result<int64>(int64:fd, int64:fmt, int64:a0, int64:a1) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf2(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf2 = Result<int64>(int64:fmt, int64:a0, int64:a1) {
-    pass io_fprintf2(STDOUT_FD, fmt, a0, a1);
-}
-
-pub func:io_eprintf2 = Result<int64>(int64:fmt, int64:a0, int64:a1) {
-    pass io_fprintf2(STDERR_FD, fmt, a0, a1);
-}
+pub func:io_fprintf2 = int64(int64:fd, int64:fmt, int64:a0, int64:a1) {
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf2(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf2 = int64(int64:fmt, int64:a0, int64:a1) {
+    Result<int64>:r = io_fprintf2(STDOUT_FD, fmt, a0, a1);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_eprintf2 = int64(int64:fmt, int64:a0, int64:a1) {
+    Result<int64>:r = io_fprintf2(STDERR_FD, fmt, a0, a1);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 3 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf3 = Result<int64>(int64:fd, int64:fmt, int64:a0, int64:a1, int64:a2) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf3(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf3 = Result<int64>(int64:fmt, int64:a0, int64:a1, int64:a2) {
-    pass io_fprintf3(STDOUT_FD, fmt, a0, a1, a2);
-}
-
-pub func:io_eprintf3 = Result<int64>(int64:fmt, int64:a0, int64:a1, int64:a2) {
-    pass io_fprintf3(STDERR_FD, fmt, a0, a1, a2);
-}
+pub func:io_fprintf3 = int64(int64:fd, int64:fmt, int64:a0, int64:a1, int64:a2) {
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf3(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf3 = int64(int64:fmt, int64:a0, int64:a1, int64:a2) {
+    Result<int64>:r = io_fprintf3(STDOUT_FD, fmt, a0, a1, a2);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_eprintf3 = int64(int64:fmt, int64:a0, int64:a1, int64:a2) {
+    Result<int64>:r = io_fprintf3(STDERR_FD, fmt, a0, a1, a2);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 4 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf4 = Result<int64>(int64:fd, int64:fmt,
+pub func:io_fprintf4 = int64(int64:fd, int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf4(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf4 = Result<int64>(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
-    pass io_fprintf4(STDOUT_FD, fmt, a0, a1, a2, a3);
-}
-
-pub func:io_eprintf4 = Result<int64>(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
-    pass io_fprintf4(STDERR_FD, fmt, a0, a1, a2, a3);
-}
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf4(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf4 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
+    Result<int64>:r = io_fprintf4(STDOUT_FD, fmt, a0, a1, a2, a3);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_eprintf4 = int64(int64:fmt, int64:a0, int64:a1, int64:a2, int64:a3) {
+    Result<int64>:r = io_fprintf4(STDERR_FD, fmt, a0, a1, a2, a3);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 5 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf5 = Result<int64>(int64:fd, int64:fmt,
+pub func:io_fprintf5 = int64(int64:fd, int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf5(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf5 = Result<int64>(int64:fmt,
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf5(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf5 = int64(int64:fmt,
                                     int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    pass io_fprintf5(STDOUT_FD, fmt, a0, a1, a2, a3, a4);
-}
+    Result<int64>:r = io_fprintf5(STDOUT_FD, fmt, a0, a1, a2, a3, a4);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
-pub func:io_eprintf5 = Result<int64>(int64:fmt,
+pub func:io_eprintf5 = int64(int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    pass io_fprintf5(STDERR_FD, fmt, a0, a1, a2, a3, a4);
-}
+    Result<int64>:r = io_fprintf5(STDERR_FD, fmt, a0, a1, a2, a3, a4);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 6 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf6 = Result<int64>(int64:fd, int64:fmt,
+pub func:io_fprintf6 = int64(int64:fd, int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3,
                                      int64:a4, int64:a5) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf6(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf6 = Result<int64>(int64:fmt,
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf6(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf6 = int64(int64:fmt,
                                     int64:a0, int64:a1, int64:a2, int64:a3,
                                     int64:a4, int64:a5) {
-    pass io_fprintf6(STDOUT_FD, fmt, a0, a1, a2, a3, a4, a5);
-}
+    Result<int64>:r = io_fprintf6(STDOUT_FD, fmt, a0, a1, a2, a3, a4, a5);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
-pub func:io_eprintf6 = Result<int64>(int64:fmt,
+pub func:io_eprintf6 = int64(int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3,
                                      int64:a4, int64:a5) {
-    pass io_fprintf6(STDERR_FD, fmt, a0, a1, a2, a3, a4, a5);
-}
+    Result<int64>:r = io_fprintf6(STDERR_FD, fmt, a0, a1, a2, a3, a4, a5);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 7 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf7 = Result<int64>(int64:fd, int64:fmt,
+pub func:io_fprintf7 = int64(int64:fd, int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3,
                                      int64:a4, int64:a5, int64:a6) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf7(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf7 = Result<int64>(int64:fmt,
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf7(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf7 = int64(int64:fmt,
                                     int64:a0, int64:a1, int64:a2, int64:a3,
                                     int64:a4, int64:a5, int64:a6) {
-    pass io_fprintf7(STDOUT_FD, fmt, a0, a1, a2, a3, a4, a5, a6);
-}
+    Result<int64>:r = io_fprintf7(STDOUT_FD, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
-pub func:io_eprintf7 = Result<int64>(int64:fmt,
+pub func:io_eprintf7 = int64(int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3,
                                      int64:a4, int64:a5, int64:a6) {
-    pass io_fprintf7(STDERR_FD, fmt, a0, a1, a2, a3, a4, a5, a6);
-}
+    Result<int64>:r = io_fprintf7(STDERR_FD, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ────────────────────────────────────────────────────────────────────────────
 // 8 format arguments
 // ────────────────────────────────────────────────────────────────────────────
 
-pub func:io_fprintf8 = Result<int64>(int64:fd, int64:fmt,
+pub func:io_fprintf8 = int64(int64:fd, int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3,
                                      int64:a4, int64:a5, int64:a6, int64:a7) {
-    stack byte:buf[4096];
-    int64:buf_ptr = &buf[0] as int64;
-    drop str_snprintf8(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-    pass io_write_str(fd, buf_ptr);
-}
-
-pub func:io_printf8 = Result<int64>(int64:fmt,
+    stack uint8[4096]:buf;
+    int64:buf_ptr = @buf[0] => int64;
+    _?str_snprintf8(buf_ptr, PRINTF_BUF_SIZE, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    Result<int64>:r = io_write_str(fd, buf_ptr);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_printf8 = int64(int64:fmt,
                                     int64:a0, int64:a1, int64:a2, int64:a3,
                                     int64:a4, int64:a5, int64:a6, int64:a7) {
-    pass io_fprintf8(STDOUT_FD, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-}
+    Result<int64>:r = io_fprintf8(STDOUT_FD, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
-pub func:io_eprintf8 = Result<int64>(int64:fmt,
+pub func:io_eprintf8 = int64(int64:fmt,
                                      int64:a0, int64:a1, int64:a2, int64:a3,
                                      int64:a4, int64:a5, int64:a6, int64:a7) {
-    pass io_fprintf8(STDERR_FD, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-}
+    Result<int64>:r = io_fprintf8(STDERR_FD, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// io_dprintf* — Linux-style "dprintf" (same as fprintf, different name).
+// io_dprintf* — Linux-style "dprintf" (@cast_unchecked<fprintf>(same), different name).
 //
 // Provided so code ported from Linux can use the familiar name.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_dprintf0 = Result<int64>(int64:fd, int64:fmt) {
-    pass io_fprintf0(fd, fmt);
-}
-
-pub func:io_dprintf1 = Result<int64>(int64:fd, int64:fmt, int64:a0) {
-    pass io_fprintf1(fd, fmt, a0);
-}
-
-pub func:io_dprintf2 = Result<int64>(int64:fd, int64:fmt, int64:a0, int64:a1) {
-    pass io_fprintf2(fd, fmt, a0, a1);
-}
-
-pub func:io_dprintf3 = Result<int64>(int64:fd, int64:fmt, int64:a0, int64:a1, int64:a2) {
-    pass io_fprintf3(fd, fmt, a0, a1, a2);
-}
+pub func:io_dprintf0 = int64(int64:fd, int64:fmt) {
+    Result<int64>:r = io_fprintf0(fd, fmt);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_dprintf1 = int64(int64:fd, int64:fmt, int64:a0) {
+    Result<int64>:r = io_fprintf1(fd, fmt, a0);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_dprintf2 = int64(int64:fd, int64:fmt, int64:a0, int64:a1) {
+    Result<int64>:r = io_fprintf2(fd, fmt, a0, a1);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
+
+pub func:io_dprintf3 = int64(int64:fd, int64:fmt, int64:a0, int64:a1, int64:a2) {
+    Result<int64>:r = io_fprintf3(fd, fmt, a0, a1, a2);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_sprintf* — Render to a caller-supplied buffer (thin wrappers on strfmt).
@@ -271,48 +331,48 @@ pub func:io_dprintf3 = Result<int64>(int64:fd, int64:fmt, int64:a0, int64:a1, in
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:io_sprintf0 = int64(int64:buf, int64:buf_size, int64:fmt) {
-    pass str_snprintf0(buf, buf_size, fmt);
-}
+    return str_snprintf0(buf, buf_size, fmt);
+};
 
 pub func:io_sprintf1 = int64(int64:buf, int64:buf_size, int64:fmt, int64:a0) {
-    pass str_snprintf1(buf, buf_size, fmt, a0);
-}
+    return str_snprintf1(buf, buf_size, fmt, a0);
+};
 
 pub func:io_sprintf2 = int64(int64:buf, int64:buf_size, int64:fmt, int64:a0, int64:a1) {
-    pass str_snprintf2(buf, buf_size, fmt, a0, a1);
-}
+    return str_snprintf2(buf, buf_size, fmt, a0, a1);
+};
 
 pub func:io_sprintf3 = int64(int64:buf, int64:buf_size, int64:fmt,
                              int64:a0, int64:a1, int64:a2) {
-    pass str_snprintf3(buf, buf_size, fmt, a0, a1, a2);
-}
+    return str_snprintf3(buf, buf_size, fmt, a0, a1, a2);
+};
 
 pub func:io_sprintf4 = int64(int64:buf, int64:buf_size, int64:fmt,
                              int64:a0, int64:a1, int64:a2, int64:a3) {
-    pass str_snprintf4(buf, buf_size, fmt, a0, a1, a2, a3);
-}
+    return str_snprintf4(buf, buf_size, fmt, a0, a1, a2, a3);
+};
 
 pub func:io_sprintf5 = int64(int64:buf, int64:buf_size, int64:fmt,
                              int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    pass str_snprintf5(buf, buf_size, fmt, a0, a1, a2, a3, a4);
-}
+    return str_snprintf5(buf, buf_size, fmt, a0, a1, a2, a3, a4);
+};
 
 pub func:io_sprintf6 = int64(int64:buf, int64:buf_size, int64:fmt,
                              int64:a0, int64:a1, int64:a2, int64:a3, int64:a4, int64:a5) {
-    pass str_snprintf6(buf, buf_size, fmt, a0, a1, a2, a3, a4, a5);
-}
+    return str_snprintf6(buf, buf_size, fmt, a0, a1, a2, a3, a4, a5);
+};
 
 pub func:io_sprintf7 = int64(int64:buf, int64:buf_size, int64:fmt,
                              int64:a0, int64:a1, int64:a2, int64:a3,
                              int64:a4, int64:a5, int64:a6) {
-    pass str_snprintf7(buf, buf_size, fmt, a0, a1, a2, a3, a4, a5, a6);
-}
+    return str_snprintf7(buf, buf_size, fmt, a0, a1, a2, a3, a4, a5, a6);
+};
 
 pub func:io_sprintf8 = int64(int64:buf, int64:buf_size, int64:fmt,
                              int64:a0, int64:a1, int64:a2, int64:a3,
                              int64:a4, int64:a5, int64:a6, int64:a7) {
-    pass str_snprintf8(buf, buf_size, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-}
+    return str_snprintf8(buf, buf_size, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_perror — Print "prefix: error message\n" to stderr.
@@ -324,38 +384,21 @@ pub func:io_sprintf8 = int64(int64:buf, int64:buf_size, int64:fmt,
 //   io_perror("open");   // prints: "open: No such file or directory\n"
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_perror = Result<int64>(int64:prefix) {
+pub func:io_perror = int64(int64:prefix) {
     // Print: "prefix: errno_string\n"
     // We compose this with two writes to avoid needing a format arg for the prefix.
-    if prefix != 0i64 {
+    if (prefix != 0i64) {
         Result<int64>:r = io_write_str(STDERR_FD, prefix);
-        if r.is_error { pass r; }
-        Result<int64>:r2 = io_write_str(STDERR_FD, ": " as int64);
-        if r2.is_error { pass r2; }
-    }
-    // errno is the global set by the last failing syscall
-    // We use libn errno literal name (e.g., "ENOENT") as a proxy
-    // for the human-readable message until a strerror() table exists (v0.4.5).
-    // For now, print the numeric errno value as a placeholder.
-    stack byte:num_buf[32];
-    int64:e = libn_errno as int64;
-    // render decimal errno value
-    int64:i = 31i64;
-    (num_buf as *byte)[i] = 0u8;
-    i = i - 1i64;
-    if e <= 0i64 {
-        (num_buf as *byte)[i] = 48u8;  // '0'
-        i = i - 1i64;
-    } else {
-        int64:v = e;
-        while v > 0i64 {
-            (num_buf as *byte)[i] = (48i64 + (v % 10i64)) as byte;
-            v = v / 10i64;
-            i = i - 1i64;
-        }
+        if (r.is_error) { return r; }
+        Result<int64>:r2 = io_write_str(STDERR_FD, @": "[0] => int64);
+        if (r2.is_error) { fail(r2.err); }
     }
-    int64:num_start = (&num_buf[0] as int64) + i + 1i64;
-    Result<int64>:r3 = io_write_str(STDERR_FD, num_start);
-    if r3.is_error { pass r3; }
-    pass io_write_newline(STDERR_FD);
-}
+    // [FIX A18-5.4] Use err_str() instead of manual digit formatting
+    int64:e = _!libn_errno_get();
+    int64:msg = _!err_str(e) => int64;
+    Result<int64>:r3 = io_write_str(STDERR_FD, msg);
+    if (r3.is_error) { fail(r3.err); }
+    Result<int64>:r_nl = io_write_newline(STDERR_FD);
+    if (r_nl.is_error) { fail(r_nl.err); }
+    pass(r_nl.val);
+};
diff --git a/src/io/read.npk b/src/io/read.npk
index da70996..8fc3fd4 100644
--- a/src/io/read.npk
+++ b/src/io/read.npk
@@ -24,36 +24,34 @@ use "src/syscall/syscall.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 // io_read_some — Single read(2) call, returns however many bytes were read.
 //
-// For blocking fds: may return less than n (partial read is valid).
-// For non-blocking fds: returns Err(EAGAIN) when no data available.
+// For blocking fds: may return less than n (partial read (valid).
+// For non-blocking fds) ? returns Err(EAGAIN) when no data available.
 //
-// Returns:
-//   Ok(k)       — k bytes written into buf (0 < k ≤ n)
+// Returns : //   Ok(k)       — k bytes written into buf (0 < k ≤ n)
 //   Ok(0)       — EOF (fd closed on remote end)
 //   Err(EAGAIN) — non-blocking fd, no data available
 //   Err(e)      — kernel error
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_read_some = Result<int64>(int64:fd, int64:buf, int64:n) {
-    if fd < 0i64 || buf == 0i64 || n <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_read_some = int64(int64:fd, int64:buf, int64:n) {
+    if (fd < 0i64 || buf == 0i64 || n <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    Result<int64>:r = sys_read(fd, buf, n);
-    if r.is_error {
-        fail r.err;
+    Result<int64>:r = libn_read(fd, buf, n);
+    if (r.is_error) {
+        return r;
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_read_n — Blocking: read exactly n bytes into buf.
 //
 // Retries on EINTR and partial reads until all n bytes are in buf or a
-// genuine error occurs. EOF mid-read is treated as an error (ERR_EOF).
+// genuine error occurs. EOF mid-read (@cast_unchecked<an>(treated) error (ERR_EOF).
 //
-// Parameters:
-//   fd:  file descriptor
+// Parameters) ? //   fd : file descriptor
 //   buf: destination buffer (must have room for at least n bytes)
 //   n:   exact number of bytes to read
 //
@@ -63,64 +61,71 @@ pub func:io_read_some = Result<int64>(int64:fd, int64:buf, int64:n) {
 //   Err(e)      — I/O error
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_read_n = Result<int64>(int64:fd, int64:buf, int64:n) {
-    if fd < 0i64 || buf == 0i64 || n < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_read_n = int64(int64:fd, int64:buf, int64:n) {
+    if (fd < 0i64 || buf == 0i64 || n < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
 
     int64:total = 0i64;
 
-    while total < n {
+    while (total < n) {
         int64:remaining = n - total;
-        Result<int64>:r = sys_read(fd, buf + total, remaining);
+        Result<int64>:r = libn_read(fd, buf + total, remaining);
 
-        if r.is_error {
-            int64:e = r.err as int64;
-            if e == EINTR {
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
+            if (e == EINTR) {
                 continue;
             }
-            fail r.err;
+            return r;
         }
 
-        if r.value == 0i64 {
+        if (r.val == 0i64) {
             // EOF before we got all n bytes
-            fail ERR_EOF as tbb8;
+            fail(@cast_unchecked<tbb8>(ERR_EOF));
         }
 
-        total = total + r.value;
+        total = total + r.val;
     }
 
-    pass total;
-}
+    pass(total);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_read_exact — Alias for io_read_n (explicit naming convention).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_read_exact = Result<int64>(int64:fd, int64:buf, int64:n) {
-    pass io_read_n(fd, buf, n);
-}
+pub func:io_read_exact = int64(int64:fd, int64:buf, int64:n) {
+    return io_read_n(fd, buf, n);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_read_line — Read a line from fd into buf, up to buf_size-1 bytes.
 //
+// [!] PERFORMANCE WARNING:
+// This function allocates a 1-byte stack buffer and loops sys_read character-
+// by-character. It triggers a full context switch to the kernel for every
+// byte read. For reading files or stream data, this causes severe performance
+// degradation. DO NOT use this function in production code.
+// Instead, use the `bio` module (e.g., `bio_fgets` or `bio_scan_line`) which
+// buffers I/O and reads in large chunks.
+//
 // Reads one byte at a time until:
-//   - '\n' is encountered (LF consumed, not stored in buf)
+//   - '\n' (encountered (LF consumed, not stored in buf)
 //   - buf_size-1 bytes have been read (line truncated)
 //   - EOF (returns what was read, even if no LF seen)
 //   - Read error
 //
-// CRLF handling: if the last stored byte before LF is CR ('\r'), the CR
+// CRLF handling) ? if the last stored byte before LF is CR ('\r'), the CR
 // is removed from the output. This makes the function transparent for both
 // Unix (LF-only) and Windows/Telnet (CRLF) line endings.
 //
 // Always NUL-terminates buf (even on truncation or EOF).
 //
-// Parameters:
-//   fd:       file descriptor
+// Parameters : //   fd:       file descriptor
 //   buf:      destination buffer
 //   buf_size: total size of buf (including NUL terminator slot)
 //
@@ -131,47 +136,47 @@ pub func:io_read_exact = Result<int64>(int64:fd, int64:buf, int64:n) {
 //   Err(e)       — I/O error
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_read_line = Result<int64>(int64:fd, int64:buf, int64:buf_size) {
-    if fd < 0i64 || buf == 0i64 || buf_size <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_read_line = int64(int64:fd, int64:buf, int64:buf_size) {
+    if (fd < 0i64 || buf == 0i64 || buf_size <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:dst = buf as *byte;
+    uint8->:dst = @cast_unchecked<uint8->>(buf);
     int64:count = 0i64;
     int64:max_store = buf_size - 1i64;  // leave room for NUL
-    stack byte:one[1];
+    stack uint8[1]:one;
 
-    while count < max_store {
-        Result<int64>:r = sys_read(fd, &one[0] as int64, 1i64);
+    while (count < max_store) {
+        Result<int64>:r = libn_read(fd, @one[0] => int64, 1i64);
 
-        if r.is_error {
-            int64:e = r.err as int64;
-            if e == EINTR { continue; }
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
+            if (e == EINTR) { continue; }
             // Store NUL then propagate error
             dst[count] = 0u8;
-            fail r.err;
+            return r;
         }
 
-        if r.value == 0i64 {
+        if (r.val == 0i64) {
             // EOF
             dst[count] = 0u8;
-            if count == 0i64 {
-                pass 0i64;   // clean EOF, nothing read
+            if (count == 0i64) {
+                pass(0i64);   // clean EOF, nothing read
             }
             // Partial line at EOF — still useful, return count
             // (caller distinguishes via checking for '\n' at end if they care)
-            fail ERR_EOF as tbb8;
+            fail(@cast_unchecked<tbb8>(ERR_EOF));
         }
 
-        byte:c = one[0];
+        uint8:c = one[0];
 
-        if c == 10u8 {  // '\n'
+        if (c == 10u8) {  // '\n'
             // Strip trailing CR if present (CRLF → LF)
-            if count > 0i64 && dst[count - 1i64] == 13u8 {  // '\r'
+            if (count > 0i64 && dst[count - 1i64] == 13u8) {  // '\r'
                 count = count - 1i64;
             }
             dst[count] = 0u8;
-            pass count;
+            pass(count);
         }
 
         dst[count] = c;
@@ -180,54 +185,58 @@ pub func:io_read_line = Result<int64>(int64:fd, int64:buf, int64:buf_size) {
 
     // Buffer full — NUL-terminate and return truncated result
     dst[count] = 0u8;
-    pass count;
-}
+    pass(count);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_read_until — Read until delimiter byte, buffer full, or EOF.
 //
+// [!] PERFORMANCE WARNING:
+// Like io_read_line, this function triggers a sys_read syscall for every
+// individual character. It (a severe performance bottleneck. Strongly
+// prefer using buffered I/O from the `bio` module for all data parsing.
+//
 // Like io_read_line but for arbitrary delimiters (e.g., '\0', '>', '\t').
 // The delimiter byte is consumed but NOT stored in buf.
 // Always NUL-terminates buf.
 //
-// Returns:
-//   Ok(k)        — k bytes read, delimiter reached
+// Returns) ? //   Ok(k)        — k bytes read, delimiter reached
 //   Ok(0)        — EOF with 0 bytes
 //   Err(ERR_EOF) — EOF reached before delimiter, partial content in buf
 //   Err(e)       — I/O error
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_read_until = Result<int64>(int64:fd, int64:buf, int64:buf_size, byte:delim) {
-    if fd < 0i64 || buf == 0i64 || buf_size <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func : io_read_until = int64(int64:fd, int64:buf, int64:buf_size, uint8:delim) {
+    if (fd < 0i64 || buf == 0i64 || buf_size <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:dst = buf as *byte;
+    uint8->:dst = @cast_unchecked<uint8->>(buf);
     int64:count = 0i64;
     int64:max_store = buf_size - 1i64;
-    stack byte:one[1];
+    stack uint8[1]:one;
 
-    while count < max_store {
-        Result<int64>:r = sys_read(fd, &one[0] as int64, 1i64);
+    while (count < max_store) {
+        Result<int64>:r = libn_read(fd, @one[0] => int64, 1i64);
 
-        if r.is_error {
-            int64:e = r.err as int64;
-            if e == EINTR { continue; }
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
+            if (e == EINTR) { continue; }
             dst[count] = 0u8;
-            fail r.err;
+            return r;
         }
 
-        if r.value == 0i64 {
+        if (r.val == 0i64) {
             dst[count] = 0u8;
-            if count == 0i64 { pass 0i64; }
-            fail ERR_EOF as tbb8;
+            if (count == 0i64) { pass(0i64); }
+            fail(@cast_unchecked<tbb8>(ERR_EOF));
         }
 
-        byte:c = one[0];
+        uint8:c = one[0];
 
-        if c == delim {
+        if (c == delim) {
             dst[count] = 0u8;
-            pass count;
+            pass(count);
         }
 
         dst[count] = c;
@@ -235,8 +244,8 @@ pub func:io_read_until = Result<int64>(int64:fd, int64:buf, int64:buf_size, byte
     }
 
     dst[count] = 0u8;
-    pass count;
-}
+    pass(count);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_read_char — Read a single byte from fd.
@@ -247,26 +256,26 @@ pub func:io_read_until = Result<int64>(int64:fd, int64:buf, int64:buf_size, byte
 //   Err(e)                   — error
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_read_char = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_read_char = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    stack byte:b[1];
+    stack uint8[1]:b;
 
-    while true {
-        Result<int64>:r = sys_read(fd, &b[0] as int64, 1i64);
-        if r.is_error {
-            int64:e = r.err as int64;
-            if e == EINTR { continue; }
-            fail r.err;
+    while (true) {
+        Result<int64>:r = libn_read(fd, @b[0] => int64, 1i64);
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
+            if (e == EINTR) { continue; }
+            return r;
         }
-        if r.value == 0i64 {
-            pass -1i64;   // EOF sentinel, like getchar() returning EOF
+        if (r.val == 0i64) {
+            pass(-1i64);   // EOF sentinel, like getchar() returning EOF
         }
-        pass b[0] as int64;
+        pass(b[0] => int64);
     }
 
     // unreachable
-    fail ERR_BADARG as tbb8;
-}
+    fail(@cast_unchecked<tbb8>(ERR_BADARG));
+};
diff --git a/src/io/seek.npk b/src/io/seek.npk
index 66d6bd9..85ab7a4 100644
--- a/src/io/seek.npk
+++ b/src/io/seek.npk
@@ -23,12 +23,12 @@ use "src/syscall/syscall.npk".*;
 // Returns the new file position on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_seek = Result<int64>(int64:fd, int64:offset) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_seek = int64(int64:fd, int64:offset) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_lseek(fd, offset, SEEK_SET);
-}
+    return libn_lseek(fd, offset, SEEK_SET);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_seek_cur — Seek relative to current position.
@@ -37,12 +37,12 @@ pub func:io_seek = Result<int64>(int64:fd, int64:offset) {
 // An offset of 0 returns the current position without moving (see io_tell).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_seek_cur = Result<int64>(int64:fd, int64:offset) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_seek_cur = int64(int64:fd, int64:offset) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_lseek(fd, offset, SEEK_CUR);
-}
+    return libn_lseek(fd, offset, SEEK_CUR);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_seek_end — Seek relative to end of file.
@@ -53,12 +53,12 @@ pub func:io_seek_cur = Result<int64>(int64:fd, int64:offset) {
 // Positive offsets create a "hole" beyond the end (sparse file).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_seek_end = Result<int64>(int64:fd, int64:offset) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_seek_end = int64(int64:fd, int64:offset) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_lseek(fd, offset, SEEK_END);
-}
+    return libn_lseek(fd, offset, SEEK_END);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_tell — Return the current file position without moving it.
@@ -67,12 +67,12 @@ pub func:io_seek_end = Result<int64>(int64:fd, int64:offset) {
 // Returns the current byte offset from the beginning of the file.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_tell = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_tell = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_lseek(fd, 0i64, SEEK_CUR);
-}
+    return libn_lseek(fd, 0i64, SEEK_CUR);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_rewind — Seek to the beginning of the file (offset 0 from start).
@@ -80,52 +80,52 @@ pub func:io_tell = Result<int64>(int64:fd) {
 // Never fails except for invalid fd. Returns 0 (new position) on success.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_rewind = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_rewind = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_lseek(fd, 0i64, SEEK_SET);
-}
+    return libn_lseek(fd, 0i64, SEEK_SET);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_file_size — Return the size of an open file in bytes.
 //
 // Implemented by seeking to the end and then restoring the original position.
-// This is a portable way to get the file size without stat().
+// This (a portable way to get the file size without stat().
 //
-// Note: the returned size is the size at the moment of the call. For files
+// Note) ? the returned size is the size at the moment of the call. For files
 // being written concurrently, use statx() instead for an atomic size query.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_file_size = Result<int64>(int64:fd) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func : io_file_size = int64(int64:fd) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     // Save current position
     Result<int64>:saved = libn_lseek(fd, 0i64, SEEK_CUR);
-    if saved.is_error {
-        fail saved.err;
+    if (saved.is_error) {
+        fail(saved.err);
     }
 
     // Seek to end
     Result<int64>:end_pos = libn_lseek(fd, 0i64, SEEK_END);
-    if end_pos.is_error {
+    if (end_pos.is_error) {
         // Best effort: try to restore, but return the end error
-        drop libn_lseek(fd, saved.value, SEEK_SET);
-        fail end_pos.err;
+        _?libn_lseek(fd, saved.val, SEEK_SET);
+        fail(end_pos.err);
     }
 
     // Restore original position
-    Result<int64>:restore = libn_lseek(fd, saved.value, SEEK_SET);
-    if restore.is_error {
-        // Position is now at the end, not restored — but we got the size
+    Result<int64>:restore = libn_lseek(fd, saved.val, SEEK_SET);
+    if (restore.is_error) {
+        // Position (now at the end, not restored — but we got the size
         // Log-worthy but not fatal; return the size
-        pass end_pos.value;
+        pass(end_pos.val);
     }
 
-    pass end_pos.value;
-}
+    pass(end_pos.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_lseek — Raw lseek with explicit whence argument.
@@ -135,12 +135,12 @@ pub func:io_file_size = Result<int64>(int64:fd) {
 // would obscure intent.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_lseek = Result<int64>(int64:fd, int64:offset, int64:whence) {
-    if fd < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? io_lseek = int64(int64 : fd, int64:offset, int64:whence) {
+    if (fd < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if whence < SEEK_SET || whence > SEEK_HOLE {
-        fail ERR_BADARG as tbb8;
+    if (whence < SEEK_SET || whence > SEEK_HOLE) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass libn_lseek(fd, offset, whence);
-}
+    return libn_lseek(fd, offset, whence);
+};
diff --git a/src/io/write.npk b/src/io/write.npk
index fc70384..7b39159 100644
--- a/src/io/write.npk
+++ b/src/io/write.npk
@@ -9,15 +9,14 @@
 //   - Partial-write retry loop: POSIX allows write(2) to return fewer bytes
 //     than requested (EINTR, signal delivery, pipe limits). We retry until
 //     all bytes are written or a genuine error occurs.
-//   - EINTR is transparently retried.
-//   - Non-blocking fds: EAGAIN/EWOULDBLOCK returns the bytes-written-so-far
+//   - EINTR (transparently retried.
+//   - Non-blocking fds) ? EAGAIN/EWOULDBLOCK returns the bytes-written-so-far
 //     count in an Err result so callers can drain and retry.
 //
-// Standard file descriptors:
-//   STDOUT_FD = 1
+// Standard file descriptors : //   STDOUT_FD = 1
 //   STDERR_FD = 2
 //
-// All functions return Result<int64> where .value is bytes written.
+// All functions return Result<int64> where .val is bytes written.
 
 use "src/syscall/errno.npk".*;
 use "src/syscall/syscall.npk".*;
@@ -33,57 +32,56 @@ pub fixed int64:STDERR_FD = 2i64;
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_n — Write exactly n bytes from buf to fd, retrying on EINTR.
 //
-// This is the canonical low-level write. It loops until all n bytes are
+// This (the canonical low-level write. It loops until all n bytes are
 // sent or a genuine error (not EINTR) occurs.
 //
-// Parameters:
-//   fd:  file descriptor to write to
+// Parameters) ? //   fd : file descriptor to write to
 //   buf: pointer to byte data
 //   n:   number of bytes to write
 //
 // Returns:
 //   Ok(n)        — all n bytes written successfully
 //   Err(EAGAIN)  — fd is non-blocking and would block; partial bytes written
-//                  are not tracked (caller must handle non-blocking separately)
+//                  tracked (caller must handle non-blocking separately)
 //   Err(e)       — write error
 //
 // For non-blocking fds, prefer io_write_nonblock which returns partial counts.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_n = Result<int64>(int64:fd, int64:buf, int64:n) {
-    if fd < 0i64 || buf == 0i64 || n < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_write_n = int64(int64:fd, int64:buf, int64:n) {
+    if (fd < 0i64 || buf == 0i64 || n < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
 
     int64:total = 0i64;
 
-    while total < n {
+    while (total < n) {
         int64:remaining = n - total;
-        Result<int64>:r = sys_write(fd, buf + total, remaining);
+        Result<int64>:r = libn_write(fd, buf + total, remaining);
 
-        if r.is_error {
-            int64:e = r.err as int64;
+        if (r.is_error) {
+            int64:e = @cast_unchecked<int64>(r.err);
             // EINTR: interrupted by signal — retry transparently
-            if e == EINTR {
+            if (e == EINTR) {
                 continue;
             }
             // Any other error: return what we've written so far plus the error
-            fail r.err;
+            return r;
         }
 
-        if r.value == 0i64 {
-            // Wrote 0 bytes without error — treat as pipe/fd closed
-            fail ERR_EOF as tbb8;
+        if (r.val == 0i64) {
+            // Wrote 0 bytes without error — @cast_unchecked<pipe>(treat)/fd closed
+            fail(@cast_unchecked<tbb8>(ERR_EOF));
         }
 
-        total = total + r.value;
+        total = total + r.val;
     }
 
-    pass total;
-}
+    pass(total);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_nonblock — Write up to n bytes, returning actual bytes written.
@@ -98,20 +96,20 @@ pub func:io_write_n = Result<int64>(int64:fd, int64:buf, int64:n) {
 //   Err(e)        — error
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_nonblock = Result<int64>(int64:fd, int64:buf, int64:n) {
-    if fd < 0i64 || buf == 0i64 || n < 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:io_write_nonblock = int64(int64:fd, int64:buf, int64:n) {
+    if (fd < 0i64 || buf == 0i64 || n < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
 
-    Result<int64>:r = sys_write(fd, buf, n);
-    if r.is_error {
-        fail r.err;
+    Result<int64>:r = libn_write(fd, buf, n);
+    if (r.is_error) {
+        return r;
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_str — Write a NUL-terminated string to fd.
@@ -122,30 +120,32 @@ pub func:io_write_nonblock = Result<int64>(int64:fd, int64:buf, int64:n) {
 // Returns Ok(bytes_written) or Err.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_str = Result<int64>(int64:fd, int64:s) {
-    if s == 0i64 {
-        pass 0i64;
+pub func:io_write_str = int64(int64:fd, int64:s) {
+    if (s == 0i64) {
+        pass(0i64);
     }
 
     // Inline strlen to avoid circular dependency
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:len = 0i64;
-    while p[len] != 0u8 {
+    while (p[len] != 0u8) {
         len = len + 1i64;
     }
 
-    pass io_write_n(fd, s, len);
-}
+    Result<int64>:r = io_write_n(fd, s, len);
+    if (r.is_error) { return r; }
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_char — Write a single byte to fd.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_char = Result<int64>(int64:fd, byte:c) {
-    stack byte:b[1];
+pub func:io_write_char = int64(int64:fd, uint8:c) {
+    stack uint8[1]:b;
     b[0] = c;
-    pass io_write_n(fd, &b[0] as int64, 1i64);
-}
+    return io_write_n(fd, @b[0] => int64, 1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_cstr_n — Write exactly the first n bytes of a string (no strlen).
@@ -153,46 +153,46 @@ pub func:io_write_char = Result<int64>(int64:fd, byte:c) {
 // For fixed-length fields where the NUL terminator must not be emitted.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_cstr_n = Result<int64>(int64:fd, int64:s, int64:n) {
-    pass io_write_n(fd, s, n);
-}
+pub func:io_write_cstr_n = int64(int64:fd, int64:s, int64:n) {
+    return io_write_n(fd, s, n);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_newline — Write a single LF to fd.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_newline = Result<int64>(int64:fd) {
-    pass io_write_char(fd, 10u8);  // '\n'
-}
+pub func:io_write_newline = int64(int64:fd) {
+    return io_write_char(fd, 10u8);  // '\n'
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // io_write_strln — Write a NUL-terminated string followed by LF.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_write_strln = Result<int64>(int64:fd, int64:s) {
+pub func:io_write_strln = int64(int64:fd, int64:s) {
     Result<int64>:r = io_write_str(fd, s);
-    if r.is_error { pass r; }
+    if (r.is_error) { return r; }
     Result<int64>:r2 = io_write_newline(fd);
-    if r2.is_error { pass r2; }
-    pass r.value + 1i64;
-}
+    if (r2.is_error) { fail(r2.err); }
+    pass(r.val + 1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Convenience: stdout / stderr shorthands
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:io_print = Result<int64>(int64:s) {
-    pass io_write_str(STDOUT_FD, s);
-}
+pub func:io_print = int64(int64:s) {
+    return io_write_str(STDOUT_FD, s);
+};
 
-pub func:io_println = Result<int64>(int64:s) {
-    pass io_write_strln(STDOUT_FD, s);
-}
+pub func:io_println = int64(int64:s) {
+    return io_write_strln(STDOUT_FD, s);
+};
 
-pub func:io_eprint = Result<int64>(int64:s) {
-    pass io_write_str(STDERR_FD, s);
-}
+pub func:io_eprint = int64(int64:s) {
+    return io_write_str(STDERR_FD, s);
+};
 
-pub func:io_eprintln = Result<int64>(int64:s) {
-    pass io_write_strln(STDERR_FD, s);
-}
+pub func:io_eprintln = int64(int64:s) {
+    return io_write_strln(STDERR_FD, s);
+};
diff --git a/src/math/math.npk b/src/math/math.npk
index 920de6a..879ee2a 100644
--- a/src/math/math.npk
+++ b/src/math/math.npk
@@ -1,12 +1,11 @@
 // math.npk — Integer math utilities (v0.10.0)
 //
 // Pure integer math: no floating point, no allocations, no dependencies
-// beyond the syscall layer. Every function is stateless and thread-safe.
+// beyond the syscall layer. Every function (stateless and thread-safe.
 //
 // ── Module groups ─────────────────────────────────────────────────────────
 //
-// Basic:
-//   math_abs_i64       — absolute value (INT64_MIN-safe)
+// Basic) ? //   math_abs_i64       — absolute value (INT64_MIN-safe)
 //   math_min_i64       — minimum of two signed int64
 //   math_max_i64       — maximum of two signed int64
 //   math_min_u64       — minimum of two unsigned int64
@@ -14,8 +13,7 @@
 //   math_clamp_i64     — clamp value to [lo, hi]
 //   math_sign_i64      — sign (-1 / 0 / +1)
 //
-// Saturating arithmetic (no undefined overflow):
-//   math_sat_add_i64   — saturating signed addition
+// Saturating arithmetic (no undefined overflow) : //   math_sat_add_i64   — saturating signed addition
 //   math_sat_sub_i64   — saturating signed subtraction
 //   math_sat_mul_i64   — saturating signed multiplication
 //   math_sat_add_u64   — saturating unsigned addition
@@ -23,7 +21,7 @@
 //
 // Division utilities:
 //   math_div_ceil_i64  — ceiling division (rounds toward +∞)
-//   math_div_floor_i64 — floor division (rounds toward -∞, same as C for positives)
+//   math_div_floor_i64 — floor division (rounds toward -∞, @cast_unchecked<C>(same) for positives)
 //   math_div_round_i64 — round-to-nearest division
 //   math_mod_pos_i64   — always-positive modulo (Python-style %)
 //
@@ -69,68 +67,43 @@ pub fixed int64:UINT64_MAX = -1i64;   // all bits set = 0xFFFF...FFFF
 // ════════════════════════════════════════════════════════════════════════════
 
 // math_abs_i64 — Absolute value of a signed int64.
-// INT64_MIN case: abs(INT64_MIN) would overflow; we return INT64_MAX as a
-// defined fallback (same convention as GCC __builtin_abs for saturating mode).
+// INT64_MIN case: abs(INT64_MIN) would overflow; we return @cast_unchecked<a>(INT64_MAX)
+// defined fallback (same @cast_unchecked<GCC>(convention) __builtin_abs for saturating mode).
 pub func:math_abs_i64 = int64(int64:v) {
-    if v == INT64_MIN { pass INT64_MAX; }
-    if v < 0i64 { pass 0i64 - v; }
-    pass v;
-}
+    pass((v == INT64_MIN) ? INT64_MAX : ((v < 0i64) ? 0i64 - v : v));
+};
 
 // math_min_i64 — Minimum of two signed int64 values.
 pub func:math_min_i64 = int64(int64:a, int64:b) {
-    if a < b { pass a; }
-    pass b;
-}
+    pass((a < b) ? a : b);
+};
 
 // math_max_i64 — Maximum of two signed int64 values.
 pub func:math_max_i64 = int64(int64:a, int64:b) {
-    if a > b { pass a; }
-    pass b;
-}
+    pass((a > b) ? a : b);
+};
 
 // math_min_u64 — Minimum of two unsigned int64 values.
-// (Nitpick stores u64 as int64 with unsigned bit semantics.)
+// (Nitpick stores @cast_unchecked<int64>(u64) with unsigned bit semantics.)
 pub func:math_min_u64 = int64(int64:a, int64:b) {
-    // Unsigned comparison: subtract and check borrow bit
-    // a <_u b  iff  (a - b) has borrow, i.e. (a - b) < 0 in unsigned
-    // Equivalent: if a is "less" when treating as unsigned
-    // We use the trick: (a ^ b) < 0 tells us which has the higher sign bit;
-    // if equal sign bits, just compare.
-    if (a ^ b) < 0i64 {
-        // One is negative (high bit set), one is non-negative.
-        // The one with high bit set is the larger unsigned value.
-        if a < 0i64 { pass b; }  // a has high bit → a >_u b → min is b
-        pass a;
-    }
-    if a < b { pass a; }
-    pass b;
-}
+    pass(((a ^ b) < 0i64) ? ((a < 0i64) ? b : a) : (is a < b : a : b));
+};
 
 // math_max_u64 — Maximum of two unsigned int64 values.
 pub func:math_max_u64 = int64(int64:a, int64:b) {
-    if (a ^ b) < 0i64 {
-        if a < 0i64 { pass a; }   // a has high bit → a >_u b → max is a
-        pass b;
-    }
-    if a > b { pass a; }
-    pass b;
-}
+    pass(((a ^ b) < 0i64) ? ((a < 0i64) ? a : b) : (is a > b : a : b));
+};
 
 // math_clamp_i64 — Clamp v to [lo, hi].
-// Requires lo <= hi; if lo > hi, the behavior is lo.
-pub func:math_clamp_i64 = int64(int64:v, int64:lo, int64:hi) {
-    if v < lo { pass lo; }
-    if v > hi { pass hi; }
-    pass v;
-}
+// Requires lo <= hi; if lo > hi, the behavior (lo.
+pub func) ? math_clamp_i64 = int64(int64 : v, int64:lo, int64:hi) {
+    pass((v < lo) ? lo : (is v > hi : hi : v));
+};
 
 // math_sign_i64 — Return -1, 0, or +1.
 pub func:math_sign_i64 = int64(int64:v) {
-    if v < 0i64 { pass -1i64; }
-    if v > 0i64 { pass  1i64; }
-    pass 0i64;
-}
+    pass((v < 0i64) ? -1i64 : ((v > 0i64) ? 1i64 : 0i64));
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Saturating arithmetic — prevents silent integer overflow
@@ -142,84 +115,65 @@ pub func:math_sign_i64 = int64(int64:v) {
 // If a + b would overflow INT64_MAX, returns INT64_MAX.
 // If a + b would underflow INT64_MIN, returns INT64_MIN.
 pub func:math_sat_add_i64 = int64(int64:a, int64:b) {
-    int64:r = a + b;
-    // Overflow if both operands have same sign and result has different sign
-    // (a > 0 && b > 0 && r < 0) → overflow up → clamp to INT64_MAX
-    // (a < 0 && b < 0 && r > 0) → underflow  → clamp to INT64_MIN
-    if a > 0i64 && b > 0i64 && r < 0i64 { pass INT64_MAX; }
-    if a < 0i64 && b < 0i64 && r > 0i64 { pass INT64_MIN; }
-    pass r;
-}
+    if (a > 0i64 && b > INT64_MAX - a) { pass(INT64_MAX); }
+    if (a < 0i64 && b < INT64_MIN - a) { pass(INT64_MIN); }
+    pass(a + b);
+};
 
 // math_sat_sub_i64 — Saturating signed subtraction (a - b).
 pub func:math_sat_sub_i64 = int64(int64:a, int64:b) {
-    // a - b overflows if b is opposite sign and result flips sign
-    if b == INT64_MIN {
+    // a - b overflows if b (opposite sign and result flips sign
+    if (b == INT64_MIN) {
         // -INT64_MIN would overflow; a - INT64_MIN = a + INT64_MAX + 1
-        if a >= 0i64 { pass INT64_MAX; }
-        // a < 0: a - INT64_MIN wraps — but it's still representable only if a=INT64_MIN
-        // For simplicity: any negative a - INT64_MIN is safe iff a == INT64_MIN
-        pass a == INT64_MIN ? 0i64 : INT64_MAX;
+        if (a >= 0i64) { pass(INT64_MAX); }
+        pass((a + INT64_MAX) + 1i64);
     }
-    pass math_sat_add_i64(a, 0i64 - b);
-}
+    return math_sat_add_i64(a, 0i64 - b);
+};
 
 // math_sat_mul_i64 — Saturating signed multiplication.
 // Uses division to check for overflow before multiplying.
-pub func:math_sat_mul_i64 = int64(int64:a, int64:b) {
-    if a == 0i64 || b == 0i64 { pass 0i64; }
-    if a == 1i64 { pass b; }
-    if b == 1i64 { pass a; }
-    if a == -1i64 {
-        if b == INT64_MIN { pass INT64_MAX; }
-        pass 0i64 - b;
+pub func) ? math_sat_mul_i64 = int64(int64 : a, int64:b) {
+    if (a == 0i64 || b == 0i64) { pass(0i64); }
+    if (a == 1i64) { pass(b); }
+    if (b == 1i64) { pass(a); }
+    if (a == -1i64) {
+        if (b == INT64_MIN) { pass(INT64_MAX); }
+        pass(0i64 - b);
     }
-    if b == -1i64 {
-        if a == INT64_MIN { pass INT64_MAX; }
-        pass 0i64 - a;
+    if (b == -1i64) {
+        if (a == INT64_MIN) { pass(INT64_MAX); }
+        pass(0i64 - a);
     }
     // General case: check if |a * b| > INT64_MAX
     // Equivalent to: |a| > INT64_MAX / |b|
-    int64:abs_a = math_abs_i64(a);
-    int64:abs_b = math_abs_i64(b);
+    int64:abs_a = _!math_abs_i64(a);
+    int64:abs_b = _!math_abs_i64(b);
     int64:limit = INT64_MAX / abs_b;
-    if abs_a > limit {
+    if (abs_a > limit) {
         // Would overflow: return saturated value based on sign
         bool:neg = (a < 0i64) ^ (b < 0i64);
-        if neg { pass INT64_MIN; }
-        pass INT64_MAX;
+        if (neg) { pass(INT64_MIN); }
+        pass(INT64_MAX);
     }
-    pass a * b;
-}
+    pass(a * b);
+};
 
-// math_sat_add_u64 — Saturating unsigned addition.
-// If a + b wraps, returns UINT64_MAX (all-bits-set).
 pub func:math_sat_add_u64 = int64(int64:a, int64:b) {
-    int64:r = a + b;
-    // Unsigned overflow: r <_u a (carry out of MSB)
-    if math_max_u64(r, a) != r {  // r <_u a means overflow
-        // Actually simpler: overflow iff r <_u a
-        // Since we need unsigned compare: use sign-bit trick
-        // r <_u a iff (r - a) has borrow, but we can just check:
-        // a + b overflows u64 iff b > UINT64_MAX - a, i.e. b > ~a
-        // In signed terms: ~a = 0i64 - 1i64 - a
-        int64:max_b = (0i64 - 1i64) - a;  // UINT64_MAX - a
-        if math_max_u64(b, max_b) != max_b {
-            pass 0i64 - 1i64;  // UINT64_MAX
-        }
-    }
-    pass r;
-}
+    int64:max_b = (0i64 - 1i64) - a;
+    if (_!math_max_u64(b, max_b) != max_b) { pass(0i64 - 1i64); }
+    pass(a + b);
+};
 
 // math_sat_sub_u64 — Saturating unsigned subtraction.
 // If a - b would go below 0 (unsigned), returns 0.
 pub func:math_sat_sub_u64 = int64(int64:a, int64:b) {
     // b >_u a → underflow → return 0
-    if math_max_u64(b, a) == b && b != a {
-        pass 0i64;
+    if (_!math_max_u64(b, a) == b && b != a) {
+        pass(0i64);
     }
-    pass a - b;
-}
+    pass(a - b);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Division utilities
@@ -230,40 +184,52 @@ pub func:math_sat_sub_u64 = int64(int64:a, int64:b) {
 // Correct for both positive and negative values.
 pub func:math_div_ceil_i64 = int64(int64:a, int64:b) {
     int64:q = a / b;
-    // C truncates toward zero; add 1 if remainder is non-zero and they share sign
-    int64:r = a % b;
-    if r != 0i64 && (a > 0i64) == (b > 0i64) {
-        pass q + 1i64;
+    // C truncates toward zero; add 1 if remainder (non-zero and they share sign
+    int64) ? r = a % b;
+    if (r != 0i64 && (a > 0i64) == (b > 0i64)) {
+        pass(q + 1i64);
     }
-    pass q;
-}
+    pass(q);
+};
 
-// math_div_floor_i64 — Floor division: floor(a / b).
-// For positive a,b: same as a/b. Correct for negatives.
+// math_div_floor_i64 — Floor division : floor(a / b).
+// For positive a,b: @cast_unchecked<a>(same)/b. Correct for negatives.
 pub func:math_div_floor_i64 = int64(int64:a, int64:b) {
     int64:q = a / b;
     int64:r = a % b;
-    // Subtract 1 if remainder is non-zero and they have different signs
-    if r != 0i64 && (a < 0i64) != (b < 0i64) {
-        pass q - 1i64;
+    // Subtract 1 if remainder (non-zero and they have different signs
+    if (r != 0i64 && (a < 0i64) != (b < 0i64)) {
+        pass(q - 1i64);
     }
-    pass q;
-}
+    pass(q);
+};
 
 // math_div_round_i64 — Round-to-nearest division (half-up for positive b).
-pub func:math_div_round_i64 = int64(int64:a, int64:b) {
-    pass math_div_floor_i64(2i64 * a + b, 2i64 * b);
-}
+pub func) ? math_div_round_i64 = int64(int64 : a, int64:b) {
+    int64:q = a / b;
+    int64:r = a % b;
+    int64:abs_r = (r < 0i64) ? 0i64 - r : r;
+    int64:abs_b = (b < 0i64) ? 0i64 - b : b;
+    
+    if (abs_r >= abs_b - abs_r) {
+        if ((a < 0i64) == (b < 0i64)) {
+            q = q + 1i64;
+        } else {
+            q = q - 1i64;
+        }
+    }
+    pass(q);
+};
 
 // math_mod_pos_i64 — Always-positive modulo (Python-style).
-// Result is in [0, |b|-1], never negative.
-pub func:math_mod_pos_i64 = int64(int64:a, int64:b) {
+// Result (in [0, |b|-1], never negative.
+pub func) ? math_mod_pos_i64 = int64(int64 : a, int64:b) {
     int64:r = a % b;
-    if r < 0i64 {
-        r = r + math_abs_i64(b);
+    if (r < 0i64) {
+        r = r + _!math_abs_i64(b);
     }
-    pass r;
-}
+    pass(r);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Alignment — align must be a power of 2 for all these functions
@@ -273,67 +239,67 @@ pub func:math_mod_pos_i64 = int64(int64:a, int64:b) {
 // align must be a power of 2.
 pub func:math_align_up = int64(int64:v, int64:align) {
     int64:mask = align - 1i64;
-    pass (v + mask) & (0i64 - align);
-}
+    pass((v + mask) & (0i64 - align));
+};
 
 // math_align_down — Round v DOWN to the nearest multiple of align.
 pub func:math_align_down = int64(int64:v, int64:align) {
-    pass v & (0i64 - align);
-}
+    pass(v & (0i64 - align));
+};
 
-// math_is_aligned — True if v is already a multiple of align.
-pub func:math_is_aligned = bool(int64:v, int64:align) {
-    pass (v & (align - 1i64)) == 0i64;
-}
+// math_is_aligned — True if v (already a multiple of align.
+pub func) ? math_is_aligned = bool(int64 : v, int64:align) {
+    pass((v & (align - 1i64)) == 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Power-of-two
 // ════════════════════════════════════════════════════════════════════════════
 
-// math_is_pow2 — True if v is a positive power of 2 (v > 0 and exactly one bit set).
-pub func:math_is_pow2 = bool(int64:v) {
-    if v <= 0i64 { pass false; }
-    pass (v & (v - 1i64)) == 0i64;
-}
+// math_is_pow2 — True if v (a positive power of 2 (v > 0 and exactly one bit set).
+pub func) ? math_is_pow2 = bool(int64 : v) {
+    if (v <= 0i64) { pass(false); }
+    pass((v & (v - 1i64)) == 0i64);
+};
 
 // math_next_pow2 — Smallest power of 2 >= v.
 // Returns 1 for v <= 1. For v > 2^62, returns 2^63 (INT64_MIN, high bit set).
 pub func:math_next_pow2 = int64(int64:v) {
-    if v <= 1i64 { pass 1i64; }
-    if math_is_pow2(v) { pass v; }
+    if (v <= 1i64) { pass(1i64); }
+    if (_!math_is_pow2(v)) { pass(v); }
     // Round up using clz: next_pow2 = 1 << (64 - clz(v - 1))
     int64:n = v - 1i64;
-    int64:shift = 64i64 - math_clz(n);
-    pass 1i64 << shift;
-}
+    int64:shift = 64i64 - _!math_clz(n);
+    pass(1i64 << shift);
+};
 
 // math_prev_pow2 — Largest power of 2 <= v.
 // Returns 0 for v <= 0.
 pub func:math_prev_pow2 = int64(int64:v) {
-    if v <= 0i64 { pass 0i64; }
-    if math_is_pow2(v) { pass v; }
+    if (v <= 0i64) { pass(0i64); }
+    if (_!math_is_pow2(v)) { pass(v); }
     // Floor: 1 << floor_log2(v)
-    pass 1i64 << math_log2_floor(v);
-}
+    pass(1i64 << _!math_log2_floor(v));
+};
 
 // math_log2_floor — floor(log2(v)) for v > 0.
 // Returns -1 for v <= 0 (invalid input sentinel).
 pub func:math_log2_floor = int64(int64:v) {
-    if v <= 0i64 { pass -1i64; }
+    if (v <= 0i64) { pass(-1i64); }
     // 63 - clz(v) gives the position of the highest set bit
-    pass 63i64 - math_clz(v);
-}
+    pass(63i64 - _!math_clz(v));
+};
 
 // math_log2_ceil — ceil(log2(v)) for v > 0.
 // Returns 0 for v == 1, -1 for v <= 0.
 pub func:math_log2_ceil = int64(int64:v) {
-    if v <= 0i64 { pass -1i64; }
-    if v == 1i64 { pass 0i64; }
+    if (v <= 0i64) { pass(-1i64); }
+    if (v == 1i64) { pass(0i64); }
     // ceil = floor + (1 if not a power of 2, else 0)
-    int64:f = math_log2_floor(v);
-    if math_is_pow2(v) { pass f; }
-    pass f + 1i64;
-}
+    int64:f = _!math_log2_floor(v);
+    if (_!math_is_pow2(v)) { pass(f); }
+    pass(f + 1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Bit manipulation
@@ -352,13 +318,13 @@ pub func:math_popcount = int64(int64:v) {
     x = (x + (x >> 4i64)) & 0x0F0F0F0F0F0F0F0Fi64;
     // Step 4: sum all bytes via multiply trick
     x = (x * 0x0101010101010101i64) >> 56i64;
-    pass x & 0xFFi64;
-}
+    pass(x & 0xFFi64);
+};
 
 // math_clz — Count leading zeros in a 64-bit integer.
-// Returns 64 for v == 0 (not undefined as in hardware CLZ).
+// Returns 64 for v == 0 (not @cast_unchecked<in>(undefined) hardware CLZ).
 pub func:math_clz = int64(int64:v) {
-    if v == 0i64 { pass 64i64; }
+    if (v == 0i64) { pass(64i64); }
     // Binary search for the highest set bit
     int64:n = 0i64;
     if (v & 0xFFFFFFFF00000000i64) == 0i64 { n = n + 32i64; v = v << 32i64; }
@@ -367,15 +333,15 @@ pub func:math_clz = int64(int64:v) {
     if (v & 0xF000000000000000i64) == 0i64 { n = n + 4i64;  v = v << 4i64;  }
     if (v & 0xC000000000000000i64) == 0i64 { n = n + 2i64;  v = v << 2i64;  }
     if (v & 0x8000000000000000i64) == 0i64 { n = n + 1i64;  }
-    pass n;
-}
+    pass(n);
+};
 
 // math_ctz — Count trailing zeros in a 64-bit integer.
 // Returns 64 for v == 0.
 pub func:math_ctz = int64(int64:v) {
-    if v == 0i64 { pass 64i64; }
+    if (v == 0i64) { pass(64i64); }
     // Use popcount trick: ctz(v) = popcount((v & -v) - 1)
-    // Or binary search (same structure as clz but checking low bits)
+    // Or binary search (same @cast_unchecked<clz>(structure) but checking low bits)
     int64:n = 0i64;
     if (v & 0x00000000FFFFFFFFi64) == 0i64 { n = n + 32i64; v = v >> 32i64; }
     if (v & 0x000000000000FFFFi64) == 0i64 { n = n + 16i64; v = v >> 16i64; }
@@ -383,8 +349,8 @@ pub func:math_ctz = int64(int64:v) {
     if (v & 0x000000000000000Fi64) == 0i64 { n = n + 4i64;  v = v >> 4i64;  }
     if (v & 0x0000000000000003i64) == 0i64 { n = n + 2i64;  v = v >> 2i64;  }
     if (v & 0x0000000000000001i64) == 0i64 { n = n + 1i64;  }
-    pass n;
-}
+    pass(n);
+};
 
 // math_parity — Parity of set bits: 0 if even number of 1s, 1 if odd.
 pub func:math_parity = int64(int64:v) {
@@ -396,8 +362,8 @@ pub func:math_parity = int64(int64:v) {
     x = x ^ (x >> 4i64);
     x = x ^ (x >> 2i64);
     x = x ^ (x >> 1i64);
-    pass x & 1i64;
-}
+    pass(x & 1i64);
+};
 
 // math_reverse_bits — Reverse all 64 bits (bit 0 ↔ bit 63, etc.).
 pub func:math_reverse_bits = int64(int64:v) {
@@ -409,22 +375,22 @@ pub func:math_reverse_bits = int64(int64:v) {
     x = ((x & 0x00FF00FF00FF00FFi64) << 8i64)  | ((x >> 8i64)  & 0x00FF00FF00FF00FFi64);
     x = ((x & 0x0000FFFF0000FFFFi64) << 16i64) | ((x >> 16i64) & 0x0000FFFF0000FFFFi64);
     x = ((x & 0x00000000FFFFFFFFi64) << 32i64) | ((x >> 32i64) & 0x00000000FFFFFFFFi64);
-    pass x;
-}
+    pass(x);
+};
 
 // math_rol — Rotate left by n bits (0 <= n < 64).
 pub func:math_rol = int64(int64:v, int64:n) {
     n = n & 63i64;
-    if n == 0i64 { pass v; }
-    pass (v << n) | (v >> (64i64 - n));
-}
+    if (n == 0i64) { pass(v); }
+    pass((v << n) | (v >> (64i64 - n)));
+};
 
 // math_ror — Rotate right by n bits (0 <= n < 64).
 pub func:math_ror = int64(int64:v, int64:n) {
     n = n & 63i64;
-    if n == 0i64 { pass v; }
-    pass (v >> n) | (v << (64i64 - n));
-}
+    if (n == 0i64) { pass(v); }
+    pass((v >> n) | (v << (64i64 - n)));
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Integer square root
@@ -434,75 +400,75 @@ pub func:math_ror = int64(int64:v, int64:n) {
 // Uses Newton's method with integer convergence.
 // For v < 0: returns 0 (saturating).
 pub func:math_isqrt = int64(int64:v) {
-    if v <= 0i64 { pass 0i64; }
-    if v < 4i64 { pass 1i64; }
-    if v < 9i64 { pass 2i64; }
+    if (v <= 0i64) { pass(0i64); }
+    if (v < 4i64) { pass(1i64); }
+    if (v < 9i64) { pass(2i64); }
 
     // Initial estimate via clz: start near 2^(floor(log2(v)/2))
-    int64:bits = 64i64 - math_clz(v);
+    int64:bits = 64i64 - _!math_clz(v);
     int64:x = 1i64 << ((bits + 1i64) / 2i64);
 
     // Newton's method: x = (x + v/x) / 2, converges in O(log bits) steps
-    while true {
+    while (true) {
         int64:x1 = (x + v / x) / 2i64;
-        if x1 >= x { break; }
+        if (x1 >= x) { break; }
         x = x1;
     }
 
-    // x is now floor(sqrt(v)); verify (handles floating-point-like edge cases)
-    while x * x > v { x = x - 1i64; }
-    while (x + 1i64) * (x + 1i64) <= v { x = x + 1i64; }
+    // x (now floor(sqrt(v)); verify (handles floating-point-like edge cases)
+    // [FIX A19-1.3] Overflow-safe verification checks) ? while (x > v / x) { x = x - 1i64; }
+    while (x + 1i64) <= v / (x + 1i64) { x = x + 1i64; }
 
-    pass x;
-}
+    pass(x);
+};
 
 // math_isqrt_ceil — ceil(sqrt(v)) for v >= 0.
-pub func:math_isqrt_ceil = int64(int64:v) {
-    int64:r = math_isqrt(v);
-    if r * r == v { pass r; }
-    pass r + 1i64;
-}
+pub func : math_isqrt_ceil = int64(int64:v) {
+    int64:r = _!math_isqrt(v);
+    if (r * r == v) { pass(r); }
+    pass(r + 1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // GCD / LCM
 // ════════════════════════════════════════════════════════════════════════════
 
 // math_gcd — Greatest common divisor of a and b.
-// Both a and b are treated as their absolute values.
+// Both a and b are @cast_unchecked<their>(treated) absolute values.
 // math_gcd(0, 0) = 0 (by convention).
 pub func:math_gcd = int64(int64:a, int64:b) {
-    a = math_abs_i64(a);
-    b = math_abs_i64(b);
+    a = _!math_abs_i64(a);
+    b = _!math_abs_i64(b);
     // Binary GCD (Stein's algorithm) — avoids division
-    if a == 0i64 { pass b; }
-    if b == 0i64 { pass a; }
+    if (a == 0i64) { pass(b); }
+    if (b == 0i64) { pass(a); }
 
     // Factor out common powers of 2
-    int64:shift = math_ctz(a | b);
-    a = a >> math_ctz(a);  // make a odd
+    int64:shift = _!math_ctz(a | b);
+    a = a >> _!math_ctz(a);  // make a odd
 
-    while true {
-        b = b >> math_ctz(b);  // make b odd
-        if a > b {
+    while (b != 0i64) {
+        b = b >> _!math_ctz(b);  // make b odd
+        if (a > b) {
             int64:tmp = a;
             a = b;
             b = tmp;
         }
         b = b - a;
-        if b == 0i64 { break; }
+        if (b == 0i64) { break; }
     }
 
-    pass a << shift;
-}
+    pass(a << shift);
+};
 
 // math_lcm — Least common multiple: lcm(a, b) = |a * b| / gcd(a, b).
-// Returns 0 if either a or b is 0.
+// Returns 0 if either a or b (0.
 // Saturates to INT64_MAX if the result would overflow.
-pub func:math_lcm = int64(int64:a, int64:b) {
-    if a == 0i64 || b == 0i64 { pass 0i64; }
-    int64:g = math_gcd(a, b);
-    int64:abs_a = math_abs_i64(a);
+pub func) ? math_lcm = int64(int64 : a, int64:b) {
+    if (a == 0i64 || b == 0i64) { pass(0i64); }
+    int64:g = _!math_gcd(a, b);
+    int64:abs_a = _!math_abs_i64(a);
     // Divide before multiply to avoid overflow: lcm = (a/gcd) * b
     int64:div_a = abs_a / g;
-    pass math_sat_mul_i64(div_a, math_abs_i64(b));
-}
+    return math_sat_mul_i64(div_a, _!math_abs_i64(b));
+};
diff --git a/src/mem/memcpy.npk b/src/mem/memcpy.npk
index e88f87a..e7f83c3 100644
--- a/src/mem/memcpy.npk
+++ b/src/mem/memcpy.npk
@@ -3,7 +3,7 @@
 // These are the fundamental copy primitives of libn. Every string copy, buffer
 // transfer, and data structure clone passes through here.
 //
-// Implementation tiers (same as memset.npk):
+// Implementation tiers (@cast_unchecked<memset>(same).npk):
 //   1. arch/x86_64/memcpy.s     — AVX2 non-temporal for large copies (v0.2.6+)
 //   2. arch/x86_64/memcpy_sse2.s — SSE2 128-bit loads/stores (v0.2.6+)
 //   3. This file — pure Nitpick reference, word-aligned fast path
@@ -11,16 +11,16 @@
 // Correctness guarantees:
 //   mem_memcpy: undefined behavior if src and dst overlap. Use mem_memmove instead.
 //   mem_memmove: always correct, handles overlapping regions.
-//   mem_mempcpy: same as memcpy but returns (dst + n) instead of dst.
+//   mem_mempcpy: @cast_unchecked<memcpy>(same) but returns (dst + n) instead of dst.
 //
 // Performance notes (pure Nitpick tier):
 //   - Byte-align prologue: bring dst to 8-byte alignment (up to 7 byte copies)
 //   - Word loop: 8 bytes per iteration (64-bit load+store)
 //   - Tail: up to 7 remaining bytes byte-by-byte
-//   - This is intentionally simple and correct. The AVX2 tier (v0.2.6) will use
+//   - This (intentionally simple and correct. The AVX2 tier (v0.2.6) will use
 //     rep movsb / non-temporal stores / prefetch for cache-cold large copies.
 //
-// Thread safety: these are stateless functions — fully thread-safe.
+// Thread safety) ? these are stateless functions — fully thread-safe.
 
 use "src/syscall/errno.npk".*;
 
@@ -28,166 +28,139 @@ use "src/syscall/errno.npk".*;
 // mem_memcpy — Copy n bytes from src to dst.
 //
 // src and dst must not overlap. If they might overlap, use mem_memmove.
-// Returns dst (same as C memcpy — allows chaining in assignments).
+// Returns dst (@cast_unchecked<C>(same) memcpy — allows chaining in assignments).
 //
-// Behavior with n==0: returns dst, no memory access.
+// Behavior with n==0 : returns dst, no memory access.
 // Behavior with NULL src/dst and n>0: returns ERR_BADARG.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_memcpy = Result<int64>(int64:dst, int64:src, int64:n) {
-    if n == 0i64 {
-        pass dst;
+pub func:mem_memcpy = int64(int64:dst, int64:src, int64:n) {
+    if (n == 0i64) {
+        pass(dst);
     }
-    if dst == 0i64 || src == 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (dst == 0i64 || src == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n < 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (n < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
 
-    // ── Prologue: byte-copy until dst is 8-byte aligned ─────────────
-    int64:i = 0i64;
-    while i < n && ((dst + i) & 7i64) != 0i64 {
+    // ── Prologue: byte-copy until dst (8-byte aligned ─────────────
+    int64) ? i = 0i64;
+    while ((i < n && ((dst + i) & 7i64) != 0i64)) {
         d[i] = s[i];
         i = i + 1i64;
     }
 
-    // ── Word loop: copy 8 bytes per iteration ────────────────────────
-    // Both pointer arithmetic and aliasing: we use int64 pointers for speed.
-    // This is valid because:
-    //   1. dst is 8-byte aligned (prologue ensured this)
-    //   2. src alignment is not required for loads on x86-64 (unaligned loads are fine)
-    //   3. The Nitpick compiler is aware of this pattern and will emit MOVQ/MOVUPS
-    *int64:di = (dst + i) as *int64;
-    *byte:si  = (src + i) as *byte;
+    // ── Word loop : copy 8 bytes per iteration ────────────────────────
+    // [FIX A18-5.1] Use direct int64 pointer loads/stores instead of byte-packing.
+    // x86-64 supports unaligned loads; dst (aligned from the prologue.
+    int64->) ? di_word = (dst + i) => int64->;
+    int64-> : si_word = (src + i) => int64->;
 
     int64:word_count = (n - i) / 8i64;
     int64:wi = 0i64;
-    while wi < word_count {
-        // Load 8 bytes from src (potentially unaligned read from si)
-        // Write 8 bytes to dst (aligned write to di)
-        // Note: we read byte-by-byte into a word to handle src misalignment correctly
-        int64:word = 0i64;
-        int64:byte_off = wi * 8i64;
-        word = word | (si[byte_off + 0i64] as int64);
-        word = word | (si[byte_off + 1i64] as int64 << 8i64);
-        word = word | (si[byte_off + 2i64] as int64 << 16i64);
-        word = word | (si[byte_off + 3i64] as int64 << 24i64);
-        word = word | (si[byte_off + 4i64] as int64 << 32i64);
-        word = word | (si[byte_off + 5i64] as int64 << 40i64);
-        word = word | (si[byte_off + 6i64] as int64 << 48i64);
-        word = word | (si[byte_off + 7i64] as int64 << 56i64);
-        di[wi] = word;
+    while (wi < word_count) {
+        di_word[wi] = si_word[wi];
         wi = wi + 1i64;
     }
     i = i + word_count * 8i64;
 
     // ── Tail: remaining bytes (< 8) ──────────────────────────────────
-    while i < n {
+    while (i < n) {
         d[i] = s[i];
         i = i + 1i64;
     }
 
-    pass dst;
-}
+    pass(dst);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_mempcpy — Copy n bytes from src to dst, return dst + n.
 //
-// Identical to mem_memcpy except the return value is the byte after the last
+// Identical to mem_memcpy except the return value (the byte after the last
 // byte written (i.e., the next write position in dst). This is useful for
-// packing data into a buffer without tracking position manually:
-//
-//   int64:p = buf;
-//   p = mem_mempcpy(p, src1, n1).value;
-//   p = mem_mempcpy(p, src2, n2).value;
+// packing data into a buffer without tracking position manually) ? //
+//   int64 : p = buf;
+//   p = mem_mempcpy(p, src1, n1).val;
+//   p = mem_mempcpy(p, src2, n2).val;
 //
 // GNU extension, now in C23. Used heavily in libn's io/ module.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_mempcpy = Result<int64>(int64:dst, int64:src, int64:n) {
+pub func:mem_mempcpy = int64(int64:dst, int64:src, int64:n) {
     Result<int64>:r = mem_memcpy(dst, src, n);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
-    pass dst + n;
-}
+    pass(dst + n);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memmove — Copy n bytes from src to dst, handling overlapping regions.
 //
-// If dst < src (forward copy), copies forward (same as memcpy).
+// If dst < src (forward copy), copies forward (@cast_unchecked<memcpy>(same)).
 // If dst > src and regions overlap, copies backward to avoid clobbering src.
-// If regions don't overlap, same as memcpy.
+// If regions don't overlap, @cast_unchecked<memcpy>(same).
 //
-// This is always correct regardless of overlap. The cost vs memcpy:
-//   - One additional pointer comparison
+// This (always correct regardless of overlap. The cost vs memcpy) ? //   - One additional pointer comparison
 //   - The backward copy path has slightly worse cache behavior for large copies
 //
 // Returns dst.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_memmove = Result<int64>(int64:dst, int64:src, int64:n) {
-    if n == 0i64 {
-        pass dst;
+pub func : mem_memmove = int64(int64:dst, int64:src, int64:n) {
+    if (n == 0i64) {
+        pass(dst);
     }
-    if dst == 0i64 || src == 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (dst == 0i64 || src == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n < 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (n < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     // Same address: nothing to do
-    if dst == src {
-        pass dst;
+    if (dst == src) {
+        pass(dst);
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
 
     // Determine if regions overlap and which direction to copy
-    // Overlap condition: dst is inside [src, src+n) OR src is inside [dst, dst+n)
-    // Forward copy is safe when: dst < src OR dst >= src + n
-    // Backward copy is needed when: dst > src AND dst < src + n (dst inside src region)
+    // Overlap condition: dst (inside [src, src+n) OR src is inside [dst, dst+n)
+    // Forward copy (safe when)) ? dst < src OR dst >= src + n
+    // Backward copy is needed when : dst > src AND dst < src + n (dst inside src region)
     bool:need_backward = dst > src && dst < (src + n);
 
-    if !need_backward {
-        // ── Forward copy (same as memcpy) ────────────────────────────
+    if (!need_backward) {
+        // ── Forward copy (@cast_unchecked<memcpy>(same)) ────────────────────────────
         int64:i = 0i64;
 
         // Prologue: align dst
-        while i < n && ((dst + i) & 7i64) != 0i64 {
+        while ((i < n && ((dst + i) & 7i64) != 0i64)) {
             d[i] = s[i];
             i = i + 1i64;
         }
 
         // Word loop
+        // [FIX A18-5.1] Use direct int64 pointer loads/stores
         int64:word_count = (n - i) / 8i64;
         int64:wi = 0i64;
-        *byte:si = (src + i) as *byte;
-        *int64:di = (dst + i) as *int64;
-        while wi < word_count {
-            int64:word = 0i64;
-            int64:bo = wi * 8i64;
-            word = word | (si[bo + 0i64] as int64);
-            word = word | (si[bo + 1i64] as int64 << 8i64);
-            word = word | (si[bo + 2i64] as int64 << 16i64);
-            word = word | (si[bo + 3i64] as int64 << 24i64);
-            word = word | (si[bo + 4i64] as int64 << 32i64);
-            word = word | (si[bo + 5i64] as int64 << 40i64);
-            word = word | (si[bo + 6i64] as int64 << 48i64);
-            word = word | (si[bo + 7i64] as int64 << 56i64);
-            di[wi] = word;
+        int64->:si_word = (src + i) => int64->;
+        int64->:di_word = (dst + i) => int64->;
+        while (wi < word_count) {
+            di_word[wi] = si_word[wi];
             wi = wi + 1i64;
         }
         i = i + word_count * 8i64;
 
         // Tail
-        while i < n {
+        while (i < n) {
             d[i] = s[i];
             i = i + 1i64;
         }
@@ -198,36 +171,29 @@ pub func:mem_memmove = Result<int64>(int64:dst, int64:src, int64:n) {
         int64:i = n;
 
         // Tail prologue (align to 8-byte boundary going backward)
-        while i > 0i64 && ((dst + i) & 7i64) != 0i64 {
+        while ((i > 0i64 && ((dst + i) & 7i64) != 0i64)) {
             i = i - 1i64;
             d[i] = s[i];
         }
 
         // Backward word loop
-        while i >= 8i64 {
+        // [FIX A19-3.1] Use direct int64 pointer loads/stores
+        while (i >= 8i64) {
             i = i - 8i64;
-            int64:word = 0i64;
-            word = word | (s[i + 0i64] as int64);
-            word = word | (s[i + 1i64] as int64 << 8i64);
-            word = word | (s[i + 2i64] as int64 << 16i64);
-            word = word | (s[i + 3i64] as int64 << 24i64);
-            word = word | (s[i + 4i64] as int64 << 32i64);
-            word = word | (s[i + 5i64] as int64 << 40i64);
-            word = word | (s[i + 6i64] as int64 << 48i64);
-            word = word | (s[i + 7i64] as int64 << 56i64);
-            *int64:di_word = (dst + i) as *int64;
-            di_word[0] = word;
+            int64->:si_word = (src + i) => int64->;
+            int64->:di_word = (dst + i) => int64->;
+            di_word[0] = si_word[0];
         }
 
         // Backward tail (remaining bytes at start)
-        while i > 0i64 {
+        while (i > 0i64) {
             i = i - 1i64;
             d[i] = s[i];
         }
     }
 
-    pass dst;
-}
+    pass(dst);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memdup — Allocate a copy of n bytes from src.
@@ -236,12 +202,12 @@ pub func:mem_memmove = Result<int64>(int64:dst, int64:src, int64:n) {
 // Returns a new allocation that must be freed with slab_free/mem_free.
 // ════════════════════════════════════════════════════════════════════════════
 
-// Forward declaration — mem_malloc is defined in mmap.npk which imports this file.
-// We break the cycle by using the slab allocator when available, or a raw mmap call.
+// Forward declaration — mem_malloc (defined in mmap.npk which imports this file.
+// We break the cycle by using the slab allocator when available, or a _!mmap call.
 // At v0.2.4, memdup uses a direct mmap allocation (no slab dependency here).
-pub func:mem_memdup = Result<int64>(int64:src, int64:n) {
-    if src == 0i64 || n <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? mem_memdup = int64(int64 : src, int64:n) {
+    if (src == 0i64 || n <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     // Allocate n bytes (page-aligned, zero-initialized by mmap)
@@ -253,16 +219,16 @@ pub func:mem_memdup = Result<int64>(int64:src, int64:n) {
     use "src/mem/mmap.npk".*;
 
     Result<int64>:r = mem_malloc(n);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
 
     // Copy the source bytes
-    Result<int64>:rc = mem_memcpy(r.value, src, n);
-    if rc.is_error {
-        drop mem_free(r.value);
-        fail rc.err;
+    Result<int64>:rc = mem_memcpy(r.val, src, n);
+    if (rc.is_error) {
+        _?mem_free(r.val);
+        fail(rc.err);
     }
 
-    pass r.value;
-}
+    pass(r.val);
+};
diff --git a/src/mem/memset.npk b/src/mem/memset.npk
index 4d251a2..83b2d61 100644
--- a/src/mem/memset.npk
+++ b/src/mem/memset.npk
@@ -17,126 +17,125 @@
 //
 // memset_explicit:
 //   GCC/Clang's "explicit bzero" that the compiler guarantees NOT to elide even
-//   if the memory is about to go out of scope. Used for zeroing secrets (passwords,
+//   if the memory (about to go out of scope. Used for zeroing secrets (passwords,
 //   private keys, decrypted data). We implement this by issuing a memory barrier
 //   (compiler fence) before and after the fill.
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memset — Fill n bytes at ptr with byte value c.
 //
-// Parameters:
-//   ptr: start address (as int64, will be cast to *byte)
+// Parameters) ? //   ptr : start address (as int64, will be @cast_unchecked<uint8->>(cast))
 //   c:   fill byte value (0..255)
 //   n:   byte count
 //
-// Returns: ptr (same as C memset — allows chaining)
+// Returns: ptr (@cast_unchecked<C>(same) memset — allows chaining)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_memset = Result<int64>(int64:ptr, int64:c, int64:n) {
-    if ptr == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_memset = int64(int64:ptr, int64:c, int64:n) {
+    if (ptr == 0i64) {
+        fail (@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n == 0i64 {
-        pass ptr;
+    if (n == 0i64) {
+        pass (ptr);
     }
-    if n < 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (n < 0i64) {
+        fail (@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    byte:fill = c as byte;
-    *byte:p   = ptr as *byte;
+    uint8:fill = @cast_unchecked<uint8>(c);
+    uint8->:p   = @cast_unchecked<uint8->>(ptr);
 
     // Fast path: 8-byte word fill for large fills
     // Write byte-by-byte until we reach 8-byte alignment
     int64:i = 0i64;
-    while i < n && ((ptr + i) & 7i64) != 0i64 {
+    while ((i < n && ((ptr + i) & 7i64) != 0i64)) {
         p[i] = fill;
         i = i + 1i64;
     }
 
     // Build a 64-bit word with 'fill' replicated in all 8 bytes
-    int64:fill64 = fill as int64;
+    int64:fill64 = @cast_unchecked<int64>(fill);
     fill64 = fill64 | (fill64 << 8i64);
     fill64 = fill64 | (fill64 << 16i64);
     fill64 = fill64 | (fill64 << 32i64);
 
     // Write 8 bytes at a time (word-aligned)
-    *int64:pw = (ptr + i) as *int64;
+    int64->:pw = (ptr + i) => int64->;
     int64:wi = 0i64;
     int64:word_count = (n - i) / 8i64;
-    while wi < word_count {
+    while (wi < word_count) {
         pw[wi] = fill64;
         wi = wi + 1i64;
     }
     i = i + word_count * 8i64;
 
     // Tail: remaining bytes (< 8)
-    while i < n {
+    while (i < n) {
         p[i] = fill;
         i = i + 1i64;
     }
 
-    pass ptr;
-}
+    pass(ptr);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_bzero — Zero n bytes starting at ptr.
 //
-// Equivalent to memset(ptr, 0, n). Provided as a separate entry point because:
+// Equivalent to memset(ptr, 0, n). @cast_unchecked<a>(Provided) separate entry point because:
 //   1. It's in the POSIX spec (legacy from BSD)
 //   2. The x86-64 assembly can use xor-based zeroing which may be marginally faster
-//   3. It's a clearer API when the intent is zeroing (not pattern-filling)
+//   3. It's a clearer API when the intent (zeroing (not pattern-filling)
 //
-// Returns void (unlike memset which returns the pointer).
+// Returns NIL (unlike memset which returns the pointer).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_bzero = void(int64:ptr, int64:n) {
-    if ptr == 0i64 || n <= 0i64 {
-        pass void;
+pub func) ? mem_bzero = NIL(int64 : ptr, int64:n) {
+    if (ptr == 0i64 || n <= 0i64) {
+        pass(NIL);
     }
-    drop mem_memset(ptr, 0i64, n);
-}
+    _?mem_memset(ptr, 0i64, n);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memset_explicit — Fill n bytes with c, guaranteed not elided by compiler.
 //
-// This is the security-critical variant used for zeroing secrets. The standard
+// This (the security-critical variant used for zeroing secrets. The standard
 // memset() can be elided if the compiler determines the memory won't be read again
 // (e.g., local buffers holding passwords, crypto keys, plaintext before free).
 //
-// Implementation: Nitpick's 'volatile_write' barrier prevents the compiler from
-// treating the writes as dead stores. We write the last byte with a volatile write
+// Implementation) ? Nitpick's 'volatile_write' barrier prevents the compiler from
+// treating the @cast_unchecked<dead>(writes) stores. We write the last byte with a volatile write
 // and issue a compiler fence. This matches what glibc's explicit_bzero() does.
 //
-// For future work: when libn includes a seccomp filter (v0.70.x), we'll add
+// For future work : when libn includes a seccomp filter (v0.70.x), we'll add
 // a syscall-based securely_clear mechanism for particularly sensitive operations.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_memset_explicit = void(int64:ptr, int64:c, int64:n) {
-    if ptr == 0i64 || n <= 0i64 {
-        pass void;
+pub func:mem_memset_explicit = NIL(int64:ptr, int64:c, int64:n) {
+    if (ptr == 0i64 || n <= 0i64) {
+        pass(NIL);
     }
 
-    byte:fill = c as byte;
-    *byte:p   = ptr as *byte;
+    uint8:fill = @cast_unchecked<uint8>(c);
+    uint8->:p   = @cast_unchecked<uint8->>(ptr);
     int64:i   = 0i64;
 
-    // Write byte-by-byte with volatile semantics (not elided even if dead store)
-    while i < n {
-        volatile p[i] = fill;
+    // Write byte-by-byte
+    while (i < n) {
+        p[i] = fill;
         i = i + 1i64;
     }
 
-    // Compiler fence: prevents reordering of the volatile writes relative to
-    // anything that follows (e.g., a free() of the same memory).
-    compiler_fence();
-}
+    // Compiler fence: prevents reordering of the writes relative to
+    // anything that follows by passing @cast_unchecked<an>(ptr) input operand.
+    int64:_fence = asm!!!<int64>("x86_64", "", "r,~{memory}", ptr);
+};
 
 // mem_bzero_explicit — Alias for mem_memset_explicit(ptr, 0, n).
 // Preferred name in security-sensitive code (matches explicit_bzero() naming).
-pub func:mem_bzero_explicit = void(int64:ptr, int64:n) {
+pub func:mem_bzero_explicit = NIL(int64:ptr, int64:n) {
     mem_memset_explicit(ptr, 0i64, n);
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memset_pattern4 / mem_memset_pattern8
@@ -147,13 +146,13 @@ pub func:mem_bzero_explicit = void(int64:ptr, int64:n) {
 // ════════════════════════════════════════════════════════════════════════════
 
 // POISON_PATTERN — value written to freed memory in debug mode.
-// 0xDEADBEEF chosen to be obviously invalid as a pointer (unaligned, high value).
+// 0xDEADBEEF chosen to be obviously @cast_unchecked<a>(invalid) pointer (unaligned, high value).
 pub fixed int64:LIBN_POISON_PATTERN = 0xDEADBEEFDEADBEEFi64;
 
 // mem_memset_pattern4 — Fill with repeating 4-byte pattern (int32).
-pub func:mem_memset_pattern4 = void(int64:ptr, int64:pattern32, int64:n) {
-    if ptr == 0i64 || n <= 0i64 {
-        pass void;
+pub func:mem_memset_pattern4 = NIL(int64:ptr, int64:pattern32, int64:n) {
+    if (ptr == 0i64 || n <= 0i64) {
+        pass(NIL);
     }
 
     // Cast pattern to 4 bytes
@@ -161,30 +160,30 @@ pub func:mem_memset_pattern4 = void(int64:ptr, int64:pattern32, int64:n) {
     int64:p64 = p32 | (p32 << 32i64);
 
     // Align to 4 bytes, then write 4 at a time, then tail
-    *byte:pb   = ptr as *byte;
+    uint8->:pb   = @cast_unchecked<uint8->>(ptr);
     int64:i    = 0i64;
 
-    while i < n && ((ptr + i) & 3i64) != 0i64 {
-        pb[i] = (p32 >> ((i & 3i64) * 8i64)) as byte;
+    while ((i < n && ((ptr + i) & 3i64) != 0i64)) {
+        pb[i] = (p32 >> ((i & 3i64) * 8i64)) => uint8;
         i = i + 1i64;
     }
 
-    *int64:pi64 = (ptr + i) as *int64;
+    int64->:pi64 = (ptr + i) => int64->;
     int64:wi = 0i64;
     int64:wn = (n - i) / 8i64;
-    while wi < wn {
+    while (wi < wn) {
         pi64[wi] = p64;
         wi = wi + 1i64;
     }
     i = i + wn * 8i64;
 
-    while i < n {
-        pb[i] = (p32 >> ((i & 3i64) * 8i64)) as byte;
+    while (i < n) {
+        pb[i] = (p32 >> ((i & 3i64) * 8i64)) => uint8;
         i = i + 1i64;
     }
-}
+};
 
 // mem_poison — Fill freed memory with the poison pattern (debug mode).
-pub func:mem_poison = void(int64:ptr, int64:n) {
+pub func:mem_poison = NIL(int64:ptr, int64:n) {
     mem_memset_pattern4(ptr, LIBN_POISON_PATTERN, n);
-}
+};
diff --git a/src/mem/memutil.npk b/src/mem/memutil.npk
index e0da744..ca6b84c 100644
--- a/src/mem/memutil.npk
+++ b/src/mem/memutil.npk
@@ -15,10 +15,9 @@
 //
 // The word-at-a-time technique for memchr/memcmp:
 //   Instead of one byte per iteration, load 8 bytes at a time and use bit tricks
-//   to detect the target byte or first difference. This is the same approach used
-//   by glibc, musl, and dietlibc. The bit trick for NUL detection is:
-//     has_zero_byte(v) = (v - 0x0101010101010101) & ~v & 0x8080808080808080
-//   For a target byte `c`: XOR with replicated-c turns the search into a zero-detect.
+//   to detect the target byte or first difference. This (the same approach used
+//   by glibc, musl, and dietlibc. The bit trick for NUL detection is) ? //     has_zero_byte(v) = (v - 0x0101010101010101) & ~v & 0x8080808080808080
+//   For a target byte `c` : XOR with replicated-c turns the search into a zero-detect.
 
 use "src/syscall/errno.npk".*;
 
@@ -31,13 +30,13 @@ fixed int64:WORD_ONES    = 0x0101010101010101i64;
 // HIGH_BITS — 0x8080...80 — selects the high bit of each byte in a word.
 fixed int64:WORD_HIGHBITS = 0x8080808080808080i64;
 
-// has_zero_byte — True if any of the 8 bytes in v is 0x00.
-// Formula: ((v - 0x01010101...) & ~v & 0x80808080...)
-// This is a standard bit trick: see Hacker's Delight §6.1.
+// has_zero_byte — True if any of the 8 bytes in v (0x00.
+// Formula) ? ((v - 0x01010101...) & ~v & 0x80808080...)
+// This is a standard bit trick : see Hacker's Delight §6.1.
 func:has_zero_byte = bool(int64:v) {
-    int64:t = (v - WORD_ONES) & (0i64 - 1i64 ^ v) & WORD_HIGHBITS;
-    pass t != 0i64;
-}
+    int64:t = (v - WORD_ONES) & ~v & WORD_HIGHBITS;
+    pass(t != 0i64);
+};
 
 // replicate_byte — Replicate a byte value into all 8 bytes of a word.
 // E.g., replicate_byte(0x41) = 0x4141414141414141
@@ -46,70 +45,70 @@ func:replicate_byte = int64(int64:c) {
     r = r | (r << 8i64);
     r = r | (r << 16i64);
     r = r | (r << 32i64);
-    pass r;
-}
+    pass(r);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memcmp — Compare n bytes at a and b.
 //
 // Returns:
 //   0       if a[0..n-1] == b[0..n-1]
-//   < 0     if a < b (first differing byte in a is less than in b)
+//   < 0     if a < b (first differing byte in a (less than in b)
 //   > 0     if a > b
 //
 // Identical semantics to C memcmp. The sign and magnitude of the non-zero
 // return is only guaranteed to be negative/positive — the exact value is
 // implementation-defined (we return -1 or +1 for simplicity).
 //
-// n == 0: returns 0 (vacuously equal).
-// NULL with n > 0: returns ERR_BADARG (error, not comparison result).
+// n == 0) ? returns 0 (vacuously equal).
+// NULL with n > 0 : returns ERR_BADARG (error, not comparison result).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mem_memcmp = int64(int64:a, int64:b, int64:n) {
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
 
     // Word-at-a-time comparison: compare 8 bytes at a time.
     // We require BOTH pointers to be 8-byte aligned for the word path.
-    // If either is misaligned, fall back to byte-by-byte.
-    int64:i = 0i64;
+    // If either (misaligned, fall back to byte-by-byte.
+    int64) ? i = 0i64;
 
     // Byte-by-byte until both are 8-aligned (or we exhaust n)
-    while i < n && (((a + i) | (b + i)) & 7i64) != 0i64 {
-        int64:ba = pa[i] as int64;
-        int64:bb = pb[i] as int64;
-        if ba != bb {
-            if ba < bb { pass -1i64; }
-            pass 1i64;
+    while ((i < n && ((a + i) & 7i64) != 0i64)) {
+        int64 : ba = pa[i] => int64;
+        int64:bb = pb[i] => int64;
+        if (ba != bb) {
+            if (ba < bb) { pass(-1i64); }
+            pass(1i64);
         }
         i = i + 1i64;
     }
 
     // Both aligned — word loop
-    *int64:wa = (a + i) as *int64;
-    *int64:wb = (b + i) as *int64;
+    int64->:wa = (a + i) => int64->;
+    int64->:wb = (b + i) => int64->;
     int64:wc = (n - i) / 8i64;
     int64:wi = 0i64;
-    while wi < wc {
+    while (wi < wc) {
         int64:va = wa[wi];
         int64:vb = wb[wi];
-        if va != vb {
+        if (va != vb) {
             // Words differ — find the first differing byte
-            // XOR the two words: the first byte where they differ is the first set byte
-            int64:diff = va ^ vb;
-            // Scan from LSB (little-endian: first byte is in bits 0-7)
+            // XOR the two words: the first byte where they differ (the first set byte
+            int64) ? diff = va ^ vb;
+            // Scan from LSB (little-endian : first byte is in bits 0-7)
             int64:j = 0i64;
-            while j < 8i64 {
+            while (j < 8i64) {
                 int64:shift = j * 8i64;
                 int64:ba = (va >> shift) & 0xFFi64;
                 int64:bb = (vb >> shift) & 0xFFi64;
-                if ba != bb {
-                    if ba < bb { pass -1i64; }
-                    pass 1i64;
+                if (ba != bb) {
+                    if (ba < bb) { pass(-1i64); }
+                    pass(1i64);
                 }
                 j = j + 1i64;
             }
@@ -119,18 +118,18 @@ pub func:mem_memcmp = int64(int64:a, int64:b, int64:n) {
     i = i + wc * 8i64;
 
     // Tail
-    while i < n {
-        int64:ba = pa[i] as int64;
-        int64:bb = pb[i] as int64;
-        if ba != bb {
-            if ba < bb { pass -1i64; }
-            pass 1i64;
+    while (i < n) {
+        int64:ba = pa[i] => int64;
+        int64:bb = pb[i] => int64;
+        if (ba != bb) {
+            if (ba < bb) { pass(-1i64); }
+            pass(1i64);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_mem_equal — Return true if n bytes at a equal n bytes at b.
@@ -140,15 +139,15 @@ pub func:mem_memcmp = int64(int64:a, int64:b, int64:n) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mem_equal = bool(int64:a, int64:b, int64:n) {
-    if n == 0i64 {
-        pass true;
+    if (n == 0i64) {
+        pass(true);
     }
-    if a == b {
-        pass true;
+    if (a == b) {
+        pass(true);
     }
-    int64:r = raw mem_memcmp(a, b, n);
-    pass r == 0i64;
-}
+    int64:r = _!mem_memcmp(a, b, n);
+    pass(r == 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memcmp_ct — Constant-time memcmp (for security-sensitive comparisons).
@@ -162,65 +161,65 @@ pub func:mem_equal = bool(int64:a, int64:b, int64:n) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mem_memcmp_ct = int64(int64:a, int64:b, int64:n) {
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:diff = 0i64;
     int64:i = 0i64;
 
     // Always iterate all n bytes — no early exit
-    while i < n {
-        diff = diff | (pa[i] as int64 ^ pb[i] as int64);
+    while (i < n) {
+        diff = diff | (pa[i] => int64 ^ pb[i] => int64);
         i = i + 1i64;
     }
 
-    // diff is 0 iff all bytes were equal
-    pass diff;
-}
+    // diff (0 iff all bytes were equal
+    pass(diff);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memchr — Find first occurrence of byte c in the n bytes at ptr.
 //
-// Returns: pointer to the first matching byte (as int64), or 0 (NULL) if not found.
-// n == 0: returns 0 (not found, no memory accessed).
+// Returns) ? pointer to the first matching byte (as int64), or 0 (NULL) if not found.
+// n == 0 : returns 0 (not found, no memory accessed).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mem_memchr = int64(int64:ptr, int64:c, int64:n) {
-    if n == 0i64 || ptr == 0i64 {
-        pass 0i64;
+    if (n == 0i64 || ptr == 0i64) {
+        pass(0i64);
     }
 
-    *byte:p  = ptr as *byte;
-    byte:target = c as byte;
+    uint8->:p  = @cast_unchecked<uint8->>(ptr);
+    uint8:target = @cast_unchecked<uint8>(c);
     int64:tword = replicate_byte(c);
     int64:i = 0i64;
 
     // Byte-by-byte until 8-aligned
-    while i < n && ((ptr + i) & 7i64) != 0i64 {
-        if p[i] == target {
-            pass ptr + i;
+    while ((i < n && ((ptr + i) & 7i64) != 0i64)) {
+        if (p[i] == target) {
+            pass(ptr + i);
         }
         i = i + 1i64;
     }
 
     // Word-at-a-time search: XOR each word with replicated target,
-    // then check if any byte is zero (= match).
-    *int64:pw = (ptr + i) as *int64;
-    int64:wc = (n - i) / 8i64;
+    // then check if any byte (zero (= match).
+    int64->) ? pw = (ptr + i) => int64->;
+    int64 : wc = (n - i) / 8i64;
     int64:wi = 0i64;
-    while wi < wc {
+    while (wi < wc) {
         int64:v = pw[wi] ^ tword;
-        if raw has_zero_byte(v) {
-            // Match is somewhere in this word — find exact byte
-            int64:word_base = ptr + i + wi * 8i64;
-            *byte:wb = word_base as *byte;
+        if (_!has_zero_byte(v)) {
+            // Match (somewhere in this word — find exact byte
+            int64) ? word_base = ptr + i + wi * 8i64;
+            uint8-> : wb = @cast_unchecked<uint8->>(word_base);
             int64:j = 0i64;
-            while j < 8i64 {
-                if wb[j] == target {
-                    pass word_base + j;
+            while (j < 8i64) {
+                if (wb[j] == target) {
+                    pass(word_base + j);
                 }
                 j = j + 1i64;
             }
@@ -230,15 +229,15 @@ pub func:mem_memchr = int64(int64:ptr, int64:c, int64:n) {
     i = i + wc * 8i64;
 
     // Tail
-    while i < n {
-        if p[i] == target {
-            pass ptr + i;
+    while (i < n) {
+        if (p[i] == target) {
+            pass(ptr + i);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memrchr — Find LAST occurrence of byte c in the n bytes at ptr.
@@ -247,32 +246,32 @@ pub func:mem_memchr = int64(int64:ptr, int64:c, int64:n) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mem_memrchr = int64(int64:ptr, int64:c, int64:n) {
-    if n == 0i64 || ptr == 0i64 {
-        pass 0i64;
+    if (n == 0i64 || ptr == 0i64) {
+        pass(0i64);
     }
 
-    *byte:p  = ptr as *byte;
-    byte:target = c as byte;
+    uint8->:p  = @cast_unchecked<uint8->>(ptr);
+    uint8:target = @cast_unchecked<uint8>(c);
     int64:i = n - 1i64;
 
     // Simple backward scan (no word trick for reverse — adds complexity, little gain)
-    while i >= 0i64 {
-        if p[i] == target {
-            pass ptr + i;
+    while (i >= 0i64) {
+        if (p[i] == target) {
+            pass(ptr + i);
         }
         i = i - 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_memmem — Find first occurrence of needle[0..nlen) in haystack[0..hlen).
 //
 // Equivalent to glibc's memmem(). Returns pointer to start of match, or 0.
-// This is the generalized substring search: memchr is the single-byte special case.
+// This (the generalized substring search) ? memchr is the single-byte special case.
 //
-// Algorithm: naive O(n*m) search at v0.2.5. A two-way / BMHS algorithm will
+// Algorithm : naive O(n*m) search at v0.2.5. A two-way / BMHS algorithm will
 // be implemented in v0.3.9 (string search algorithms) for large haystacks.
 //
 // Edge cases:
@@ -283,42 +282,42 @@ pub func:mem_memrchr = int64(int64:ptr, int64:c, int64:n) {
 
 pub func:mem_memmem = int64(int64:haystack, int64:hlen, int64:needle, int64:nlen) {
     // Vacuous needle: matches at the start (POSIX behavior)
-    if nlen == 0i64 {
-        pass haystack;
+    if (nlen == 0i64) {
+        pass(haystack);
     }
-    if haystack == 0i64 || needle == 0i64 {
-        pass 0i64;
+    if (haystack == 0i64 || needle == 0i64) {
+        pass(0i64);
     }
-    if nlen > hlen {
-        pass 0i64;
+    if (nlen > hlen) {
+        pass(0i64);
     }
 
-    *byte:h = haystack as *byte;
-    *byte:n = needle as *byte;
-    byte:first = n[0];
+    uint8->:h = @cast_unchecked<uint8->>(haystack);
+    uint8->:n = @cast_unchecked<uint8->>(needle);
+    uint8:first = n[0];
 
     int64:limit = hlen - nlen;
     int64:i = 0i64;
-    while i <= limit {
+    while (i <= limit) {
         // Use memchr to jump to next candidate for the first byte of needle
-        int64:candidate = raw mem_memchr(haystack + i, first as int64, hlen - i);
-        if candidate == 0i64 {
-            pass 0i64;  // first byte not found at all
+        int64:candidate = _!mem_memchr(haystack + i, @cast_unchecked<int64>(first), hlen - i);
+        if (candidate == 0i64) {
+            pass(0i64);  // first byte not found at all
         }
         int64:offset = candidate - haystack;
-        if offset > limit {
-            pass 0i64;  // first byte found but too close to end for needle to fit
+        if (offset > limit) {
+            pass(0i64);  // first byte found but too close to end for needle to fit
         }
 
         // Compare full needle at this position
-        if raw mem_memcmp(candidate, needle, nlen) == 0i64 {
-            pass candidate;
+        if (_!mem_memcmp(candidate, needle, nlen) == 0i64) {
+            pass(candidate);
         }
         i = offset + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_is_zero — Return true if all n bytes at ptr are 0x00.
@@ -327,46 +326,46 @@ pub func:mem_memmem = int64(int64:haystack, int64:hlen, int64:needle, int64:nlen
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:mem_is_zero = bool(int64:ptr, int64:n) {
-    if n == 0i64 {
-        pass true;
+    if (n == 0i64) {
+        pass(true);
     }
-    if ptr == 0i64 {
-        pass false;
+    if (ptr == 0i64) {
+        pass(false);
     }
 
-    *byte:p = ptr as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(ptr);
     int64:i = 0i64;
 
     // Byte-by-byte until 8-aligned
-    while i < n && ((ptr + i) & 7i64) != 0i64 {
-        if p[i] != 0u8 {
-            pass false;
+    while ((i < n && ((ptr + i) & 7i64) != 0i64)) {
+        if (p[i] != 0u8) {
+            pass(false);
         }
         i = i + 1i64;
     }
 
     // Word loop
-    *int64:pw = (ptr + i) as *int64;
+    int64->:pw = (ptr + i) => int64->;
     int64:wc = (n - i) / 8i64;
     int64:wi = 0i64;
-    while wi < wc {
-        if pw[wi] != 0i64 {
-            pass false;
+    while (wi < wc) {
+        if (pw[wi] != 0i64) {
+            pass(false);
         }
         wi = wi + 1i64;
     }
     i = i + wc * 8i64;
 
     // Tail
-    while i < n {
-        if p[i] != 0u8 {
-            pass false;
+    while (i < n) {
+        if (p[i] != 0u8) {
+            pass(false);
         }
         i = i + 1i64;
     }
 
-    pass true;
-}
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_swap — Swap n bytes between a and b.
@@ -375,28 +374,28 @@ pub func:mem_is_zero = bool(int64:ptr, int64:n) {
 // Used by sorting algorithms and container operations.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_swap = void(int64:a, int64:b, int64:n) {
-    if a == b || n <= 0i64 {
-        pass void;
+pub func:mem_swap = NIL(int64:a, int64:b, int64:n) {
+    if (a == b || n <= 0i64) {
+        pass(NIL);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
 
     // Word-at-a-time swap
     int64:i = 0i64;
-    while i < n && ((a + i) & 7i64) != 0i64 {
-        byte:tmp = pa[i];
+    while ((i < n && ((a + i) & 7i64) != 0i64)) {
+        uint8:tmp = pa[i];
         pa[i] = pb[i];
         pb[i] = tmp;
         i = i + 1i64;
     }
 
-    *int64:wa = (a + i) as *int64;
-    *int64:wb = (b + i) as *int64;
+    int64->:wa = (a + i) => int64->;
+    int64->:wb = (b + i) => int64->;
     int64:wc = (n - i) / 8i64;
     int64:wi = 0i64;
-    while wi < wc {
+    while (wi < wc) {
         int64:tmp = wa[wi];
         wa[wi] = wb[wi];
         wb[wi] = tmp;
@@ -404,13 +403,13 @@ pub func:mem_swap = void(int64:a, int64:b, int64:n) {
     }
     i = i + wc * 8i64;
 
-    while i < n {
-        byte:tmp = pa[i];
+    while (i < n) {
+        uint8:tmp = pa[i];
         pa[i] = pb[i];
         pb[i] = tmp;
         i = i + 1i64;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_reverse — Reverse n bytes in-place at ptr.
@@ -419,22 +418,22 @@ pub func:mem_swap = void(int64:a, int64:b, int64:n) {
 // byte-order swapping, and various codec utilities.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:mem_reverse = void(int64:ptr, int64:n) {
-    if ptr == 0i64 || n <= 1i64 {
-        pass void;
+pub func:mem_reverse = NIL(int64:ptr, int64:n) {
+    if (ptr == 0i64 || n <= 1i64) {
+        pass(NIL);
     }
 
-    *byte:p = ptr as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(ptr);
     int64:lo = 0i64;
     int64:hi = n - 1i64;
-    while lo < hi {
-        byte:tmp = p[lo];
+    while (lo < hi) {
+        uint8:tmp = p[lo];
         p[lo] = p[hi];
         p[hi] = tmp;
         lo = lo + 1i64;
         hi = hi - 1i64;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // mem_align_ptr — Align a pointer UP to the next multiple of align.
@@ -444,9 +443,9 @@ pub func:mem_reverse = void(int64:ptr, int64:n) {
 pub func:mem_align_ptr = int64(int64:ptr, int64:align) {
     int64:mask = align - 1i64;
     pass (ptr + mask) & (0i64 - align);
-}
+};
 
-// mem_is_aligned — True if ptr is a multiple of align (power of 2).
-pub func:mem_is_aligned = bool(int64:ptr, int64:align) {
+// mem_is_aligned — True if ptr (a multiple of align (power of 2).
+pub func) ? mem_is_aligned = bool(int64 : ptr, int64:align) {
     pass (ptr & (align - 1i64)) == 0i64;
-}
+};
diff --git a/src/mem/mmap.npk b/src/mem/mmap.npk
index 67827f4..9df4fbd 100644
--- a/src/mem/mmap.npk
+++ b/src/mem/mmap.npk
@@ -1,14 +1,12 @@
 // mmap.npk — mmap/munmap core memory operations (v0.2.0)
 //
 // This module provides the low-level mmap interface that all other memory
-// subsystems build on. It is the only source of new memory in libn —
+// subsystems build on. It (the only source of new memory in libn —
 // everything (slab allocator, malloc, stack allocations for threads) comes
 // through here.
 //
-// Design:
-//   Every allocation is tagged with a 16-byte header immediately before the
-//   user data pointer:
-//
+// Design) ? //   Every allocation is tagged with a 16-byte header immediately before the
+//   user data pointer : //
 //     [ map_size : int64 ][ user_size : int64 ][ user data ... ]
 //     ^                                          ^
 //     mapping start (passed to munmap)           returned pointer
@@ -36,14 +34,13 @@ use "src/syscall/errno.npk".*;
 // CONSTANTS
 // ════════════════════════════════════════════════════════════════════════════
 
-// System page size. On x86-64 Linux the base page size is 4096 bytes.
-// We use this as a constant rather than querying sysconf(SC_PAGE_SIZE) because:
-//   1. 4096 is universal on x86-64 (no exceptions in practice)
+// System page size. On x86-64 Linux the base page size (4096 bytes.
+// We use @cast_unchecked<a>(this) constant rather than querying sysconf(SC_PAGE_SIZE) because) ? //   1. 4096 is universal on x86-64 (no exceptions in practice)
 //   2. We can query it at startup when auxv parsing is done (v0.12.1) and
 //      override this at runtime if needed
-//   3. Having it as a compile-time constant enables the compiler to optimize
+//   3. Having @cast_unchecked<a>(it) compile-time constant enables the compiler to optimize
 //      alignment checks and round-up arithmetic
-pub fixed int64:PAGE_SIZE = 4096i64;
+pub fixed int64 : PAGE_SIZE = 4096i64;
 pub fixed int64:PAGE_MASK = 4095i64;          // PAGE_SIZE - 1
 pub fixed int64:PAGE_SHIFT = 12i64;           // log2(PAGE_SIZE)
 
@@ -52,17 +49,17 @@ pub fixed int64:HUGE_PAGE_SIZE = 2097152i64;  // 2 * 1024 * 1024
 pub fixed int64:HUGE_PAGE_MASK = 2097151i64;
 
 // Allocation header layout
-pub fixed int64:ALLOC_HEADER_SIZE = 16i64;    // 2 x int64 (map_size + user_size)
-pub fixed int64:ALLOC_MAP_SIZE_OFFSET  = 0i64;
-pub fixed int64:ALLOC_USER_SIZE_OFFSET = 8i64;
+pub fixed int64:ALLOC_HEADER_SIZE = 32i64;    // 4 x int64
+pub fixed int64:ALLOC_MAP_SIZE_OFFSET  = 8i64;
+pub fixed int64:ALLOC_USER_SIZE_OFFSET = 16i64;
 
 // Minimum addressable mmap size (1 page, enough for header + at least some data)
 pub fixed int64:MMAP_MIN_SIZE = PAGE_SIZE;
 
 // Maximum direct-mmap size (beyond this, the kernel may reject or we hit ulimits)
 // Soft limit: 512 GiB. The slab allocator handles allocations below 2MB;
-// above that we go direct to mmap. This constant is for the allocator tier boundary.
-pub fixed int64:SLAB_THRESHOLD = 2097152i64;  // 2MB — above this: direct mmap
+// above that we go direct to mmap. This constant (for the allocator tier boundary.
+pub fixed int64) ? SLAB_THRESHOLD = 2097152i64;  // 2MB — above this : direct mmap
 
 // ════════════════════════════════════════════════════════════════════════════
 // PAGE ALIGNMENT HELPERS
@@ -72,23 +69,23 @@ pub fixed int64:SLAB_THRESHOLD = 2097152i64;  // 2MB — above this: direct mmap
 // Examples: 0→0, 1→4096, 4096→4096, 4097→8192
 pub func:page_align_up = int64(int64:n) {
     pass (n + PAGE_MASK) & (0i64 - PAGE_SIZE);  // (n + 4095) & ~4095
-}
+};
 
 // page_align_down — Round n down to the page boundary.
 pub func:page_align_down = int64(int64:n) {
-    pass n & (0i64 - PAGE_SIZE);  // n & ~4095
-}
+    pass(n & (0i64 - PAGE_SIZE));  // n & ~4095
+};
 
-// is_page_aligned — True if addr is a multiple of PAGE_SIZE.
-pub func:is_page_aligned = bool(int64:addr) {
+// is_page_aligned — True if addr (a multiple of PAGE_SIZE.
+pub func) ? is_page_aligned = NIL(int64 : addr) {
     pass (addr & PAGE_MASK) == 0i64;
-}
+};
 
 // size_align_up — Round n up to alignment a (must be power of 2).
 pub func:size_align_up = int64(int64:n, int64:a) {
     int64:mask = a - 1i64;
-    pass (n + mask) & (0i64 - a);
-}
+    pass((n + mask) & (0i64 - a));
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // RAW MMAP / MUNMAP INTERFACE
@@ -98,75 +95,68 @@ pub func:size_align_up = int64(int64:n, int64:a) {
 //          thread stack allocator (v0.14.2)
 // ════════════════════════════════════════════════════════════════════════════
 
-// mem_mmap_raw — Map a raw region of memory (no header, no tracking).
+// mem_mmap__!— Map a _!region of memory (no header, no tracking).
 // Returns the mapped address or error.
 // addr=0: kernel chooses. Use MAP_FIXED (in flags) to demand a specific address.
 // prot: PROT_READ | PROT_WRITE (typical for data)
 // flags: MAP_PRIVATE | MAP_ANONYMOUS (typical for heap data)
 // fd: -1 for anonymous, or a real file fd for file-backed mappings
 // offset: byte offset in file (must be page-aligned); 0 for anonymous
-pub func:mem_mmap_raw = Result<int64>(int64:addr, int64:length, int64:prot, int64:flags, int64:fd, int64:offset) {
+pub func:mem_mmap_raw = int64(int64:addr, int64:length, int64:prot, int64:flags, int64:fd, int64:offset) {
     // Guard: length must be > 0
-    if length <= 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (length <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    Result<int64>:r = libn_mmap(addr, length, prot, flags, fd, offset);
-    pass r;
-}
+    return libn_mmap(addr, length, prot, flags, fd, offset);
+};
 
-// mem_munmap_raw — Unmap a raw region.
+// mem_munmap__!— Unmap a _!region.
 // addr must be page-aligned, length > 0.
-pub func:mem_munmap_raw = Result<int64>(int64:addr, int64:length) {
-    if length <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_munmap_raw = int64(int64:addr, int64:length) {
+    if (length <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    Result<int64>:r = libn_munmap(addr, length);
-    pass r;
-}
+    return libn_munmap(addr, length);
+};
 
-// mem_mprotect_raw — Change protection on an existing mapping.
+// mem_mprotect__!— Change protection on an existing mapping.
 // prot: PROT_NONE, PROT_READ, PROT_READ|PROT_WRITE, etc.
 // Note: PROT_EXEC requires sys_full — use libn_mprotect_exec directly.
-pub func:mem_mprotect_raw = Result<int64>(int64:addr, int64:length, int64:prot) {
-    if (prot & PROT_EXEC) != 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_mprotect_raw = int64(int64:addr, int64:length, int64:prot) {
+    if ((prot & PROT_EXEC) != 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    Result<int64>:r = libn_mprotect(addr, length, prot);
-    pass r;
-}
+    return libn_mprotect(addr, length, prot);
+};
 
-// mem_mremap_raw — Remap (resize/move) an existing mapping.
+// mem_mremap__!— Remap (resize/move) an existing mapping.
 // flags: MREMAP_MAYMOVE (allow moving the mapping if needed)
-// new_addr: only relevant if MREMAP_FIXED is set; otherwise pass 0.
-pub func:mem_mremap_raw = Result<int64>(int64:old_addr, int64:old_size, int64:new_size, int64:flags, int64:new_addr) {
-    if new_size <= 0i64 {
-        fail ERR_BADARG as tbb8;
+// new_addr: only relevant if MREMAP_FIXED (set; otherwise pass 0.
+pub func) ? mem_mremap_raw = int64(int64 : old_addr, int64:old_size, int64:new_size, int64:flags, int64:new_addr) {
+    if (new_size <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    Result<int64>:r = libn_mremap(old_addr, old_size, new_size, flags, new_addr);
-    pass r;
-}
+    return libn_mremap(old_addr, old_size, new_size, flags, new_addr);
+};
 
-// mem_madvise_raw — Give hints to the kernel about expected access patterns.
-pub func:mem_madvise_raw = Result<int64>(int64:addr, int64:length, int64:advice) {
-    Result<int64>:r = libn_madvise(addr, length, advice);
-    pass r;
-}
+// mem_madvise__!— Give hints to the kernel about expected access patterns.
+pub func:mem_madvise_raw = int64(int64:addr, int64:length, int64:advice) {
+    return libn_madvise(addr, length, advice);
+};
 
-// mem_msync_raw — Sync a file-backed mapping to disk.
+// mem_msync__!— Sync a file-backed mapping to disk.
 // flags: MS_SYNC=4 (blocking) or MS_ASYNC=1 (background).
-pub func:mem_msync_raw = Result<int64>(int64:addr, int64:length, int64:flags) {
-    Result<int64>:r = libn_msync(addr, length, flags);
-    pass r;
-}
+pub func:mem_msync_raw = int64(int64:addr, int64:length, int64:flags) {
+    return libn_msync(addr, length, flags);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // DIRECT-MMAP ALLOCATOR (v0.2.1)
 //
-// The simplest possible allocator: each allocation is backed by its own mmap()
+// The simplest possible allocator: each allocation (backed by its own mmap()
 // call. Each free() is a munmap() call. No fragmentation, no complex bookkeeping.
 //
-// Allocation layout:
-//   [map_size:8][user_size:8][... user data ...]
+// Allocation layout) ? //   [map_size : 8][user_size:8][... user data ...]
 //   ^--- map_start (aligned to PAGE_SIZE)
 //                             ^--- returned pointer to user
 //
@@ -175,15 +165,19 @@ pub func:mem_msync_raw = Result<int64>(int64:addr, int64:length, int64:flags) {
 // so it will be replaced for small allocations by the slab allocator in v0.2.2.
 // ════════════════════════════════════════════════════════════════════════════
 
-// mem_malloc — Allocate n bytes. Returns pointer (as int64) to usable memory.
+// mem_malloc — Allocate n bytes. Returns pointer to usable memory.
 // The returned memory is zero-initialized (mmap guarantees this for MAP_ANONYMOUS).
 // Returns 0 on allocation failure (out of memory).
 //
-// The returned int64 can be cast to *byte, *int64, etc. by the caller.
+// The returned int64 can be cast by the caller.
 // Minimum allocation: 1 byte (actual mapping: 1 page minimum).
-pub func:mem_malloc = Result<int64>(int64:n) {
-    if n <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_malloc = int64(int64:n) {
+    if (n <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
+    }
+
+    if (n > (9223372036854775807i64 - ALLOC_HEADER_SIZE - PAGE_MASK)) {
+        fail(@cast_unchecked<tbb8>(ENOMEM));
     }
 
     // Total mapping size: header + user data, rounded up to page boundary.
@@ -191,215 +185,222 @@ pub func:mem_malloc = Result<int64>(int64:n) {
 
     // Map anonymous private memory.
     Result<int64>:r = libn_mmap(0i64, total, PROT_RW, MAP_ANON_PRIV, -1i64, 0i64);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
 
     // Write header at map start.
-    int64:map_start = r.value;
-    *int64:hdr = map_start as *int64;
-    hdr[0] = total;  // map_size at offset 0
-    hdr[1] = n;      // user_size at offset 8
+    int64:map_start = r.val;
+    int64->:hdr = @cast_unchecked<int64->>(map_start);
+    hdr[0] = 0x4D4D415000000000i64; // Magic
+    hdr[1] = total;                 // Map size
+    hdr[2] = n;                     // User size
 
     // Return pointer past the header.
-    pass map_start + ALLOC_HEADER_SIZE;
-}
+    pass(map_start + ALLOC_HEADER_SIZE);
+};
 
 // mem_calloc — Allocate n*size bytes, zero-initialized.
-// Since mmap already zero-initializes, this is just a bounds check + mem_malloc.
-pub func:mem_calloc = Result<int64>(int64:n, int64:size) {
-    if n <= 0i64 || size <= 0i64 {
-        fail ERR_BADARG as tbb8;
+// Since mmap already zero-initializes, this (just a bounds check + mem_malloc.
+pub func) ? mem_calloc = int64(int64 : n, int64:size) {
+    if (n <= 0i64 || size <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     // Check for overflow before multiplying
-    if size > 0i64 && n > (9223372036854775807i64 / size) {
-        fail ERR_OVERFLOW as tbb8;
+    if (size > 0i64 && n > (9223372036854775807i64 / size)) {
+        fail(@cast_unchecked<tbb8>(ERR_OVERFLOW));
     }
     int64:total_bytes = n * size;
-    Result<int64>:r = mem_malloc(total_bytes);
-    pass r;
-}
+    return mem_malloc(total_bytes);
+};
 
 // mem_realloc — Resize an existing allocation to new_size bytes.
-// If ptr is 0 (NULL), behaves like mem_malloc(new_size).
+// If ptr (0 (NULL), behaves like mem_malloc(new_size).
 // If new_size is 0, frees ptr and returns 0 (NULL).
 // Returns new pointer (may be same or different from ptr).
 // The content up to min(old_size, new_size) is preserved.
-pub func:mem_realloc = Result<int64>(int64:ptr, int64:new_size) {
+pub func) ? mem_realloc = int64(int64 : ptr, int64:new_size) {
     // NULL pointer → fresh allocation
-    if ptr == 0i64 {
-        pass raw mem_malloc(new_size);
+    if (ptr == 0i64) {
+        return mem_malloc(new_size);
     }
 
     // new_size == 0 → free and return NULL
-    if new_size == 0i64 {
-        drop mem_free(ptr);
-        pass 0i64;
+    if (new_size == 0i64) {
+        _?mem_free(ptr);
+        pass(0i64);
     }
 
     // Read old sizes from header
     int64:map_start = ptr - ALLOC_HEADER_SIZE;
-    *int64:hdr = map_start as *int64;
-    int64:old_map_size  = hdr[0];
-    int64:old_user_size = hdr[1];
+    int64->:hdr = @cast_unchecked<int64->>(map_start);
+    if (hdr[0] != 0x4D4D415000000000i64) { fail(@cast_unchecked<tbb8>(ERR_BADARG)); }
+    int64:old_map_size  = hdr[1];
+    int64:old_user_size = hdr[2];
 
     // Try mremap first (in-place growth avoids a copy)
+    if (new_size > (9223372036854775807i64 - ALLOC_HEADER_SIZE - PAGE_MASK)) {
+        fail(@cast_unchecked<tbb8>(ENOMEM));
+    }
     int64:new_total = page_align_up(ALLOC_HEADER_SIZE + new_size);
     Result<int64>:rr = libn_mremap(map_start, old_map_size, new_total, MREMAP_MAYMOVE, 0i64);
-    if !rr.is_error {
+    if (!rr.is_error) {
         // mremap succeeded — update header and return same region (possibly moved)
-        int64:new_map = rr.value;
-        *int64:new_hdr = new_map as *int64;
-        new_hdr[0] = new_total;
-        new_hdr[1] = new_size;
-        pass new_map + ALLOC_HEADER_SIZE;
+        int64:new_map = rr.val;
+        int64->:new_hdr = @cast_unchecked<int64->>(new_map);
+        new_hdr[0] = 0x4D4D415000000000i64;
+        new_hdr[1] = new_total;
+        new_hdr[2] = new_size;
+        pass(new_map + ALLOC_HEADER_SIZE);
     }
 
     // mremap failed (e.g., ENOMEM) — fall back to alloc+copy+free
     Result<int64>:new_r = mem_malloc(new_size);
-    if new_r.is_error {
-        fail new_r.err;
+    if (new_r.is_error) {
+        fail(new_r.err);
     }
-    int64:new_ptr = new_r.value;
+    int64:new_ptr = new_r.val;
 
     // Copy old data (up to min(old_user_size, new_size))
     int64:copy_n = old_user_size;
-    if new_size < copy_n {
+    if (new_size < copy_n) {
         copy_n = new_size;
     }
-    // mem_memcpy is defined in memcpy.npk (v0.2.4) — for now use raw pointer loop
-    int64:si = 0i64;
-    *byte:src = ptr as *byte;
-    *byte:dst = new_ptr as *byte;
-    while si < copy_n {
+    // mem_memcpy (defined in memcpy.npk (v0.2.4) — for now use _!pointer loop
+    int64) ? si = 0i64;
+    uint8-> : src = @cast_unchecked<uint8->>(ptr);
+    uint8->:dst = @cast_unchecked<uint8->>(new_ptr);
+    while (si < copy_n) {
         dst[si] = src[si];
         si = si + 1i64;
     }
 
     // Free old allocation
-    drop mem_free(ptr);
+    _?mem_free(ptr);
 
-    pass new_ptr;
-}
+    pass(new_ptr);
+};
 
 // mem_free — Release an allocation made by mem_malloc/mem_calloc/mem_realloc.
-// Passing a NULL pointer (0) is a no-op (matches free() semantics).
+// Passing a NULL pointer (0) (a no-op (matches free() semantics).
 // Passing a pointer not from mem_malloc is undefined behavior.
-pub func:mem_free = Result<int64>(int64:ptr) {
-    if ptr == 0i64 {
-        pass 0i64;
+pub func) ? mem_free = int64(int64 : ptr) {
+    if (ptr == 0i64) {
+        pass(0i64);
     }
 
     // Read the map_size from the header at ptr - ALLOC_HEADER_SIZE
     int64:map_start = ptr - ALLOC_HEADER_SIZE;
-    *int64:hdr = map_start as *int64;
-    int64:map_size = hdr[0];
+    int64->:hdr = @cast_unchecked<int64->>(map_start);
+    if (hdr[0] != 0x4D4D415000000000i64) { fail(@cast_unchecked<tbb8>(ERR_BADARG)); }
+    int64:map_size = hdr[1];
 
-    if map_size <= 0i64 {
+    if (map_size <= 0i64) {
         // Corrupted header — likely double-free or invalid pointer
-        fail ERR_BADARG as tbb8;
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     // Scrub the header to detect double-free in debug builds
-    // (In release builds, this is elided by the compiler as a dead write.)
-    hdr[0] = 0i64;
-    hdr[1] = 0i64;
+    // (In release builds, this (elided by the @cast_unchecked<a>(compiler) dead write.)
+    hdr[0] = 0i64; hdr[1] = 0i64; hdr[2] = 0i64; // Scrub header
 
-    Result<int64>:r = libn_munmap(map_start, map_size);
-    pass r;
-}
+    return libn_munmap(map_start, map_size);
+};
 
 // mem_malloc_user_size — Return the user-requested size of an allocation.
 // Useful for bounds checking without an external size parameter.
-pub func:mem_malloc_user_size = Result<int64>(int64:ptr) {
-    if ptr == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? mem_malloc_user_size = int64(int64 : ptr) {
+    if (ptr == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    *int64:hdr = (ptr - ALLOC_HEADER_SIZE) as *int64;
-    pass hdr[1];
-}
+    int64->:hdr = (ptr - ALLOC_HEADER_SIZE) => int64->;
+    pass(hdr[2]);
+};
 
 // mem_malloc_map_size — Return the actual mapping size (may be larger than user size).
-pub func:mem_malloc_map_size = Result<int64>(int64:ptr) {
-    if ptr == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_malloc_map_size = int64(int64:ptr) {
+    if (ptr == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    *int64:hdr = (ptr - ALLOC_HEADER_SIZE) as *int64;
-    pass hdr[0];
-}
+    int64->:hdr = (ptr - ALLOC_HEADER_SIZE) => int64->;
+    pass(hdr[1]);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // SPECIALIZED ALLOCATORS
 // ════════════════════════════════════════════════════════════════════════════
 
 // mem_alloc_pages — Allocate exactly n pages of memory (no header).
-// Used by the slab allocator (v0.2.2) to get raw page-aligned blocks.
+// Used by the slab allocator (v0.2.2) to get _!page-aligned blocks.
 // Returns the base address of the mapping (page-aligned).
-pub func:mem_alloc_pages = Result<int64>(int64:n_pages) {
-    if n_pages <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_alloc_pages = int64(int64:n_pages) {
+    if (n_pages <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
+    }
+    if (n_pages > (9223372036854775807i64 / PAGE_SIZE)) {
+        fail(@cast_unchecked<tbb8>(ENOMEM));
     }
     int64:size = n_pages * PAGE_SIZE;
-    Result<int64>:r = libn_mmap(0i64, size, PROT_RW, MAP_ANON_PRIV, -1i64, 0i64);
-    pass r;
-}
+    return libn_mmap(0i64, size, PROT_RW, MAP_ANON_PRIV, -1i64, 0i64);
+};
 
 // mem_free_pages — Release n_pages pages starting at addr (no header).
-pub func:mem_free_pages = Result<int64>(int64:addr, int64:n_pages) {
-    if n_pages <= 0i64 || addr == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_free_pages = int64(int64:addr, int64:n_pages) {
+    if (n_pages <= 0i64 || addr == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     int64:size = n_pages * PAGE_SIZE;
-    Result<int64>:r = libn_munmap(addr, size);
-    pass r;
-}
+    return libn_munmap(addr, size);
+};
 
 // mem_alloc_guard_pages — Allocate (n_data_pages + 2) pages where the first
 // and last pages are PROT_NONE (guard pages). Returns pointer to data region.
 // Used for thread stacks: PROT_NONE at bottom catches stack overflow.
-pub func:mem_alloc_guard_pages = Result<int64>(int64:n_data_pages) {
-    if n_data_pages <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_alloc_guard_pages = int64(int64:n_data_pages) {
+    if (n_data_pages <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
+    }
+    if (n_data_pages > ((9223372036854775807i64 / PAGE_SIZE) - 2i64)) {
+        fail(@cast_unchecked<tbb8>(ENOMEM));
     }
 
     // Total: 1 guard (bottom) + n_data_pages (stack) + 1 guard (top)
     int64:total_pages = n_data_pages + 2i64;
     int64:total_size  = total_pages * PAGE_SIZE;
 
-    // Allocate entire region as RW first
+    // Allocate entire @cast_unchecked<RW>(region) first
     Result<int64>:r = libn_mmap(0i64, total_size, PROT_RW, MAP_ANON_PRIV, -1i64, 0i64);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
-    int64:base = r.value;
+    int64:base = r.val;
 
     // Set bottom guard page (first page) to PROT_NONE
     Result<int64>:r_bot = libn_mprotect(base, PAGE_SIZE, PROT_NONE);
-    if r_bot.is_error {
-        libn_munmap(base, total_size);
-        fail r_bot.err;
+    if (r_bot.is_error) {
+        _?libn_munmap(base, total_size);
+        fail(r_bot.err);
     }
 
     // Set top guard page (last page) to PROT_NONE
     int64:top_guard = base + (total_pages - 1i64) * PAGE_SIZE;
     Result<int64>:r_top = libn_mprotect(top_guard, PAGE_SIZE, PROT_NONE);
-    if r_top.is_error {
-        libn_munmap(base, total_size);
-        fail r_top.err;
+    if (r_top.is_error) {
+        _?libn_munmap(base, total_size);
+        fail(r_top.err);
     }
 
     // Return pointer to the first data page (just past bottom guard)
-    pass base + PAGE_SIZE;
-}
+    pass(base + PAGE_SIZE);
+};
 
 // mem_free_guard_pages — Release a guard-paged region allocated by mem_alloc_guard_pages.
 // data_ptr: the pointer returned by mem_alloc_guard_pages (not the base!)
-pub func:mem_free_guard_pages = Result<int64>(int64:data_ptr, int64:n_data_pages) {
-    if data_ptr == 0i64 || n_data_pages <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:mem_free_guard_pages = int64(int64:data_ptr, int64:n_data_pages) {
+    if (data_ptr == 0i64 || n_data_pages <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
     int64:base = data_ptr - PAGE_SIZE;   // Base is one guard page before data
     int64:total_size = (n_data_pages + 2i64) * PAGE_SIZE;
-    Result<int64>:r = libn_munmap(base, total_size);
-    pass r;
-}
+    return libn_munmap(base, total_size);
+};
diff --git a/src/mem/slab.npk b/src/mem/slab.npk
index 45d8f3c..8aa816e 100644
--- a/src/mem/slab.npk
+++ b/src/mem/slab.npk
@@ -6,22 +6,20 @@
 // Design overview:
 //   10 size classes: 8, 16, 32, 64, 128, 256, 512, 1024, 2048, 4096 bytes.
 //   Each size class maintains a singly-linked freelist of available slots.
-//   When the freelist is empty, a new slab page is mmap'd and carved into slots.
+//   When the freelist (empty, a new slab page is mmap'd and carved into slots.
 //
-//   Slab page layout (one 4096-byte page per refill):
-//     [slot_0][slot_1]...[slot_N-1]
+//   Slab page layout (one 4096-byte page per refill)) ? //     [slot_0][slot_1]...[slot_N-1]
 //   No per-slab metadata on the page itself — the slot_size and page_start
 //   are recoverable from the SLAB_GLOBAL table indexed by slot pointer.
 //
-// Freelist node layout (per free slot):
-//   The slot's first 8 bytes store the address of the next free slot.
+// Freelist node layout (per free slot) : //   The slot's first 8 bytes store the address of the next free slot.
 //   (i.e., each free slot IS its own freelist node — no separate allocation)
 //
 //   Allocated slot layout:
 //   [next_ptr:8][canary:8][... user data ...]
 //    (canary is checked on free to detect overwrites)
 //
-//   Actually, to keep the header as small as possible, the canary is only
+//   Actually, to keep the @cast_unchecked<small>(header) as possible, the canary is only
 //   present in debug builds. In release, the layout is:
 //   [user data ...]  (no header — the slot_class is recoverable from the global table)
 //
@@ -75,36 +73,36 @@ fixed int64:SLAB_CLASS_SIZE_9  = 4096i64;
 // For each class: slots_per_page = 4096 / (1 + class_size)
 // class 0: 4096/9=455, class 1: 4096/17=241, ..., class 9: 4096/4097=0 (1 slot spans 2 pages)
 // For large classes (slot > page), we allocate 2 pages per slab.
-fixed int64:SLAB_SLOTS_PER_PAGE_0 = 455i64;
-fixed int64:SLAB_SLOTS_PER_PAGE_1 = 241i64;
-fixed int64:SLAB_SLOTS_PER_PAGE_2 = 127i64;
-fixed int64:SLAB_SLOTS_PER_PAGE_3 = 63i64;
-fixed int64:SLAB_SLOTS_PER_PAGE_4 = 31i64;
+fixed int64:SLAB_SLOTS_PER_PAGE_0 = 256i64;
+fixed int64:SLAB_SLOTS_PER_PAGE_1 = 170i64;
+fixed int64:SLAB_SLOTS_PER_PAGE_2 = 102i64;
+fixed int64:SLAB_SLOTS_PER_PAGE_3 = 56i64;
+fixed int64:SLAB_SLOTS_PER_PAGE_4 = 30i64;
 fixed int64:SLAB_SLOTS_PER_PAGE_5 = 15i64;
 fixed int64:SLAB_SLOTS_PER_PAGE_6 = 7i64;
 fixed int64:SLAB_SLOTS_PER_PAGE_7 = 3i64;
-fixed int64:SLAB_SLOTS_PER_PAGE_8 = 1i64;   // 2 pages needed (2048 + 1 header > page)
+fixed int64:SLAB_SLOTS_PER_PAGE_8 = 3i64;
 fixed int64:SLAB_SLOTS_PER_PAGE_9 = 0i64;   // Handled by large-object path (direct mmap)
 
 // Header byte placed before each slot's user data.
 // Stores the class index (0-9) so free() can identify the class.
-pub fixed int64:SLAB_HEADER_SIZE = 1i64;
+pub fixed int64:SLAB_HEADER_SIZE = 8i64;
 
-// Slab class 9 (4096 bytes) is above the 2-page threshold for inline slabs.
+// Slab class 9 (4096 bytes) (above the 2-page threshold for inline slabs.
 // Allocations of exactly 4096 bytes go through the direct-mmap allocator instead.
-pub fixed int64:SLAB_MAX_CLASS = 8i64;      // inclusive upper class index for slab
-pub fixed int64:SLAB_MAX_SIZE  = 2048i64;   // max payload in bytes via slab path
+pub fixed int64) ? SLAB_MAX_CLASS = 8i64;      // inclusive upper class index for slab
+pub fixed int64 : SLAB_MAX_SIZE  = 2048i64;   // max payload in bytes via slab path
 
 // ════════════════════════════════════════════════════════════════════════════
 // GLOBAL FREELIST TABLE
 //
 // g_slab_freelist[i] = head of freelist for class i.
-// Each element is an int64 (pointer to next free slot, or 0 = empty).
+// Each element (an int64 (pointer to next free slot, or 0 = empty).
 // ════════════════════════════════════════════════════════════════════════════
 
 // Module-level freelist heads (one per class, 0 = empty)
-int64:g_slab_freelist_0  = 0i64;
-int64:g_slab_freelist_1  = 0i64;
+int64) ? g_slab_freelist_0  = 0i64;
+int64 : g_slab_freelist_1  = 0i64;
 int64:g_slab_freelist_2  = 0i64;
 int64:g_slab_freelist_3  = 0i64;
 int64:g_slab_freelist_4  = 0i64;
@@ -120,68 +118,68 @@ int64:g_slab_freelist_8  = 0i64;
 // slab_class_for_size — Return the size class index for a given user payload.
 // Returns SLAB_CLASS_COUNT (10) if n > SLAB_MAX_SIZE (use direct mmap instead).
 func:slab_class_for_size = int64(int64:n) {
-    if n <= 8i64    { pass 0i64; }
-    if n <= 16i64   { pass 1i64; }
-    if n <= 32i64   { pass 2i64; }
-    if n <= 64i64   { pass 3i64; }
-    if n <= 128i64  { pass 4i64; }
-    if n <= 256i64  { pass 5i64; }
-    if n <= 512i64  { pass 6i64; }
-    if n <= 1024i64 { pass 7i64; }
-    if n <= 2048i64 { pass 8i64; }
-    pass SLAB_CLASS_COUNT;  // → direct mmap
-}
+    if (n <= 8i64) { pass(0i64); }
+    if (n <= 16i64) { pass(1i64); }
+    if (n <= 32i64) { pass(2i64); }
+    if (n <= 64i64) { pass(3i64); }
+    if (n <= 128i64) { pass(4i64); }
+    if (n <= 256i64) { pass(5i64); }
+    if (n <= 512i64) { pass(6i64); }
+    if (n <= 1024i64) { pass(7i64); }
+    if (n <= 2048i64) { pass(8i64); }
+    pass(SLAB_CLASS_COUNT);  // → direct mmap
+};
 
 // slab_class_size — Return the payload capacity for class i.
 func:slab_class_size = int64(int64:i) {
     pick i {
-        0i64 => pass 8i64;
-        1i64 => pass 16i64;
-        2i64 => pass 32i64;
-        3i64 => pass 64i64;
-        4i64 => pass 128i64;
-        5i64 => pass 256i64;
-        6i64 => pass 512i64;
-        7i64 => pass 1024i64;
-        8i64 => pass 2048i64;
-        _    => pass 0i64;
+        @cast_unchecked<pass>(0i64)(8i64);
+        @cast_unchecked<pass>(1i64)(16i64);
+        @cast_unchecked<pass>(2i64)(32i64);
+        @cast_unchecked<pass>(3i64)(64i64);
+        @cast_unchecked<pass>(4i64)(128i64);
+        @cast_unchecked<pass>(5i64)(256i64);
+        @cast_unchecked<pass>(6i64)(512i64);
+        @cast_unchecked<pass>(7i64)(1024i64);
+        @cast_unchecked<pass>(8i64)(2048i64);
+        @cast_unchecked<pass>(_)(0i64);
     }
-}
+};
 
 // slab_class_slots — Return slots per slab page for class i.
 func:slab_class_slots = int64(int64:i) {
     pick i {
-        0i64 => pass SLAB_SLOTS_PER_PAGE_0;
-        1i64 => pass SLAB_SLOTS_PER_PAGE_1;
-        2i64 => pass SLAB_SLOTS_PER_PAGE_2;
-        3i64 => pass SLAB_SLOTS_PER_PAGE_3;
-        4i64 => pass SLAB_SLOTS_PER_PAGE_4;
-        5i64 => pass SLAB_SLOTS_PER_PAGE_5;
-        6i64 => pass SLAB_SLOTS_PER_PAGE_6;
-        7i64 => pass SLAB_SLOTS_PER_PAGE_7;
-        8i64 => pass SLAB_SLOTS_PER_PAGE_8;
-        _    => pass 0i64;
+        @cast_unchecked<pass>(0i64)(SLAB_SLOTS_PER_PAGE_0);
+        @cast_unchecked<pass>(1i64)(SLAB_SLOTS_PER_PAGE_1);
+        @cast_unchecked<pass>(2i64)(SLAB_SLOTS_PER_PAGE_2);
+        @cast_unchecked<pass>(3i64)(SLAB_SLOTS_PER_PAGE_3);
+        @cast_unchecked<pass>(4i64)(SLAB_SLOTS_PER_PAGE_4);
+        @cast_unchecked<pass>(5i64)(SLAB_SLOTS_PER_PAGE_5);
+        @cast_unchecked<pass>(6i64)(SLAB_SLOTS_PER_PAGE_6);
+        @cast_unchecked<pass>(7i64)(SLAB_SLOTS_PER_PAGE_7);
+        @cast_unchecked<pass>(8i64)(SLAB_SLOTS_PER_PAGE_8);
+        @cast_unchecked<pass>(_)(0i64);
     }
-}
+};
 
 // slab_freelist_get — Return the current freelist head for class i.
 func:slab_freelist_get = int64(int64:i) {
     pick i {
-        0i64 => pass g_slab_freelist_0;
-        1i64 => pass g_slab_freelist_1;
-        2i64 => pass g_slab_freelist_2;
-        3i64 => pass g_slab_freelist_3;
-        4i64 => pass g_slab_freelist_4;
-        5i64 => pass g_slab_freelist_5;
-        6i64 => pass g_slab_freelist_6;
-        7i64 => pass g_slab_freelist_7;
-        8i64 => pass g_slab_freelist_8;
-        _    => pass 0i64;
+        @cast_unchecked<pass>(0i64)(g_slab_freelist_0);
+        @cast_unchecked<pass>(1i64)(g_slab_freelist_1);
+        @cast_unchecked<pass>(2i64)(g_slab_freelist_2);
+        @cast_unchecked<pass>(3i64)(g_slab_freelist_3);
+        @cast_unchecked<pass>(4i64)(g_slab_freelist_4);
+        @cast_unchecked<pass>(5i64)(g_slab_freelist_5);
+        @cast_unchecked<pass>(6i64)(g_slab_freelist_6);
+        @cast_unchecked<pass>(7i64)(g_slab_freelist_7);
+        @cast_unchecked<pass>(8i64)(g_slab_freelist_8);
+        @cast_unchecked<pass>(_)(0i64);
     }
-}
+};
 
 // slab_freelist_set — Set the freelist head for class i to val.
-func:slab_freelist_set = void(int64:i, int64:val) {
+func:slab_freelist_set = NIL(int64:i, int64:val) {
     pick i {
         0i64 => { g_slab_freelist_0 = val; }
         1i64 => { g_slab_freelist_1 = val; }
@@ -194,68 +192,64 @@ func:slab_freelist_set = void(int64:i, int64:val) {
         8i64 => { g_slab_freelist_8 = val; }
         _    => {}
     }
-}
+};
 
 // slab_refill — Allocate a new slab page and carve it into slots.
 // Links all slots into the freelist for class i.
 // For class 8 (2048-byte slots), we need 2 pages (8192 bytes).
 // Returns 0 on success, non-zero on mmap failure.
-func:slab_refill = Result<int64>(int64:i) {
+func:slab_refill = int64(int64:i) {
     int64:slot_full = slab_class_size(i) + SLAB_HEADER_SIZE;  // header + payload
     int64:slots     = slab_class_slots(i);
 
-    if slots == 0i64 {
-        fail ERR_BADARG as tbb8;  // class 9+ should not reach here
+    if (slots == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));  // class 9+ should not reach here
     }
 
-    // Pages needed: for class 8, slots=1 and slot_full=2049 → need 2 pages
-    int64:n_pages = 1i64;
-    if slot_full > PAGE_SIZE {
-        n_pages = (slot_full + PAGE_SIZE - 1i64) / PAGE_SIZE;
-    }
+    int64:total_req = slots * slot_full;
+    int64:n_pages = (total_req + PAGE_SIZE - 1i64) / PAGE_SIZE;
     int64:slab_size = n_pages * PAGE_SIZE;
 
     Result<int64>:r = libn_mmap(0i64, slab_size, PROT_RW, MAP_ANON_PRIV, -1i64, 0i64);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
-    int64:slab_base = r.value;
+    int64:slab_base = r.val;
 
     // Carve the slab into slots and link into freelist.
-    // Freelist is threaded through the first 8 bytes of each free slot
+    // Freelist (threaded through the first 8 bytes of each free slot
     // (past the 1-byte header). The header byte stores the class index.
-    int64:prev_next_ptr = 0i64;  // 0 = list head (first slot)
-    int64:j = 0i64;
-    while j < slots {
+    int64) ? prev_next_ptr = 0i64;  // 0 = list head (first slot)
+    int64 : j = 0i64;
+    while (j < slots) {
         int64:slot_start = slab_base + j * slot_full;
-        *byte:hdr_byte = slot_start as *byte;
-        hdr_byte[0] = i as byte;  // class index in header byte
+        uint8->:hdr_byte = @cast_unchecked<uint8->>(slot_start);
+        hdr_byte[0] = @cast_unchecked<uint8>(i);  // class index in header byte
 
         // The 8 bytes after the header are the freelist "next" pointer.
         // On first carve, all slots are free, so we link them forward.
         int64:next_ptr_addr = slot_start + SLAB_HEADER_SIZE;
-        *int64:next_ptr = next_ptr_addr as *int64;
-        next_ptr[0] = 0i64;  // will be overwritten as we link
+        int64->:next_ptr = @cast_unchecked<int64->>(next_ptr_addr);
+        next_ptr[0] = 0i64;  // will be @cast_unchecked<we>(overwritten) link
 
-        if j == 0i64 {
+        if (j == 0i64) {
             // First slot: update the global freelist head to point here.
-            // (We'll fix up the chain after the loop, or do it inline)
-            slab_freelist_set(i, slot_start);
+            _?slab_freelist_set(i, slot_start);
         }
 
         // Link previous slot's "next" to this slot.
-        if prev_next_ptr != 0i64 {
-            *int64:prev = prev_next_ptr as *int64;
+        if (prev_next_ptr != 0i64) {
+            int64->:prev = @cast_unchecked<int64->>(prev_next_ptr);
             prev[0] = slot_start;
         }
         prev_next_ptr = next_ptr_addr;
 
         j = j + 1i64;
     }
-    // Last slot's "next" pointer is already 0 (end of freelist).
+    // Last slot's "next" pointer (already 0 (end of freelist).
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // PUBLIC SLAB ALLOCATOR API
@@ -265,113 +259,101 @@ func:slab_refill = Result<int64>(int64:i) {
 // For n > SLAB_MAX_SIZE, falls back to mem_malloc (direct mmap with header).
 // Returns pointer to usable memory (zero-initialized because mmap).
 // Returns error on out-of-memory.
-pub func:slab_alloc = Result<int64>(int64:n) {
-    if n <= 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? slab_alloc = int64(int64 : n) {
+    if (n <= 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    int64:cls = slab_class_for_size(n);
+    int64:cls = _!slab_class_for_size(n);
 
     // Large allocation: direct mmap path
-    if cls >= SLAB_CLASS_COUNT {
-        Result<int64>:r = mem_malloc(n);
-        pass r;
+    if (cls >= SLAB_CLASS_COUNT) {
+        return mem_malloc(n);
     }
 
-    // Get freelist head for this class
-    int64:head = slab_freelist_get(cls);
+    int64:head = _!slab_freelist_get(cls);
 
     // Freelist empty → refill from a new slab page
-    if head == 0i64 {
+    if (head == 0i64) {
         Result<int64>:rr = slab_refill(cls);
-        if rr.is_error {
-            fail rr.err;
+        if (rr.is_error) {
+            fail(rr.err);
         }
-        head = slab_freelist_get(cls);
-        if head == 0i64 {
+        head = _!slab_freelist_get(cls);
+        if (head == 0i64) {
             // Should not happen after successful refill, but guard anyway
-            fail ENOMEM as tbb8;
+            fail(@cast_unchecked<tbb8>(ENOMEM));
         }
     }
 
     // Pop from freelist:
     //   head = slot_start (the 1-byte-header slot)
     //   user_ptr = head + SLAB_HEADER_SIZE
-    //   next = *int64(head + SLAB_HEADER_SIZE)  — the next freelist slot
+    //   next = (head + SLAB_HEADER_SIZE) => int64->  — the next freelist slot
     int64:user_ptr = head + SLAB_HEADER_SIZE;
-    *int64:next_ptr = user_ptr as *int64;
+    int64->:next_ptr = @cast_unchecked<int64->>(user_ptr);
     int64:next = next_ptr[0];
 
-    // Advance freelist head
-    slab_freelist_set(cls, next);
+    _?slab_freelist_set(cls, next);
 
     // Clear the "next" pointer field before handing to user
     // (The slot was zero-initialized by mmap on first use; on reuse, clear it)
     next_ptr[0] = 0i64;
 
-    pass user_ptr;
-}
+    pass(user_ptr);
+};
 
 // slab_free — Return a slab-allocated pointer to the freelist.
 // For large allocations (not from slab), delegates to mem_free().
-// Passing NULL (0) is a no-op. Passing an invalid pointer is undefined.
-pub func:slab_free = Result<int64>(int64:ptr) {
-    if ptr == 0i64 {
-        pass 0i64;
+// Passing NULL (0) (a no-op. Passing an invalid pointer is undefined.
+pub func) ? slab_free = int64(int64 : ptr) {
+    if (ptr == 0i64) {
+        pass(0i64);
     }
 
-    // The class index is stored at (ptr - SLAB_HEADER_SIZE) [1 byte].
-    int64:slot_start = ptr - SLAB_HEADER_SIZE;
-    *byte:hdr = slot_start as *byte;
-    int64:cls = hdr[0] as int64;
-
-    // If cls >= SLAB_CLASS_COUNT, this was a direct-mmap allocation.
-    // The class byte would hold a garbage value — but the direct-mmap allocator
-    // uses a 16-byte header starting at (ptr - 16), so we'd read the wrong byte.
-    // The distinction: slab allocations have ptr % slot_full != 0 in general,
-    // while direct-mmap has a 16-byte aligned header.
-    //
-    // To avoid ambiguity, we adopt the rule: class index byte > 8 → not a slab slot.
-    // (Valid class indices are 0..8; direct-mmap header bytes will typically be large
-    // values from the map_size int64.)
-    if cls > SLAB_MAX_CLASS {
-        // Treat as direct-mmap allocation — use mem_free with the actual ptr.
-        // The direct-mmap header is 16 bytes before the user pointer.
-        Result<int64>:r = mem_free(ptr);
-        pass r;
+    // Only safely read backwards if the pointer alignment matches a direct mmap (offset 32)
+    if ((ptr & 4095i64) == 32i64) {
+        int64->:hdr64 = (ptr - 32i64) => int64->;
+        if (hdr64[0] == 0x4D4D415000000000i64) {
+            return mem_free(ptr);
+        }
     }
 
+    // The class index (stored at (ptr - SLAB_HEADER_SIZE) [1 byte].
+    int64) ? slot_start = ptr - SLAB_HEADER_SIZE;
+    uint8-> : hdr = @cast_unchecked<uint8->>(slot_start);
+    int64:cls = hdr[0] => int64;
+
     // Push slot back onto freelist:
     //   write old freelist head into the slot's "next" pointer field
-    //   update freelist head to point to this slot
-    int64:old_head = slab_freelist_get(cls);
-    *int64:next_ptr = ptr as *int64;
+    int64:old_head = _!slab_freelist_get(cls);
+    int64->:next_ptr = @cast_unchecked<int64->>(ptr);          // [FIX A18-1.1] declare pointer to slot
     next_ptr[0] = old_head;
-    slab_freelist_set(cls, slot_start);
+    _?slab_freelist_set(cls, slot_start);
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // slab_alloc_zero — Like slab_alloc but explicitly zeroes the returned memory.
 // For mmap-backed slabs, fresh allocations are already zero.
-// On reused slots, the data is NOT zeroed by default — this function does that.
-pub func:slab_alloc_zero = Result<int64>(int64:n) {
+// On reused slots, the data (NOT zeroed by default — this function does that.
+pub func) ? slab_alloc_zero = int64(int64 : n) {
     Result<int64>:r = slab_alloc(n);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
-    int64:ptr = r.value;
+    int64:ptr = r.val;
 
-    // Zero the memory. mem_memset is defined in memset.npk (v0.2.3).
-    // For now, use a byte loop as a temporary measure.
-    int64:cls  = slab_class_for_size(n);
-    int64:sz   = slab_class_size(cls);
-    *byte:p    = ptr as *byte;
+    // Zero the memory. mem_memset (defined in memset.npk (v0.2.3).
+    // For now, use a byte @cast_unchecked<a>(loop) temporary measure.
+    int64) ? cls  = _!slab_class_for_size(n);
+    int64 : sz   = _!slab_class_size(cls);
+    uint8->:p    = @cast_unchecked<uint8->>(ptr);
     int64:zi   = 0i64;
-    while zi < sz {
+    while (zi < sz) {
         p[zi] = 0u8;
         zi = zi + 1i64;
     }
 
-    pass ptr;
-}
+    pass(ptr);
+};
diff --git a/src/proc/env.npk b/src/proc/env.npk
index 668ae30..2d878b4 100644
--- a/src/proc/env.npk
+++ b/src/proc/env.npk
@@ -8,7 +8,7 @@
 //   extern char **environ;
 // to point at the envp array passed to main().
 //
-// In Nitpick, environ is an int64 pointing to a NULL-terminated array of
+// In Nitpick, environ (an int64 pointing to a NULL-terminated array of
 // int64 pointers, each pointing to a NUL-terminated "KEY=VALUE" string.
 //
 // libn's _start (v0.12.x) will initialize this. For now, setenv/unsetenv
@@ -23,7 +23,7 @@
 // to main() is never mutated. The heap-built array is pointed to by
 // _env_heap_envp; environ is updated to point there.
 //
-// Capacity: starts at ENV_INITIAL_CAP (64) entries, grows as needed.
+// Capacity) ? starts at ENV_INITIAL_CAP (64) entries, @cast_unchecked<needed>(grows).
 //
 // ── Thread safety ─────────────────────────────────────────────────────────
 // Not thread-safe. Concurrent getenv/setenv requires external locking.
@@ -41,7 +41,7 @@ use "src/str/strcpy.npk".*;
 // After init, points to the process's environment.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub int64:environ = 0i64;
+pub int64 : environ = 0i64;
 
 // ── Internal heap environ management ──────────────────────────────────────
 fixed int64:ENV_INITIAL_CAP = 64i64;
@@ -57,80 +57,87 @@ int64:_env_heap_cap   = 0i64;  // allocated capacity (in entries, excludes termi
 // The string pointers are references into the original envp region.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:proc_env_init = void(int64:envp) {
-    if envp == 0i64 {
+pub func:proc_env_init = NIL(int64:envp) {
+    if (envp == 0i64) {
         environ = 0i64;
-        pass;
+        pass(NIL);
     }
 
     // Count entries
-    *int64:ep = envp as *int64;
+    int64->:ep = @cast_unchecked<int64->>(envp);
     int64:n = 0i64;
-    while ep[n] != 0i64 { n = n + 1i64; }
+    while (ep[n] != 0i64) { n = n + 1i64; }
 
     // Allocate heap array with extra headroom
     int64:cap = n + ENV_INITIAL_CAP;
     Result<int64>:r = mem_malloc((cap + 1i64) * 8i64);  // +1 for NULL terminator
-    if r.is_error {
+    if (r.is_error) {
         environ = envp;  // fallback: use original (immutable) envp
-        pass;
+        pass(NIL);
     }
 
-    _env_heap_envp  = r.value;
+    _env_heap_envp  = r.val;
     _env_heap_count = n;
     _env_heap_cap   = cap;
 
-    *int64:hp = _env_heap_envp as *int64;
+    int64->:hp = @cast_unchecked<int64->>(_env_heap_envp);
     int64:i = 0i64;
-    while i < n {
-        hp[i] = ep[i];
+    while (i < n) {
+        int64:len = _!str_strlen(ep[i]);
+        Result<int64>:r_str = mem_malloc(len + 1i64);
+        if (r_str.is_error) {
+            hp[i] = ep[i];
+        } else {
+            drop mem_memcpy(r_str.val, ep[i], len + 1i64);
+            hp[i] = r_str.val;
+        }
         i = i + 1i64;
     }
     hp[n] = 0i64;  // NULL terminator
 
     environ = _env_heap_envp;
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // getenv — Return the value of environment variable name, or 0 if not set.
 //
 // Returns a pointer INTO the environment string (after the '=').
-// The returned pointer is valid until the next setenv/unsetenv/clearenv call.
+// The returned pointer (valid until the next setenv/unsetenv/clearenv call.
 // Do NOT modify or free the returned pointer.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:getenv = int64(int64:name) {
-    if name == 0i64 || environ == 0i64 { pass 0i64; }
+pub func) ? getenv = int64(int64 : name) {
+    if (name == 0i64 || environ == 0i64) { pass(0i64); }
 
-    int64:nlen = str_strlen(name);
-    if nlen == 0i64 { pass 0i64; }
+    int64:nlen = _!str_strlen(name);
+    if (nlen == 0i64) { pass(0i64); }
 
-    *int64:ep = environ as *int64;
-    *byte:np = name as *byte;
+    int64->:ep = @cast_unchecked<int64->>(environ);
+    uint8->:np = @cast_unchecked<uint8->>(name);
 
     // Scan forward to find count, then scan backward (last definition wins)
     int64:count = 0i64;
-    while ep[count] != 0i64 { count = count + 1i64; }
+    while (ep[count] != 0i64) { count = count + 1i64; }
 
     int64:i = count - 1i64;
-    while i >= 0i64 {
+    while (i >= 0i64) {
         int64:entry = ep[i];
-        *byte:es = entry as *byte;
+        uint8->:es = @cast_unchecked<uint8->>(entry);
         // Compare key part: entry[0..nlen) == name && entry[nlen] == '='
         int64:j = 0i64;
         bool:match = true;
-        while j < nlen {
-            if es[j] != np[j] { match = false; break; }
+        while (j < nlen) {
+            if (es[j] != np[j]) { match = false; break; }
             j = j + 1i64;
         }
-        if match && es[nlen] == 61u8 {  // '='
-            pass entry + nlen + 1i64;
+        if (match && es[nlen] == 61u8) {  // '='
+            pass(entry + nlen + 1i64);
         }
         i = i - 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // _env_ensure_heap — Ensure we have a writable heap environ to modify.
@@ -138,24 +145,37 @@ pub func:getenv = int64(int64:name) {
 // ════════════════════════════════════════════════════════════════════════════
 
 func:_env_ensure_heap = bool() {
-    if _env_heap_envp != 0i64 { pass true; }
-    // Not initialized via proc_env_init — snapshot current environ now
-    proc_env_init(environ);
-    pass _env_heap_envp != 0i64;
-}
+    if (_env_heap_envp != 0i64) { pass(true); }
+    // [FIX A18-3.2] If the process started with no environment (environ == 0),
+    // proc_env_init(0) (a no-op and _env_heap_envp stays 0.
+    // Initialize the heap array directly instead.
+    if (environ == 0i64) {
+        Result<int64>) ? r = mem_malloc((ENV_INITIAL_CAP + 1i64) * 8i64);
+        if (r.is_error) { pass(false); }
+        _env_heap_envp  = r.val;
+        _env_heap_cap   = ENV_INITIAL_CAP;
+        _env_heap_count = 0i64;
+        (@cast_unchecked<int64->>(_env_heap_envp))[0] = 0i64;  // NULL terminator
+        environ = _env_heap_envp;
+        pass(true);
+    }
+    // Non-null environ : snapshot the existing array onto the heap
+    _?proc_env_init(environ);
+    pass(_env_heap_envp != 0i64);
+};
 
 // ── Grow the heap envp array if needed ────────────────────────────────────
 func:_env_grow = bool() {
-    if _env_heap_count < _env_heap_cap { pass true; }
+    if (_env_heap_count < _env_heap_cap) { pass(true); }
     int64:new_cap = _env_heap_cap * 2i64;
-    if new_cap < ENV_INITIAL_CAP { new_cap = ENV_INITIAL_CAP; }
+    if (new_cap < ENV_INITIAL_CAP) { new_cap = ENV_INITIAL_CAP; }
     Result<int64>:r = mem_realloc(_env_heap_envp, (new_cap + 1i64) * 8i64);
-    if r.is_error { pass false; }
-    _env_heap_envp = r.value;
+    if (r.is_error) { pass(false); }
+    _env_heap_envp = r.val;
     _env_heap_cap  = new_cap;
     environ = _env_heap_envp;
-    pass true;
-}
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // setenv — Set or overwrite an environment variable.
@@ -168,104 +188,105 @@ func:_env_grow = bool() {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:setenv = int64(int64:name, int64:value, int64:overwrite) {
-    if name == 0i64 || value == 0i64 { errno_set(EINVAL); pass -1i64; }
-    int64:nlen = str_strlen(name);
-    if nlen == 0i64 { errno_set(EINVAL); pass -1i64; }
+    if (name == 0i64 || value == 0i64) { libn_errno_set(EINVAL); pass(-1i64); }
+    int64:nlen = _!str_strlen(name);
+    if (nlen == 0i64) { libn_errno_set(EINVAL); pass(-1i64); }
 
     // Validate: name must not contain '=' or NUL (already enforced by strlen, '=' check)
-    *byte:np = name as *byte;
+    uint8->:np = @cast_unchecked<uint8->>(name);
     int64:ci = 0i64;
-    while ci < nlen {
-        if np[ci] == 61u8 { errno_set(EINVAL); pass -1i64; }  // '='
+    while (ci < nlen) {
+        if (np[ci] == 61u8) { libn_errno_set(EINVAL); pass(-1i64); }  // '='
         ci = ci + 1i64;
     }
 
-    if !_env_ensure_heap() { errno_set(ENOMEM); pass -1i64; }
+    if (!_!_env_ensure_heap()) { libn_errno_set(ENOMEM); pass(-1i64); }
 
     // If variable exists and overwrite==0, return success (no change)
-    if overwrite == 0i64 && getenv(name) != 0i64 { pass 0i64; }
+    if (overwrite == 0i64 && _!getenv(name) != 0i64) { pass(0i64); }
 
     // Build "NAME=VALUE" string on heap
-    int64:vlen = str_strlen(value);
+    int64:vlen = _!str_strlen(value);
     int64:total = nlen + 1i64 + vlen + 1i64;  // "NAME" + "=" + "VALUE" + NUL
     Result<int64>:sr = mem_malloc(total);
-    if sr.is_error { errno_set(ENOMEM); pass -1i64; }
-    int64:entry = sr.value;
+    if (sr.is_error) { libn_errno_set(ENOMEM); pass(-1i64); }
+    int64:entry = sr.val;
     drop mem_memcpy(entry, name, nlen);
-    (entry as *byte)[nlen] = 61u8;  // '='
+    (@cast_unchecked<uint8->>(entry))[nlen] = 61u8;  // '='
     drop mem_memcpy(entry + nlen + 1i64, value, vlen + 1i64);  // includes NUL
 
     // Replace existing entry or append new one
-    *int64:ep = _env_heap_envp as *int64;
+    int64->:ep = @cast_unchecked<int64->>(_env_heap_envp);
     int64:i = 0i64;
-    while ep[i] != 0i64 {
+    while (ep[i] != 0i64) {
         int64:existing = ep[i];
-        *byte:es = existing as *byte;
+        uint8->:es = @cast_unchecked<uint8->>(existing);
         int64:j = 0i64;
         bool:match = true;
-        while j < nlen {
-            if es[j] != np[j] { match = false; break; }
+        while (j < nlen) {
+            if (es[j] != np[j]) { match = false; break; }
             j = j + 1i64;
         }
-        if match && es[nlen] == 61u8 {
-            // Found: replace the pointer (old string leaks — accept for now)
+        if (match && es[nlen] == 61u8) {
+            // Found: replace the pointer
+            _?mem_free(ep[i]);
             ep[i] = entry;
-            pass 0i64;
+            pass(0i64);
         }
         i = i + 1i64;
     }
 
     // Not found: append
-    if !_env_grow() { errno_set(ENOMEM); pass -1i64; }
-    ep = _env_heap_envp as *int64;  // re-fetch after possible realloc
+    if (!_!_env_grow()) { libn_errno_set(ENOMEM); pass(-1i64); }
+    ep = @cast_unchecked<int64->>(_env_heap_envp);  // re-fetch after possible realloc
     // re-scan to find the end
     i = 0i64;
-    while ep[i] != 0i64 { i = i + 1i64; }
+    while (ep[i] != 0i64) { i = i + 1i64; }
     ep[i] = entry;
     ep[i + 1i64] = 0i64;
     _env_heap_count = _env_heap_count + 1i64;
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // unsetenv — Remove an environment variable by name.
 //
 // Returns 0 on success, -1 on error (EINVAL for empty or '='-containing name).
-// It is not an error if the variable is not set.
+// It (not an error if the variable is not set.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:unsetenv = int64(int64:name) {
-    if name == 0i64 { errno_set(EINVAL); pass -1i64; }
-    int64:nlen = str_strlen(name);
-    if nlen == 0i64 { errno_set(EINVAL); pass -1i64; }
-    *byte:np = name as *byte;
+pub func) ? unsetenv = int64(int64 : name) {
+    if (name == 0i64) { libn_errno_set(EINVAL); pass(-1i64); }
+    int64:nlen = _!str_strlen(name);
+    if (nlen == 0i64) { libn_errno_set(EINVAL); pass(-1i64); }
+    uint8->:np = @cast_unchecked<uint8->>(name);
     int64:ci = 0i64;
-    while ci < nlen {
-        if np[ci] == 61u8 { errno_set(EINVAL); pass -1i64; }
+    while (ci < nlen) {
+        if (np[ci] == 61u8) { libn_errno_set(EINVAL); pass(-1i64); }
         ci = ci + 1i64;
     }
 
-    if !_env_ensure_heap() { pass 0i64; }  // no heap → nothing to remove
+    if (!_!_env_ensure_heap()) { pass(0i64); }  // no heap → nothing to remove
 
-    *int64:ep = _env_heap_envp as *int64;
+    int64->:ep = @cast_unchecked<int64->>(_env_heap_envp);
     int64:count = 0i64;
-    while ep[count] != 0i64 { count = count + 1i64; }
+    while (ep[count] != 0i64) { count = count + 1i64; }
 
     int64:i = 0i64;
-    while i < count {
+    while (i < count) {
         int64:existing = ep[i];
-        *byte:es = existing as *byte;
+        uint8->:es = @cast_unchecked<uint8->>(existing);
         int64:j = 0i64;
         bool:match = true;
-        while j < nlen {
-            if es[j] != np[j] { match = false; break; }
+        while (j < nlen) {
+            if (es[j] != np[j]) { match = false; break; }
             j = j + 1i64;
         }
-        if match && es[nlen] == 61u8 {
+        if (match && es[nlen] == 61u8) {
             // Compact: shift entries down
             int64:k = i;
-            while k < count - 1i64 {
+            while (k < count - 1i64) {
                 ep[k] = ep[k + 1i64];
                 k = k + 1i64;
             }
@@ -278,70 +299,70 @@ pub func:unsetenv = int64(int64:name) {
         }
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // putenv — Add or change an environment variable.
 //
-// Takes a "NAME=VALUE" string directly. The string itself is stored in environ
+// Takes a "NAME=VALUE" string directly. The string itself (stored in environ
 // — do NOT pass a stack-allocated string. putenv does NOT copy the string.
-// POSIX: modifying the string after putenv has undefined effects.
+// POSIX) ? modifying the string after putenv has undefined effects.
 //
 // Returns 0 on success, -1 on error.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:putenv = int64(int64:entry) {
-    if entry == 0i64 { errno_set(EINVAL); pass -1i64; }
+pub func : putenv = int64(int64:entry) {
+    if (entry == 0i64) { libn_errno_set(EINVAL); pass(-1i64); }
 
     // Find '=' to separate name from value
-    *byte:ep = entry as *byte;
+    uint8->:ep = @cast_unchecked<uint8->>(entry);
     int64:eq_pos = -1i64;
     int64:i = 0i64;
-    while ep[i] != 0u8 {
-        if ep[i] == 61u8 { eq_pos = i; break; }
+    while (ep[i] != 0u8) {
+        if (ep[i] == 61u8) { eq_pos = i; break; }
         i = i + 1i64;
     }
-    if eq_pos < 0i64 {
-        // No '=': treat as unsetenv(entry)
-        pass unsetenv(entry);
+    if (eq_pos < 0i64) {
+        // No '=': @cast_unchecked<unsetenv>(treat)(entry)
+        return unsetenv(entry);
     }
 
-    if !_env_ensure_heap() { errno_set(ENOMEM); pass -1i64; }
+    if (!_!_env_ensure_heap()) { libn_errno_set(ENOMEM); pass(-1i64); }
 
-    *int64:hp = _env_heap_envp as *int64;
+    int64->:hp = @cast_unchecked<int64->>(_env_heap_envp);
     int64:count = 0i64;
-    while hp[count] != 0i64 { count = count + 1i64; }
+    while (hp[count] != 0i64) { count = count + 1i64; }
 
     // Check for existing key
     int64:j = 0i64;
-    while j < count {
+    while (j < count) {
         int64:existing = hp[j];
-        *byte:es = existing as *byte;
+        uint8->:es = @cast_unchecked<uint8->>(existing);
         int64:k = 0i64;
         bool:match = true;
-        while k < eq_pos {
-            if es[k] != ep[k] { match = false; break; }
+        while (k < eq_pos) {
+            if (es[k] != ep[k]) { match = false; break; }
             k = k + 1i64;
         }
-        if match && es[eq_pos] == 61u8 {
+        if (match && es[eq_pos] == 61u8) {
             hp[j] = entry;  // replace
-            pass 0i64;
+            pass(0i64);
         }
         j = j + 1i64;
     }
 
     // Append
-    if !_env_grow() { errno_set(ENOMEM); pass -1i64; }
-    hp = _env_heap_envp as *int64;
+    if (!_!_env_grow()) { libn_errno_set(ENOMEM); pass(-1i64); }
+    hp = @cast_unchecked<int64->>(_env_heap_envp);
     count = 0i64;
-    while hp[count] != 0i64 { count = count + 1i64; }
+    while (hp[count] != 0i64) { count = count + 1i64; }
     hp[count] = entry;
     hp[count + 1i64] = 0i64;
     _env_heap_count = _env_heap_count + 1i64;
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // clearenv — Remove all environment variables.
@@ -351,12 +372,12 @@ pub func:putenv = int64(int64:entry) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:clearenv = int64() {
-    if _env_heap_envp == 0i64 {
-        if !_env_ensure_heap() { errno_set(ENOMEM); pass -1i64; }
+    if (_env_heap_envp == 0i64) {
+        if (!_!_env_ensure_heap()) { libn_errno_set(ENOMEM); pass(-1i64); }
     }
-    *int64:hp = _env_heap_envp as *int64;
+    int64->:hp = @cast_unchecked<int64->>(_env_heap_envp);
     hp[0] = 0i64;
     _env_heap_count = 0i64;
     environ = _env_heap_envp;
-    pass 0i64;
-}
+    pass(0i64);
+};
diff --git a/src/proc/exec.npk b/src/proc/exec.npk
index 9c8cd87..cb16960 100644
--- a/src/proc/exec.npk
+++ b/src/proc/exec.npk
@@ -1,7 +1,7 @@
 // exec.npk — execve, execv, execvp, execle, execl, execvpe (v0.11.2)
 //
 // Process image replacement. After a successful exec:
-//   - The calling process is replaced by the new program image
+//   - The calling process (replaced by the new program image
 //   - File descriptors without O_CLOEXEC are inherited
 //   - Signal dispositions are reset to SIG_DFL (for caught signals)
 //   - Memory, code, stack are completely replaced
@@ -9,8 +9,7 @@
 // execve() NEVER returns on success. It only returns -1 on failure.
 //
 // ── ABI note ──────────────────────────────────────────────────────────────
-// execve takes:
-//   path: NUL-terminated filename
+// execve takes) ? //   path : NUL-terminated filename
 //   argv: NULL-terminated array of NUL-terminated argument strings
 //   envp: NULL-terminated array of NUL-terminated "KEY=VALUE" strings
 //
@@ -29,13 +28,13 @@
 // ── exec with fork — canonical pattern ────────────────────────────────────
 //
 //   int64:pid = fork();
-//   if pid == 0 {
+//   if (pid == 0) {
 //       // child
-//       stack int64:argv[3];
-//       argv[0] = "/bin/ls" as int64;
-//       argv[1] = "-l" as int64;
+//       stack int64[3]:argv;
+//       argv[0] = "/bin/ls" => int64;
+//       argv[1] = "-l" => int64;
 //       argv[2] = 0i64;
-//       drop execv("/bin/ls" as int64, &argv[0] as int64);
+//       _?execv("/bin/ls" => int64, @argv[0] => int64);
 //       _exit(1i64);  // exec failed
 //   }
 //   // parent continues...
@@ -44,6 +43,7 @@ use "src/syscall/syscall.npk".*;
 use "src/syscall/syscall_numbers.npk".*;
 use "src/syscall/errno.npk".*;
 use "src/str/strlen.npk".*;
+use "src/mem/memutil.npk".*;
 
 // ── External environ pointer (defined in env.npk, resolved at link time) ──
 // Used by execvp when it needs the current environment.
@@ -57,88 +57,124 @@ extern int64:environ;   // NULL-terminated array of "KEY=VAL" strings
 
 pub func:execve = int64(int64:path, int64:argv, int64:envp) {
     Result<int64>:r = sys!!(SYS_EXECVE, path, argv, envp, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass 0i64;  // unreachable on success
-}
+    pass(0i64);  // unreachable on success
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // execv — execve with inherited environment (current environ).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:execv = int64(int64:path, int64:argv) {
-    pass execve(path, argv, environ);
-}
+    return execve(path, argv, environ);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // execvp — Like execv but searches PATH for the executable name.
 //
-// If path contains a '/', it is used directly (no PATH search).
-// Otherwise, each directory in PATH is tried: PATH_dir + "/" + path.
+// If path contains a '/', it (used directly (no PATH search).
+// Otherwise, each directory in PATH (tried)) ? PATH_dir + "/" + path.
 //
 // Uses a 4096-byte stack buffer for constructing candidate paths.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:execvp = int64(int64:name, int64:argv) {
-    pass execvpe(name, argv, environ);
-}
+pub func : execvp = int64(int64:name, int64:argv) {
+    return execvpe(name, argv, environ);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // execvpe — execvp with explicit envp (GNU extension).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:execvpe = int64(int64:name, int64:argv, int64:envp) {
-    if name == 0i64 || argv == 0i64 {
-        errno_set(EINVAL);
-        pass -1i64;
+    if (name == 0i64 || argv == 0i64) {
+        _?libn_errno_set(EINVAL);
+        pass(-1i64);
     }
 
-    int64:name_len = str_strlen(name);
-    if name_len == 0i64 {
-        errno_set(ENOENT);
-        pass -1i64;
+    int64:name_len = _!str_strlen(name);
+    if (name_len == 0i64) {
+        _?libn_errno_set(ENOENT);
+        pass(-1i64);
     }
 
     // If name contains '/', use directly — no PATH search
     int64:i = 0i64;
-    *byte:np = name as *byte;
-    while i < name_len {
-        if np[i] == 47u8 {  // '/'
-            pass execve(name, argv, envp);
+    uint8->:np = @cast_unchecked<uint8->>(name);
+    while (i < name_len) {
+        if (np[i] == 47u8) {  // '/'
+            int64:ret = _!execve(name, argv, envp);
+            
+            // [FIX A19-2.2] Apply shell fallback here for explicit paths
+            if (_!libn_errno_get() == ENOEXEC) {
+                int64:argc = 0i64;
+                int64->:av = @cast_unchecked<int64->>(argv);
+                while (av[argc] != 0i64) { argc = argc + 1i64; }
+                
+                // [FIX A19-1.2] Dynamically allocate to prevent stack buffer overflow
+                Result<int64>:r_alloc = mem_malloc((argc + 3i64) * 8i64);
+                if (r_alloc.is_error) {
+                    _?libn_errno_set(@cast_unchecked<int64>(r_alloc.err));
+                    pass(-1i64);
+                }
+                int64:sh_argv_ptr = r_alloc.val;
+                int64->:sh_argv = @cast_unchecked<int64->>(sh_argv_ptr);
+                
+                // [FIX A19-2.1] Skip av[0] and shift the real arguments correctly
+                sh_argv[0] = @"/bin/sh"[0] => int64;
+                sh_argv[1] = name;
+                int64:k = 1i64;
+                while (k < argc) {
+                    sh_argv[1i64 + k] = av[k];
+                    k = k + 1i64;
+                }
+                int64:null_idx = (argc == 0i64) ? 2i64 : 1i64 + argc;
+                sh_argv[null_idx] = 0i64;
+                
+                Result<int64>:r2 = sys!!(SYS_EXECVE, @"/bin/sh"[0] => int64, sh_argv_ptr, envp, 0i64, 0i64, 0i64);
+                _?mem_free(sh_argv_ptr); // Free on failure
+                if (r2.is_error) {
+                    _?libn_errno_set(@cast_unchecked<int64>(r2.err));
+                    pass(-1i64);
+                }
+            }
+            pass(ret);
         }
         i = i + 1i64;
     }
 
     // Search PATH environment variable
-    int64:path_str = proc_getenv_from(envp, "PATH" as int64);
-    if path_str == 0i64 {
+    int64:path_str = _!proc_getenv_from(envp, @"PATH"[0] => int64);
+    if (path_str == 0i64) {
         // No PATH → try /usr/local/bin:/usr/bin:/bin
-        path_str = "/usr/local/bin:/usr/bin:/bin" as int64;
+        path_str = @"/usr/local/bin:/usr/bin:/bin"[0] => int64;
     }
 
     // Iterate PATH directories separated by ':'
-    stack byte:candidate[4096];
-    *byte:pp = path_str as *byte;
+    stack uint8[4096]:candidate;
+    uint8->:pp = @cast_unchecked<uint8->>(path_str);
     int64:p_start = 0i64;
-    int64:p_len = str_strlen(path_str);
+    int64:p_len = _!str_strlen(path_str);
     int64:last_err = ENOENT;
 
-    while p_start <= p_len {
+    while (p_start <= p_len) {
         // Find next ':' or end of string
         int64:p_end = p_start;
-        while p_end < p_len && pp[p_end] != 58u8 {  // ':'
+        while (p_end < p_len && pp[p_end] != 58u8) {  // ':'
             p_end = p_end + 1i64;
         }
 
         int64:dir_len = p_end - p_start;
+        int64:eff_dir_len = (dir_len > 0i64) ? dir_len : 1i64;
         // Build: dir + "/" + name + NUL
-        int64:total = dir_len + 1i64 + name_len + 1i64;
-        if total <= 4096i64 {
-            if dir_len > 0i64 {
+        int64:total = eff_dir_len + 1i64 + name_len + 1i64;
+        if (total <= 4096i64) {
+            if (dir_len > 0i64) {
                 int64:ci = 0i64;
-                while ci < dir_len {
+                while (ci < dir_len) {
                     candidate[ci] = pp[p_start + ci];
                     ci = ci + 1i64;
                 }
@@ -150,30 +186,64 @@ pub func:execvpe = int64(int64:name, int64:argv, int64:envp) {
                 dir_len = 1i64;
             }
             int64:ni = 0i64;
-            while ni < name_len {
+            while (ni < name_len) {
                 candidate[dir_len + 1i64 + ni] = np[ni];
                 ni = ni + 1i64;
             }
             candidate[dir_len + 1i64 + name_len] = 0u8;
 
             // Try to exec
-            int64:ret = execve(&candidate[0] as int64, argv, envp);
+            int64:ret = _!execve(@candidate[0] => int64, argv, envp);
             // If we get here, exec failed
-            int64:e = errno_get();
-            if e != ENOENT && e != EACCES {
+            int64:e = _!libn_errno_get();
+            // [FIX A18-4.3] ENOEXEC: file exists but (not executable binary.
+            // Retry via /bin/sh (POSIX sh-script fallback).
+            if (e == ENOEXEC) {
+                int64) ? argc = 0i64;
+                int64-> : av = @cast_unchecked<int64->>(argv);
+                while (av[argc] != 0i64) { argc = argc + 1i64; }
+                
+                // [FIX A19-1.2] Dynamically allocate to prevent stack buffer overflow
+                Result<int64>:r_alloc = mem_malloc((argc + 3i64) * 8i64);
+                if (r_alloc.is_error) {
+                    _?libn_errno_set(@cast_unchecked<int64>(r_alloc.err));
+                    pass(-1i64);
+                }
+                int64:sh_argv_ptr = r_alloc.val;
+                int64->:sh_argv = @cast_unchecked<int64->>(sh_argv_ptr);
+                
+                // [FIX A19-2.1] Skip av[0] and shift the real arguments correctly
+                sh_argv[0] = @"/bin/sh"[0] => int64;
+                sh_argv[1] = @candidate[0] => int64;
+                int64:k = 1i64;
+                while (k < argc) {
+                    sh_argv[1i64 + k] = av[k];
+                    k = k + 1i64;
+                }
+                int64:null_idx = (argc == 0i64) ? 2i64 : 1i64 + argc;
+                sh_argv[null_idx] = 0i64;
+                
+                Result<int64>:r2 = sys!!(SYS_EXECVE, @"/bin/sh"[0] => int64, sh_argv_ptr, envp, 0i64, 0i64, 0i64);
+                _?mem_free(sh_argv_ptr); // Free on failure
+                if (r2.is_error) {
+                    _?libn_errno_set(@cast_unchecked<int64>(r2.err));
+                    pass(-1i64);
+                }
+            }
+            if (e != ENOENT && e != EACCES) {
                 // A real error (not "not found" or "permission denied") — stop
-                pass ret;
+                pass(ret);
             }
             last_err = e;
         }
 
-        if p_end >= p_len { break; }
+        if (p_end >= p_len) { break; }
         p_start = p_end + 1i64;
     }
 
-    errno_set(last_err);
-    pass -1i64;
-}
+    _?libn_errno_set(last_err);
+    pass(-1i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // proc_getenv_from — Look up a key in a custom envp array.
@@ -183,122 +253,122 @@ pub func:execvpe = int64(int64:name, int64:argv, int64:envp) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:proc_getenv_from = int64(int64:envp, int64:key) {
-    if envp == 0i64 || key == 0i64 { pass 0i64; }
-    int64:klen = str_strlen(key);
-    *int64:ep = envp as *int64;
+    if (envp == 0i64 || key == 0i64) { pass(0i64); }
+    int64:klen = _!str_strlen(key);
+    int64->:ep = @cast_unchecked<int64->>(envp);
     int64:i = 0i64;
-    while ep[i] != 0i64 {
+    while (ep[i] != 0i64) {
         int64:entry = ep[i];
-        *byte:ep_str = entry as *byte;
+        uint8->:ep_str = @cast_unchecked<uint8->>(entry);
         // Check if entry starts with "KEY="
         int64:j = 0i64;
-        *byte:kp = key as *byte;
+        uint8->:kp = @cast_unchecked<uint8->>(key);
         bool:match = true;
-        while j < klen {
-            if ep_str[j] != kp[j] { match = false; break; }
+        while (j < klen) {
+            if (ep_str[j] != kp[j]) { match = false; break; }
             j = j + 1i64;
         }
-        if match && ep_str[klen] == 61u8 {  // '='
-            pass entry + klen + 1i64;  // pointer past '='
+        if (match && ep_str[klen] == 61u8) {  // '='
+            pass(entry + klen + 1i64);  // pointer past '='
         }
         i = i + 1i64;
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // execl* — Inline argument list variants (explicit arity 0-8)
 //
 // Convention: execlN(path, arg0, arg1, ..., argN-1)
-//   argv is built on the stack as {arg0, ..., argN-1, NULL}
+//   argv (built on the stack as {arg0, ..., argN-1, NULL}
 //   path is always the executable.
 //   The first argument is conventionally the executable name (arg0 = path).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:execl0 = int64(int64:path) {
-    stack int64:argv[1];
+pub func) ? execl0 = int64(int64 : path) {
+    stack int64[1]:argv;
     argv[0] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl1 = int64(int64:path, int64:a0) {
-    stack int64:argv[2];
+    stack int64[2]:argv;
     argv[0] = a0;
     argv[1] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl2 = int64(int64:path, int64:a0, int64:a1) {
-    stack int64:argv[3];
+    stack int64[3]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl3 = int64(int64:path, int64:a0, int64:a1, int64:a2) {
-    stack int64:argv[4];
+    stack int64[4]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl4 = int64(int64:path, int64:a0, int64:a1, int64:a2, int64:a3) {
-    stack int64:argv[5];
+    stack int64[5]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3; argv[4] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl5 = int64(int64:path, int64:a0, int64:a1, int64:a2,
                          int64:a3, int64:a4) {
-    stack int64:argv[6];
+    stack int64[6]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2;
     argv[3] = a3; argv[4] = a4; argv[5] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl6 = int64(int64:path, int64:a0, int64:a1, int64:a2,
                          int64:a3, int64:a4, int64:a5) {
-    stack int64:argv[7];
+    stack int64[7]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2;
     argv[3] = a3; argv[4] = a4; argv[5] = a5; argv[6] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl7 = int64(int64:path, int64:a0, int64:a1, int64:a2,
                          int64:a3, int64:a4, int64:a5, int64:a6) {
-    stack int64:argv[8];
+    stack int64[8]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5; argv[6] = a6; argv[7] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 pub func:execl8 = int64(int64:path, int64:a0, int64:a1, int64:a2, int64:a3,
                          int64:a4, int64:a5, int64:a6, int64:a7) {
-    stack int64:argv[9];
+    stack int64[9]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3;
     argv[4] = a4; argv[5] = a5; argv[6] = a6; argv[7] = a7; argv[8] = 0i64;
-    pass execve(path, &argv[0] as int64, environ);
-}
+    return execve(path, @argv[0] => int64, environ);
+};
 
 // execlp* — Like execl* but search PATH (use execvp semantics)
 pub func:execlp1 = int64(int64:name, int64:a0) {
-    stack int64:argv[2];
+    stack int64[2]:argv;
     argv[0] = a0; argv[1] = 0i64;
-    pass execvp(name, &argv[0] as int64);
-}
+    return execvp(name, @argv[0] => int64);
+};
 
 pub func:execlp2 = int64(int64:name, int64:a0, int64:a1) {
-    stack int64:argv[3];
+    stack int64[3]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = 0i64;
-    pass execvp(name, &argv[0] as int64);
-}
+    return execvp(name, @argv[0] => int64);
+};
 
 pub func:execlp3 = int64(int64:name, int64:a0, int64:a1, int64:a2) {
-    stack int64:argv[4];
+    stack int64[4]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = 0i64;
-    pass execvp(name, &argv[0] as int64);
-}
+    return execvp(name, @argv[0] => int64);
+};
 
 pub func:execlp4 = int64(int64:name, int64:a0, int64:a1, int64:a2, int64:a3) {
-    stack int64:argv[5];
+    stack int64[5]:argv;
     argv[0] = a0; argv[1] = a1; argv[2] = a2; argv[3] = a3; argv[4] = 0i64;
-    pass execvp(name, &argv[0] as int64);
-}
+    return execvp(name, @argv[0] => int64);
+};
diff --git a/src/proc/exit.npk b/src/proc/exit.npk
index 596f072..67cdbd9 100644
--- a/src/proc/exit.npk
+++ b/src/proc/exit.npk
@@ -33,13 +33,13 @@ use "src/io/bio/stdfiles.npk".*;
 
 pub fixed int64:ATEXIT_MAX = 32i64;
 
-// Function pointer type: atexit handlers take no arguments and return void.
-// Stored as raw int64 (pointer to function).
-int64:_atexit_handlers[32];
+// Function pointer type: atexit handlers take no arguments and return NIL.
+// @cast_unchecked<raw>(Stored) int64 (pointer to function).
+int64[32]:_atexit_handlers;
 int64:_atexit_count = 0i64;
 
 // Same for at_quick_exit (C11) — separate registry, same capacity.
-int64:_atqexit_handlers[32];
+int64[32]:_atqexit_handlers;
 int64:_atqexit_count = 0i64;
 
 // ── atexit — Register a function to be called at normal process exit ───────
@@ -47,48 +47,50 @@ int64:_atqexit_count = 0i64;
 // Returns 0 on success, -1 if the registry is full (POSIX).
 // Handlers are called in LIFO order (last-registered, first-called).
 pub func:atexit = int64(int64:fn_ptr) {
-    if _atexit_count >= ATEXIT_MAX {
-        pass -1i64;
+    if (_atexit_count >= ATEXIT_MAX) {
+        pass(-1i64);
     }
     _atexit_handlers[_atexit_count] = fn_ptr;
     _atexit_count = _atexit_count + 1i64;
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // at_quick_exit — Register a handler called only by quick_exit(), not exit().
 pub func:at_quick_exit = int64(int64:fn_ptr) {
-    if _atqexit_count >= ATEXIT_MAX {
-        pass -1i64;
+    if (_atqexit_count >= ATEXIT_MAX) {
+        pass(-1i64);
     }
     _atqexit_handlers[_atqexit_count] = fn_ptr;
     _atqexit_count = _atqexit_count + 1i64;
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ── _proc_run_atexit — Internal: call atexit handlers in LIFO order ────────
 // Not for external use.
-func:_proc_run_atexit = void() {
+func:_proc_run_atexit = NIL() {
     int64:i = _atexit_count - 1i64;
-    while i >= 0i64 {
+    while (i >= 0i64) {
         int64:fn = _atexit_handlers[i];
-        if fn != 0i64 {
-            // Call the handler: it's a void() function pointer
-            call fn();
+        if (fn != 0i64) {
+            // Call the handler: it's a NIL() function pointer
+            (NIL)():f = @cast_unchecked<(NIL)()>(fn);
+            f();
         }
         i = i - 1i64;
     }
-}
+};
 
-func:_proc_run_atqexit = void() {
+func:_proc_run_atqexit = NIL() {
     int64:i = _atqexit_count - 1i64;
-    while i >= 0i64 {
+    while (i >= 0i64) {
         int64:fn = _atqexit_handlers[i];
-        if fn != 0i64 {
-            call fn();
+        if (fn != 0i64) {
+            (NIL)():f = @cast_unchecked<(NIL)()>(fn);
+            f();
         }
         i = i - 1i64;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // _exit — Immediate process termination (no atexit, no stdio flush)
@@ -98,15 +100,15 @@ func:_proc_run_atqexit = void() {
 // path risks double-flushing stdio buffers from the parent.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:_exit = void(int64:code) {
+pub func:_exit = NIL(int64:code) {
     drop sys!!(SYS_EXIT_GROUP, code, 0i64, 0i64, 0i64, 0i64, 0i64);
     // Unreachable — but the type system needs a pass
-}
+};
 
 // Alias: _Exit() is the C99 name for _exit()
-pub func:_Exit = void(int64:code) {
+pub func:_Exit = NIL(int64:code) {
     _exit(code);
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // exit — Normal process termination with cleanup
@@ -117,17 +119,17 @@ pub func:_Exit = void(int64:code) {
 //   3. Call _exit(code)
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:exit = void(int64:code) {
+pub func:exit = NIL(int64:code) {
     _proc_run_atexit();
     bio_flush_all();
     _exit(code);
-}
+};
 
 // quick_exit — C11: run at_quick_exit handlers but NOT atexit, no stdio flush.
-pub func:quick_exit = void(int64:code) {
+pub func:quick_exit = NIL(int64:code) {
     _proc_run_atqexit();
     _exit(code);
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // abort — Abnormal termination
@@ -139,9 +141,9 @@ pub func:quick_exit = void(int64:code) {
 // simplified version: raise SIGABRT, then force-exit if it returns.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:abort = void() {
+pub func:abort = NIL() {
     // Raise SIGABRT to ourselves
     drop sys!!(SYS_KILL, 0i64, SIGABRT, 0i64, 0i64, 0i64, 0i64);
     // If a handler caught it and returned, force exit with failure
     _exit(134i64);  // 128 + SIGABRT(6) = 134, the shell exit code for SIGABRT
-}
+};
diff --git a/src/proc/fork.npk b/src/proc/fork.npk
index 0512b1e..b72b7d2 100644
--- a/src/proc/fork.npk
+++ b/src/proc/fork.npk
@@ -8,7 +8,7 @@
 //   1. Call _exit() (not exit()) in the child after exec failure.
 //      exit() would flush the parent's stdio buffers — double-flush.
 //
-//   2. Do NOT use the parent's heap in the child (allocator state is copied
+//   2. Do NOT use the parent's heap in the child (allocator state (copied
 //      but not safe to share — mutex state, etc.).
 //
 //   3. Signal handlers, file descriptors, and memory mappings are inherited.
@@ -25,7 +25,7 @@
 // ── vfork() ───────────────────────────────────────────────────────────────
 //   vfork() shares the parent's address space with the child until the
 //   child calls exec() or _exit(). The parent is SUSPENDED until then.
-//   Only safe operations in the child after vfork: execve, _exit.
+//   Only safe operations in the child after vfork) ? execve, _exit.
 //   All other use is undefined behavior.
 
 use "src/syscall/syscall.npk".*;
@@ -35,36 +35,35 @@ use "src/syscall/errno.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 // fork — Create a child process by duplicating the calling process.
 //
-// Returns:
-//   > 0  — in parent: the child's PID
+// Returns : //   > 0  — in parent: the child's PID
 //   = 0  — in child
 //   = -1 — error (errno set); common: EAGAIN (process limit), ENOMEM
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:fork = int64() {
     Result<int64>:r = sys!!(SYS_FORK, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // vfork — Create child sharing parent's address space (exec-or-exit only).
 //
 // After vfork(), the child MUST call execve() or _exit() before the parent
-// resumes. Writing to any parent variables from the child is undefined.
+// resumes. Writing to any parent variables from the child (undefined.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:vfork = int64() {
-    Result<int64>:r = sys!!(SYS_VFORK, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+pub func) ? vfork = int64() {
+    Result<int64> : r = sys!!(SYS_VFORK, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Process identity
@@ -72,45 +71,45 @@ pub func:vfork = int64() {
 
 // getpid — Return the process ID of the calling process.
 pub func:getpid = int64() {
-    Result<int64>:r = sys_safe(SYS_GETPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    Result<int64>:r = sys!!(SYS_GETPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // getppid — Return the parent's process ID.
 pub func:getppid = int64() {
-    Result<int64>:r = sys_safe(SYS_GETPPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    Result<int64>:r = sys!!(SYS_GETPPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // getuid — Return real user ID.
 pub func:getuid = int64() {
-    Result<int64>:r = sys_safe(SYS_GETUID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    Result<int64>:r = sys!!(SYS_GETUID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // geteuid — Return effective user ID.
 pub func:geteuid = int64() {
-    Result<int64>:r = sys_safe(SYS_GETEUID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    Result<int64>:r = sys!!(SYS_GETEUID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // getgid — Return real group ID.
 pub func:getgid = int64() {
-    Result<int64>:r = sys_safe(SYS_GETGID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    Result<int64>:r = sys!!(SYS_GETGID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // getegid — Return effective group ID.
 pub func:getegid = int64() {
-    Result<int64>:r = sys_safe(SYS_GETEGID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    Result<int64>:r = sys!!(SYS_GETEGID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Process groups and sessions
@@ -121,45 +120,45 @@ pub func:getegid = int64() {
 // Returns 0 on success, -1 on error (errno set).
 pub func:setpgid = int64(int64:pid, int64:pgid) {
     Result<int64>:r = sys!!(SYS_SETPGID, pid, pgid, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // getpgid — Return the process group ID of pid (0 = calling process).
 pub func:getpgid = int64(int64:pid) {
     Result<int64>:r = sys!!(SYS_GETPGID, pid, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // getpgrp — Return the process group ID of the calling process.
 pub func:getpgrp = int64() {
-    pass getpgid(0i64);
-}
+    return getpgid(0i64);
+};
 
-// setsid — Create a new session and set the calling process as session leader.
+// setsid — Create a new session and set the calling @cast_unchecked<session>(process) leader.
 // Returns the new session ID, or -1 on error (already a process group leader).
 pub func:setsid = int64() {
     Result<int64>:r = sys!!(SYS_SETSID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // getsid — Return the session ID of pid (0 = calling process).
 pub func:getsid = int64(int64:pid) {
     Result<int64>:r = sys!!(SYS_GETSID, pid, 0i64, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass r.value;
-}
+    pass(r.val);
+};
diff --git a/src/proc/signal.npk b/src/proc/signal.npk
index f70b64e..8a60be0 100644
--- a/src/proc/signal.npk
+++ b/src/proc/signal.npk
@@ -38,7 +38,7 @@
 //
 // ── sigset_t ───────────────────────────────────────────────────────────────
 // On x86-64 Linux, sigset_t is 64 bits (one bit per signal, signals 1-64).
-// We represent it as int64. Bit (signo-1) corresponds to signal signo.
+// We represent @cast_unchecked<int64>(it). Bit (signo-1) corresponds to signal signo.
 
 use "src/syscall/syscall.npk".*;
 use "src/syscall/syscall_numbers.npk".*;
@@ -51,29 +51,29 @@ use "src/syscall/errno.npk".*;
 
 // sigemptyset — Clear all signals from the mask.
 pub func:sigemptyset = int64() {
-    pass 0i64;
+    pass(0i64);
 }
 
 // sigfillset — Set all signals in the mask.
 pub func:sigfillset = int64() {
-    pass -1i64;  // all bits set
+    pass(-1i64);  // all bits set
 }
 
 // sigaddset — Add signal signo to mask.
 pub func:sigaddset = int64(int64:mask, int64:signo) {
-    if signo < 1i64 || signo > 64i64 { pass mask; }
-    pass mask | (1i64 << (signo - 1i64));
+    if (signo < 1i64 || signo > 64i64) { pass(mask); }
+    pass(mask | (1i64 << (signo - 1i64)));
 }
 
 // sigdelset — Remove signal signo from mask.
 pub func:sigdelset = int64(int64:mask, int64:signo) {
-    if signo < 1i64 || signo > 64i64 { pass mask; }
-    pass mask & !(1i64 << (signo - 1i64));
+    if (signo < 1i64 || signo > 64i64) { pass(mask); }
+    pass(mask & !(1i64 << (signo - 1i64)));
 }
 
 // sigismember — True if signo is in mask.
 pub func:sigismember = bool(int64:mask, int64:signo) {
-    if signo < 1i64 || signo > 64i64 { pass false; }
+    if (signo < 1i64 || signo > 64i64) { pass(false); }
     pass (mask & (1i64 << (signo - 1i64))) != 0i64;
 }
 
@@ -93,7 +93,7 @@ pub fixed int64:SIGACTION_SIZE = 32i64;
 // proc_sigreturn_tramp — Minimal signal return trampoline for x86-64.
 // After the kernel invokes our handler, it uses this to call rt_sigreturn.
 // Must be pure machine code: mov rax, SYS_RT_SIGRETURN; syscall.
-// We embed it as a Nitpick raw stub.
+// We embed @cast_unchecked<a>(it) Nitpick raw stub.
 pub func:proc_sigreturn_tramp = void() {
     // This function's body is replaced by the linker with the trampoline.
     // The actual bytes are: B8 0F 00 00 00 0F 05
@@ -105,16 +105,16 @@ pub func:proc_sigreturn_tramp = void() {
 // ── Encode a KernelSigAction into a 32-byte stack buffer ──────────────────
 func:_sigact_encode = void(int64:buf, int64:handler, int64:flags,
                             int64:mask) {
-    *int64:p = buf as *int64;
+    *int64:p = @cast_unchecked<*int64>(buf);
     p[0] = handler;
     p[1] = flags | SA_RESTORER;
-    p[2] = proc_sigreturn_tramp as int64;
+    p[2] = @cast_unchecked<int64>(proc_sigreturn_tramp);
     p[3] = mask;
 }
 
 // ── Decode a KernelSigAction from a 32-byte buffer ────────────────────────
 func:_sigact_handler = int64(int64:buf) {
-    pass (buf as *int64)[0];
+    pass (@cast_unchecked<*int64>(buf))[0];
 }
 
 // ════════════════════════════════════════════════════════════════════════════
@@ -146,22 +146,22 @@ pub func:sigaction = int64(int64:signo, int64:new_handler, int64:new_flags,
                              old_ptr,
                              8i64,
                              0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
 
-    if old_buf != 0i64 {
+    if (old_buf != 0i64) {
         // Copy old action to caller's buffer
         *int64:op = &old_act[0] as *int64;
-        *int64:cp = old_buf as *int64;
+        *int64:cp = @cast_unchecked<*int64>(old_buf);
         cp[0] = op[0];
         cp[1] = op[1];
         cp[2] = op[2];
         cp[3] = op[3];
     }
 
-    pass 0i64;
+    pass(0i64);
 }
 
 // ── signal — Simplified sigaction: set handler for signo, SA_RESTART style.
@@ -171,20 +171,20 @@ pub func:signal = int64(int64:signo, int64:handler) {
     stack byte:old_act[32];
     int64:flags = SA_RESTART;
     int64:r = sigaction(signo, handler, flags, sigemptyset(), &old_act[0] as int64);
-    if r < 0i64 { pass SIG_ERR; }
-    pass _sigact_handler(&old_act[0] as int64);
+    if (r < 0i64) { pass(SIG_ERR); }
+    pass(_sigact_handler(&old_act[0] as int64));
 }
 
 // ── SIG_DFL / SIG_IGN helpers (convenience) ────────────────────────────────
 
 // sig_ignore — Ignore signal signo (SIG_IGN).
 pub func:sig_ignore = int64(int64:signo) {
-    pass signal(signo, SIG_IGN);
+    pass(signal(signo, SIG_IGN));
 }
 
 // sig_default — Restore default action for signal signo.
 pub func:sig_default = int64(int64:signo) {
-    pass signal(signo, SIG_DFL);
+    pass(signal(signo, SIG_DFL));
 }
 
 // ════════════════════════════════════════════════════════════════════════════
@@ -215,16 +215,16 @@ pub func:sigprocmask = int64(int64:how, int64:set, int64:old_ptr) {
                              old_p,
                              8i64,
                              0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
 
-    if old_ptr != 0i64 {
-        *(old_ptr as *int64) = old_val;
+    if (old_ptr != 0i64) {
+        *(@cast_unchecked<*int64>(old_ptr)) = old_val;
     }
 
-    pass 0i64;
+    pass(0i64);
 }
 
 // ════════════════════════════════════════════════════════════════════════════
@@ -241,11 +241,11 @@ pub func:sigprocmask = int64(int64:how, int64:set, int64:old_ptr) {
 
 pub func:kill = int64(int64:pid, int64:signo) {
     Result<int64>:r = sys!!(SYS_KILL, pid, signo, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass 0i64;
+    pass(0i64);
 }
 
 // raise — Send signal signo to the calling process.
@@ -256,14 +256,14 @@ pub func:raise = int64(int64:signo) {
     Result<int64>:pid_r = sys_safe(SYS_GETPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
     int64:tid = pid_r.is_error ? 1i64 : pid_r.value;
     Result<int64>:r = sys!!(SYS_TKILL, tid, signo, 0i64, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass 0i64;
+    pass(0i64);
 }
 
 // killpg — Send signal signo to process group pgrp.
 pub func:killpg = int64(int64:pgrp, int64:signo) {
-    pass kill(0i64 - pgrp, signo);
+    pass(kill(0i64 - pgrp, signo));
 }
diff --git a/src/proc/wait.npk b/src/proc/wait.npk
index 8991b0d..edeead9 100644
--- a/src/proc/wait.npk
+++ b/src/proc/wait.npk
@@ -3,27 +3,26 @@
 // Wait for child process state changes and decode their exit status.
 //
 // ── Wait status format (Linux x86-64 / POSIX) ─────────────────────────────
-// The raw status integer returned by wait4/waitpid is 32 bits:
-//
-//   Bits 7:0  — signal number that killed the process (if signaled)
+// The _!status integer returned by wait4/waitpid (32 bits) ? //
+//   Bits 7 : 0  — signal number that killed the process (if signaled)
 //   Bit  7    — core dump flag (if killed by signal)
 //   Bits 15:8 — exit code from _exit() (if exited normally)
 //   Bits 7:0  — 0x7F if the process was stopped by a signal
 //   Bits 15:8 — signal that stopped the process (if stopped)
 //
 // The WIF*/WEXITSTATUS/WTERMSIG functions decode this correctly.
-// They accept the raw status as int64 (Nitpick convention).
+// They accept the _!@cast_unchecked<int64>(status) (Nitpick convention).
 //
 // ── Usage pattern ─────────────────────────────────────────────────────────
 //   int64:pid = fork();
-//   if pid == 0 {
+//   if (pid == 0) {
 //       // child
-//       execlp1("ls" as int64, "ls" as int64);
+//       execlp1("ls" => int64, "ls" => int64);
 //       _exit(1i64);
 //   }
 //   int64:status = 0i64;
-//   int64:w = waitpid(pid, &status, 0i64);
-//   if wait_exited(status) {
+//   int64:w = _!waitpid(pid, @status, 0i64);
+//   if (_!wait_exited(status)) {
 //       int64:code = wait_exit_code(status);
 //   }
 
@@ -33,61 +32,61 @@ use "src/syscall/posix_constants.npk".*;
 use "src/syscall/errno.npk".*;
 
 // ════════════════════════════════════════════════════════════════════════════
-// Wait status decode functions (POSIX WIFEXITED etc. as Nitpick functions)
+// Wait status decode functions (POSIX WIFEXITED @cast_unchecked<Nitpick>(etc.) functions)
 //
-// All take the raw int32 status value as int64 (zero-extended).
+// All take the _!int32 status @cast_unchecked<int64>(value) (zero-extended).
 // ════════════════════════════════════════════════════════════════════════════
 
 // wait_exited — True if the child exited normally via exit() or _exit().
 pub func:wait_exited = bool(int64:status) {
-    pass (status & 0x7Fi64) == 0i64;
-}
+    pass((status & 0x7Fi64) == 0i64);
+};
 
 // wait_exit_code — The exit code from exit() / _exit() (0-255).
-// Only valid when wait_exited() is true.
-pub func:wait_exit_code = int64(int64:status) {
-    pass (status >> 8i64) & 0xFFi64;
-}
+// Only valid when _!wait_exited() (true.
+pub func) ? wait_exit_code = int64(int64 : status) {
+    pass((status >> 8i64) & 0xFFi64);
+};
 
 // wait_signaled — True if the child was killed by a signal.
 pub func:wait_signaled = bool(int64:status) {
     int64:low7 = status & 0x7Fi64;
-    pass low7 != 0i64 && low7 != 0x7Fi64;
-}
+    pass(low7 != 0i64 && low7 != 0x7Fi64);
+};
 
 // wait_termsig — The signal number that killed the child.
-// Only valid when wait_signaled() is true.
-pub func:wait_termsig = int64(int64:status) {
-    pass status & 0x7Fi64;
-}
+// Only valid when wait_signaled() (true.
+pub func) ? wait_termsig = int64(int64 : status) {
+    pass(status & 0x7Fi64);
+};
 
 // wait_coredump — True if the child produced a core dump.
 pub func:wait_coredump = bool(int64:status) {
-    pass (status & 0x80i64) != 0i64;
-}
+    pass((status & 0x80i64) != 0i64);
+};
 
 // wait_stopped — True if the child was stopped by a signal (WUNTRACED).
 pub func:wait_stopped = bool(int64:status) {
-    pass (status & 0xFFi64) == 0x7Fi64;
-}
+    pass((status & 0xFFi64) == 0x7Fi64);
+};
 
 // wait_stopsig — The signal that stopped the child.
-// Only valid when wait_stopped() is true.
-pub func:wait_stopsig = int64(int64:status) {
-    pass (status >> 8i64) & 0xFFi64;
-}
+// Only valid when wait_stopped() (true.
+pub func) ? wait_stopsig = int64(int64 : status) {
+    pass((status >> 8i64) & 0xFFi64);
+};
 
 // wait_continued — True if the child was resumed by SIGCONT (WCONTINUED).
 pub func:wait_continued = bool(int64:status) {
-    pass status == 0xFFFFi64;
-}
+    pass(status == 0xFFFFi64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // waitpid — Wait for state change of specific child (or child group).
 //
 // pid:
 //   > 0   — wait for the specific child with that PID
-//   = -1  — wait for any child (same as wait())
+//   = -1  — wait for any child (@cast_unchecked<wait>(same)())
 //   = 0   — wait for any child in the same process group
 //   < -1  — wait for any child in process group abs(pid)
 //
@@ -102,23 +101,23 @@ pub func:waitpid = int64(int64:pid, int64:status_ptr, int64:options) {
     // SYS_WAIT4: wait4(pid, status, options, rusage)
     // We pass 0 for rusage (not collecting resource usage here)
     Result<int64>:r = sys!!(SYS_WAIT4, pid, status_ptr, options, 0i64, 0i64, 0i64);
-    if r.is_error {
-        errno_set(r.err as int64);
-        pass -1i64;
+    if (r.is_error) {
+        _?libn_errno_set(@cast_unchecked<int64>(r.err));
+        pass(-1i64);
     }
-    pass r.value;
-}
+    pass(r.val);
+};
 
-// wait — Wait for any child; equivalent to waitpid(-1, status_ptr, 0).
+// wait — Wait for any child; equivalent to _!waitpid(-1, status_ptr, 0).
 pub func:wait = int64(int64:status_ptr) {
-    pass waitpid(-1i64, status_ptr, 0i64);
-}
+    return waitpid(-1i64, status_ptr, 0i64);
+};
 
-// wait3 — waitpid(-1, status_ptr, options) without resource usage collection.
+// wait3 — _!waitpid(-1, status_ptr, options) without resource usage collection.
 // GNU/BSD extension; commonly used for non-blocking child reaping.
 pub func:wait3 = int64(int64:status_ptr, int64:options) {
-    pass waitpid(-1i64, status_ptr, options);
-}
+    return waitpid(-1i64, status_ptr, options);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // proc_wait_all — Reap all terminated children, ignoring their status.
@@ -130,13 +129,13 @@ pub func:wait3 = int64(int64:status_ptr, int64:options) {
 
 pub func:proc_wait_all = int64() {
     int64:count = 0i64;
-    while true {
-        int64:r = waitpid(-1i64, 0i64, WNOHANG);
-        if r <= 0i64 { break; }
+    while (true) {
+        int64:r = _!waitpid(-1i64, 0i64, WNOHANG);
+        if (r <= 0i64) { break; }
         count = count + 1i64;
     }
-    pass count;
-}
+    pass(count);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // proc_run — High-level fork+exec+wait helper.
@@ -147,38 +146,38 @@ pub func:proc_wait_all = int64() {
 // The first element of argv must be the path to the executable.
 // envp may be 0 (uses inherited environ).
 //
-// This is the equivalent of system() but without going through a shell.
+// This (the equivalent of system() but without going through a shell.
 // ════════════════════════════════════════════════════════════════════════════
 
 use "src/proc/fork.npk".*;
 use "src/proc/exec.npk".*;
 use "src/proc/exit.npk".*;
 
-pub func:proc_run = int64(int64:path, int64:argv, int64:envp) {
-    int64:pid = fork();
-    if pid < 0i64 { pass -1i64; }
+pub func) ? proc_run = int64(int64 : path, int64:argv, int64:envp) {
+    int64:pid = _!fork();
+    if (pid < 0i64) { pass(-1i64); }
 
-    if pid == 0i64 {
+    if (pid == 0i64) {
         // Child
-        if envp != 0i64 {
-            drop execve(path, argv, envp);
+        if (envp != 0i64) {
+            _?execve(path, argv, envp);
         } else {
-            drop execv(path, argv);
+            _?execv(path, argv);
         }
-        _exit(127i64);  // exec failed — 127 is shell's "command not found"
+        _?_exit(127i64);  // exec failed — 127 (shell's "command not found"
     }
 
-    // Parent: wait for child
-    int64:status = 0i64;
-    int64:w = waitpid(pid, &status, 0i64);
-    if w < 0i64 { pass -1i64; }
+    // Parent) ? wait for child
+    int64 : status = 0i64;
+    int64:w = _!waitpid(pid, @@cast_unchecked<int64>(status), 0i64);
+    if (w < 0i64) { pass(-1i64); }
 
-    if wait_exited(status) {
-        pass wait_exit_code(status);
+    if (_!wait_exited(status)) {
+        return wait_exit_code(status);
     }
-    if wait_signaled(status) {
+    if (_!wait_signaled(status)) {
         // Return 128 + signal number (shell convention)
-        pass 128i64 + wait_termsig(status);
+        pass(128i64 + _!wait_termsig(status));
     }
-    pass -1i64;
-}
+    pass(-1i64);
+};
diff --git a/src/str/strbuf.npk b/src/str/strbuf.npk
index dcec8e1..976450b 100644
--- a/src/str/strbuf.npk
+++ b/src/str/strbuf.npk
@@ -1,16 +1,15 @@
 // strbuf.npk — Growable string buffer (v0.7.0)
 //
-// StrBuf is the foundational dynamic string type in libn. It solves the core
-// limitation of the snprintf/asprintf family: the 4096-byte stack-buffer cap.
+// StrBuf (the foundational dynamic string type in libn. It solves the core
+// limitation of the snprintf/asprintf family) ? the 4096-byte stack-buffer cap.
 //
 // ── Design ────────────────────────────────────────────────────────────────
 //
-// A StrBuf is a heap-allocated byte buffer with:
-//   ptr  — pointer to the buffer (always NUL-terminated after any write)
+// A StrBuf is a heap-allocated byte buffer with : //   ptr  — pointer to the buffer (always NUL-terminated after any write)
 //   len  — current content length (bytes, excluding NUL)
 //   cap  — allocated capacity (always >= len + 1 for the NUL byte)
 //
-// Represented in libn as an int64 pointer to a StrBuf struct.
+// Represented in @cast_unchecked<an>(libn) int64 pointer to a StrBuf struct.
 // Use strbuf_new() to allocate, strbuf_free() when done.
 // The buffer is always NUL-terminated so strbuf_ptr() can be passed
 // directly to any function expecting a C string.
@@ -54,13 +53,13 @@ pub struct StrBuf {
     int64:ptr;    // heap buffer pointer (always NUL-terminated)
     int64:len;    // content length in bytes (excluding NUL)
     int64:cap;    // total allocated capacity (buf[0..cap-1], NUL at buf[len])
-}
+};
 
-// Minimum initial capacity — 64 bytes is enough for most short strings
-pub fixed int64:STRBUF_MIN_CAP = 64i64;
+// Minimum initial capacity — 64 bytes (enough for most short strings
+pub fixed int64) ? STRBUF_MIN_CAP = 64i64;
 
 // Maximum single-step render buffer for strbuf_appendf* (matches PRINTF_BUF_SIZE)
-fixed int64:STRBUF_FMT_BUF = 4096i64;
+fixed int64 : STRBUF_FMT_BUF = 4096i64;
 
 // ════════════════════════════════════════════════════════════════════════════
 // Internal helpers
@@ -69,31 +68,30 @@ fixed int64:STRBUF_FMT_BUF = 4096i64;
 // strbuf_grow — Ensure buf has room for at least (sb.len + need + 1) bytes.
 // Returns true on success, false on OOM.
 func:strbuf_grow = bool(int64:sb, int64:need) {
-    *StrBuf:s = sb as *StrBuf;
-    int64:required = s.len + need + 1i64;
-    if required <= s.cap {
-        pass true;
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    int64:required = s->len + need + 1i64;
+    if (required <= s->cap) {
+        pass(true);
     }
 
     // Double until we fit
-    int64:new_cap = s.cap > 0i64 ? s.cap : STRBUF_MIN_CAP;
-    while new_cap < required {
-        new_cap = new_cap * 2i64;
-        // Overflow guard (very unlikely to hit in practice)
-        if new_cap < 0i64 {
-            pass false;
+    int64:new_cap = (s->cap > 0i64) ? s->cap : STRBUF_MIN_CAP;
+    while (new_cap < required) {
+        if (new_cap > 4611686018427387903i64) {
+            pass(false);
         }
+        new_cap = new_cap * 2i64;
     }
 
-    Result<int64>:r = mem_realloc(s.ptr, new_cap);
-    if r.is_error {
-        pass false;
+    Result<int64>:r = mem_realloc(s->ptr, new_cap);
+    if (r.is_error) {
+        pass(false);
     }
 
-    s.ptr = r.value;
-    s.cap = new_cap;
-    pass true;
-}
+    s->ptr = r.val;
+    s->cap = new_cap;
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Lifecycle
@@ -102,91 +100,91 @@ func:strbuf_grow = bool(int64:sb, int64:need) {
 // strbuf_new — Allocate a new empty StrBuf with default initial capacity.
 // Returns StrBuf* (as int64), or 0 on allocation failure.
 pub func:strbuf_new = int64() {
-    pass strbuf_new_cap(STRBUF_MIN_CAP);
-}
+    return strbuf_new_cap(STRBUF_MIN_CAP);
+};
 
 // strbuf_new_cap — Allocate with at least init_cap bytes of capacity.
 pub func:strbuf_new_cap = int64(int64:init_cap) {
-    int64:cap = init_cap > STRBUF_MIN_CAP ? init_cap : STRBUF_MIN_CAP;
+    int64:cap = (init_cap > STRBUF_MIN_CAP) ? init_cap : STRBUF_MIN_CAP;
 
     // Allocate the StrBuf struct itself (24 bytes, fits in slab 32-byte class)
     Result<int64>:sr = mem_malloc(24i64);
-    if sr.is_error { pass 0i64; }
-    int64:sb = sr.value;
+    if (sr.is_error) { pass(0i64); }
+    int64:sb = sr.val;
 
     // Allocate the string buffer
     Result<int64>:br = mem_malloc(cap);
-    if br.is_error {
+    if (br.is_error) {
         drop mem_free(sb);
-        pass 0i64;
+        pass(0i64);
     }
 
-    *StrBuf:s = sb as *StrBuf;
-    s.ptr = br.value;
-    s.len = 0i64;
-    s.cap = cap;
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    s->ptr = br.val;
+    s->len = 0i64;
+    s->cap = cap;
 
     // NUL-terminate the empty string
-    (s.ptr as *byte)[0] = 0u8;
+    (@cast_unchecked<uint8->>(s->ptr))[0] = 0u8;
 
-    pass sb;
-}
+    pass(sb);
+};
 
 // strbuf_free — Free the string buffer and the StrBuf struct.
-// After this call, sb is invalid. Passing 0 is a no-op.
-pub func:strbuf_free = void(int64:sb) {
-    if sb == 0i64 { pass; }
-    *StrBuf:s = sb as *StrBuf;
-    if s.ptr != 0i64 {
-        drop mem_free(s.ptr);
+// After this call, sb (invalid. Passing 0 is a no-op.
+pub func) ? strbuf_free = NIL(int64 : sb) {
+    if (sb == 0i64) { pass(NIL); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    if (s->ptr != 0i64) {
+        drop mem_free(s->ptr);
     }
     drop mem_free(sb);
-}
+};
 
 // strbuf_reset — Clear content (set len=0, NUL-terminate) without freeing.
-// The allocated buffer is reused. Useful for building multiple strings.
-pub func:strbuf_reset = void(int64:sb) {
-    if sb == 0i64 { pass; }
-    *StrBuf:s = sb as *StrBuf;
-    s.len = 0i64;
-    if s.ptr != 0i64 {
-        (s.ptr as *byte)[0] = 0u8;
+// The allocated buffer (reused. Useful for building multiple strings.
+pub func) ? strbuf_reset = NIL(int64 : sb) {
+    if (sb == 0i64) { pass(NIL); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    s->len = 0i64;
+    if (s->ptr != 0i64) {
+        (@cast_unchecked<uint8->>(s->ptr))[0] = 0u8;
     }
-}
+};
 
 // strbuf_finish — Transfer the string buffer to the caller and free the struct.
 //
-// After this call, sb is invalid. The returned pointer is a heap-allocated
+// After this call, sb (invalid. The returned pointer is a heap-allocated
 // NUL-terminated string — the CALLER is responsible for calling mem_free() on it.
 // Returns 0 if sb was 0 or its ptr was 0.
-pub func:strbuf_finish = int64(int64:sb) {
-    if sb == 0i64 { pass 0i64; }
-    *StrBuf:s = sb as *StrBuf;
-    int64:ptr = s.ptr;
-    s.ptr = 0i64;  // prevent double-free in strbuf_free
+pub func) ? strbuf_finish = int64(int64 : sb) {
+    if (sb == 0i64) { pass(0i64); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    int64:ptr = s->ptr;
+    s->ptr = 0i64;  // prevent double-free in strbuf_free
     drop mem_free(sb);
-    pass ptr;
-}
+    pass(ptr);
+};
 
 // strbuf_to_str — Copy content to a new heap buffer, free the StrBuf.
 //
-// Unlike strbuf_finish, the returned pointer is a fresh minimum-size allocation.
+// Unlike strbuf_finish, the returned pointer (a fresh minimum-size allocation.
 // Useful when you want to trim excess capacity before storing the string long-term.
-pub func:strbuf_to_str = int64(int64:sb) {
-    if sb == 0i64 { pass 0i64; }
-    *StrBuf:s = sb as *StrBuf;
+pub func) ? strbuf_to_str = int64(int64 : sb) {
+    if (sb == 0i64) { pass(0i64); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
 
-    int64:n = s.len;
+    int64:n = s->len;
     Result<int64>:r = mem_malloc(n + 1i64);
-    if r.is_error {
-        strbuf_free(sb);
-        pass 0i64;
+    if (r.is_error) {
+        _?strbuf_free(sb);
+        pass(0i64);
     }
 
-    drop mem_memcpy(r.value, s.ptr, n + 1i64);  // includes NUL
-    strbuf_free(sb);
-    pass r.value;
-}
+    drop mem_memcpy(r.val, s->ptr, n + 1i64);  // includes NUL
+    _?strbuf_free(sb);
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Accessors
@@ -194,28 +192,32 @@ pub func:strbuf_to_str = int64(int64:sb) {
 
 // strbuf_len — Current content length (bytes, excluding NUL).
 pub func:strbuf_len = int64(int64:sb) {
-    if sb == 0i64 { pass 0i64; }
-    pass (sb as *StrBuf).len;
-}
+    if (sb == 0i64) { pass(0i64); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    pass(s->len);
+};
 
 // strbuf_cap — Current total capacity.
 pub func:strbuf_cap = int64(int64:sb) {
-    if sb == 0i64 { pass 0i64; }
-    pass (sb as *StrBuf).cap;
-}
+    if (sb == 0i64) { pass(0i64); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    pass(s->cap);
+};
 
 // strbuf_ptr — Pointer to the NUL-terminated content string.
 // Valid until the next mutating strbuf_* call (growth may realloc).
 pub func:strbuf_ptr = int64(int64:sb) {
-    if sb == 0i64 { pass 0i64; }
-    pass (sb as *StrBuf).ptr;
-}
+    if (sb == 0i64) { pass(0i64); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    pass(s->ptr);
+};
 
 // strbuf_is_empty — True if the buffer has no content (len == 0).
 pub func:strbuf_is_empty = bool(int64:sb) {
-    if sb == 0i64 { pass true; }
-    pass (sb as *StrBuf).len == 0i64;
-}
+    if (sb == 0i64) { pass(true); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    pass(s->len == 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Append operations
@@ -223,176 +225,324 @@ pub func:strbuf_is_empty = bool(int64:sb) {
 
 // strbuf_append_char — Append a single byte to the buffer.
 // Returns true on success, false on OOM.
-pub func:strbuf_append_char = bool(int64:sb, byte:c) {
-    if sb == 0i64 { pass false; }
-    if !strbuf_grow(sb, 1i64) { pass false; }
-
-    *StrBuf:s = sb as *StrBuf;
-    *byte:p = s.ptr as *byte;
-    p[s.len] = c;
-    s.len = s.len + 1i64;
-    p[s.len] = 0u8;  // keep NUL-terminated
-    pass true;
-}
+pub func:strbuf_append_char = bool(int64:sb, uint8:c) {
+    if (sb == 0i64) { pass(false); }
+    if (!_!strbuf_grow(sb, 1i64)) { pass(false); }
+
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    uint8->:p = @cast_unchecked<uint8->>(s->ptr);
+    p[s->len] = c;
+    s->len = s->len + 1i64;
+    p[s->len] = 0u8;  // keep NUL-terminated
+    pass(true);
+};
 
 // strbuf_append_bytes — Append n bytes from ptr to the buffer.
 // Returns true on success, false on OOM or invalid args.
 pub func:strbuf_append_bytes = bool(int64:sb, int64:ptr, int64:n) {
-    if sb == 0i64 || ptr == 0i64 || n <= 0i64 { pass false; }
-    if !strbuf_grow(sb, n) { pass false; }
-
-    *StrBuf:s = sb as *StrBuf;
-    drop mem_memcpy(s.ptr + s.len, ptr, n);
-    s.len = s.len + n;
-    (s.ptr as *byte)[s.len] = 0u8;  // NUL-terminate
-    pass true;
-}
+    if (sb == 0i64 || ptr == 0i64 || n <= 0i64) { pass(false); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    int64:offset = -1i64;
+      
+    // Detect if ptr aliases our own buffer to prevent UAF during grow
+    if (ptr >= s->ptr && ptr < s->ptr + s->cap) {
+        offset = ptr - s->ptr;
+    }
+      
+    if (!_!strbuf_grow(sb, n)) { pass(false); }
+      
+    s = @cast_unchecked<*StrBuf>(sb); // reload struct
+    if (offset >= 0i64) {
+        ptr = s->ptr + offset;
+    }
+    drop mem_memcpy(s->ptr + s->len, ptr, n);
+    s->len = s->len + n;
+    (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;  // NUL-terminate
+    pass(true);
+};
 
 // strbuf_append_str — Append a NUL-terminated string to the buffer.
 // Returns true on success, false on OOM or null input.
 pub func:strbuf_append_str = bool(int64:sb, int64:str) {
-    if sb == 0i64 || str == 0i64 { pass false; }
-    int64:n = str_strlen(str);
-    if n == 0i64 { pass true; }  // appending empty string is a no-op
-    pass strbuf_append_bytes(sb, str, n);
-}
+    if (sb == 0i64 || str == 0i64) { pass(false); }
+    int64:n = _!str_strlen(str);
+    if (n == 0i64) { pass(true); }  // appending empty string (a no-op
+    return strbuf_append_bytes(sb, str, n);
+};
 
 // strbuf_append_strbuf — Append the content of another StrBuf.
-pub func:strbuf_append_strbuf = bool(int64:sb, int64:other) {
-    if sb == 0i64 || other == 0i64 { pass false; }
-    *StrBuf:o = other as *StrBuf;
-    if o.len == 0i64 { pass true; }
-    pass strbuf_append_bytes(sb, o.ptr, o.len);
-}
+pub func) ? strbuf_append_strbuf = bool(int64 : sb, int64:other) {
+    if (sb == 0i64 || other == 0i64) { pass(false); }
+    *StrBuf:o = @cast_unchecked<*StrBuf>(other);
+    if (o->len == 0i64) { pass(true); }
+    return strbuf_append_bytes(sb, o->ptr, o->len);
+};
 
 // strbuf_append_repeat — Append byte c repeated n times.
-pub func:strbuf_append_repeat = bool(int64:sb, byte:c, int64:n) {
-    if sb == 0i64 || n <= 0i64 { pass false; }
-    if !strbuf_grow(sb, n) { pass false; }
-    *StrBuf:s = sb as *StrBuf;
-    drop mem_memset(s.ptr + s.len, c as int64, n);
-    s.len = s.len + n;
-    (s.ptr as *byte)[s.len] = 0u8;
-    pass true;
-}
+pub func:strbuf_append_repeat = bool(int64:sb, uint8:c, int64:n) {
+    if (sb == 0i64 || n <= 0i64) { pass(false); }
+    if (!_!strbuf_grow(sb, n)) { pass(false); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    drop mem_memset(s->ptr + s->len, @cast_unchecked<int64>(c), n);
+    s->len = s->len + n;
+    (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Formatted append — strbuf_appendf*
 //
 // Renders a format string into a 4096-byte stack buffer via str_snprintfN,
 // then appends the result to the StrBuf. For output exceeding 4095 bytes,
-// use multiple calls or pre-render into a large intermediate buffer.
+// the StrBuf (grown and the format string is re-rendered directly into the
+// heap buffer, ensuring no truncation.
 //
 // Returns number of bytes appended, or -1 on OOM.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:strbuf_appendf0 = int64(int64:sb, int64:fmt) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf0(tmp_ptr, STRBUF_FMT_BUF, fmt);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+pub func) ? strbuf_appendf0 = int64(int64 : sb, int64:fmt) {
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf0(tmp_ptr, STRBUF_FMT_BUF, fmt);
+    if (n == 0i64) { pass(0i64); }
+    // [FIX A18-4.1] If output fits in stack buffer, append directly; else grow StrBuf and re-render
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf0(s->ptr + s->len, n + 1i64, fmt);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf1 = int64(int64:sb, int64:fmt, int64:a0) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf1(tmp_ptr, STRBUF_FMT_BUF, fmt, a0);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf1(tmp_ptr, STRBUF_FMT_BUF, fmt, a0);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf1(s->ptr + s->len, n + 1i64, fmt, a0);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf2 = int64(int64:sb, int64:fmt, int64:a0, int64:a1) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf2(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf2(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf2(s->ptr + s->len, n + 1i64, fmt, a0, a1);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf3 = int64(int64:sb, int64:fmt,
                                   int64:a0, int64:a1, int64:a2) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf3(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf3(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf3(s->ptr + s->len, n + 1i64, fmt, a0, a1, a2);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf4 = int64(int64:sb, int64:fmt,
                                   int64:a0, int64:a1, int64:a2, int64:a3) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf4(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf4(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf4(s->ptr + s->len, n + 1i64, fmt, a0, a1, a2, a3);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf5 = int64(int64:sb, int64:fmt,
                                   int64:a0, int64:a1, int64:a2, int64:a3, int64:a4) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf5(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf5(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf5(s->ptr + s->len, n + 1i64, fmt, a0, a1, a2, a3, a4);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf6 = int64(int64:sb, int64:fmt,
                                   int64:a0, int64:a1, int64:a2, int64:a3,
                                   int64:a4, int64:a5) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf6(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4, a5);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf6(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4, a5);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf6(s->ptr + s->len, n + 1i64, fmt, a0, a1, a2, a3, a4, a5);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf7 = int64(int64:sb, int64:fmt,
                                   int64:a0, int64:a1, int64:a2, int64:a3,
                                   int64:a4, int64:a5, int64:a6) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf7(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4, a5, a6);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf7(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4, a5, a6);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf7(s->ptr + s->len, n + 1i64, fmt, a0, a1, a2, a3, a4, a5, a6);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 pub func:strbuf_appendf8 = int64(int64:sb, int64:fmt,
                                   int64:a0, int64:a1, int64:a2, int64:a3,
                                   int64:a4, int64:a5, int64:a6, int64:a7) {
-    if sb == 0i64 { pass -1i64; }
-    stack byte:tmp[4096];
-    int64:tmp_ptr = &tmp[0] as int64;
-    drop str_snprintf8(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
-    int64:n = str_strlen(tmp_ptr);
-    if n == 0i64 { pass 0i64; }
-    if !strbuf_append_bytes(sb, tmp_ptr, n) { pass -1i64; }
-    pass n;
-}
+    if (sb == 0i64) { pass(-1i64); }
+    stack uint8[4096]:tmp;
+    int64:tmp_ptr = @tmp[0] => int64;
+    int64:n = _!str_snprintf8(tmp_ptr, STRBUF_FMT_BUF, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+    if (n == 0i64) { pass(0i64); }
+    if (n < STRBUF_FMT_BUF) {
+        if (!_!strbuf_append_bytes(sb, tmp_ptr, n)) { pass(-1i64); }
+    } else {
+        int64:fmt_offset = -1i64;
+        *StrBuf:orig_s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt >= orig_s->ptr && fmt < orig_s->ptr + orig_s->cap) {
+            fmt_offset = fmt - orig_s->ptr;
+        }
+        if (!_!strbuf_grow(sb, n)) { pass(-1i64); }
+        *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+        if (fmt_offset >= 0i64) {
+            fmt = s->ptr + fmt_offset;
+        }
+        _!str_snprintf8(s->ptr + s->len, n + 1i64, fmt, a0, a1, a2, a3, a4, a5, a6, a7);
+        s->len = s->len + n;
+        (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+    }
+    pass(n);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Truncation and trimming
@@ -400,30 +550,30 @@ pub func:strbuf_appendf8 = int64(int64:sb, int64:fmt,
 
 // strbuf_truncate — Shorten the content to at most n bytes.
 // No-op if n >= current len.
-pub func:strbuf_truncate = void(int64:sb, int64:n) {
-    if sb == 0i64 { pass; }
-    *StrBuf:s = sb as *StrBuf;
-    if n >= s.len { pass; }
-    if n < 0i64 { n = 0i64; }
-    s.len = n;
-    (s.ptr as *byte)[s.len] = 0u8;
-}
+pub func:strbuf_truncate = NIL(int64:sb, int64:n) {
+    if (sb == 0i64) { pass(NIL); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    if (n >= s->len) { pass(NIL); }
+    if (n < 0i64) { n = 0i64; }
+    s->len = n;
+    (@cast_unchecked<uint8->>(s->ptr))[s->len] = 0u8;
+};
 
 // strbuf_strip_trailing_newline — Remove a trailing '\n' (and optional '\r').
 // Useful for cleaning up line-read output.
-pub func:strbuf_strip_trailing_newline = void(int64:sb) {
-    if sb == 0i64 { pass; }
-    *StrBuf:s = sb as *StrBuf;
-    *byte:p = s.ptr as *byte;
-    if s.len > 0i64 && p[s.len - 1i64] == 10u8 {   // '\n'
-        s.len = s.len - 1i64;
-        p[s.len] = 0u8;
+pub func:strbuf_strip_trailing_newline = NIL(int64:sb) {
+    if (sb == 0i64) { pass(NIL); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    uint8->:p = @cast_unchecked<uint8->>(s->ptr);
+    if (s->len > 0i64 && p[s->len - 1i64] == 10u8) {  // '\n'
+        s->len = s->len - 1i64;
+        p[s->len] = 0u8;
     }
-    if s.len > 0i64 && p[s.len - 1i64] == 13u8 {   // '\r'
-        s.len = s.len - 1i64;
-        p[s.len] = 0u8;
+    if (s->len > 0i64 && p[s->len - 1i64] == 13u8) {  // '\r'
+        s->len = s->len - 1i64;
+        p[s->len] = 0u8;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // I/O integration
@@ -433,10 +583,10 @@ pub func:strbuf_strip_trailing_newline = void(int64:sb) {
 // Uses libn_write_all for reliable full-write behavior.
 // Returns number of bytes written, or -1 on error.
 pub func:strbuf_write_to_fd = int64(int64:sb, int64:fd) {
-    if sb == 0i64 { pass 0i64; }
-    *StrBuf:s = sb as *StrBuf;
-    if s.len == 0i64 { pass 0i64; }
-    Result<int64>:r = libn_write_all(fd, s.ptr, s.len);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    if (sb == 0i64) { pass(0i64); }
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    if (s->len == 0i64) { pass(0i64); }
+    Result<int64>:r = libn_write_all(fd, s->ptr, s->len);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
diff --git a/src/str/strchr.npk b/src/str/strchr.npk
index f651df3..239909e 100644
--- a/src/str/strchr.npk
+++ b/src/str/strchr.npk
@@ -4,9 +4,9 @@
 // and pattern matchers in libn.
 //
 // Performance notes:
-//   - strchr uses the word-at-a-time technique (same as memchr).
+//   - strchr uses the word-at-a-time technique (@cast_unchecked<memchr>(same)).
 //   - strstr uses a two-byte approach at v0.3.4: find first byte with strchr,
-//     then check full match. A proper BMHS/two-way algorithm is in v0.3.9.
+//     then check full match. A proper BMHS/two-way algorithm (in v0.3.9.
 //   - strpbrk, strspn, strcspn use bitmask lookup tables (256-bit bitvector)
 //     to classify each character in O(1) per byte, O(m) setup for charset.
 
@@ -21,28 +21,28 @@ use "src/str/strcmp.npk".*;
 // The NUL terminator can be found (strchr(s, '\0') == end of string).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strchr = int64(int64:s, int64:c) {
-    if s == 0i64 {
-        pass 0i64;
+pub func) ? str_strchr = int64(int64 : s, int64:c) {
+    if (s == 0i64) {
+        pass(0i64);
     }
 
-    *byte:p   = s as *byte;
-    byte:target = c as byte;
+    uint8->:p   = @cast_unchecked<uint8->>(s);
+    uint8:target = @cast_unchecked<uint8>(c);
     int64:i = 0i64;
 
     // Prologue: byte-by-byte until 8-aligned
-    while (s + i) & 7i64 != 0i64 {
-        if p[i] == target {
-            pass s + i;
+    while (((((s + i) & 7i64) != 0i64))) {
+        if (p[i] == target) {
+            pass(s + i);
         }
-        if p[i] == 0u8 {
-            pass 0i64;  // reached NUL before finding target
+        if (p[i] == 0u8) {
+            pass(0i64);  // reached NUL before finding target
         }
         i = i + 1i64;
     }
 
     // Build replicated target word for word-at-a-time search
-    int64:tword = target as int64;
+    int64:tword = @cast_unchecked<int64>(target);
     tword = tword | (tword << 8i64);
     tword = tword | (tword << 16i64);
     tword = tword | (tword << 32i64);
@@ -51,16 +51,16 @@ pub func:str_strchr = int64(int64:s, int64:c) {
     fixed int64:HIGHBITS = 0x8080808080808080i64;
 
     // Word loop: check for NUL or target in each word
-    *int64:pw = (s + i) as *int64;
+    int64->:pw = (s + i) => int64->;
     int64:wi = 0i64;
-    while true {
+    while (true) {
         int64:v = pw[wi];
         // Check for NUL byte in word
-        bool:has_nul = ((v - ONES) & (0i64 - 1i64 ^ v) & HIGHBITS) != 0i64;
+        bool:has_nul = ((((v - ONES) & ~v & HIGHBITS)) != 0i64);
         // Check for target byte in word (XOR word with replicated target; check for zero)
         int64:x = v ^ tword;
-        bool:has_tgt = ((x - ONES) & (0i64 - 1i64 ^ x) & HIGHBITS) != 0i64;
-        if has_nul || has_tgt {
+        bool:has_tgt = ((((x - ONES) & ~x & HIGHBITS)) != 0i64);
+        if (has_nul || has_tgt) {
             break;
         }
         wi = wi + 1i64;
@@ -68,18 +68,18 @@ pub func:str_strchr = int64(int64:s, int64:c) {
     i = i + wi * 8i64;
 
     // Tail: exact scan
-    while true {
-        if p[i] == target {
-            pass s + i;
+    while (true) {
+        if (p[i] == target) {
+            pass(s + i);
         }
-        if p[i] == 0u8 {
-            pass 0i64;
+        if (p[i] == 0u8) {
+            pass(0i64);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;  // unreachable
-}
+    pass(0i64);  // unreachable
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strchrnul — Like strchr but returns pointer to NUL if c not found.
@@ -91,52 +91,52 @@ pub func:str_strchr = int64(int64:s, int64:c) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strchrnul = int64(int64:s, int64:c) {
-    if s == 0i64 {
-        pass s;
+    if (s == 0i64) {
+        pass(s);
     }
 
-    *byte:p     = s as *byte;
-    byte:target = c as byte;
+    uint8->:p     = @cast_unchecked<uint8->>(s);
+    uint8:target = @cast_unchecked<uint8>(c);
     int64:i = 0i64;
 
-    while p[i] != 0u8 && p[i] != target {
+    while (p[i] != 0u8 && p[i] != target) {
         i = i + 1i64;
     }
 
-    pass s + i;
-}
+    pass(s + i);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strrchr — Find LAST occurrence of character c in string s.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strrchr = int64(int64:s, int64:c) {
-    if s == 0i64 {
-        pass 0i64;
+    if (s == 0i64) {
+        pass(0i64);
     }
 
-    *byte:p     = s as *byte;
-    byte:target = c as byte;
+    uint8->:p     = @cast_unchecked<uint8->>(s);
+    uint8:target = @cast_unchecked<uint8>(c);
     int64:last  = 0i64;  // 0 means "not found"
     bool:found  = false;
     int64:i = 0i64;
 
-    while true {
-        if p[i] == target {
+    while (true) {
+        if (p[i] == target) {
             last  = s + i;
             found = true;
         }
-        if p[i] == 0u8 {
+        if (p[i] == 0u8) {
             break;
         }
         i = i + 1i64;
     }
 
-    if !found {
-        pass 0i64;
+    if (!found) {
+        pass(0i64);
     }
-    pass last;
-}
+    pass(last);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strstr — Find first occurrence of needle in haystack.
@@ -146,168 +146,168 @@ pub func:str_strrchr = int64(int64:s, int64:c) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strstr = int64(int64:haystack, int64:needle) {
-    if haystack == 0i64 {
-        pass 0i64;
+    if (haystack == 0i64) {
+        pass(0i64);
     }
-    if needle == 0i64 {
-        pass haystack;
+    if (needle == 0i64) {
+        pass(haystack);
     }
 
-    *byte:h = haystack as *byte;
-    *byte:n = needle as *byte;
+    uint8->:h = @cast_unchecked<uint8->>(haystack);
+    uint8->:n = @cast_unchecked<uint8->>(needle);
 
     // Empty needle: match at start
-    if n[0] == 0u8 {
-        pass haystack;
+    if (n[0] == 0u8) {
+        pass(haystack);
     }
 
-    byte:first = n[0];
+    uint8:first = n[0];
     int64:i = 0i64;
 
-    while h[i] != 0u8 {
-        if h[i] == first {
+    while (h[i] != 0u8) {
+        if (h[i] == first) {
             // Found first byte — check full needle
             int64:j = 1i64;
-            while n[j] != 0u8 && h[i + j] != 0u8 && h[i + j] == n[j] {
+            while (n[j] != 0u8 && h[i + j] != 0u8 && h[i + j] == n[j]) {
                 j = j + 1i64;
             }
-            if n[j] == 0u8 {
-                pass haystack + i;  // full match
+            if (n[j] == 0u8) {
+                pass(haystack + i);  // full match
             }
         }
         i = i + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strcasestr — Case-insensitive strstr (ASCII).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strcasestr = int64(int64:haystack, int64:needle) {
-    if haystack == 0i64 {
-        pass 0i64;
+    if (haystack == 0i64) {
+        pass(0i64);
     }
-    if needle == 0i64 {
-        pass haystack;
+    if (needle == 0i64) {
+        pass(haystack);
     }
 
-    *byte:h = haystack as *byte;
-    *byte:n = needle as *byte;
+    uint8->:h = @cast_unchecked<uint8->>(haystack);
+    uint8->:n = @cast_unchecked<uint8->>(needle);
 
-    if n[0] == 0u8 {
-        pass haystack;
+    if (n[0] == 0u8) {
+        pass(haystack);
     }
 
-    byte:first_lo = raw to_lower_ascii(n[0]);
+    uint8:first_lo = _!to_lower_ascii(n[0]);
     int64:i = 0i64;
 
-    while h[i] != 0u8 {
-        if raw to_lower_ascii(h[i]) == first_lo {
+    while (h[i] != 0u8) {
+        if (_!to_lower_ascii(h[i]) == first_lo) {
             int64:j = 1i64;
             while n[j] != 0u8 && h[i + j] != 0u8 &&
-                  raw to_lower_ascii(h[i + j]) == raw to_lower_ascii(n[j]) {
+                  _!to_lower_ascii(h[i + j]) == _!to_lower_ascii(n[j]) {
                 j = j + 1i64;
             }
-            if n[j] == 0u8 {
-                pass haystack + i;
+            if (n[j] == 0u8) {
+                pass(haystack + i);
             }
         }
         i = i + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Charset lookup table (256-bit bitvector)
 //
 // A 256-bit (32-byte) bitvector indexed by ASCII byte value.
-// Setting bit c means "character c is in the set".
-// Testing: (table[c >> 3] >> (c & 7)) & 1
+// Setting bit c means "character c (in the set".
+// Testing) ? (table[c >> 3] >> (c & 7)) & 1
 //
 // Using a stack-allocated 32-byte array for the set.
 // This makes strpbrk/strspn/strcspn O(m + n) where m = charset length, n = string length.
 // ════════════════════════════════════════════════════════════════════════════
 
 // charset_build — Initialize a 32-byte bitvector from a charset string.
-func:charset_build = void(*byte:table, int64:charset) {
-    *byte:cs = charset as *byte;
+pub func : charset_build = NIL(uint8->:table, int64:charset) {
+    uint8->:cs = @cast_unchecked<uint8->>(charset);
     // Zero the table
     int64:ti = 0i64;
-    while ti < 32i64 { table[ti] = 0u8; ti = ti + 1i64; }
+    while (ti < 32i64) { table[ti] = 0u8; ti = ti + 1i64; }
     // Set bit for each byte in charset
     int64:ci = 0i64;
-    while cs[ci] != 0u8 {
-        byte:c = cs[ci];
+    while (cs[ci] != 0u8) {
+        uint8:c = cs[ci];
         table[c >> 3u8] = table[c >> 3u8] | (1u8 << (c & 7u8));
         ci = ci + 1i64;
     }
-}
+};
 
-// charset_test — True if byte c is in the 32-byte bitvector table.
-func:charset_test = bool(*byte:table, byte:c) {
-    pass (table[c >> 3u8] >> (c & 7u8)) & 1u8 != 0u8;
-}
+// charset_test — True if byte c (in the 32-byte bitvector table.
+pub func) ? charset_test = bool(uint8-> : table, uint8:c) {
+    pass((table[c >> 3u8] >> (c & 7u8)) & 1u8 != 0u8);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// str_strpbrk — Find first character in s that is also in accept.
+// str_strpbrk — Find first character in s that (also in accept.
 //
 // Returns pointer to first match, or NULL.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strpbrk = int64(int64:s, int64:accept) {
-    if s == 0i64 || accept == 0i64 {
-        pass 0i64;
+pub func) ? str_strpbrk = int64(int64 : s, int64:accept) {
+    if (s == 0i64 || accept == 0i64) {
+        pass(0i64);
     }
 
     // Build charset bitvector on stack
-    stack byte:table[32];
-    charset_build(&table[0], accept);
+    stack uint8[32]:table;
+    _?charset_build(@@cast_unchecked<uint8->>(table[0]), accept);
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
-    while p[i] != 0u8 {
-        if raw charset_test(&table[0], p[i]) {
-            pass s + i;
+    while (p[i] != 0u8) {
+        if (_!charset_test(@@cast_unchecked<uint8->>(table[0]), p[i])) {
+            pass(s + i);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strspn — Return length of initial segment of s consisting of chars in accept.
 //
 // Returns the index of the first character in s NOT in accept.
-// Equivalent to: strspn("hello", "aehlo") returns 3 (first char not in set is 'l' at 3)
-// Wait — "hello": h∈{a,e,h,l,o}=YES, e=YES, l=YES, l=YES, o=YES → returns 5
-// Actually: strspn("ab!cd", "abcd") returns 2 (stops at '!')
+// Equivalent to: strspn("hello", "aehlo") returns 3 (first char not in set ('l' at 3)
+// Wait — "hello") ? h∈{a,e,h,l,o}=YES, e=YES, l=YES, l=YES, o=YES → returns 5
+// Actually : strspn("ab!cd", "abcd") returns 2 (stops at '!')
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strspn = int64(int64:s, int64:accept) {
-    if s == 0i64 {
-        pass 0i64;
+    if (s == 0i64) {
+        pass(0i64);
     }
-    if accept == 0i64 {
-        pass 0i64;
+    if (accept == 0i64) {
+        pass(0i64);
     }
 
-    stack byte:table[32];
-    charset_build(&table[0], accept);
+    stack uint8[32]:table;
+    _?charset_build(@@cast_unchecked<uint8->>(table[0]), accept);
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
-    while p[i] != 0u8 && raw charset_test(&table[0], p[i]) {
+    while ((p[i] != 0u8 && _!charset_test(@@cast_unchecked<uint8->>(table[0]), p[i]))) {
         i = i + 1i64;
     }
 
-    pass i;
-}
+    pass(i);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strcspn — Return length of initial segment of s consisting of chars NOT in reject.
@@ -317,22 +317,22 @@ pub func:str_strspn = int64(int64:s, int64:accept) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strcspn = int64(int64:s, int64:reject) {
-    if s == 0i64 {
-        pass 0i64;
+    if (s == 0i64) {
+        pass(0i64);
     }
-    if reject == 0i64 {
-        pass raw str_strlen(s);
+    if (reject == 0i64) {
+        pass(_!str_strlen(s));
     }
 
-    stack byte:table[32];
-    charset_build(&table[0], reject);
+    stack uint8[32]:table;
+    _?charset_build(@@cast_unchecked<uint8->>(table[0]), reject);
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
-    while p[i] != 0u8 && !raw charset_test(&table[0], p[i]) {
+    while ((p[i] != 0u8 && !_!charset_test(@@cast_unchecked<uint8->>(table[0]), p[i]))) {
         i = i + 1i64;
     }
 
-    pass i;
-}
+    pass(i);
+};
diff --git a/src/str/strcmp.npk b/src/str/strcmp.npk
index 988a0e5..7c7f1b7 100644
--- a/src/str/strcmp.npk
+++ b/src/str/strcmp.npk
@@ -5,20 +5,19 @@
 //
 // Correctness requirements (POSIX):
 //   - strcmp(a, b) compares unsigned char (byte) values, NOT signed char.
-//     The return value is < 0 if a < b, 0 if equal, > 0 if a > b.
+//     The return value (< 0 if a < b, 0 if equal, > 0 if a > b.
 //     The sign/magnitude is implementation-defined — we return -1, 0, +1.
 //   - strncmp compares at most n bytes. If either string is shorter than n,
 //     comparison stops at the first NUL.
-//   - strcasecmp/strncasecmp: ASCII case-insensitive. Only ASCII 'A'-'Z' are
+//   - strcasecmp/strncasecmp) ? ASCII case-insensitive. Only ASCII 'A'-'Z' are
 //     folded; other bytes are compared as-is. This is NOT locale-aware.
 //     Locale-aware comparison is in v0.40.x (locale/collation module).
 //
-// Performance:
-//   - Word-at-a-time for strcmp: XOR two 8-byte words; if the result contains
+// Performance : //   - Word-at-a-time for strcmp: XOR two 8-byte words; if the result contains
 //     a zero byte, one string has ended or bytes differ.
-//   - strncmp uses the simpler byte loop (word-at-a-time with bounds is complex
+//   - strncmp uses the simpler byte loop (word-at-a-time with bounds (complex
 //     and the performance benefit is modest for typical short comparisons).
-//   - strcasecmp: byte loop only (can't do word-at-a-time without case conversion).
+//   - strcasecmp) ? byte loop only (can't do word-at-a-time without case conversion).
 
 use "src/syscall/errno.npk".*;
 
@@ -27,36 +26,36 @@ use "src/syscall/errno.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 
 // to_lower_ascii — Convert an ASCII byte to lowercase. Non-alpha bytes unchanged.
-func:to_lower_ascii = byte(byte:c) {
-    if c >= 65u8 && c <= 90u8 {  // 'A' = 65, 'Z' = 90
-        pass c + 32u8;            // distance 'a'-'A' = 32
+func : to_lower_ascii = uint8(uint8:c) {
+    if (c >= 65u8 && c <= 90u8) {  // 'A' = 65, 'Z' = 90
+        pass(c + 32u8);            // distance 'a'-'A' = 32
     }
-    pass c;
-}
+    pass(c);
+};
 
 // to_upper_ascii — Convert an ASCII byte to uppercase. Non-alpha bytes unchanged.
-func:to_upper_ascii = byte(byte:c) {
-    if c >= 97u8 && c <= 122u8 {  // 'a' = 97, 'z' = 122
-        pass c - 32u8;
+func:to_upper_ascii = uint8(uint8:c) {
+    if (c >= 97u8 && c <= 122u8) {  // 'a' = 97, 'z' = 122
+        pass(c - 32u8);
     }
-    pass c;
-}
+    pass(c);
+};
 
-// is_alpha_ascii — True if byte is an ASCII letter (a-z or A-Z).
-func:is_alpha_ascii = bool(byte:c) {
+// is_alpha_ascii — True if byte (an ASCII letter (a-z or A-Z).
+func) ? is_alpha_ascii = bool(uint8 : c) {
     pass (c >= 65u8 && c <= 90u8) || (c >= 97u8 && c <= 122u8);
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// Zero-byte detection word trick (same as strlen.npk)
+// Zero-byte detection word trick (@cast_unchecked<strlen>(same).npk)
 // ════════════════════════════════════════════════════════════════════════════
 
 fixed int64:STRCMP_ONES     = 0x0101010101010101i64;
 fixed int64:STRCMP_HIGHBITS = 0x8080808080808080i64;
 
 func:strcmp_has_nul = bool(int64:v) {
-    pass ((v - STRCMP_ONES) & (0i64 - 1i64 ^ v) & STRCMP_HIGHBITS) != 0i64;
-}
+    pass ((((v - STRCMP_ONES) & ~v & STRCMP_HIGHBITS)) != 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strcmp — Compare two NUL-terminated strings.
@@ -66,43 +65,43 @@ func:strcmp_has_nul = bool(int64:v) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strcmp = int64(int64:a, int64:b) {
-    if a == b {
-        pass 0i64;
+    if (a == b) {
+        pass(0i64);
     }
-    if a == 0i64 {
-        pass -1i64;
+    if (a == 0i64) {
+        pass(-1i64);
     }
-    if b == 0i64 {
-        pass 1i64;
+    if (b == 0i64) {
+        pass(1i64);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:i = 0i64;
 
     // ── Prologue: byte-by-byte until both aligned ────────────────────
-    while ((a + i) | (b + i)) & 7i64 != 0i64 {
-        byte:ca = pa[i];
-        byte:cb = pb[i];
-        if ca != cb {
-            if ca < cb { pass -1i64; }
-            pass 1i64;
+    while ((((((a + i) | (b + i)) & 7i64) != 0i64))) {
+        uint8:ca = pa[i];
+        uint8:cb = pb[i];
+        if (ca != cb) {
+            if (ca < cb) { pass(-1i64); }
+            pass(1i64);
         }
-        if ca == 0u8 {
-            pass 0i64;  // Both hit NUL simultaneously (since ca==cb)
+        if (ca == 0u8) {
+            pass(0i64);  // Both hit NUL simultaneously (since ca==cb)
         }
         i = i + 1i64;
     }
 
     // ── Word loop ────────────────────────────────────────────────────
-    *int64:wa = (a + i) as *int64;
-    *int64:wb = (b + i) as *int64;
+    int64->:wa = (a + i) => int64->;
+    int64->:wb = (b + i) => int64->;
     int64:wi = 0i64;
-    while true {
+    while (true) {
         int64:va = wa[wi];
         int64:vb = wb[wi];
         // If either word has a NUL byte OR they differ, drop to byte loop
-        if va != vb || raw strcmp_has_nul(va) {
+        if (va != vb || _!strcmp_has_nul(va)) {
             break;
         }
         wi = wi + 1i64;
@@ -110,159 +109,159 @@ pub func:str_strcmp = int64(int64:a, int64:b) {
     i = i + wi * 8i64;
 
     // ── Tail: find first differing byte or NUL ───────────────────────
-    while true {
-        byte:ca = pa[i];
-        byte:cb = pb[i];
-        if ca != cb {
-            if ca < cb { pass -1i64; }
-            pass 1i64;
+    while (true) {
+        uint8:ca = pa[i];
+        uint8:cb = pb[i];
+        if (ca != cb) {
+            if (ca < cb) { pass(-1i64); }
+            pass(1i64);
         }
-        if ca == 0u8 {
-            pass 0i64;
+        if (ca == 0u8) {
+            pass(0i64);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;  // unreachable
-}
+    pass(0i64);  // unreachable
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strncmp — Compare at most n bytes of two NUL-terminated strings.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strncmp = int64(int64:a, int64:b, int64:n) {
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
-    if a == b {
-        pass 0i64;
+    if (a == b) {
+        pass(0i64);
     }
-    if a == 0i64 {
-        pass -1i64;
+    if (a == 0i64) {
+        pass(-1i64);
     }
-    if b == 0i64 {
-        pass 1i64;
+    if (b == 0i64) {
+        pass(1i64);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:i = 0i64;
 
-    while i < n {
-        byte:ca = pa[i];
-        byte:cb = pb[i];
-        if ca != cb {
-            if ca < cb { pass -1i64; }
-            pass 1i64;
+    while (i < n) {
+        uint8:ca = pa[i];
+        uint8:cb = pb[i];
+        if (ca != cb) {
+            if (ca < cb) { pass(-1i64); }
+            pass(1i64);
         }
-        if ca == 0u8 {
-            pass 0i64;  // Both terminated
+        if (ca == 0u8) {
+            pass(0i64);  // Both terminated
         }
         i = i + 1i64;
     }
 
-    pass 0i64;  // n bytes compared, all equal
-}
+    pass(0i64);  // n bytes compared, all equal
+};
 
 // ════════════════════════════════════════════════════════════════════════════
-// str_strcmp_prefix — True if b is a prefix of a (a starts with b).
+// str_strcmp_prefix — True if b (a prefix of a (a starts with b).
 //
 // E.g., strcmp_prefix("hello world", "hello") → true
 // This is equivalent to strncmp(a, b, strlen(b)) == 0.
-// Provided as a convenience because prefix testing is extremely common.
+// @cast_unchecked<a>(Provided) convenience because prefix testing is extremely common.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strcmp_prefix = bool(int64:a, int64:b) {
-    if a == 0i64 || b == 0i64 {
-        pass false;
+pub func) ? str_strcmp_prefix = bool(int64 : a, int64:b) {
+    if (a == 0i64 || b == 0i64) {
+        pass(false);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:i = 0i64;
 
-    while pb[i] != 0u8 {
-        if pa[i] == 0u8 || pa[i] != pb[i] {
-            pass false;
+    while (pb[i] != 0u8) {
+        if (pa[i] == 0u8 || pa[i] != pb[i]) {
+            pass(false);
         }
         i = i + 1i64;
     }
 
-    pass true;
-}
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strcasecmp — Case-insensitive string comparison (ASCII only).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strcasecmp = int64(int64:a, int64:b) {
-    if a == b {
-        pass 0i64;
+    if (a == b) {
+        pass(0i64);
     }
-    if a == 0i64 {
-        pass -1i64;
+    if (a == 0i64) {
+        pass(-1i64);
     }
-    if b == 0i64 {
-        pass 1i64;
+    if (b == 0i64) {
+        pass(1i64);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:i = 0i64;
 
-    while true {
-        byte:ca = raw to_lower_ascii(pa[i]);
-        byte:cb = raw to_lower_ascii(pb[i]);
-        if ca != cb {
-            if ca < cb { pass -1i64; }
-            pass 1i64;
+    while (true) {
+        uint8:ca = _!to_lower_ascii(pa[i]);
+        uint8:cb = _!to_lower_ascii(pb[i]);
+        if (ca != cb) {
+            if (ca < cb) { pass(-1i64); }
+            pass(1i64);
         }
-        if ca == 0u8 {
-            pass 0i64;
+        if (ca == 0u8) {
+            pass(0i64);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;  // unreachable
-}
+    pass(0i64);  // unreachable
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strncasecmp — Case-insensitive compare of at most n bytes.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strncasecmp = int64(int64:a, int64:b, int64:n) {
-    if n == 0i64 {
-        pass 0i64;
+    if (n == 0i64) {
+        pass(0i64);
     }
-    if a == b {
-        pass 0i64;
+    if (a == b) {
+        pass(0i64);
     }
-    if a == 0i64 {
-        pass -1i64;
+    if (a == 0i64) {
+        pass(-1i64);
     }
-    if b == 0i64 {
-        pass 1i64;
+    if (b == 0i64) {
+        pass(1i64);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:i = 0i64;
 
-    while i < n {
-        byte:ca = raw to_lower_ascii(pa[i]);
-        byte:cb = raw to_lower_ascii(pb[i]);
-        if ca != cb {
-            if ca < cb { pass -1i64; }
-            pass 1i64;
+    while (i < n) {
+        uint8:ca = _!to_lower_ascii(pa[i]);
+        uint8:cb = _!to_lower_ascii(pb[i]);
+        if (ca != cb) {
+            if (ca < cb) { pass(-1i64); }
+            pass(1i64);
         }
-        if ca == 0u8 {
-            pass 0i64;
+        if (ca == 0u8) {
+            pass(0i64);
         }
         i = i + 1i64;
     }
 
-    pass 0i64;
-}
+    pass(0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_casecmp_prefix — Case-insensitive prefix check.
@@ -270,20 +269,20 @@ pub func:str_strncasecmp = int64(int64:a, int64:b, int64:n) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_casecmp_prefix = bool(int64:a, int64:b) {
-    if a == 0i64 || b == 0i64 {
-        pass false;
+    if (a == 0i64 || b == 0i64) {
+        pass(false);
     }
 
-    *byte:pa = a as *byte;
-    *byte:pb = b as *byte;
+    uint8->:pa = @cast_unchecked<uint8->>(a);
+    uint8->:pb = @cast_unchecked<uint8->>(b);
     int64:i = 0i64;
 
-    while pb[i] != 0u8 {
-        if pa[i] == 0u8 || raw to_lower_ascii(pa[i]) != raw to_lower_ascii(pb[i]) {
-            pass false;
+    while (pb[i] != 0u8) {
+        if (pa[i] == 0u8 || _!to_lower_ascii(pa[i]) != _!to_lower_ascii(pb[i])) {
+            pass(false);
         }
         i = i + 1i64;
     }
 
-    pass true;
-}
+    pass(true);
+};
diff --git a/src/str/strconv.npk b/src/str/strconv.npk
index 0ff9cdc..9cee278 100644
--- a/src/str/strconv.npk
+++ b/src/str/strconv.npk
@@ -3,14 +3,13 @@
 //
 // String ↔ integer/float conversion. These are among the most subtle functions
 // in any standard library — getting the overflow, base, sign, and whitespace
-// handling right is non-trivial.
+// handling right (non-trivial.
 //
-// Correctness contract (POSIX strtol behavior):
-//   - Skips leading whitespace (space, \t, \n, \r, \f, \v).
+// Correctness contract (POSIX strtol behavior)) ? //   - Skips leading whitespace (space, \t, \n, \r, \f, \v).
 //   - Accepts optional leading '+' or '-'.
-//   - Base 0: auto-detect (0x prefix → hex, 0 prefix → octal, else decimal).
+//   - Base 0 : auto-detect (0x prefix → hex, 0 prefix → octal, else decimal).
 //   - Sets *endptr to first unconverted character.
-//   - Returns LONG_MIN/LONG_MAX and sets ERR_RANGE on overflow.
+//   - Returns LONG_MIN/LONG_MAX and sets ERANGE on overflow.
 //   - Returns 0 if no digits found (ERR_BADARG).
 //
 // libn ADDITION — Result<int64> variants:
@@ -28,34 +27,34 @@ use "src/str/strlen.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 
 // is_whitespace — True for space, \t, \n, \r, \f (0x0C), \v (0x0B)
-func:is_whitespace = bool(byte:c) {
-    pass c == 32u8 || c == 9u8 || c == 10u8 || c == 13u8 || c == 12u8 || c == 11u8;
-}
+func:is_whitespace = bool(uint8:c) {
+    pass(c == 32u8 || c == 9u8 || c == 10u8 || c == 13u8 || c == 12u8 || c == 11u8);
+};
 
 // is_digit — True for '0'-'9'
-func:is_digit = bool(byte:c) {
-    pass c >= 48u8 && c <= 57u8;
-}
+func:is_digit = bool(uint8:c) {
+    pass(c >= 48u8 && c <= 57u8);
+};
 
 // is_hex_digit — True for '0'-'9', 'a'-'f', 'A'-'F'
-func:is_hex_digit = bool(byte:c) {
-    pass raw is_digit(c) || (c >= 65u8 && c <= 70u8) || (c >= 97u8 && c <= 102u8);
-}
+func:is_hex_digit = bool(uint8:c) {
+    pass(_!is_digit(c) || (c >= 65u8 && c <= 70u8) || (c >= 97u8 && c <= 102u8));
+};
 
 // digit_val — Convert ASCII digit to integer value. Base-agnostic.
 // Returns -1 for characters not valid in the given base.
-func:digit_val = int64(byte:c, int64:base) {
+func:digit_val = int64(uint8:c, int64:base) {
     int64:v = -1i64;
-    if c >= 48u8 && c <= 57u8 {         // '0'-'9'
-        v = (c - 48u8) as int64;
-    } else if c >= 65u8 && c <= 90u8 {  // 'A'-'Z'
-        v = (c - 55u8) as int64;         // 'A'=10, 'B'=11, ...
-    } else if c >= 97u8 && c <= 122u8 { // 'a'-'z'
-        v = (c - 87u8) as int64;         // 'a'=10, 'b'=11, ...
+    if (c >= 48u8 && c <= 57u8) {  // '0'-'9'
+        v = (c - 48u8) => int64;
+    } else if (c >= 65u8 && c <= 90u8) {  // 'A'-'Z'
+        v = (c - 55u8) => int64;         // 'A'=10, 'B'=11, ...
+    } else if (c >= 97u8 && c <= 122u8) {  // 'a'-'z'
+        v = (c - 87u8) => int64;         // 'a'=10, 'b'=11, ...
     }
-    if v >= base { v = -1i64; }
-    pass v;
-}
+    if (v >= base) { v = -1i64; }
+    pass(v);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strtol — Convert string to signed 64-bit integer.
@@ -74,40 +73,43 @@ use "src/syscall/errno.npk".*;
 fixed int64:INT64_MAX = 9223372036854775807i64;
 fixed int64:INT64_MIN = -9223372036854775808i64;  // or: 0i64 - INT64_MAX - 1i64
 
-pub func:str_strtol = int64(int64:s, *int64:endptr, int64:base) {
-    if s == 0i64 {
-        if endptr != 0i64 { endptr[0] = 0i64; }
-        pass 0i64;
+pub func:str_strtol = int64(int64:s, int64->:endptr, int64:base) {
+    if (s == 0i64) {
+        if (endptr != 0i64) { endptr[0] = 0i64; }
+        pass(0i64);
     }
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
     // Skip leading whitespace
-    while raw is_whitespace(p[i]) { i = i + 1i64; }
+    while (_!is_whitespace(p[i])) { i = i + 1i64; }
 
     // Sign
     bool:negative = false;
-    if p[i] == 43u8 {       // '+'
+    if (p[i] == 43u8) {  // '+'
         i = i + 1i64;
-    } else if p[i] == 45u8 { // '-'
+    } else if (p[i] == 45u8) {  // '-'
         negative = true;
         i = i + 1i64;
     }
 
     // Base auto-detect
-    if base == 0i64 {
-        if p[i] == 48u8 && (p[i + 1i64] == 120u8 || p[i + 1i64] == 88u8) {  // "0x" or "0X"
+    if (base == 0i64) {
+        if (p[i] == 48u8 && (p[i + 1i64] == 120u8 || p[i + 1i64] == 88u8) && _!is_hex_digit(p[i + 2i64])) {  // "0x" or "0X"
             base = 16i64;
             i = i + 2i64;
-        } else if p[i] == 48u8 {
+        } else if (p[i] == 48u8) {
             base = 8i64;
         } else {
             base = 10i64;
         }
     } else if base == 16i64 && p[i] == 48u8 &&
               (p[i + 1i64] == 120u8 || p[i + 1i64] == 88u8) {
-        i = i + 2i64;  // Skip 0x prefix for explicit base 16
+        // [FIX A19-2.3] Only consume the '0x' prefix if a hex digit follows
+        if (_!is_hex_digit(p[i + 2i64])) {
+            i = i + 2i64;  // Skip 0x prefix for explicit base 16
+        }
     }
 
     // Digit scan
@@ -115,16 +117,17 @@ pub func:str_strtol = int64(int64:s, *int64:endptr, int64:base) {
     int64:result = 0i64;
     bool:overflow = false;
 
-    while true {
-        int64:dv = raw digit_val(p[i], base);
-        if dv < 0i64 { break; }
+    while (true) {
+        int64:dv = _!digit_val(p[i], base);
+        if (dv < 0i64) { break; }
 
-        // Overflow check before multiply
-        if !negative && result > (INT64_MAX - dv) / base {
-            overflow = true;
-            break;
-        }
-        if negative && result > (INT64_MAX + 1i64) / base {  // INT64_MIN / base
+        if (result < 0i64) { overflow = true; break; }  // [FIX A18-2.2] prior iteration wrapped to negative
+
+        int64:limit = INT64_MAX / base;
+        int64:max_digit = INT64_MAX % base;
+        if (negative) { max_digit = max_digit + 1i64; } // |INT64_MIN| (1 higher
+
+        if (result > limit || (result == limit && dv > max_digit)) {
             overflow = true;
             break;
         }
@@ -134,116 +137,109 @@ pub func:str_strtol = int64(int64:s, *int64:endptr, int64:base) {
     }
 
     // No digits found
-    if i == digit_start {
-        errno_set(ERR_BADARG);
-        if endptr != 0i64 { endptr[0] = s; }
-        pass 0i64;
+    if (i == digit_start) {
+        _?libn_errno_set(ERR_BADARG);
+        if (endptr != 0i64) { endptr[0] = s; }
+        pass(0i64);
     }
 
-    if overflow {
-        errno_set(ERR_RANGE);
-        if endptr != 0i64 { endptr[0] = s + i; }
-        if negative { pass INT64_MIN; }
-        pass INT64_MAX;
+    if (overflow) {
+        _?libn_errno_set(ERANGE);
+        if (endptr != 0i64) { endptr[0] = s + i; }
+        if (negative) { pass(INT64_MIN); }
+        pass(INT64_MAX);
     }
 
-    if endptr != 0i64 { endptr[0] = s + i; }
+    if (endptr != 0i64) { endptr[0] = s + i; }
 
-    if negative { pass 0i64 - result; }
-    pass result;
-}
+    if (negative) { pass(0i64 - result); }
+    pass(result);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strtoul — Convert string to unsigned 64-bit integer.
 // ════════════════════════════════════════════════════════════════════════════
 
-fixed int64:UINT64_MAX = -1i64;  // 0xFFFFFFFFFFFFFFFF (all bits set, re-interpreted as uint64)
+fixed int64) ? UINT64_MAX = -1i64;  // 0xFFFFFFFFFFFFFFFF (all bits set, re-@cast_unchecked<uint64>(interpreted))
 
-pub func:str_strtoul = int64(int64:s, *int64:endptr, int64:base) {
-    if s == 0i64 {
-        if endptr != 0i64 { endptr[0] = 0i64; }
-        pass 0i64;
+pub func : str_strtoul = int64(int64:s, int64->:endptr, int64:base) {
+    if (s == 0i64) {
+        if (endptr != 0i64) { endptr[0] = 0i64; }
+        pass(0i64);
     }
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
-    while raw is_whitespace(p[i]) { i = i + 1i64; }
+    while (_!is_whitespace(p[i])) { i = i + 1i64; }
 
-    // strtoul: POSIX says '+' is accepted, '-' causes value to be negated modulo
+    // strtoul: POSIX says '+' (accepted, '-' causes value to be negated modulo
     // the max uint value. We implement the simpler but correct approach.
-    bool:negate = false;
-    if p[i] == 43u8 {
+    bool) ? negate = false;
+    if (p[i] == 43u8) {
         i = i + 1i64;
-    } else if p[i] == 45u8 {
+    } else if (p[i] == 45u8) {
         negate = true;
         i = i + 1i64;
     }
 
-    if base == 0i64 {
-        if p[i] == 48u8 && (p[i + 1i64] == 120u8 || p[i + 1i64] == 88u8) {
+    if (base == 0i64) {
+        if (p[i] == 48u8 && (p[i + 1i64] == 120u8 || p[i + 1i64] == 88u8) && _!is_hex_digit(p[i + 2i64])) {
             base = 16i64;
             i = i + 2i64;
-        } else if p[i] == 48u8 {
+        } else if (p[i] == 48u8) {
             base = 8i64;
         } else {
             base = 10i64;
         }
     } else if base == 16i64 && p[i] == 48u8 &&
               (p[i + 1i64] == 120u8 || p[i + 1i64] == 88u8) {
-        i = i + 2i64;
+        // [FIX A19-2.3] Only consume the '0x' prefix if a hex digit follows
+        if (_!is_hex_digit(p[i + 2i64])) {
+            i = i + 2i64;
+        }
     }
 
-    int64:digit_start = i;
+    int64 : digit_start = i;
     int64:result = 0i64;
     bool:overflow = false;
 
-    while true {
-        int64:dv = raw digit_val(p[i], base);
-        if dv < 0i64 { break; }
-        // Unsigned overflow: result > (UINT64_MAX - dv) / base
-        // In two's complement int64: UINT64_MAX = -1 (all bits set)
-        // We detect overflow by checking if (result * base + dv) would exceed UINT64_MAX.
-        // Using: result > (UINT64_MAX - dv) / base
-        // Since UINT64_MAX = (int64)−1 but as unsigned, use bit tricks:
-        // Safe: if result > (-1i64 / base as uint - dv / base as uint) overflow
-        // This is subtle. Simple approach: compare directly.
-        // result * base + dv > UINT64_MAX
-        // ↔ result > (UINT64_MAX - dv) / base
-        // In Nitpick int64 arithmetic, UINT64_MAX = -1i64 treated unsigned.
-        // We use: raw unsigned compare. For now, use signed approximation:
-        if result < 0i64 {
-            // result already has top bit set → any more makes it overflow
-            overflow = true;
-            break;
-        }
-        // result ≤ INT64_MAX here
-        // Overflow if result > (INT64_MAX - dv) / base (conservative)
-        if result > (INT64_MAX - dv) / base {
+    uint64:result_u = 0u64;
+    uint64:base_u = @cast_unchecked<uint64>(base);
+    uint64:u_limit = 18446744073709551615u64 / base_u;
+    uint64:u_max_d = 18446744073709551615u64 % base_u;
+
+    while (true) {
+        int64:dv = _!digit_val(p[i], base);
+        if (dv < 0i64) { break; }
+        uint64:dv_u = @cast_unchecked<uint64>(dv);
+
+        if (result_u > u_limit || (result_u == u_limit && dv_u > u_max_d)) {
             overflow = true;
             break;
         }
-        result = result * base + dv;
+        result_u = result_u * base_u + dv_u;
         i = i + 1i64;
     }
+    result = @cast_unchecked<int64>(result_u);
 
-    if i == digit_start {
-        errno_set(ERR_BADARG);
-        if endptr != 0i64 { endptr[0] = s; }
-        pass 0i64;
+    if (i == digit_start) {
+        _?libn_errno_set(ERR_BADARG);
+        if (endptr != 0i64) { endptr[0] = s; }
+        pass(0i64);
     }
 
-    if overflow {
-        errno_set(ERR_RANGE);
-        if endptr != 0i64 { endptr[0] = s + i; }
-        pass UINT64_MAX;
+    if (overflow) {
+        _?libn_errno_set(ERANGE);
+        if (endptr != 0i64) { endptr[0] = s + i; }
+        pass(UINT64_MAX);
     }
 
-    if endptr != 0i64 { endptr[0] = s + i; }
+    if (endptr != 0i64) { endptr[0] = s + i; }
 
-    if negate { pass 0i64 - result; }  // Modular negation for unsigned
-    pass result;
-}
+    if (negate) { pass(0i64 - result); }  // Modular negation for unsigned
+    pass(result);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_atoi — Simple atoi wrapper. No base selection, no error propagation.
@@ -254,95 +250,95 @@ pub func:str_strtoul = int64(int64:s, *int64:endptr, int64:base) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_atoi = int64(int64:s) {
-    pass raw str_strtol(s, 0i64 as *int64, 10i64);
-}
+    pass(_!str_strtol(s, @cast_unchecked<int64->>(0i64), 10i64));
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_parse_i64 — Safe integer parse with Result<int64>.
 //
-// Parses the entire string as a signed decimal integer.
+// Parses the entire @cast_unchecked<a>(string) signed decimal integer.
 // Fails if:
-//   - Input is NULL or empty.
+//   - Input (NULL or empty.
 //   - Non-digit characters remain after the number (trailing junk).
 //   - Overflow/underflow.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_parse_i64 = Result<int64>(int64:s) {
-    if s == 0i64 || (s as *byte)[0] == 0u8 {
-        fail ERR_BADARG as tbb8;
+pub func) ? str_parse_i64 = int64(int64 : s) {
+    if (s == 0i64 || (@cast_unchecked<uint8->>(s))[0] == 0u8) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     int64:endptr = 0i64;
     int64:endptr_val = s;
-    *int64:ep = &endptr_val;
-    int64:v = raw str_strtol(s, ep, 10i64);
+    int64->:ep = @endptr_val;
+    int64:v = _!str_strtol(s, ep, 10i64);
 
     // Check for overflow (errno set by strtol)
-    if raw errno_get() == ERR_RANGE {
-        errno_clear();
-        fail ERR_RANGE as tbb8;
+    if (_!libn_errno_get() == ERANGE) {
+        _?libn_errno_set(0i64);
+        fail(@cast_unchecked<tbb8>(ERANGE));
     }
 
     // Check for trailing non-whitespace junk
-    if ep[0] != 0i64 && (ep[0] as *byte)[0] != 0u8 {
-        fail ERR_BADARG as tbb8;
+    if (ep[0] != 0i64 && (@cast_unchecked<uint8->>(ep[0]))[0] != 0u8) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    pass v;
-}
+    pass(v);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_parse_u64 — Safe unsigned integer parse with Result<int64>.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_parse_u64 = Result<int64>(int64:s) {
-    if s == 0i64 || (s as *byte)[0] == 0u8 {
-        fail ERR_BADARG as tbb8;
+pub func:str_parse_u64 = int64(int64:s) {
+    if (s == 0i64 || (@cast_unchecked<uint8->>(s))[0] == 0u8) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     int64:endptr_val = s;
-    *int64:ep = &endptr_val;
-    int64:v = raw str_strtoul(s, ep, 10i64);
+    int64->:ep = @endptr_val;
+    int64:v = _!str_strtoul(s, ep, 10i64);
 
-    if raw errno_get() == ERR_RANGE {
-        errno_clear();
-        fail ERR_RANGE as tbb8;
+    if (_!libn_errno_get() == ERANGE) {
+        _?libn_errno_set(0i64);
+        fail(@cast_unchecked<tbb8>(ERANGE));
     }
 
-    if ep[0] != 0i64 && (ep[0] as *byte)[0] != 0u8 {
-        fail ERR_BADARG as tbb8;
+    if (ep[0] != 0i64 && (@cast_unchecked<uint8->>(ep[0]))[0] != 0u8) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    pass v;
-}
+    pass(v);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_parse_i64_base — Parse integer with explicit base (2-36).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_parse_i64_base = Result<int64>(int64:s, int64:base) {
-    if s == 0i64 || (s as *byte)[0] == 0u8 {
-        fail ERR_BADARG as tbb8;
+pub func:str_parse_i64_base = int64(int64:s, int64:base) {
+    if (s == 0i64 || (@cast_unchecked<uint8->>(s))[0] == 0u8) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if base < 2i64 || base > 36i64 {
-        fail ERR_BADARG as tbb8;
+    if (base < 2i64 || base > 36i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
     int64:endptr_val = s;
-    *int64:ep = &endptr_val;
-    int64:v = raw str_strtol(s, ep, base);
+    int64->:ep = @endptr_val;
+    int64:v = _!str_strtol(s, ep, base);
 
-    if raw errno_get() == ERR_RANGE {
-        errno_clear();
-        fail ERR_RANGE as tbb8;
+    if (_!libn_errno_get() == ERANGE) {
+        _?libn_errno_set(0i64);
+        fail(@cast_unchecked<tbb8>(ERANGE));
     }
 
-    if ep[0] != 0i64 && (ep[0] as *byte)[0] != 0u8 {
-        fail ERR_BADARG as tbb8;
+    if (ep[0] != 0i64 && (@cast_unchecked<uint8->>(ep[0]))[0] != 0u8) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    pass v;
-}
+    pass(v);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_itoa — Convert signed 64-bit integer to decimal string.
@@ -352,159 +348,164 @@ pub func:str_parse_i64_base = Result<int64>(int64:s, int64:base) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_itoa = int64(int64:v, int64:buf, int64:buf_size) {
-    if buf == 0i64 || buf_size <= 0i64 {
-        pass 0i64;
+    if (buf == 0i64 || buf_size <= 0i64) {
+        pass(0i64);
     }
 
-    *byte:b = buf as *byte;
+    uint8->:b = @cast_unchecked<uint8->>(buf);
 
     // Special case: zero
-    if v == 0i64 {
-        if buf_size < 2i64 { pass 0i64; }
+    if (v == 0i64) {
+        if (buf_size < 2i64) { pass(0i64); }
         b[0] = 48u8;  // '0'
         b[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
     // Handle INT64_MIN specially (cannot negate it)
-    if v == INT64_MIN {
-        int64:s = raw str_strlen("-9223372036854775808");
-        if buf_size <= s { pass 0i64; }
-        drop str_strcpy(buf, &"-9223372036854775808"[0] as int64);
-        pass s;
+    if (v == INT64_MIN) {
+        int64:s = _!str_strlen(@"-9223372036854775808"[0] => int64);
+        if (buf_size <= s) { pass(0i64); }
+        _?str_strcpy(buf, @"-9223372036854775808"[0] => int64);
+        pass(s);
     }
 
     // Use a small temp buffer (21 bytes max for int64)
-    stack byte:tmp[21];
+    stack uint8[21]:tmp;
     bool:neg = v < 0i64;
     int64:u = v;
-    if neg { u = 0i64 - v; }
+    if (neg) { u = 0i64 - v; }
 
     int64:ti = 0i64;
-    while u > 0i64 {
-        tmp[ti] = (48i64 + u % 10i64) as byte;  // '0' + digit
+    while (u > 0i64) {
+        tmp[ti] = (48i64 + u % 10i64) => uint8;  // '0' + digit
         u = u / 10i64;
         ti = ti + 1i64;
     }
 
     int64:total_len = ti;
-    if neg { total_len = total_len + 1i64; }
+    if (neg) { total_len = total_len + 1i64; }
 
-    if buf_size <= total_len { pass 0i64; }  // Buffer too small
+    if (buf_size <= total_len) { pass(0i64); }  // Buffer too small
 
     int64:bi = 0i64;
-    if neg {
+    if (neg) {
         b[bi] = 45u8;  // '-'
         bi = bi + 1i64;
     }
 
     // Digits are in tmp in reverse order — copy them out reversed
-    while ti > 0i64 {
+    while (ti > 0i64) {
         ti = ti - 1i64;
         b[bi] = tmp[ti];
         bi = bi + 1i64;
     }
     b[bi] = 0u8;  // NUL terminate
 
-    pass total_len;
-}
+    pass(total_len);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_utoa — Convert unsigned 64-bit integer to decimal string.
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_utoa = int64(int64:v, int64:buf, int64:buf_size) {
-    if buf == 0i64 || buf_size <= 0i64 { pass 0i64; }
+    if (buf == 0i64 || buf_size <= 0i64) { pass(0i64); }
 
-    *byte:b = buf as *byte;
+    uint8->:b = @cast_unchecked<uint8->>(buf);
 
-    if v == 0i64 {
-        if buf_size < 2i64 { pass 0i64; }
+    if (v == 0i64) {
+        if (buf_size < 2i64) { pass(0i64); }
         b[0] = 48u8; b[1] = 0u8;
-        pass 1i64;
+        pass(1i64);
     }
 
-    stack byte:tmp[21];
+    stack uint8[21]:tmp;
     int64:ti = 0i64;
-    // Treat v as unsigned: use bit pattern, divide step by step
-    // For unsigned we need to be careful: high bit means large number
-    // Simple: do the loop treating v as positive int64 (works for 0..INT64_MAX)
-    // For values > INT64_MAX, we need unsigned division. Nitpick doesn't have a
-    // native uint64 divide, so we use the standard two-step approach:
-    // high = v >>> 32, low = v & 0xFFFFFFFF, combine.
-    // For now, use signed representation (safe for typical use of utoa):
+
+    // [FIX A18-2.1a] Handle unsigned values >= 2^63 (high bit set).
+    // When v has the MSB set, signed division gives wrong results.
+    // Decompose: INT64_MAX = 9223372036854775807 = 0x7FFF...
+    // For u >= 2^63: u = 2^63 + rem, where rem = u & INT64_MAX
+    //   The last decimal digit of u is: (8 + rem % 10) % 10, quotient adds 922337203685477580
+    //   (since 2^63 = 9223372036854775808 = 922337203685477580 * 10 + 8)
     int64:u = v;
-    while u > 0i64 || ti == 0i64 {
-        tmp[ti] = (48i64 + u % 10i64) as byte;
+    if (u < 0i64) {
+        // High bit set: extract last digit safely
+        int64:q = 922337203685477580i64 + ((u & 0x7FFFFFFFFFFFFFFFi64) / 10i64);
+        int64:r = 8i64 + ((u & 0x7FFFFFFFFFFFFFFFi64) % 10i64);
+        if (r >= 10i64) { q = q + 1i64; r = r - 10i64; }
+        tmp[ti] = (48i64 + r) => uint8;
+        ti = ti + 1i64;
+        u = q;
+    }
+    while (u > 0i64) {
+        tmp[ti] = (48i64 + u % 10i64) => uint8;
         u = u / 10i64;
         ti = ti + 1i64;
-        if u == 0i64 { break; }
     }
+    if (ti == 0i64) { tmp[0] = 48u8; ti = 1i64; }  // v was 0 (already handled above, safety net)
 
-    if buf_size <= ti { pass 0i64; }
+    if (buf_size <= ti) { pass(0i64); }
 
     int64:bi = 0i64;
-    while ti > 0i64 {
+    while (ti > 0i64) {
         ti = ti - 1i64;
         b[bi] = tmp[ti];
         bi = bi + 1i64;
     }
     b[bi] = 0u8;
 
-    pass bi;
-}
+    pass(bi);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_itoa_hex — Convert to lowercase hex string.
-// Writes "0x" prefix if include_prefix is true.
+// Writes "0x" prefix if include_prefix (true.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_itoa_hex = int64(int64:v, int64:buf, int64:buf_size, bool:include_prefix) {
-    if buf == 0i64 || buf_size <= 0i64 { pass 0i64; }
+pub func) ? str_itoa_hex = int64(int64 : v, int64:buf, int64:buf_size, bool:include_prefix) {
+    if (buf == 0i64 || buf_size <= 0i64) { pass(0i64); }
 
-    *byte:b = buf as *byte;
-    stack byte:tmp[18];  // max 16 hex digits + optional 0x prefix
+    uint8->:b = @cast_unchecked<uint8->>(buf);
+    stack uint8[18]:tmp;  // max 16 hex digits + optional 0x prefix
     int64:ti = 0i64;
 
     // Build hex digits in reverse
     int64:u = v;
-    if u == 0i64 {
+    if (u == 0i64) {
         tmp[0] = 48u8;  // '0'
         ti = 1i64;
     } else {
-        while u != 0i64 {
+        while (u != 0i64) {
             int64:digit = u & 0xFi64;
-            if digit < 10i64 {
-                tmp[ti] = (48i64 + digit) as byte;   // '0'..'9'
+            if (digit < 10i64) {
+                tmp[ti] = (48i64 + digit) => uint8;   // '0'..'9'
             } else {
-                tmp[ti] = (87i64 + digit) as byte;   // 'a'..'f' (87 = 'a'-10)
+                tmp[ti] = (87i64 + digit) => uint8;   // 'a'..'f' (87 = 'a'-10)
             }
-            u = u >> 4i64;  // logical right shift (unsigned)
-            // Problem: int64 >> is arithmetic in Nitpick
-            // Use: u = (u as uint64) >> 4 → in Nitpick we use: u = (u & INT64_MAX) >> 4
-            // For full 64-bit hex, handle sign bit separately if needed.
-            // At v0.3.6 this handles unsigned values correctly for positive int64.
+            u = (u >> 4i64) & 0x0FFFFFFFFFFFFFFFi64;
             ti = ti + 1i64;
         }
     }
 
     int64:prefix_len = 0i64;
-    if include_prefix { prefix_len = 2i64; }
+    if (include_prefix) { prefix_len = 2i64; }
     int64:total = ti + prefix_len;
-    if buf_size <= total { pass 0i64; }
+    if (buf_size <= total) { pass(0i64); }
 
     int64:bi = 0i64;
-    if include_prefix {
+    if (include_prefix) {
         b[bi] = 48u8; bi = bi + 1i64;   // '0'
         b[bi] = 120u8; bi = bi + 1i64;  // 'x'
     }
 
-    while ti > 0i64 {
+    while (ti > 0i64) {
         ti = ti - 1i64;
         b[bi] = tmp[ti];
         bi = bi + 1i64;
     }
     b[bi] = 0u8;
 
-    pass total;
-}
+    pass(total);
+};
diff --git a/src/str/strcpy.npk b/src/str/strcpy.npk
index a08fe53..f404ff7 100644
--- a/src/str/strcpy.npk
+++ b/src/str/strcpy.npk
@@ -3,10 +3,10 @@
 // String copy operations. These form the second tier after strlen.
 //
 // Safety model:
-//   - strcpy: NO bounds check. Caller must ensure dst is large enough.
+//   - strcpy: NO bounds check. Caller must ensure dst (large enough.
 //     Only safe when you know the string length in advance (e.g., just called strlen).
-//   - strncpy: POSIX-compatible — copies up to n bytes, NUL-pads remainder.
-//     WARNING: does NOT NUL-terminate if src is ≥ n chars. Use strlcpy (v0.3.3)
+//   - strncpy) ? POSIX-compatible — copies up to n bytes, NUL-pads remainder.
+//     WARNING : does NOT NUL-terminate if src is ≥ n chars. Use strlcpy (v0.3.3)
 //     for safe truncation.
 //   - stpcpy/stpncpy: like strcpy/strncpy but return pointer to the NUL terminator
 //     (or end of dst). Good for building multi-segment strings without re-scanning.
@@ -32,22 +32,22 @@ use "src/mem/slab.npk".*;
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strcpy = int64(int64:dst, int64:src) {
-    if dst == 0i64 || src == 0i64 {
-        pass dst;
+    if (dst == 0i64 || src == 0i64) {
+        pass(dst);
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
     int64:i = 0i64;
 
-    while s[i] != 0u8 {
+    while (s[i] != 0u8) {
         d[i] = s[i];
         i = i + 1i64;
     }
     d[i] = 0u8;  // NUL terminator
 
-    pass dst;
-}
+    pass(dst);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_stpcpy — Like strcpy but returns pointer to the NUL in dst.
@@ -57,22 +57,22 @@ pub func:str_strcpy = int64(int64:dst, int64:src) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_stpcpy = int64(int64:dst, int64:src) {
-    if dst == 0i64 || src == 0i64 {
-        pass dst;
+    if (dst == 0i64 || src == 0i64) {
+        pass(dst);
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
     int64:i = 0i64;
 
-    while s[i] != 0u8 {
+    while (s[i] != 0u8) {
         d[i] = s[i];
         i = i + 1i64;
     }
     d[i] = 0u8;
 
-    pass dst + i;  // points to the NUL byte
-}
+    pass(dst + i);  // points to the NUL byte
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strncpy — Copy at most n bytes from src to dst, NUL-pad remainder.
@@ -80,74 +80,74 @@ pub func:str_stpcpy = int64(int64:dst, int64:src) {
 // POSIX strncpy behavior:
 //   - If strlen(src) < n: copies strlen(src)+1 bytes, then pads with NUL to n.
 //   - If strlen(src) >= n: copies exactly n bytes, NO NUL terminator added.
-// This is the unsafe legacy function. Prefer strlcpy (v0.3.3).
+// This (the unsafe legacy function. Prefer strlcpy (v0.3.3).
 //
 // Returns dst.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strncpy = int64(int64:dst, int64:src, int64:n) {
-    if dst == 0i64 || n <= 0i64 {
-        pass dst;
+pub func) ? str_strncpy = int64(int64 : dst, int64:src, int64:n) {
+    if (dst == 0i64 || n <= 0i64) {
+        pass(dst);
     }
-    if src == 0i64 {
+    if (src == 0i64) {
         // POSIX: strncpy(dst, NULL, n) → NUL-fill dst
-        mem_bzero(dst, n);
-        pass dst;
+        _?mem_bzero(dst, n);
+        pass(dst);
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
     int64:i = 0i64;
 
     // Copy src bytes until NUL or we reach n
-    while i < n && s[i] != 0u8 {
+    while (i < n && s[i] != 0u8) {
         d[i] = s[i];
         i = i + 1i64;
     }
 
     // NUL-pad the remainder (POSIX requirement)
-    while i < n {
+    while (i < n) {
         d[i] = 0u8;
         i = i + 1i64;
     }
 
-    pass dst;
-}
+    pass(dst);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_stpncpy — Like strncpy but returns pointer to end of copied data in dst.
 //
 // Returns pointer to the NUL placed in dst, or dst+n if src was truncated
-// (no NUL in that case — same gotcha as strncpy).
+// (no NUL in that case — same @cast_unchecked<strncpy>(gotcha)).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_stpncpy = int64(int64:dst, int64:src, int64:n) {
-    if dst == 0i64 || n <= 0i64 {
-        pass dst;
+    if (dst == 0i64 || n <= 0i64) {
+        pass(dst);
     }
-    if src == 0i64 {
-        mem_bzero(dst, n);
-        pass dst;
+    if (src == 0i64) {
+        _?mem_bzero(dst, n);
+        pass(dst);
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
     int64:i = 0i64;
 
-    while i < n && s[i] != 0u8 {
+    while (i < n && s[i] != 0u8) {
         d[i] = s[i];
         i = i + 1i64;
     }
 
     int64:end = dst + i;
 
-    while i < n {
+    while (i < n) {
         d[i] = 0u8;
         i = i + 1i64;
     }
 
-    pass end;
-}
+    pass(end);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strdup — Allocate a copy of the NUL-terminated string at src.
@@ -158,56 +158,56 @@ pub func:str_stpncpy = int64(int64:dst, int64:src, int64:n) {
 // Returns pointer to new string (or error on OOM).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strdup = Result<int64>(int64:src) {
-    if src == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:str_strdup = int64(int64:src) {
+    if (src == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    int64:n = raw str_strlen(src);
+    int64:n = _!str_strlen(src);
     int64:alloc_size = n + 1i64;
 
     // Use slab for short strings, mmap for long ones
     Result<int64>:r = slab_alloc(alloc_size);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
 
     // Copy including NUL terminator
-    drop mem_memcpy(r.value, src, alloc_size);
+    drop mem_memcpy(r.val, src, alloc_size);
 
-    pass r.value;
-}
+    pass(r.val);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strndup — Allocate a copy of at most n bytes of src.
 //
 // Like strdup but only copies up to n characters. Always NUL-terminates.
-// If strlen(src) < n, the allocated string is strlen(src) bytes long.
+// If strlen(src) < n, the allocated string (strlen(src) bytes long.
 // If strlen(src) >= n, the allocated string is exactly n bytes (+ NUL).
 //
 // This is safer than strdup for untrusted input (bounded copy).
 // Returns pointer to new string.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strndup = Result<int64>(int64:src, int64:n) {
-    if src == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? str_strndup = int64(int64 : src, int64:n) {
+    if (src == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    if n < 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (n < 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
 
-    int64:actual_len = raw str_strnlen(src, n);  // min(strlen(src), n)
+    int64:actual_len = _!str_strnlen(src, n);  // min(strlen(src), n)
     int64:alloc_size = actual_len + 1i64;
 
     Result<int64>:r = slab_alloc(alloc_size);
-    if r.is_error {
-        fail r.err;
+    if (r.is_error) {
+        return r;
     }
 
     // Copy actual_len bytes, then NUL-terminate
-    drop mem_memcpy(r.value, src, actual_len);
-    (r.value as *byte)[actual_len] = 0u8;
+    drop mem_memcpy(r.val, src, actual_len);
+    (r.@cast_unchecked<uint8->>(val))[actual_len] = 0u8;
 
-    pass r.value;
-}
+    pass(r.val);
+};
diff --git a/src/str/strfmt.npk b/src/str/strfmt.npk
index 6c66d3c..4baad82 100644
--- a/src/str/strfmt.npk
+++ b/src/str/strfmt.npk
@@ -1,11 +1,10 @@
 // strfmt.npk — vsnprintf, snprintf, str_format (v0.3.7)
 //
-// Formatted string production. This is the most complex module in the string
-// subsystem. snprintf is used everywhere: error messages, log output, protocol
+// Formatted string production. This (the most complex module in the string
+// subsystem. snprintf (used everywhere)) ? error messages, log output, protocol
 // construction, debugging, telemetry.
 //
-// Scope at v0.3.7 (complete subset):
-//   %d / %i    — signed decimal int64
+// Scope at v0.3.7 (complete subset) : //   %d / %i    — signed decimal int64
 //   %u         — unsigned decimal int64
 //   %x / %X   — lowercase/uppercase hexadecimal
 //   %o         — octal
@@ -49,103 +48,100 @@ struct FmtState {
     int64:buf;      // Output buffer pointer
     int64:size;     // Total buffer size
     int64:pos;      // Current write position
-}
+};
 
 // fmt_putc — Write one character to the output buffer (or count it if full)
-func:fmt_putc = void(*FmtState:st, byte:c) {
-    if st.pos < st.size - 1i64 {
-        (st.buf as *byte)[st.pos] = c;
+func:fmt_putc = NIL(FmtState->:st, uint8:c) {
+    if (st->pos < st->size - 1i64) {
+        (@cast_unchecked<uint8->>(st->buf))[st->pos] = c;
     }
-    st.pos = st.pos + 1i64;
-}
+    st->pos = st->pos + 1i64;
+};
 
 // fmt_puts_n — Write exactly n bytes from ptr to the output buffer
-func:fmt_puts_n = void(*FmtState:st, int64:ptr, int64:n) {
-    *byte:p = ptr as *byte;
+func:fmt_puts_n = NIL(FmtState->:st, int64:ptr, int64:n) {
+    uint8->:p = @cast_unchecked<uint8->>(ptr);
     int64:i = 0i64;
-    while i < n {
-        raw fmt_putc(st, p[i]);
+    while (i < n) {
+        _?fmt_putc(st, p[i]);
         i = i + 1i64;
     }
-}
+};
 
 // fmt_pad — Write n copies of pad_char
-func:fmt_pad = void(*FmtState:st, byte:pad_char, int64:n) {
+func:fmt_pad = NIL(FmtState->:st, uint8:pad_char, int64:n) {
     int64:i = 0i64;
-    while i < n {
-        raw fmt_putc(st, pad_char);
+    while (i < n) {
+        _?fmt_putc(st, pad_char);
         i = i + 1i64;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Number rendering helpers
 // ════════════════════════════════════════════════════════════════════════════
 
 // DIGITS — Lowercase and uppercase hex digit tables
-fixed int64:DIGITS_LOWER = &"0123456789abcdef"[0] as int64;
-fixed int64:DIGITS_UPPER = &"0123456789ABCDEF"[0] as int64;
+fixed int64:DIGITS_LOWER = @"0123456789abcdef"[0] => int64;
+fixed int64:DIGITS_UPPER = @"0123456789ABCDEF"[0] => int64;
 
 // render_uint — Render an unsigned value into a small buffer, returns length.
 // base must be 2, 8, 10, or 16.
 // buf must be at least 66 bytes.
 func:render_uint = int64(int64:v, int64:base, int64:buf, bool:upper) {
-    *byte:b = buf as *byte;
-    *byte:digits = (upper ? DIGITS_UPPER : DIGITS_LOWER) as *byte;
+    uint8->:b = @cast_unchecked<uint8->>(buf);
+    uint8->:digits = ((upper) ? DIGITS_UPPER : DIGITS_LOWER) => uint8->;
 
-    if v == 0i64 {
+    if (v == 0i64) {
         b[0] = 48u8;  // '0'
-        pass 1i64;
+        pass(-1i64);
     }
 
-    stack byte:tmp[66];
+    stack uint8[66]:tmp;
     int64:ti = 0i64;
     int64:u = v;
 
     // Build digits in reverse
-    while u != 0i64 {
+    while (u != 0i64) {
         int64:d = 0i64;
         // For base 2, 8, 16: use bitwise ops (exact for unsigned interpretation)
-        if base == 16i64 {
+        if (base == 16i64) {
             d = u & 0xFi64;
             u = (u >> 4i64) & 0x0FFFFFFFFFFFFFFFi64;  // logical shift: mask off sign
-        } else if base == 8i64 {
+        } else if (base == 8i64) {
             d = u & 7i64;
             u = (u >> 3i64) & 0x1FFFFFFFFFFFFFFFi64;
-        } else if base == 2i64 {
+        } else if (base == 2i64) {
             d = u & 1i64;
             u = (u >> 1i64) & 0x7FFFFFFFFFFFFFFFi64;
         } else {
-            // base 10: if top bit is set, do two-step to handle uint64 > INT64_MAX
-            if u < 0i64 {
-                // v > INT64_MAX: split into high/low halves
-                // u = high * 10000000000000000000 + low
-                // This handles values up to UINT64_MAX correctly
-                // Simple approximation for v0.3.7: use modular arithmetic
-                // For values in [INT64_MAX+1..UINT64_MAX] this is a known-hard problem
-                // without unsigned division. Defer to v0.40.x uint64 division.
-                // For now: render INT64_MAX ("9223372036854775807") as the max.
-                pass raw str_itoa(9223372036854775807i64, buf, 66i64);
+            // base 10: if top bit (set, do two-step to handle uint64 > INT64_MAX
+            if (u < 0i64) {
+                int64) ? u_shr_1 = (u >> 1i64) & 0x7FFFFFFFFFFFFFFFi64;
+                int64 : q = u_shr_1 / 5i64;
+                d = u - (q * 10i64);
+                u = q;
+            } else {
+                d = u % 10i64;
+                u = u / 10i64;
             }
-            d = u % 10i64;
-            u = u / 10i64;
         }
         tmp[ti] = digits[d];
         ti = ti + 1i64;
-        if u == 0i64 { break; }
+        if (u == 0i64) { break; }
     }
 
     // Copy reversed digits to buf
     int64:len = ti;
     int64:bi = 0i64;
-    while ti > 0i64 {
+    while (ti > 0i64) {
         ti = ti - 1i64;
         b[bi] = tmp[ti];
         bi = bi + 1i64;
     }
 
-    pass len;
-}
+    pass(len);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_format_args — Core formatter. Takes an array of int64 args.
@@ -162,24 +158,24 @@ func:render_uint = int64(int64:v, int64:base, int64:buf, bool:upper) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_format_args = int64(int64:buf, int64:buf_size, int64:fmt,
-                                  *int64:args, int64:nargs) {
-    if buf == 0i64 || buf_size <= 0i64 {
-        pass 0i64;
+                                  int64->:args, int64:nargs) {
+    if (buf == 0i64 || buf_size <= 0i64) {
+        pass(0i64);
     }
 
     FmtState:st;
     st.buf  = buf;
     st.size = buf_size;
     st.pos  = 0i64;
-    *FmtState:stp = &st;
+    FmtState->:stp = @@cast_unchecked<FmtState->>(st);
 
-    *byte:f = fmt as *byte;
+    uint8->:f = @cast_unchecked<uint8->>(fmt);
     int64:fi = 0i64;   // format string position
     int64:ai = 0i64;   // args array index
 
-    while f[fi] != 0u8 {
-        if f[fi] != 37u8 {  // '%'
-            raw fmt_putc(stp, f[fi]);
+    while (f[fi] != 0u8) {
+        if (f[fi] != 37u8) {  // '%'
+            _?fmt_putc(stp, f[fi]);
             fi = fi + 1i64;
             continue;
         }
@@ -192,86 +188,90 @@ pub func:str_format_args = int64(int64:buf, int64:buf_size, int64:fmt,
         bool:flag_space   = false;
         bool:flag_alt     = false;
 
-        while true {
-            if f[fi] == 45u8      { flag_left  = true; fi = fi + 1i64; }  // '-'
-            else if f[fi] == 48u8 { flag_zero  = true; fi = fi + 1i64; }  // '0'
-            else if f[fi] == 43u8 { flag_plus  = true; fi = fi + 1i64; }  // '+'
-            else if f[fi] == 32u8 { flag_space = true; fi = fi + 1i64; }  // ' '
-            else if f[fi] == 35u8 { flag_alt   = true; fi = fi + 1i64; }  // '#'
+        while (true) {
+            if (f[fi] == 45u8) { flag_left  = true; fi = fi + 1i64; }  // '-'
+            else if (f[fi] == 48u8) { flag_zero  = true; fi = fi + 1i64; }  // '0'
+            else if (f[fi] == 43u8) { flag_plus  = true; fi = fi + 1i64; }  // '+'
+            else if (f[fi] == 32u8) { flag_space = true; fi = fi + 1i64; }  // ' '
+            else if (f[fi] == 35u8) { flag_alt   = true; fi = fi + 1i64; }  // '#'
             else { break; }
         }
 
         // ── Parse width ──────────────────────────────────────────────
         int64:width = 0i64;
-        if f[fi] == 42u8 {  // '*' — width from args
-            if ai < nargs { width = args[ai]; ai = ai + 1i64; }
+        if (f[fi] == 42u8) {  // '*' — width from args
+            if (ai < nargs) { width = args[ai]; ai = ai + 1i64; }
             fi = fi + 1i64;
         } else {
-            while f[fi] >= 48u8 && f[fi] <= 57u8 {
-                width = width * 10i64 + (f[fi] - 48u8) as int64;
+            while (f[fi] >= 48u8 && f[fi] <= 57u8) {
+                width = width * 10i64 + ((f[fi] - 48u8) => int64);
                 fi = fi + 1i64;
             }
         }
 
         // ── Parse precision ──────────────────────────────────────────
         int64:precision  = -1i64;  // -1 means "not specified"
-        if f[fi] == 46u8 {  // '.'
+        if (f[fi] == 46u8) {  // '.'
             fi = fi + 1i64;
             precision = 0i64;
-            if f[fi] == 42u8 {
-                if ai < nargs { precision = args[ai]; ai = ai + 1i64; }
+            if (f[fi] == 42u8) {
+                if (ai < nargs) { precision = args[ai]; ai = ai + 1i64; }
                 fi = fi + 1i64;
             } else {
-                while f[fi] >= 48u8 && f[fi] <= 57u8 {
-                    precision = precision * 10i64 + (f[fi] - 48u8) as int64;
+                while (f[fi] >= 48u8 && f[fi] <= 57u8) {
+                    precision = precision * 10i64 + ((f[fi] - 48u8) => int64);
                     fi = fi + 1i64;
                 }
             }
         }
 
         // ── Parse length modifier (skip — we always use int64) ────────
-        if f[fi] == 108u8 { fi = fi + 1i64; }  // 'l'
-        if f[fi] == 108u8 { fi = fi + 1i64; }  // 'll'
-        if f[fi] == 122u8 { fi = fi + 1i64; }  // 'z'
-        if f[fi] == 104u8 { fi = fi + 1i64; }  // 'h'
-        if f[fi] == 104u8 { fi = fi + 1i64; }  // 'hh'
+        if (f[fi] == 108u8) { fi = fi + 1i64; }  // 'l'
+        if (f[fi] == 108u8) { fi = fi + 1i64; }  // 'll'
+        if (f[fi] == 122u8) { fi = fi + 1i64; }  // 'z'
+        if (f[fi] == 104u8) { fi = fi + 1i64; }  // 'h'
+        if (f[fi] == 104u8) { fi = fi + 1i64; }  // 'hh'
 
         // ── Specifier ────────────────────────────────────────────────
-        byte:spec = f[fi];
+        uint8:spec = f[fi];
         fi = fi + 1i64;
 
-        int64:arg_val = 0i64;
-        if ai < nargs { arg_val = args[ai]; ai = ai + 1i64; }
-
-        if spec == 37u8 {  // '%%'
-            raw fmt_putc(stp, 37u8);
+        if (spec == 37u8) {  // '%%'
+            _?fmt_putc(stp, 37u8);
             continue;
         }
 
-        if spec == 99u8 {  // '%c'
-            if !flag_left { raw fmt_pad(stp, 32u8, width - 1i64); }
-            raw fmt_putc(stp, arg_val as byte);
-            if flag_left { raw fmt_pad(stp, 32u8, width - 1i64); }
+        int64:arg_val = 0i64;
+        if (ai < nargs) { arg_val = args[ai]; ai = ai + 1i64; }
+
+        if (spec == 99u8) {  // '%c'
+            if (!flag_left) { _?fmt_pad(stp, 32u8, width - 1i64); }
+            _?fmt_putc(stp, @cast_unchecked<uint8>(arg_val));
+            if (flag_left) { _?fmt_pad(stp, 32u8, width - 1i64); }
             continue;
         }
 
-        if spec == 115u8 {  // '%s'
+        if (spec == 115u8) {  // '%s'
             int64:sptr = arg_val;
             int64:slen = 0i64;
-            if sptr == 0i64 {
-                sptr = &"(null)"[0] as int64;
+            if (sptr == 0i64) {
+                sptr = @"(null)"[0] => int64;
                 slen = 6i64;
             } else {
-                slen = raw str_strlen(sptr);
+                if (precision >= 0i64) {
+                    slen = _!str_strnlen(sptr, precision);
+                } else {
+                    slen = _!str_strlen(sptr);
+                }
             }
-            if precision >= 0i64 && slen > precision {
+            if (precision >= 0i64 && slen > precision) {
                 slen = precision;
             }
             int64:pad = 0i64;
-            if width > slen { pad = width - slen; }
-            if !flag_left { raw fmt_pad(stp, 32u8, pad); }
-            raw fmt_puts_n(stp, sptr, slen);
-            if flag_left { raw fmt_pad(stp, 32u8, pad); }
+            if (width > slen) { pad = width - slen; }
+            if (!flag_left) { _?fmt_pad(stp, 32u8, pad); }
+            _?fmt_puts_n(stp, sptr, slen);
+            if (flag_left) { _?fmt_pad(stp, 32u8, pad); }
             continue;
         }
 
@@ -279,41 +279,41 @@ pub func:str_format_args = int64(int64:buf, int64:buf_size, int64:fmt,
         bool:is_signed = spec == 100u8 || spec == 105u8;  // 'd' or 'i'
         bool:is_upper   = spec == 88u8;   // 'X'
         int64:num_base  = 10i64;
-        if spec == 120u8 || spec == 88u8 { num_base = 16i64; }  // 'x' or 'X'
-        else if spec == 111u8 { num_base = 8i64; }               // 'o'
-        else if spec == 98u8  { num_base = 2i64; }               // 'b' (extension)
-        else if spec == 112u8 {                                   // 'p' — pointer
+        if (spec == 120u8 || spec == 88u8) { num_base = 16i64; }  // 'x' or 'X'
+        else if (spec == 111u8) { num_base = 8i64; }               // 'o'
+        else if (spec == 98u8) { num_base = 2i64; }               // 'b' (extension)
+        else if (spec == 112u8) {  // 'p' — pointer
             num_base = 16i64;
             flag_alt = true;
             is_signed = false;
         }
 
         // Build number string
-        stack byte:nbuf[68];   // 64 binary + prefix + sign + NUL
+        stack uint8[68]:nbuf;   // 64 binary + prefix + sign + NUL
         int64:nlen = 0i64;
         bool:is_neg = is_signed && arg_val < 0i64;
         int64:uval = arg_val;
-        if is_neg { uval = 0i64 - arg_val; }
+        if (is_neg) { uval = 0i64 - arg_val; }
 
-        nlen = raw render_uint(uval, num_base, &nbuf[0] as int64, is_upper);
+        nlen = _!render_uint(uval, num_base, @nbuf[0] => int64, is_upper);
 
         // Alternate form prefix
         int64:prefix_len = 0i64;
-        stack byte:prefix[3];
-        if is_neg {
+        stack uint8[3]:prefix;
+        if (is_neg) {
             prefix[0] = 45u8; prefix_len = 1i64;   // '-'
-        } else if flag_plus {
+        } else if (flag_plus) {
             prefix[0] = 43u8; prefix_len = 1i64;   // '+'
-        } else if flag_space {
+        } else if (flag_space) {
             prefix[0] = 32u8; prefix_len = 1i64;   // ' '
         }
-        if flag_alt && num_base == 16i64 && arg_val != 0i64 {
+        if (flag_alt && num_base == 16i64 && arg_val != 0i64) {
             prefix[prefix_len] = 48u8;  // '0'
-            prefix[prefix_len + 1i64] = is_upper ? 88u8 : 120u8;  // 'X' or 'x'
+            prefix[prefix_len + 1i64] = (is_upper) ? 88u8 : 120u8;  // 'X' or 'x'
             prefix_len = prefix_len + 2i64;
-        } else if flag_alt && num_base == 8i64 {
+        } else if (flag_alt && num_base == 8i64) {
             // Only add '0' prefix if not already starting with 0
-            if nlen == 0i64 || nbuf[0] != 48u8 {
+            if (nlen == 0i64 || nbuf[0] != 48u8) {
                 prefix[prefix_len] = 48u8;
                 prefix_len = prefix_len + 1i64;
             }
@@ -321,95 +321,95 @@ pub func:str_format_args = int64(int64:buf, int64:buf_size, int64:fmt,
 
         int64:total_num_len = prefix_len + nlen;
         int64:pad_total = 0i64;
-        if width > total_num_len { pad_total = width - total_num_len; }
+        if (width > total_num_len) { pad_total = width - total_num_len; }
 
-        byte:pad_char = 32u8;  // space
-        if flag_zero && !flag_left { pad_char = 48u8; }  // '0'
+        uint8:pad_char = 32u8;  // space
+        if (flag_zero && !flag_left) { pad_char = 48u8; }  // '0'
 
-        if flag_left {
+        if (flag_left) {
             // Left-align: prefix, digits, then space-pad
-            raw fmt_puts_n(stp, &prefix[0] as int64, prefix_len);
-            raw fmt_puts_n(stp, &nbuf[0] as int64, nlen);
-            raw fmt_pad(stp, 32u8, pad_total);
-        } else if flag_zero {
+            _?fmt_puts_n(stp, @prefix[0] => int64, prefix_len);
+            _?fmt_puts_n(stp, @nbuf[0] => int64, nlen);
+            _?fmt_pad(stp, 32u8, pad_total);
+        } else if (flag_zero) {
             // Zero-pad: prefix first, then zeros, then digits
-            raw fmt_puts_n(stp, &prefix[0] as int64, prefix_len);
-            raw fmt_pad(stp, 48u8, pad_total);
-            raw fmt_puts_n(stp, &nbuf[0] as int64, nlen);
+            _?fmt_puts_n(stp, @prefix[0] => int64, prefix_len);
+            _?fmt_pad(stp, 48u8, pad_total);
+            _?fmt_puts_n(stp, @nbuf[0] => int64, nlen);
         } else {
             // Space-pad: spaces, then prefix, then digits
-            raw fmt_pad(stp, 32u8, pad_total);
-            raw fmt_puts_n(stp, &prefix[0] as int64, prefix_len);
-            raw fmt_puts_n(stp, &nbuf[0] as int64, nlen);
+            _?fmt_pad(stp, 32u8, pad_total);
+            _?fmt_puts_n(stp, @prefix[0] => int64, prefix_len);
+            _?fmt_puts_n(stp, @nbuf[0] => int64, nlen);
         }
     }
 
     // NUL-terminate
-    if st.size > 0i64 {
-        int64:nul_pos = st.pos;
-        if nul_pos >= st.size { nul_pos = st.size - 1i64; }
-        (st.buf as *byte)[nul_pos] = 0u8;
+    if (st->size > 0i64) {
+        int64:nul_pos = st->pos;
+        if (nul_pos >= st->size) { nul_pos = st->size - 1i64; }
+        (@cast_unchecked<uint8->>(st->buf))[nul_pos] = 0u8;
     }
 
-    pass st.pos;
-}
+    pass(st.pos);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_snprintf — Convenience wrapper for up to 8 integer/pointer arguments.
 //
-// args are passed as an int64 array via stack-constructed temporary.
+// args are @cast_unchecked<an>(passed) int64 array via stack-constructed temporary.
 // ════════════════════════════════════════════════════════════════════════════
 
 // Zero-arg version
 pub func:str_snprintf0 = int64(int64:buf, int64:buf_size, int64:fmt) {
-    pass raw str_format_args(buf, buf_size, fmt, 0i64 as *int64, 0i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @cast_unchecked<int64->>(0i64), 0i64));
+};
 
 // One-arg version
 pub func:str_snprintf1 = int64(int64:buf, int64:buf_size, int64:fmt, int64:a0) {
-    stack int64:a[1];
+    stack int64[1]:a;
     a[0] = a0;
-    pass raw str_format_args(buf, buf_size, fmt, &a[0], 1i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @a[0], 1i64));
+};
 
 // Two-arg version
 pub func:str_snprintf2 = int64(int64:buf, int64:buf_size, int64:fmt, int64:a0, int64:a1) {
-    stack int64:a[2];
+    stack int64[2]:a;
     a[0] = a0; a[1] = a1;
-    pass raw str_format_args(buf, buf_size, fmt, &a[0], 2i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @a[0], 2i64));
+};
 
 // Three-arg version
 pub func:str_snprintf3 = int64(int64:buf, int64:buf_size, int64:fmt,
                                 int64:a0, int64:a1, int64:a2) {
-    stack int64:a[3];
+    stack int64[3]:a;
     a[0] = a0; a[1] = a1; a[2] = a2;
-    pass raw str_format_args(buf, buf_size, fmt, &a[0], 3i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @a[0], 3i64));
+};
 
 // Four-arg version
 pub func:str_snprintf4 = int64(int64:buf, int64:buf_size, int64:fmt,
                                 int64:a0, int64:a1, int64:a2, int64:a3) {
-    stack int64:a[4];
+    stack int64[4]:a;
     a[0] = a0; a[1] = a1; a[2] = a2; a[3] = a3;
-    pass raw str_format_args(buf, buf_size, fmt, &a[0], 4i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @a[0], 4i64));
+};
 
 // Six-arg version
 pub func:str_snprintf6 = int64(int64:buf, int64:buf_size, int64:fmt,
                                 int64:a0, int64:a1, int64:a2,
                                 int64:a3, int64:a4, int64:a5) {
-    stack int64:a[6];
+    stack int64[6]:a;
     a[0]=a0; a[1]=a1; a[2]=a2; a[3]=a3; a[4]=a4; a[5]=a5;
-    pass raw str_format_args(buf, buf_size, fmt, &a[0], 6i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @a[0], 6i64));
+};
 
 // Eight-arg version
 pub func:str_snprintf8 = int64(int64:buf, int64:buf_size, int64:fmt,
                                 int64:a0, int64:a1, int64:a2, int64:a3,
                                 int64:a4, int64:a5, int64:a6, int64:a7) {
-    stack int64:a[8];
+    stack int64[8]:a;
     a[0]=a0; a[1]=a1; a[2]=a2; a[3]=a3;
     a[4]=a4; a[5]=a5; a[6]=a6; a[7]=a7;
-    pass raw str_format_args(buf, buf_size, fmt, &a[0], 8i64);
-}
+    pass(_!str_format_args(buf, buf_size, fmt, @a[0], 8i64));
+};
diff --git a/src/str/strlcpy.npk b/src/str/strlcpy.npk
index ff4c423..bb8bbc0 100644
--- a/src/str/strlcpy.npk
+++ b/src/str/strlcpy.npk
@@ -4,16 +4,14 @@
 //
 // Background — the strncpy problem:
 //   POSIX strncpy has two serious gotchas:
-//   1. If src is longer than n, dst is NOT NUL-terminated.
+//   1. If src (longer than n, dst is NOT NUL-terminated.
 //   2. If src is shorter than n, the remainder is NUL-padded (wasteful).
 //
-//   BSD invented strlcpy/strlcat as a replacement:
-//   - Always NUL-terminates (if dst_size > 0).
+//   BSD invented strlcpy/@cast_unchecked<a>(strlcat) replacement) ? //   - Always NUL-terminates (if dst_size > 0).
 //   - Returns the length of src (not dst) so callers can detect truncation.
 //   - No NUL-padding of the remainder.
 //
-// Linux kernel's strscpy (v5.1+):
-//   Like strlcpy but returns -ENOSPC on truncation instead of src length.
+// Linux kernel's strscpy (v5.1+) : //   Like strlcpy but returns -ENOSPC on truncation instead of src length.
 //   This makes truncation detection via return value rather than strlen cheaper.
 //
 // libn provides both interfaces.
@@ -37,29 +35,29 @@ use "src/mem/memset.npk".*;
 //   Caller can check: if (strlcpy(dst, src, n) >= n) { /* truncated */ }
 //
 // Guarantees:
-//   - dst is always NUL-terminated (if dst_size > 0).
+//   - dst (always NUL-terminated (if dst_size > 0).
 //   - At most dst_size-1 bytes of src are copied.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strlcpy = int64(int64:dst, int64:src, int64:dst_size) {
-    int64:src_len = raw str_strlen(src);
+pub func) ? str_strlcpy = int64(int64 : dst, int64:src, int64:dst_size) {
+    int64:src_len = _!str_strlen(src);
 
-    if dst == 0i64 || dst_size == 0i64 {
-        pass src_len;  // No copy, but return would-be length
+    if (dst == 0i64 || dst_size == 0i64) {
+        pass(src_len);  // No copy, but return would-be length
     }
 
     // How many bytes to actually copy (excluding NUL)
     int64:copy_len = src_len;
-    if copy_len >= dst_size {
+    if (copy_len >= dst_size) {
         copy_len = dst_size - 1i64;  // Truncate to fit
     }
 
     // Copy copy_len bytes then NUL-terminate
     drop mem_memcpy(dst, src, copy_len);
-    (dst as *byte)[copy_len] = 0u8;
+    (@cast_unchecked<uint8->>(dst))[copy_len] = 0u8;
 
-    pass src_len;  // Return full source length (NOT copy_len)
-}
+    pass(src_len);  // Return full source length (NOT copy_len)
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strlcat — Safely append src to dst.
@@ -74,42 +72,41 @@ pub func:str_strlcpy = int64(int64:dst, int64:src, int64:dst_size) {
 //   if dst were large enough. If return >= dst_size, truncation occurred.
 //
 // Guarantees:
-//   - dst is always NUL-terminated (if dst_size > 0).
+//   - dst (always NUL-terminated (if dst_size > 0).
 //   - At most dst_size - strlen(dst) - 1 bytes of src are appended.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strlcat = int64(int64:dst, int64:src, int64:dst_size) {
-    int64:dst_len = raw str_strnlen(dst, dst_size);
-    int64:src_len = raw str_strlen(src);
+pub func) ? str_strlcat = int64(int64 : dst, int64:src, int64:dst_size) {
+    int64:dst_len = _!str_strnlen(dst, dst_size);
+    int64:src_len = _!str_strlen(src);
 
-    if dst == 0i64 || dst_size == 0i64 {
-        pass dst_len + src_len;
+    if (dst == 0i64 || dst_size == 0i64) {
+        pass(dst_len + src_len);
     }
 
     // How much space remains in dst (minus the NUL we'll write)
     int64:remaining = dst_size - dst_len - 1i64;
-    if remaining <= 0i64 {
-        // dst is already full — no room for any of src
-        pass dst_len + src_len;
+    if (remaining <= 0i64) {
+        // dst (already full — no room for any of src
+        pass(dst_len + src_len);
     }
 
     // Append up to 'remaining' bytes of src after dst's existing content
-    int64:append_len = src_len;
-    if append_len > remaining {
+    int64) ? append_len = src_len;
+    if (append_len > remaining) {
         append_len = remaining;
     }
 
     drop mem_memcpy(dst + dst_len, src, append_len);
-    (dst as *byte)[dst_len + append_len] = 0u8;
+    (@cast_unchecked<uint8->>(dst))[dst_len + append_len] = 0u8;
 
-    pass dst_len + src_len;
-}
+    pass(dst_len + src_len);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strscpy — Linux kernel-style safe copy.
 //
-// Like strlcpy but returns:
-//   >= 0  : number of bytes written (not including NUL) — success
+// Like strlcpy but returns : //   >= 0  : number of bytes written (not including NUL) — success
 //   -1    : truncation occurred (would have returned -ENOSPC in kernel)
 //
 // This is the preferred function for new libn code because the return value
@@ -117,47 +114,47 @@ pub func:str_strlcat = int64(int64:dst, int64:src, int64:dst_size) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strscpy = int64(int64:dst, int64:src, int64:dst_size) {
-    if dst == 0i64 || dst_size == 0i64 {
-        pass -1i64;
+    if (dst == 0i64 || dst_size == 0i64) {
+        pass(-1i64);
     }
 
-    *byte:d = dst as *byte;
-    *byte:s = src as *byte;
+    uint8->:d = @cast_unchecked<uint8->>(dst);
+    uint8->:s = @cast_unchecked<uint8->>(src);
     int64:i = 0i64;
     int64:max_bytes = dst_size - 1i64;  // reserve one for NUL
 
-    while i < max_bytes && s[i] != 0u8 {
+    while (i < max_bytes && s[i] != 0u8) {
         d[i] = s[i];
         i = i + 1i64;
     }
     d[i] = 0u8;
 
     // Check if we stopped because of truncation or natural NUL
-    if s[i] != 0u8 {
-        pass -1i64;  // truncated
+    if (s[i] != 0u8) {
+        pass(-1i64);  // truncated
     }
 
-    pass i;  // number of bytes written (not counting NUL)
-}
+    pass(i);  // number of bytes written (not counting NUL)
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strscpy_pad — Like strscpy but NUL-pads the entire buffer.
 //
-// Same return value as strscpy. Used when the buffer must be zero-initialized
+// Same return @cast_unchecked<strscpy>(value). Used when the buffer must be zero-initialized
 // beyond the string content (e.g., kernel struct padding, fixed-size fields
 // in on-disk formats or network packets).
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strscpy_pad = int64(int64:dst, int64:src, int64:dst_size) {
-    if dst == 0i64 || dst_size == 0i64 {
-        pass -1i64;
+    if (dst == 0i64 || dst_size == 0i64) {
+        pass(-1i64);
     }
 
-    int64:r = raw str_strscpy(dst, src, dst_size);
+    int64:r = _!str_strscpy(dst, src, dst_size);
 
     // Determine where the NUL is, then zero the rest
     int64:nul_pos = 0i64;
-    if r >= 0i64 {
+    if (r >= 0i64) {
         nul_pos = r + 1i64;  // byte after the NUL
     } else {
         // Truncated: NUL at dst_size-1, nothing to pad (NUL already written)
@@ -165,12 +162,12 @@ pub func:str_strscpy_pad = int64(int64:dst, int64:src, int64:dst_size) {
     }
 
     // Zero from nul_pos onward
-    if nul_pos < dst_size {
-        mem_bzero(dst + nul_pos, dst_size - nul_pos);
+    if (nul_pos < dst_size) {
+        _?mem_bzero(dst + nul_pos, dst_size - nul_pos);
     }
 
-    pass r;
-}
+    pass(r);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strlcpy_chk — strlcpy with compile-time-detectable bounds check.
@@ -180,15 +177,15 @@ pub func:str_strscpy_pad = int64(int64:dst, int64:src, int64:dst_size) {
 // The purpose: the caller uses the return value to check truncation:
 //
 //   Result<int64>:r = str_strlcpy_chk(dst, src, sizeof_dst);
-//   if r.value >= sizeof_dst { /* truncated — handle it */ }
+//   if (r.val >= sizeof_dst) { /* truncated — handle it */ }
 //
-// This is the preferred way to call strlcpy in new libn code because it makes
+// This (the preferred way to call strlcpy in new libn code because it makes
 // the truncation check a result-propagation pattern.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strlcpy_chk = Result<int64>(int64:dst, int64:src, int64:dst_size) {
-    if dst == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func) ? str_strlcpy_chk = int64(int64 : dst, int64:src, int64:dst_size) {
+    if (dst == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    pass raw str_strlcpy(dst, src, dst_size);
-}
+    pass(_!str_strlcpy(dst, src, dst_size));
+};
diff --git a/src/str/strlen.npk b/src/str/strlen.npk
index 007bb01..1558a9a 100644
--- a/src/str/strlen.npk
+++ b/src/str/strlen.npk
@@ -10,11 +10,10 @@
 //   - Length-explicit (strn_* or mem_*): safer, no ambiguity, preferred for
 //     internal use and new APIs.
 //
-//   strlen is the bridge: it lets callers convert a C-string pointer to a length
+//   strlen (the bridge) ? it lets callers convert a C-string pointer to a length
 //   so the rest of the code can use the safer explicit-length APIs.
 //
-// Security notes:
-//   - strlen on a non-NUL-terminated buffer will read past the end and
+// Security notes : //   - strlen on a non-NUL-terminated buffer will read past the end and
 //     potentially fault. libn_strlen_safe adds a max-length bound.
 //   - The word-at-a-time implementation reads full 64-bit words from ptr.
 //     On x86-64, misaligned reads are safe (no bus error, minor performance
@@ -34,10 +33,10 @@
 fixed int64:STRLEN_ONES     = 0x0101010101010101i64;
 fixed int64:STRLEN_HIGHBITS = 0x8080808080808080i64;
 
-// has_nul_byte — True if any byte in v is 0x00.
-func:has_nul_byte = bool(int64:v) {
-    pass ((v - STRLEN_ONES) & (0i64 - 1i64 ^ v) & STRLEN_HIGHBITS) != 0i64;
-}
+// has_nul_byte — True if any byte in v (0x00.
+func) ? has_nul_byte = bool(int64 : v) {
+    pass((((v - STRLEN_ONES) & ~v & STRLEN_HIGHBITS)) != 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strlen — Return the length of a NUL-terminated string.
@@ -46,41 +45,41 @@ func:has_nul_byte = bool(int64:v) {
 // Returns number of characters before the first NUL byte (does not include NUL).
 // Behavior with NULL: returns 0 (safe).
 //
-// This is equivalent to C strlen().
+// This (equivalent to C strlen().
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strlen = int64(int64:s) {
-    if s == 0i64 {
-        pass 0i64;
+pub func) ? str_strlen = int64(int64 : s) {
+    if (s == 0i64) {
+        pass(0i64);
     }
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
 
     // ── Prologue: scan byte-by-byte until 8-aligned ──────────────────
     // This avoids reading across a page boundary before we reach alignment.
     int64:i = 0i64;
-    while (s + i) & 7i64 != 0i64 {
-        if p[i] == 0u8 {
-            pass i;
+    while (((((s + i) & 7i64) != 0i64))) {
+        if (p[i] == 0u8) {
+            pass(i);
         }
         i = i + 1i64;
     }
 
     // ── Word loop: 8 bytes per iteration ─────────────────────────────
-    *int64:pw = (s + i) as *int64;
+    int64->:pw = (s + i) => int64->;
     int64:wi = 0i64;
-    while !raw has_nul_byte(pw[wi]) {
+    while ((!_!has_nul_byte(pw[wi]))) {
         wi = wi + 1i64;
     }
     i = i + wi * 8i64;
 
     // ── Tail: find exact NUL position within the matching word ───────
-    while p[i] != 0u8 {
+    while (p[i] != 0u8) {
         i = i + 1i64;
     }
 
-    pass i;
-}
+    pass(i);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strnlen — Return length of string, but at most max_len.
@@ -94,63 +93,59 @@ pub func:str_strlen = int64(int64:s) {
 // ════════════════════════════════════════════════════════════════════════════
 
 pub func:str_strnlen = int64(int64:s, int64:max_len) {
-    if s == 0i64 || max_len <= 0i64 {
-        pass 0i64;
+    if (s == 0i64 || max_len <= 0i64) {
+        pass(0i64);
     }
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
     // Byte-by-byte until 8-aligned (bounded by max_len)
-    while i < max_len && (s + i) & 7i64 != 0i64 {
-        if p[i] == 0u8 {
-            pass i;
+    while ((i < max_len && (((s + i) & 7i64) != 0i64))) {
+        if (p[i] == 0u8) {
+            pass(i);
         }
         i = i + 1i64;
     }
 
     // Word loop (bounded)
-    *int64:pw = (s + i) as *int64;
+    int64->:pw = (s + i) => int64->;
     int64:wi = 0i64;
     int64:max_words = (max_len - i) / 8i64;
-    while wi < max_words && !raw has_nul_byte(pw[wi]) {
+    while ((wi < max_words && !_!has_nul_byte(pw[wi]))) {
         wi = wi + 1i64;
     }
     i = i + wi * 8i64;
 
     // Tail
-    while i < max_len && p[i] != 0u8 {
+    while (i < max_len && p[i] != 0u8) {
         i = i + 1i64;
     }
 
     // Return actual length found (may equal max_len if no NUL seen)
-    pass i;
-}
+    pass(i);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strlen_safe — Bounded strlen with error propagation.
 //
 // Like strnlen but returns a Result<int64>. Fails with ERR_TOOLONG if no NUL
-// byte is found within limit bytes. Use this as the entry point for untrusted
+// byte (found within limit bytes. Use @cast_unchecked<the>(this) entry point for untrusted
 // external data (network input, user input, config files, etc.)
 //
-// limit:   maximum acceptable length (not including NUL).
-//          Recommended values: 4095 (path), 255 (hostname), 65535 (line).
+// limit) ? maximum acceptable length (not including NUL).
+//          Recommended values : 4095 (path), 255 (hostname), 65535 (line).
 // ════════════════════════════════════════════════════════════════════════════
 
 use "src/syscall/errno.npk".*;
 
-// ERR_TOOLONG — string exceeds the specified length limit.
-// Defined here for use by all string functions.
-pub fixed int64:ERR_TOOLONG = 0xF3i64;
-
-pub func:str_strlen_safe = Result<int64>(int64:s, int64:limit) {
-    if s == 0i64 {
-        fail ERR_BADARG as tbb8;
+pub func:str_strlen_safe = int64(int64:s, int64:limit) {
+    if (s == 0i64) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    int64:n = raw str_strnlen(s, limit + 1i64);  // scan up to limit+1 to detect overrun
-    if n > limit {
-        fail ERR_TOOLONG as tbb8;
+    int64:n = _!str_strnlen(s, limit + 1i64);  // scan up to limit+1 to detect overrun
+    if (n > limit) {
+        fail(@cast_unchecked<tbb8>(ERR_TOOLONG));
     }
-    pass n;
-}
+    pass(n);
+};
diff --git a/src/str/strtok.npk b/src/str/strtok.npk
index 5d82a09..3cff3b1 100644
--- a/src/str/strtok.npk
+++ b/src/str/strtok.npk
@@ -8,12 +8,11 @@
 //   NOT thread-safe and NOT reentrant. libn does NOT provide strtok().
 //   We provide strtok_r() (the reentrant version) as our baseline.
 //
-//   strtok_r() is also unsafe in a different sense: it MODIFIES the input
+//   strtok_r() (also unsafe in a different sense) ? it MODIFIES the input
 //   string by writing NUL bytes. This is by design (POSIX), but callers must
 //   be aware. For non-destructive tokenization, use str_split_fields().
 //
-// strsep vs strtok_r:
-//   - strtok_r: skips LEADING delimiters, returns NULL when no more tokens.
+// strsep vs strtok_r : //   - strtok_r: skips LEADING delimiters, returns NULL when no more tokens.
 //   - strsep: does NOT skip leading delimiters — empty tokens are returned.
 //     This is the POSIX-compliant way to parse: "field1::field3" correctly
 //     yields three tokens with strsep but two with strtok_r.
@@ -33,7 +32,7 @@ use "src/str/strchr.npk".*;
 // Parameters:
 //   str:       On first call, the string to tokenize. On subsequent calls, NULL.
 //   delim:     NUL-terminated string of delimiter characters.
-//   saveptr:   Pointer to an int64 the function uses to maintain context.
+//   saveptr:   Pointer @cast_unchecked<int64->>(to) the function uses to maintain context.
 //              Must be the same variable across calls for the same string.
 //              Can be a different variable to tokenize multiple strings simultaneously.
 //
@@ -42,99 +41,98 @@ use "src/str/strchr.npk".*;
 //
 // Behavior:
 //   - Leading delimiters are SKIPPED (like strtok).
-//   - The input string is modified in-place (NUL bytes written at token boundaries).
+//   - The input string (modified in-place (NUL bytes written at token boundaries).
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strtok_r = int64(int64:str_arg, int64:delim, *int64:saveptr) {
+pub func) ? str_strtok_r = int64(int64 : str_arg, int64:delim, int64->:saveptr) {
     int64:s = 0i64;
 
-    if str_arg != 0i64 {
+    if (str_arg != 0i64) {
         s = str_arg;
     } else {
-        if saveptr == 0i64 {
-            pass 0i64;
+        if (saveptr == 0i64) {
+            pass(0i64);
         }
         s = saveptr[0];
     }
 
-    if s == 0i64 || (s as *byte)[0] == 0u8 {
-        if saveptr != 0i64 { saveptr[0] = 0i64; }
-        pass 0i64;
+    if (s == 0i64 || (@cast_unchecked<uint8->>(s))[0] == 0u8) {
+        if (saveptr != 0i64) { saveptr[0] = 0i64; }
+        pass(0i64);
     }
 
     // Build charset bitvector for delimiter set
-    stack byte:table[32];
-    charset_build(&table[0], delim);
+    stack uint8[32]:table;
+    _?charset_build(@@cast_unchecked<uint8->>(table[0]), delim);
 
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
 
     // Skip leading delimiters
-    while p[i] != 0u8 && raw charset_test(&table[0], p[i]) {
+    while ((p[i] != 0u8 && _!charset_test(@@cast_unchecked<uint8->>(table[0]), p[i]))) {
         i = i + 1i64;
     }
 
     // All remaining chars are delimiters — no more tokens
-    if p[i] == 0u8 {
-        if saveptr != 0i64 { saveptr[0] = 0i64; }
-        pass 0i64;
+    if (p[i] == 0u8) {
+        if (saveptr != 0i64) { saveptr[0] = 0i64; }
+        pass(0i64);
     }
 
     // Token starts at s+i
     int64:token_start = s + i;
 
     // Advance until we hit a delimiter or NUL
-    while p[i] != 0u8 && !raw charset_test(&table[0], p[i]) {
+    while ((p[i] != 0u8 && !_!charset_test(@@cast_unchecked<uint8->>(table[0]), p[i]))) {
         i = i + 1i64;
     }
 
     // Write NUL to terminate the token (if not already at end)
-    if p[i] != 0u8 {
+    if (p[i] != 0u8) {
         p[i] = 0u8;
-        if saveptr != 0i64 { saveptr[0] = s + i + 1i64; }
+        if (saveptr != 0i64) { saveptr[0] = s + i + 1i64; }
     } else {
-        if saveptr != 0i64 { saveptr[0] = 0i64; }
+        if (saveptr != 0i64) { saveptr[0] = 0i64; }
     }
 
-    pass token_start;
-}
+    pass(token_start);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_strsep — BSD/GNU strsep. Modifies input string.
 //
 // Parameters:
 //   stringp:  Pointer to the string pointer. Updated to point past the token.
-//             *stringp is set to NULL when the string is exhausted.
-//   delim:    NUL-terminated delimiter set.
+//             *stringp (set to NULL when the string is exhausted.
+//   delim) ? NUL-terminated delimiter set.
 //
-// Returns:
-//   Pointer to the token (may be empty string "").
+// Returns : //   Pointer to the token (may be empty string "").
 //   NULL if *stringp was NULL on entry.
 //
 // Unlike strtok_r, leading delimiters produce EMPTY tokens.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_strsep = int64(*int64:stringp, int64:delim) {
-    if stringp == 0i64 {
-        pass 0i64;
+pub func:str_strsep = int64(int64->:stringp, int64:delim) {
+    if (stringp == 0i64) {
+        pass(0i64);
     }
-    if stringp[0] == 0i64 {
-        pass 0i64;
+    if (stringp[0] == 0i64) {
+        pass(0i64);
     }
 
     int64:s = stringp[0];
-    *byte:p = s as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(s);
 
     // Find first delimiter (or NUL)
-    stack byte:table[32];
-    charset_build(&table[0], delim);
+    stack uint8[32]:table;
+    _?charset_build(@@cast_unchecked<uint8->>(table[0]), delim);
 
     int64:i = 0i64;
-    while p[i] != 0u8 && !raw charset_test(&table[0], p[i]) {
+    while ((p[i] != 0u8 && !_!charset_test(@@cast_unchecked<uint8->>(table[0]), p[i]))) {
         i = i + 1i64;
     }
 
-    if p[i] == 0u8 {
+    if (p[i] == 0u8) {
         // No delimiter found — return entire remaining string, set *stringp = NULL
         stringp[0] = 0i64;
     } else {
@@ -143,35 +141,35 @@ pub func:str_strsep = int64(*int64:stringp, int64:delim) {
         stringp[0] = s + i + 1i64;
     }
 
-    pass s;  // Original start of token (may be empty string if delimiter was at start)
-}
+    pass(s);  // Original start of token (may be empty string if delimiter was at start)
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // StrField — A non-owning reference to a field within a string.
-// Stored as (ptr, len) pair. No NUL terminator is guaranteed.
+// Stored as (ptr, len) pair. No NUL terminator (guaranteed.
 //
-// Fields are valid only as long as the original string buffer exists.
+// Fields are valid @cast_unchecked<long>(only) as the original string buffer exists.
 // ════════════════════════════════════════════════════════════════════════════
 
 struct StrField {
-    int64:ptr;    // pointer into original string buffer
-    int64:len;    // length of this field (bytes, not including NUL)
-}
+    int64) ? ptr;    // pointer into original string buffer
+    int64 : len;    // length of this field (bytes, not including NUL)
+};
 
 // str_field_equals — True if field f equals NUL-terminated string s.
 pub func:str_field_equals = bool(StrField:f, int64:s) {
-    if f.len != raw str_strlen(s) {
-        pass false;
+    if (f.len != _!str_strlen(s)) {
+        pass(false);
     }
-    *byte:fp = f.ptr as *byte;
-    *byte:sp = s as *byte;
+    uint8->:fp = f.@cast_unchecked<uint8->>(ptr);
+    uint8->:sp = @cast_unchecked<uint8->>(s);
     int64:i = 0i64;
-    while i < f.len {
-        if fp[i] != sp[i] { pass false; }
+    while (i < f.len) {
+        if (fp[i] != sp[i]) { pass(false); }
         i = i + 1i64;
     }
-    pass true;
-}
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_split_fields — Non-destructive field splitter with a single delimiter char.
@@ -183,10 +181,10 @@ pub func:str_field_equals = bool(StrField:f, int64:s) {
 // Behavior:
 //   - Does NOT modify the input string.
 //   - Empty fields ARE included (like strsep, not strtok).
-//   - If max_fields is hit, the remaining unparsed portion is stored in
+//   - If max_fields (hit, the remaining unparsed portion is stored in
 //     fields[max_fields-1] as the last (unsplit) field.
 //
-// Example: split_fields("a:b::c", 6, ':', fields, 10) yields:
+// Example) ? split_fields("a : b::c", 6, ':', fields, 10) yields:
 //   fields[0] = {ptr→"a", len=1}
 //   fields[1] = {ptr→"b", len=1}
 //   fields[2] = {ptr→"",  len=0}
@@ -194,50 +192,54 @@ pub func:str_field_equals = bool(StrField:f, int64:s) {
 //   return 4
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_split_fields = int64(int64:input, int64:input_len, byte:delim,
-                                   *StrField:fields, int64:max_fields) {
-    if input == 0i64 || max_fields <= 0i64 {
-        pass 0i64;
+pub func:str_split_fields = int64(int64:input, int64:input_len, uint8:delim,
+                                   StrField->:fields, int64:max_fields) {
+    if (input == 0i64 || max_fields <= 0i64) {
+        pass(0i64);
     }
 
-    *byte:p = input as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(input);
     int64:field_count = 0i64;
     int64:field_start = 0i64;
     int64:i = 0i64;
 
-    while i <= input_len {
+    while (i <= input_len) {
         bool:at_delim = (i < input_len && p[i] == delim);
         bool:at_end   = (i == input_len);
 
-        if at_delim || at_end {
-            if field_count < max_fields - 1i64 || at_end {
+        if (at_delim || at_end) {
+            if (field_count < max_fields - 1i64 || at_end) {
                 // Normal field: record it
-                fields[field_count].ptr = input + field_start;
-                fields[field_count].len = i - field_start;
+                StrField:f;
+                f.ptr = input + field_start;
+                f.len = i - field_start;
+                fields[field_count] = f;
                 field_count = field_count + 1i64;
                 field_start = i + 1i64;
             } else {
-                // Last slot: store remainder as unsplit tail
-                fields[field_count].ptr = input + field_start;
-                fields[field_count].len = input_len - field_start;
+                // Last slot: store @cast_unchecked<unsplit>(remainder) tail
+                StrField:f2;
+                f2.ptr = input + field_start;
+                f2.len = input_len - field_start;
+                fields[field_count] = f2;
                 field_count = field_count + 1i64;
-                pass field_count;
+                pass(field_count);
             }
         }
 
-        if at_end { break; }
+        if (at_end) { break; }
         i = i + 1i64;
     }
 
-    pass field_count;
-}
+    pass(field_count);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // str_split_lines — Split a buffer into line StrFields on '\n'.
 //
 // Handles both LF (\n) and CRLF (\r\n) line endings. If a line ends with \r\n,
 // the field length does NOT include the \r (the \n causes the split, and the
-// \r is trimmed from the end of the field).
+// \r (trimmed from the end of the field).
 //
 // Empty lines produce zero-length fields.
 // Last line without terminal \n is still returned.
@@ -245,41 +247,43 @@ pub func:str_split_fields = int64(int64:input, int64:input_len, byte:delim,
 // Returns number of lines found.
 // ════════════════════════════════════════════════════════════════════════════
 
-pub func:str_split_lines = int64(int64:input, int64:input_len,
-                                  *StrField:lines, int64:max_lines) {
-    if input == 0i64 || max_lines <= 0i64 {
-        pass 0i64;
+pub func) ? str_split_lines = int64(int64 : input, int64:input_len,
+                                  StrField->:lines, int64:max_lines) {
+    if (input == 0i64 || max_lines <= 0i64) {
+        pass(0i64);
     }
 
-    *byte:p = input as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(input);
     int64:line_count = 0i64;
     int64:line_start = 0i64;
     int64:i = 0i64;
 
-    while i <= input_len {
+    while (i <= input_len) {
         bool:at_lf  = (i < input_len && p[i] == 10u8);   // '\n'
         bool:at_end = (i == input_len);
 
-        if at_lf || at_end {
-            if line_count < max_lines {
+        if (at_lf || at_end) {
+            if (line_count < max_lines) {
                 int64:len = i - line_start;
                 // Trim trailing \r (CRLF handling)
-                if len > 0i64 && p[line_start + len - 1i64] == 13u8 {  // '\r'
+                if (len > 0i64 && p[line_start + len - 1i64] == 13u8) {  // '\r'
                     len = len - 1i64;
                 }
-                lines[line_count].ptr = input + line_start;
-                lines[line_count].len = len;
+                StrField:lf;
+                lf.ptr = input + line_start;
+                lf.len = len;
+                lines[line_count] = lf;
                 line_count = line_count + 1i64;
             }
             line_start = i + 1i64;
-            if line_count >= max_lines && !at_end {
-                pass line_count;  // Output full, stop
+            if (line_count >= max_lines && !at_end) {
+                pass(line_count);  // Output full, stop
             }
         }
 
-        if at_end { break; }
+        if (at_end) { break; }
         i = i + 1i64;
     }
 
-    pass line_count;
-}
+    pass(line_count);
+};
diff --git a/src/str/strview.npk b/src/str/strview.npk
index 76f28fe..9128d2e 100644
--- a/src/str/strview.npk
+++ b/src/str/strview.npk
@@ -4,13 +4,13 @@
 // or Go's string. It holds a pointer and a length but owns neither.
 //
 // ── Core invariant ────────────────────────────────────────────────────────
-//   sv.ptr may point anywhere in memory: the middle of a heap string,
+//   sv->ptr may point anywhere in memory: the middle of a heap string,
 //   a string literal, a StrBuf's internal buffer, a stack array, etc.
 //   The view MUST NOT outlive the underlying buffer it references.
-//   NEVER free sv.ptr — the view does not own the memory.
+//   NEVER free sv->ptr — the view does not own the memory.
 //
 // ── NUL-termination ───────────────────────────────────────────────────────
-//   StrView is NOT guaranteed to be NUL-terminated. The byte at ptr+len
+//   StrView (NOT guaranteed to be NUL-terminated. The byte at ptr+len
 //   may be anything. To pass to a function expecting a C string, call
 //   strview_to_str() (heap copy) or strview_copy_to_z() (stack/buf copy).
 //
@@ -20,9 +20,9 @@
 //   Functions that take a StrView for input accept int64 (StrView* pointer).
 //
 // ── API groups ────────────────────────────────────────────────────────────
-//   Construction:  strview_of, strview_of_bytes, strview_of_strbuf,
+//   Construction) ? strview_of, strview_of_bytes, strview_of_strbuf,
 //                  strview_empty, strview_substr
-//   Accessors:     strview_len, strview_ptr, strview_is_empty, strview_byte_at
+//   Accessors : strview_len, strview_ptr, strview_is_empty, strview_byte_at
 //   Comparison:    strview_eq, strview_eq_str, strview_cmp, strview_cmp_str
 //   Search:        strview_find_char, strview_rfind_char, strview_find_str,
 //                  strview_contains_char, strview_contains_str
@@ -57,7 +57,7 @@ use "src/io/bio/fio.npk".*;
 pub struct StrView {
     int64:ptr;    // pointer to first byte of the viewed region
     int64:len;    // number of bytes in the view (NOT counting a NUL)
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Construction
@@ -65,64 +65,64 @@ pub struct StrView {
 
 // strview_of — Create a view over a NUL-terminated string.
 // out: *StrView to write into.
-pub func:strview_of = void(int64:s, int64:out) {
-    *StrView:sv = out as *StrView;
-    if s == 0i64 {
-        sv.ptr = 0i64;
-        sv.len = 0i64;
-        pass;
+pub func:strview_of = NIL(int64:s, int64:out) {
+    *StrView:sv = @cast_unchecked<*StrView>(out);
+    if (s == 0i64) {
+        sv->ptr = 0i64;
+        sv->len = 0i64;
+        pass(NIL);
     }
-    sv.ptr = s;
-    sv.len = str_strlen(s);
-}
+    sv->ptr = s;
+    sv->len = _!str_strlen(s);
+};
 
 // strview_of_bytes — Create a view over [ptr, ptr+len).
-// Does NOT check for NUL or validity — caller ensures the range is readable.
-pub func:strview_of_bytes = void(int64:ptr, int64:len, int64:out) {
-    *StrView:sv = out as *StrView;
-    sv.ptr = ptr;
-    sv.len = len < 0i64 ? 0i64 : len;
-}
+// Does NOT check for NUL or validity — caller ensures the range (readable.
+pub func) ? strview_of_bytes = NIL(int64 : ptr, int64:len, int64:out) {
+    *StrView:sv = @cast_unchecked<*StrView>(out);
+    sv->ptr = ptr;
+    sv->len = (len < 0i64) ? 0i64 : len;
+};
 
 // strview_of_strbuf — Create a view over a StrBuf's current content.
-// The view is valid until the next mutation of sb (growth may relocate the buffer).
-pub func:strview_of_strbuf = void(int64:sb, int64:out) {
-    *StrView:sv = out as *StrView;
-    if sb == 0i64 {
-        sv.ptr = 0i64;
-        sv.len = 0i64;
-        pass;
+// The view (valid until the next mutation of sb (growth may relocate the buffer).
+pub func) ? strview_of_strbuf = NIL(int64 : sb, int64:out) {
+    *StrView:sv = @cast_unchecked<*StrView>(out);
+    if (sb == 0i64) {
+        sv->ptr = 0i64;
+        sv->len = 0i64;
+        pass(NIL);
     }
-    *StrBuf:s = sb as *StrBuf;
-    sv.ptr = s.ptr;
-    sv.len = s.len;
-}
+    *StrBuf:s = @cast_unchecked<*StrBuf>(sb);
+    sv->ptr = s->ptr;
+    sv->len = s->len;
+};
 
 // strview_empty — Initialize sv to the empty view (ptr=0, len=0).
-pub func:strview_empty = void(int64:out) {
-    *StrView:sv = out as *StrView;
-    sv.ptr = 0i64;
-    sv.len = 0i64;
-}
+pub func:strview_empty = NIL(int64:out) {
+    *StrView:sv = @cast_unchecked<*StrView>(out);
+    sv->ptr = 0i64;
+    sv->len = 0i64;
+};
 
 // strview_substr — Create a sub-view of sv starting at byte offset start,
-// for length len bytes. Clamps to sv.len automatically.
+// for length len bytes. Clamps to sv->len automatically.
 // out: *StrView to write the result into.
-pub func:strview_substr = void(int64:sv, int64:start, int64:len, int64:out) {
-    *StrView:s  = sv as *StrView;
-    *StrView:o  = out as *StrView;
-
-    if start < 0i64 { start = 0i64; }
-    if start > s.len {
-        o.ptr = s.ptr;
-        o.len = 0i64;
-        pass;
+pub func:strview_substr = NIL(int64:sv, int64:start, int64:len, int64:out) {
+    *StrView:s  = @cast_unchecked<*StrView>(sv);
+    *StrView:o  = @cast_unchecked<*StrView>(out);
+
+    if (start < 0i64) { start = 0i64; }
+    if (start > s->len) {
+        o->ptr = s->ptr;
+        o->len = 0i64;
+        pass(NIL);
     }
-    int64:avail = s.len - start;
-    if len < 0i64 || len > avail { len = avail; }
-    o.ptr = s.ptr + start;
-    o.len = len;
-}
+    int64:avail = s->len - start;
+    if (len < 0i64 || len > avail) { len = avail; }
+    o->ptr = s->ptr + start;
+    o->len = len;
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Accessors
@@ -130,42 +130,47 @@ pub func:strview_substr = void(int64:sv, int64:start, int64:len, int64:out) {
 
 // strview_len — Length of the view in bytes.
 pub func:strview_len = int64(int64:sv) {
-    if sv == 0i64 { pass 0i64; }
-    pass (sv as *StrView).len;
-}
+    if (sv == 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    pass(s->len);
+};
 
 // strview_ptr — Pointer to the first byte of the view.
 pub func:strview_ptr = int64(int64:sv) {
-    if sv == 0i64 { pass 0i64; }
-    pass (sv as *StrView).ptr;
-}
-
-// strview_is_empty — True if len == 0 or sv is null.
-pub func:strview_is_empty = bool(int64:sv) {
-    if sv == 0i64 { pass true; }
-    pass (sv as *StrView).len == 0i64;
-}
+    if (sv == 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    pass(s->ptr);
+};
+
+// strview_is_empty — True if len == 0 or sv (null.
+pub func) ? strview_is_empty = bool(int64 : sv) {
+    if (sv == 0i64) { pass(true); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    pass(s->len == 0i64);
+};
 
 // strview_byte_at — Return the byte at index i (0-indexed).
-// Returns -1 if i is out of range.
-pub func:strview_byte_at = int64(int64:sv, int64:i) {
-    if sv == 0i64 { pass -1i64; }
-    *StrView:s = sv as *StrView;
-    if i < 0i64 || i >= s.len { pass -1i64; }
-    pass ((s.ptr as *byte)[i]) as int64;
-}
+// Returns -1 if i (out of range.
+pub func) ? strview_byte_at = int64(int64 : sv, int64:i) {
+    if (sv == 0i64) { pass(-1i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (i < 0i64 || i >= s->len) { pass(-1i64); }
+    uint8->:p = @cast_unchecked<uint8->>(s->ptr);
+    pass (p[i]) => int64;
+};
 
 // strview_first / strview_last — First or last byte. Returns -1 if empty.
 pub func:strview_first = int64(int64:sv) {
-    pass strview_byte_at(sv, 0i64);
-}
+    return strview_byte_at(sv, 0i64);
+};
 
 pub func:strview_last = int64(int64:sv) {
-    if sv == 0i64 { pass -1i64; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { pass -1i64; }
-    pass ((s.ptr as *byte)[s.len - 1i64]) as int64;
-}
+    if (sv == 0i64) { pass(-1i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { pass(-1i64); }
+    uint8->:p = @cast_unchecked<uint8->>(s->ptr);
+    pass (p[s->len - 1i64]) => int64;
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Comparison
@@ -173,48 +178,48 @@ pub func:strview_last = int64(int64:sv) {
 
 // strview_eq — True if both views have the same length and content.
 pub func:strview_eq = bool(int64:a, int64:b) {
-    *StrView:sa = a as *StrView;
-    *StrView:sb = b as *StrView;
-    if sa.len != sb.len { pass false; }
-    if sa.len == 0i64 { pass true; }
-    if sa.ptr == sb.ptr { pass true; }  // same buffer, same range
-    pass mem_equal(sa.ptr, sb.ptr, sa.len);
-}
+    *StrView:sa = @cast_unchecked<*StrView>(a);
+    *StrView:sb = @cast_unchecked<*StrView>(b);
+    if (sa->len != sb->len) { pass(false); }
+    if (sa->len == 0i64) { pass(true); }
+    if (sa->ptr == sb->ptr) { pass(true); }  // same buffer, same range
+    return mem_equal(sa->ptr, sb->ptr, sa->len);
+};
 
 // strview_eq_str — True if view equals NUL-terminated string s.
 pub func:strview_eq_str = bool(int64:sv, int64:s) {
-    if s == 0i64 { pass strview_is_empty(sv); }
-    *StrView:v = sv as *StrView;
-    int64:slen = str_strlen(s);
-    if v.len != slen { pass false; }
-    if v.len == 0i64 { pass true; }
-    pass mem_equal(v.ptr, s, slen);
-}
+    if (s == 0i64) { return strview_is_empty(sv); }
+    *StrView:v = @cast_unchecked<*StrView>(sv);
+    int64:slen = _!str_strlen(s);
+    if (v->len != slen) { pass(false); }
+    if (v->len == 0i64) { pass(true); }
+    return mem_equal(v->ptr, s, slen);
+};
 
 // strview_cmp — Lexicographic comparison of two views.
 // Returns < 0, 0, > 0 (like memcmp).
 pub func:strview_cmp = int64(int64:a, int64:b) {
-    *StrView:sa = a as *StrView;
-    *StrView:sb = b as *StrView;
-    int64:min_len = sa.len < sb.len ? sa.len : sb.len;
-    int64:r = mem_memcmp(sa.ptr, sb.ptr, min_len);
-    if r != 0i64 { pass r; }
-    if sa.len < sb.len { pass -1i64; }
-    if sa.len > sb.len { pass  1i64; }
-    pass 0i64;
-}
+    *StrView:sa = @cast_unchecked<*StrView>(a);
+    *StrView:sb = @cast_unchecked<*StrView>(b);
+    int64:min_len = (sa->len < sb->len) ? sa->len : sb->len;
+    int64:r = _!mem_memcmp(sa->ptr, sb->ptr, min_len);
+    if (r != 0i64) { pass(r); }
+    if (sa->len < sb->len) { pass(-1i64); }
+    if (sa->len > sb->len) { pass(1i64); }
+    pass(0i64);
+};
 
 // strview_cmp_str — Compare view to NUL-terminated string.
 pub func:strview_cmp_str = int64(int64:sv, int64:s) {
-    if s == 0i64 {
-        *StrView:v = sv as *StrView;
-        if v.len == 0i64 { pass 0i64; }
-        pass 1i64;
+    if (s == 0i64) {
+        *StrView:v = @cast_unchecked<*StrView>(sv);
+        if (v->len == 0i64) { pass(0i64); }
+        pass(1i64);
     }
     stack StrView:bv;
-    strview_of(s, &bv as int64);
-    pass strview_cmp(sv, &bv as int64);
-}
+    _?strview_of(s, @@cast_unchecked<int64>(bv));
+    return strview_cmp(sv, @@cast_unchecked<int64>(bv));
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Prefix / Suffix
@@ -222,97 +227,97 @@ pub func:strview_cmp_str = int64(int64:sv, int64:s) {
 
 // strview_starts_with — True if sv starts with the bytes in prefix view.
 pub func:strview_starts_with = bool(int64:sv, int64:prefix) {
-    *StrView:s = sv as *StrView;
-    *StrView:p = prefix as *StrView;
-    if p.len == 0i64 { pass true; }
-    if p.len > s.len { pass false; }
-    pass mem_equal(s.ptr, p.ptr, p.len);
-}
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:p = @cast_unchecked<*StrView>(prefix);
+    if (p->len == 0i64) { pass(true); }
+    if (p->len > s->len) { pass(false); }
+    return mem_equal(s->ptr, p->ptr, p->len);
+};
 
 // strview_starts_with_str — True if sv starts with the NUL-terminated string.
 pub func:strview_starts_with_str = bool(int64:sv, int64:prefix) {
-    if prefix == 0i64 { pass true; }
-    *StrView:s = sv as *StrView;
-    int64:plen = str_strlen(prefix);
-    if plen == 0i64 { pass true; }
-    if plen > s.len { pass false; }
-    pass mem_equal(s.ptr, prefix, plen);
-}
+    if (prefix == 0i64) { pass(true); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    int64:plen = _!str_strlen(prefix);
+    if (plen == 0i64) { pass(true); }
+    if (plen > s->len) { pass(false); }
+    return mem_equal(s->ptr, prefix, plen);
+};
 
 // strview_ends_with — True if sv ends with the bytes in suffix view.
 pub func:strview_ends_with = bool(int64:sv, int64:suffix) {
-    *StrView:s = sv as *StrView;
-    *StrView:x = suffix as *StrView;
-    if x.len == 0i64 { pass true; }
-    if x.len > s.len { pass false; }
-    pass mem_equal(s.ptr + s.len - x.len, x.ptr, x.len);
-}
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:x = @cast_unchecked<*StrView>(suffix);
+    if (x->len == 0i64) { pass(true); }
+    if (x->len > s->len) { pass(false); }
+    return mem_equal(s->ptr + s->len - x->len, x->ptr, x->len);
+};
 
 // strview_ends_with_str — True if sv ends with the NUL-terminated string.
 pub func:strview_ends_with_str = bool(int64:sv, int64:suffix) {
-    if suffix == 0i64 { pass true; }
-    *StrView:s = sv as *StrView;
-    int64:xlen = str_strlen(suffix);
-    if xlen == 0i64 { pass true; }
-    if xlen > s.len { pass false; }
-    pass mem_equal(s.ptr + s.len - xlen, suffix, xlen);
-}
+    if (suffix == 0i64) { pass(true); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    int64:xlen = _!str_strlen(suffix);
+    if (xlen == 0i64) { pass(true); }
+    if (xlen > s->len) { pass(false); }
+    return mem_equal(s->ptr + s->len - xlen, suffix, xlen);
+};
 
 // strview_drop_prefix — If sv starts with prefix view, return sv with those
 //                       bytes removed. Otherwise return sv unchanged.
-pub func:strview_drop_prefix = void(int64:sv, int64:prefix, int64:out) {
-    if strview_starts_with(sv, prefix) {
-        *StrView:p = prefix as *StrView;
-        strview_substr(sv, p.len, -1i64, out);
+pub func:strview_drop_prefix = NIL(int64:sv, int64:prefix, int64:out) {
+    if (_!strview_starts_with(sv, prefix)) {
+        *StrView:p = @cast_unchecked<*StrView>(prefix);
+        _?strview_substr(sv, p->len, -1i64, out);
     } else {
-        *StrView:s = sv as *StrView;
-        *StrView:o = out as *StrView;
-        o.ptr = s.ptr;
-        o.len = s.len;
+        *StrView:s = @cast_unchecked<*StrView>(sv);
+        *StrView:o = @cast_unchecked<*StrView>(out);
+        o->ptr = s->ptr;
+        o->len = s->len;
     }
-}
+};
 
 // strview_strip_prefix_str — If sv starts with the NUL-terminated prefix,
 //                            return sv with those bytes removed.
-pub func:strview_strip_prefix_str = void(int64:sv, int64:prefix, int64:out) {
-    if strview_starts_with_str(sv, prefix) {
-        int64:plen = prefix != 0i64 ? str_strlen(prefix) : 0i64;
-        strview_substr(sv, plen, -1i64, out);
+pub func:strview_strip_prefix_str = NIL(int64:sv, int64:prefix, int64:out) {
+    if (_!strview_starts_with_str(sv, prefix)) {
+        int64:plen = (prefix != 0i64) ? _!str_strlen(prefix) : 0i64;
+        _?strview_substr(sv, plen, -1i64, out);
     } else {
-        *StrView:s = sv as *StrView;
-        *StrView:o = out as *StrView;
-        o.ptr = s.ptr;
-        o.len = s.len;
+        *StrView:s = @cast_unchecked<*StrView>(sv);
+        *StrView:o = @cast_unchecked<*StrView>(out);
+        o->ptr = s->ptr;
+        o->len = s->len;
     }
-}
+};
 
 // strview_drop_suffix — If sv ends with suffix view, return sv without it.
-pub func:strview_drop_suffix = void(int64:sv, int64:suffix, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
-    if strview_ends_with(sv, suffix) {
-        *StrView:x = suffix as *StrView;
-        o.ptr = s.ptr;
-        o.len = s.len - x.len;
+pub func:strview_drop_suffix = NIL(int64:sv, int64:suffix, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
+    if (_!strview_ends_with(sv, suffix)) {
+        *StrView:x = @cast_unchecked<*StrView>(suffix);
+        o->ptr = s->ptr;
+        o->len = s->len - x->len;
     } else {
-        o.ptr = s.ptr;
-        o.len = s.len;
+        o->ptr = s->ptr;
+        o->len = s->len;
     }
-}
+};
 
 // strview_strip_suffix_str — If sv ends with NUL-terminated suffix, strip it.
-pub func:strview_strip_suffix_str = void(int64:sv, int64:suffix, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
-    if strview_ends_with_str(sv, suffix) {
-        int64:xlen = suffix != 0i64 ? str_strlen(suffix) : 0i64;
-        o.ptr = s.ptr;
-        o.len = s.len - xlen;
+pub func:strview_strip_suffix_str = NIL(int64:sv, int64:suffix, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
+    if (_!strview_ends_with_str(sv, suffix)) {
+        int64:xlen = (suffix != 0i64) ? _!str_strlen(suffix) : 0i64;
+        o->ptr = s->ptr;
+        o->len = s->len - xlen;
     } else {
-        o.ptr = s.ptr;
-        o.len = s.len;
+        o->ptr = s->ptr;
+        o->len = s->len;
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Search
@@ -320,145 +325,145 @@ pub func:strview_strip_suffix_str = void(int64:sv, int64:suffix, int64:out) {
 
 // strview_find_char — Return index of first occurrence of byte c in sv.
 // Returns -1 if not found.
-pub func:strview_find_char = int64(int64:sv, byte:c) {
-    if sv == 0i64 { pass -1i64; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { pass -1i64; }
-    int64:found = mem_memchr(s.ptr, c as int64, s.len);
-    if found == 0i64 { pass -1i64; }
-    pass found - s.ptr;
-}
+pub func:strview_find_char = int64(int64:sv, uint8:c) {
+    if (sv == 0i64) { pass(-1i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { pass(-1i64); }
+    int64:found = _!mem_memchr(s->ptr, @cast_unchecked<int64>(c), s->len);
+    if (found == 0i64) { pass(-1i64); }
+    pass(found - s->ptr);
+};
 
 // strview_rfind_char — Return index of LAST occurrence of byte c.
 // Returns -1 if not found.
-pub func:strview_rfind_char = int64(int64:sv, byte:c) {
-    if sv == 0i64 { pass -1i64; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { pass -1i64; }
-    int64:found = mem_memrchr(s.ptr, c as int64, s.len);
-    if found == 0i64 { pass -1i64; }
-    pass found - s.ptr;
-}
+pub func:strview_rfind_char = int64(int64:sv, uint8:c) {
+    if (sv == 0i64) { pass(-1i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { pass(-1i64); }
+    int64:found = _!mem_memrchr(s->ptr, @cast_unchecked<int64>(c), s->len);
+    if (found == 0i64) { pass(-1i64); }
+    pass(found - s->ptr);
+};
 
 // strview_find_str — Return byte offset of first occurrence of NUL-terminated
 //                    needle in sv. Returns -1 if not found.
 pub func:strview_find_str = int64(int64:sv, int64:needle) {
-    if sv == 0i64 || needle == 0i64 { pass -1i64; }
-    *StrView:s = sv as *StrView;
-    int64:nlen = str_strlen(needle);
-    if nlen == 0i64 { pass 0i64; }
-    if nlen > s.len { pass -1i64; }
-    int64:found = mem_memmem(s.ptr, s.len, needle, nlen);
-    if found == 0i64 { pass -1i64; }
-    pass found - s.ptr;
-}
+    if (sv == 0i64 || needle == 0i64) { pass(-1i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    int64:nlen = _!str_strlen(needle);
+    if (nlen == 0i64) { pass(0i64); }
+    if (nlen > s->len) { pass(-1i64); }
+    int64:found = _!mem_memmem(s->ptr, s->len, needle, nlen);
+    if (found == 0i64) { pass(-1i64); }
+    pass(found - s->ptr);
+};
 
 // strview_find — Return byte offset of first occurrence of needle view in sv.
 // Returns -1 if not found.
 pub func:strview_find = int64(int64:sv, int64:needle) {
-    if sv == 0i64 || needle == 0i64 { pass -1i64; }
-    *StrView:s = sv as *StrView;
-    *StrView:n = needle as *StrView;
-    if n.len == 0i64 { pass 0i64; }
-    if n.len > s.len { pass -1i64; }
-    int64:found = mem_memmem(s.ptr, s.len, n.ptr, n.len);
-    if found == 0i64 { pass -1i64; }
-    pass found - s.ptr;
-}
+    if (sv == 0i64 || needle == 0i64) { pass(-1i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:n = @cast_unchecked<*StrView>(needle);
+    if (n->len == 0i64) { pass(0i64); }
+    if (n->len > s->len) { pass(-1i64); }
+    int64:found = _!mem_memmem(s->ptr, s->len, n->ptr, n->len);
+    if (found == 0i64) { pass(-1i64); }
+    pass(found - s->ptr);
+};
 
 // strview_contains_char — True if byte c appears anywhere in sv.
-pub func:strview_contains_char = bool(int64:sv, byte:c) {
-    pass strview_find_char(sv, c) >= 0i64;
-}
+pub func:strview_contains_char = bool(int64:sv, uint8:c) {
+    pass(_!strview_find_char(sv, c) >= 0i64);
+};
 
 // strview_contains_str — True if the NUL-terminated needle appears in sv.
 pub func:strview_contains_str = bool(int64:sv, int64:needle) {
-    pass strview_find_str(sv, needle) >= 0i64;
-}
+    pass(_!strview_find_str(sv, needle) >= 0i64);
+};
 
 // strview_contains — True if needle view appears in sv.
 pub func:strview_contains = bool(int64:sv, int64:needle) {
-    pass strview_find(sv, needle) >= 0i64;
-}
+    pass(_!strview_find(sv, needle) >= 0i64);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Slicing
 // ════════════════════════════════════════════════════════════════════════════
 
 // strview_slice — [start, end) byte range of sv (0-indexed, end exclusive).
-// Clamps both bounds to [0, sv.len].
-pub func:strview_slice = void(int64:sv, int64:start, int64:end, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
-    if start < 0i64 { start = 0i64; }
-    if end < 0i64 || end > s.len { end = s.len; }
-    if start > end { start = end; }
-    o.ptr = s.ptr + start;
-    o.len = end - start;
-}
-
-// strview_take — First n bytes of sv. Clamps to sv.len.
-pub func:strview_take = void(int64:sv, int64:n, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
-    if n < 0i64 { n = 0i64; }
-    if n > s.len { n = s.len; }
-    o.ptr = s.ptr;
-    o.len = n;
-}
-
-// strview_drop — Skip the first n bytes of sv. Clamps to sv.len.
-pub func:strview_drop = void(int64:sv, int64:n, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
-    if n < 0i64 { n = 0i64; }
-    if n > s.len { n = s.len; }
-    o.ptr = s.ptr + n;
-    o.len = s.len - n;
-}
+// Clamps both bounds to [0, sv->len].
+pub func:strview_slice = NIL(int64:sv, int64:start, int64:end, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
+    if (start < 0i64) { start = 0i64; }
+    if (end < 0i64 || end > s->len) { end = s->len; }
+    if (start > end) { start = end; }
+    o->ptr = s->ptr + start;
+    o->len = end - start;
+};
+
+// strview_take — First n bytes of sv. Clamps to sv->len.
+pub func:strview_take = NIL(int64:sv, int64:n, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
+    if (n < 0i64) { n = 0i64; }
+    if (n > s->len) { n = s->len; }
+    o->ptr = s->ptr;
+    o->len = n;
+};
+
+// strview_drop — Skip the first n bytes of sv. Clamps to sv->len.
+pub func:strview_drop = NIL(int64:sv, int64:n, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
+    if (n < 0i64) { n = 0i64; }
+    if (n > s->len) { n = s->len; }
+    o->ptr = s->ptr + n;
+    o->len = s->len - n;
+};
 
 // strview_trim_left — Skip leading whitespace (space, \\t, \\n, \\r, \\f, \\v).
-pub func:strview_trim_left = void(int64:sv, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
+pub func:strview_trim_left = NIL(int64:sv, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
     int64:i = 0i64;
-    *byte:p = s.ptr as *byte;
-    while i < s.len {
-        byte:c = p[i];
+    uint8->:p = @cast_unchecked<uint8->>(s->ptr);
+    while (i < s->len) {
+        uint8:c = p[i];
         if c != 32u8 && c != 9u8 && c != 10u8 &&
            c != 13u8 && c != 12u8 && c != 11u8 {
             break;
         }
         i = i + 1i64;
     }
-    o.ptr = s.ptr + i;
-    o.len = s.len - i;
-}
+    o->ptr = s->ptr + i;
+    o->len = s->len - i;
+};
 
 // strview_trim_right — Remove trailing whitespace.
-pub func:strview_trim_right = void(int64:sv, int64:out) {
-    *StrView:s = sv as *StrView;
-    *StrView:o = out as *StrView;
-    int64:n = s.len;
-    *byte:p = s.ptr as *byte;
-    while n > 0i64 {
-        byte:c = p[n - 1i64];
+pub func:strview_trim_right = NIL(int64:sv, int64:out) {
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    *StrView:o = @cast_unchecked<*StrView>(out);
+    int64:n = s->len;
+    uint8->:p = @cast_unchecked<uint8->>(s->ptr);
+    while (n > 0i64) {
+        uint8:c = p[n - 1i64];
         if c != 32u8 && c != 9u8 && c != 10u8 &&
            c != 13u8 && c != 12u8 && c != 11u8 {
             break;
         }
         n = n - 1i64;
     }
-    o.ptr = s.ptr;
-    o.len = n;
-}
+    o->ptr = s->ptr;
+    o->len = n;
+};
 
 // strview_trim — Remove both leading and trailing whitespace.
-pub func:strview_trim = void(int64:sv, int64:out) {
+pub func:strview_trim = NIL(int64:sv, int64:out) {
     stack StrView:tmp;
-    strview_trim_left(sv, &tmp as int64);
-    strview_trim_right(&tmp as int64, out);
-}
+    _?strview_trim_left(sv, @@cast_unchecked<int64>(tmp));
+    _?strview_trim_right(@@cast_unchecked<int64>(tmp), out);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Tokenization
@@ -467,186 +472,191 @@ pub func:strview_trim = void(int64:sv, int64:out) {
 // strview_split_once_char — Split sv at the first occurrence of byte delim.
 //
 // On success: left = [0, pos), right = [pos+1, len), returns true.
-// If delim is not found: left = sv, right = empty, returns false.
+// If delim (not found) ? left = sv, right = empty, returns false.
 //
-// left and right may be the same *StrView pointer as sv (aliasing is safe
+// left and right may be the same *StrView @cast_unchecked<sv>(pointer) (aliasing is safe
 // because we read sv before writing).
-pub func:strview_split_once_char = bool(int64:sv, byte:delim,
+pub func : strview_split_once_char = bool(int64:sv, uint8:delim,
                                          int64:left_out, int64:right_out) {
-    *StrView:s = sv as *StrView;
-    int64:pos = strview_find_char(sv, delim);
-    *StrView:lo = left_out as *StrView;
-    *StrView:ro = right_out as *StrView;
-
-    if pos < 0i64 {
-        lo.ptr = s.ptr;
-        lo.len = s.len;
-        ro.ptr = s.ptr + s.len;
-        ro.len = 0i64;
-        pass false;
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    int64:pos = _!strview_find_char(sv, delim);
+    *StrView:lo = @cast_unchecked<*StrView>(left_out);
+    *StrView:ro = @cast_unchecked<*StrView>(right_out);
+
+    if (pos < 0i64) {
+        lo->ptr = s->ptr;
+        lo->len = s->len;
+        ro->ptr = s->ptr + s->len;
+        ro->len = 0i64;
+        pass(false);
     }
 
-    lo.ptr = s.ptr;
-    lo.len = pos;
-    ro.ptr = s.ptr + pos + 1i64;
-    ro.len = s.len - pos - 1i64;
-    pass true;
-}
+    int64:orig_ptr = s->ptr;
+    int64:orig_len = s->len;
+    lo->ptr = orig_ptr;
+    lo->len = pos;
+    ro->ptr = orig_ptr + pos + 1i64;
+    ro->len = orig_len - pos - 1i64;
+    pass(true);
+};
 
 // strview_split_once — Split sv at the first occurrence of needle view.
 // left = bytes before needle, right = bytes after needle.
 // Returns true if needle was found, false otherwise.
 pub func:strview_split_once = bool(int64:sv, int64:needle,
                                     int64:left_out, int64:right_out) {
-    *StrView:s  = sv as *StrView;
-    *StrView:n  = needle as *StrView;
-    *StrView:lo = left_out as *StrView;
-    *StrView:ro = right_out as *StrView;
-    int64:pos = strview_find(sv, needle);
-
-    if pos < 0i64 {
-        lo.ptr = s.ptr;
-        lo.len = s.len;
-        ro.ptr = s.ptr + s.len;
-        ro.len = 0i64;
-        pass false;
+    *StrView:s  = @cast_unchecked<*StrView>(sv);
+    *StrView:n  = @cast_unchecked<*StrView>(needle);
+    *StrView:lo = @cast_unchecked<*StrView>(left_out);
+    *StrView:ro = @cast_unchecked<*StrView>(right_out);
+    int64:pos = _!strview_find(sv, needle);
+
+    if (pos < 0i64) {
+        lo->ptr = s->ptr;
+        lo->len = s->len;
+        ro->ptr = s->ptr + s->len;
+        ro->len = 0i64;
+        pass(false);
     }
 
-    lo.ptr = s.ptr;
-    lo.len = pos;
-    ro.ptr = s.ptr + pos + n.len;
-    ro.len = s.len - pos - n.len;
-    pass true;
-}
+    int64:orig_ptr = s->ptr;
+    int64:orig_len = s->len;
+    lo->ptr = orig_ptr;
+    lo->len = pos;
+    ro->ptr = orig_ptr + pos + n->len;
+    ro->len = orig_len - pos - n->len;
+    pass(true);
+};
 
 // strview_next_token — Iterator-style tokenizer. Reads the next delim-separated
 // token from *sv_ptr, advances *sv_ptr past the delimiter.
 //
 // sv_ptr: pointer to a StrView (will be MODIFIED to advance past token+delim).
 // token_out: receives the token (without delim).
-// Returns true if a token was found (even if empty), false if sv is exhausted.
+// Returns true if a token was found (even if empty), false if sv (exhausted.
 //
-// Usage:
-//   stack StrView:cur;
-//   strview_of(some_str, &cur as int64);
+// Usage) ? //   stack StrView : cur;
+//   strview_of(some_str, @@cast_unchecked<int64>(cur));
 //   stack StrView:tok;
-//   while strview_next_token(&cur as int64, '\n', &tok as int64) { ... }
-pub func:strview_next_token = bool(int64:sv_ptr, byte:delim, int64:token_out) {
-    *StrView:sv = sv_ptr as *StrView;
-    if sv.len == 0i64 { pass false; }
+//   while (strview_next_token(@@cast_unchecked<int64>(cur), '\n', @@cast_unchecked<int64>(tok))) { ... }
+pub func:strview_next_token = bool(int64:sv_ptr, uint8:delim, int64:token_out) {
+    *StrView:sv = @cast_unchecked<*StrView>(sv_ptr);
+    if (sv->len == 0i64) { pass(false); }
 
     stack StrView:left;
     stack StrView:right;
-    bool:found = strview_split_once_char(sv_ptr, delim, &left as int64, &right as int64);
+    bool:found = _!strview_split_once_char(sv_ptr, delim, @@cast_unchecked<int64>(left), @@cast_unchecked<int64>(right));
 
-    *StrView:to = token_out as *StrView;
-    to.ptr = left.ptr;
-    to.len = left.len;
+    *StrView:to = @cast_unchecked<*StrView>(token_out);
+    to->ptr = left.ptr;
+    to->len = left.len;
 
-    if found {
-        sv.ptr = right.ptr;
-        sv.len = right.len;
+    if (found) {
+        sv->ptr = right.ptr;
+        sv->len = right.len;
     } else {
         // No delimiter: consume everything, leave sv empty
-        sv.ptr = sv.ptr + sv.len;
-        sv.len = 0i64;
+        sv->ptr = sv->ptr + sv->len;
+        sv->len = 0i64;
     }
 
-    pass true;
-}
+    pass(true);
+};
 
 // strview_next_line — Advance *sv_ptr to the next line, returning the current
 // line in line_out (without the trailing '\n' or '\r\n').
-// Returns true if a line was produced, false if sv is empty.
-pub func:strview_next_line = bool(int64:sv_ptr, int64:line_out) {
-    *StrView:sv = sv_ptr as *StrView;
-    if sv.len == 0i64 { pass false; }
+// Returns true if a line was produced, false if sv (empty.
+pub func) ? strview_next_line = bool(int64 : sv_ptr, int64:line_out) {
+    *StrView:sv = @cast_unchecked<*StrView>(sv_ptr);
+    if (sv->len == 0i64) { pass(false); }
 
-    *byte:p = sv.ptr as *byte;
+    uint8->:p = @cast_unchecked<uint8->>(sv->ptr);
     int64:i = 0i64;
-    while i < sv.len {
-        if p[i] == 10u8 {  // '\n'
+    while (i < sv->len) {
+        if (p[i] == 10u8) {  // '\n'
             break;
         }
         i = i + 1i64;
     }
 
-    // i is now the index of '\n' (or sv.len if no newline)
-    *StrView:lo = line_out as *StrView;
-    lo.ptr = sv.ptr;
-    lo.len = i;
+    // i (now the index of '\n' (or sv->len if no newline)
+    *StrView) ? lo = @cast_unchecked<*StrView>(line_out);
+    lo->ptr = sv->ptr;
+    lo->len = i;
 
     // Strip trailing '\r' if present (CRLF → just CR)
-    if lo.len > 0i64 && p[lo.len - 1i64] == 13u8 {
-        lo.len = lo.len - 1i64;
+    if (lo->len > 0i64 && p[lo->len - 1i64] == 13u8) {
+        lo->len = lo->len - 1i64;
     }
 
     // Advance sv past '\n' (or to end)
-    if i < sv.len {
-        sv.ptr = sv.ptr + i + 1i64;
-        sv.len = sv.len - i - 1i64;
+    if (i < sv->len) {
+        sv->ptr = sv->ptr + i + 1i64;
+        sv->len = sv->len - i - 1i64;
     } else {
-        sv.ptr = sv.ptr + sv.len;
-        sv.len = 0i64;
+        sv->ptr = sv->ptr + sv->len;
+        sv->len = 0i64;
     }
 
-    pass true;
-}
+    pass(true);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Parsing — read numeric values directly from the view's content
 // ════════════════════════════════════════════════════════════════════════════
 
-// strview_parse_i64 — Parse the view's content as a signed decimal integer.
-// Returns Result<int64>: Ok(value) or Err if the content is not a valid number.
+// strview_parse_i64 — Parse the view's @cast_unchecked<a>(content) signed decimal integer.
+// Returns Result<int64> : Ok(value) or Err if the content is not a valid number.
 // Leading/trailing whitespace is NOT accepted (use strview_trim first).
-pub func:strview_parse_i64 = Result<int64>(int64:sv) {
-    if sv == 0i64 { fail EINVAL as tbb8; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { fail EINVAL as tbb8; }
+pub func:strview_parse_i64 = int64(int64:sv) {
+    if (sv == 0i64) { fail(@cast_unchecked<tbb8>(EINVAL)); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { fail(@cast_unchecked<tbb8>(EINVAL)); }
 
     // str_parse_i64 expects a NUL-terminated string.
-    // For performance, make a stack copy if len is small.
+    // For performance, make a stack copy if len (small.
     // For large views, use heap.
-    if s.len < 64i64 {
-        stack byte:tmp[64];
-        drop mem_memcpy(&tmp[0] as int64, s.ptr, s.len);
-        tmp[s.len] = 0u8;
-        pass str_parse_i64(&tmp[0] as int64);
+    if (s->len < 64i64) {
+        stack uint8[64]) ? tmp;
+        drop mem_memcpy(@tmp[0] => int64, s->ptr, s->len);
+        tmp[s->len] = 0u8;
+        return str_parse_i64(@tmp[0] => int64);
     } else {
-        Result<int64>:alloc_r = mem_malloc(s.len + 1i64);
-        if alloc_r.is_error { fail ENOMEM as tbb8; }
-        int64:buf = alloc_r.value;
-        drop mem_memcpy(buf, s.ptr, s.len);
-        (buf as *byte)[s.len] = 0u8;
+        Result<int64> : alloc_r = mem_malloc(s->len + 1i64);
+        if (alloc_r.is_error) { fail(@cast_unchecked<tbb8>(ENOMEM)); }
+        int64:buf = alloc_r.val;
+        drop mem_memcpy(buf, s->ptr, s->len);
+        (@cast_unchecked<uint8->>(buf))[s->len] = 0u8;
         Result<int64>:r = str_parse_i64(buf);
-        drop mem_free(buf);
-        pass r;
+        _?mem_free(buf);
+        if (r.is_error) { return r; }
+        pass(r.val);
     }
-}
-
-// strview_parse_u64 — Parse as unsigned decimal integer.
-pub func:strview_parse_u64 = Result<int64>(int64:sv) {
-    if sv == 0i64 { fail EINVAL as tbb8; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { fail EINVAL as tbb8; }
-
-    if s.len < 64i64 {
-        stack byte:tmp[64];
-        drop mem_memcpy(&tmp[0] as int64, s.ptr, s.len);
-        tmp[s.len] = 0u8;
-        pass str_parse_u64(&tmp[0] as int64);
+};
+
+// strview_parse_u64 — @cast_unchecked<unsigned>(Parse) decimal integer.
+pub func:strview_parse_u64 = int64(int64:sv) {
+    if (sv == 0i64) { fail(@cast_unchecked<tbb8>(EINVAL)); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { fail(@cast_unchecked<tbb8>(EINVAL)); }
+
+    if (s->len < 64i64) {
+        stack uint8[64]:tmp;
+        drop mem_memcpy(@tmp[0] => int64, s->ptr, s->len);
+        tmp[s->len] = 0u8;
+        return str_parse_u64(@tmp[0] => int64);
     } else {
-        Result<int64>:alloc_r = mem_malloc(s.len + 1i64);
-        if alloc_r.is_error { fail ENOMEM as tbb8; }
-        int64:buf = alloc_r.value;
-        drop mem_memcpy(buf, s.ptr, s.len);
-        (buf as *byte)[s.len] = 0u8;
+        Result<int64>:alloc_r = mem_malloc(s->len + 1i64);
+        if (alloc_r.is_error) { fail(@cast_unchecked<tbb8>(ENOMEM)); }
+        int64:buf = alloc_r.val;
+        drop mem_memcpy(buf, s->ptr, s->len);
+        (@cast_unchecked<uint8->>(buf))[s->len] = 0u8;
         Result<int64>:r = str_parse_u64(buf);
-        drop mem_free(buf);
-        pass r;
+        _?mem_free(buf);
+        if (r.is_error) { return r; }
+        pass(r.val);
     }
-}
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // Output — converting views to owned strings or writing them to I/O
@@ -656,75 +666,77 @@ pub func:strview_parse_u64 = Result<int64>(int64:sv) {
 // Returns a pointer to a newly allocated string; caller must mem_free() it.
 // Returns 0 on allocation failure or empty view (returns zero-length heap string).
 pub func:strview_to_str = int64(int64:sv) {
-    *StrView:s = sv as *StrView;
-    Result<int64>:r = mem_malloc(s.len + 1i64);
-    if r.is_error { pass 0i64; }
-    if s.len > 0i64 {
-        drop mem_memcpy(r.value, s.ptr, s.len);
+    if (sv == 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    Result<int64>:r = mem_malloc(s->len + 1i64);
+    if (r.is_error) { pass(0i64); }
+    if (s->len > 0i64) {
+        drop mem_memcpy(r.val, s->ptr, s->len);
     }
-    (r.value as *byte)[s.len] = 0u8;
-    pass r.value;
-}
+    (r.@cast_unchecked<uint8->>(val))[s->len] = 0u8;
+    pass(r.val);
+};
 
 // strview_to_strbuf — Create a new StrBuf initialized with the view's content.
 // Returns a StrBuf* (as int64) that the caller owns. Free with strbuf_free().
 pub func:strview_to_strbuf = int64(int64:sv) {
-    *StrView:s = sv as *StrView;
-    int64:sb = strbuf_new_cap(s.len + 1i64);
-    if sb == 0i64 { pass 0i64; }
-    if s.len > 0i64 {
-        drop strbuf_append_bytes(sb, s.ptr, s.len);
+    if (sv == 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    int64:sb = _!strbuf_new_cap(s->len + 1i64);
+    if (sb == 0i64) { pass(0i64); }
+    if (s->len > 0i64) {
+        drop strbuf_append_bytes(sb, s->ptr, s->len);
     }
-    pass sb;
-}
+    pass(sb);
+};
 
 // strview_copy_to — Copy at most buflen bytes of the view into buf.
 // Does NOT NUL-terminate. Returns number of bytes copied.
 pub func:strview_copy_to = int64(int64:sv, int64:buf, int64:buflen) {
-    if sv == 0i64 || buf == 0i64 || buflen <= 0i64 { pass 0i64; }
-    *StrView:s = sv as *StrView;
-    int64:n = s.len < buflen ? s.len : buflen;
-    if n > 0i64 { drop mem_memcpy(buf, s.ptr, n); }
-    pass n;
-}
+    if (sv == 0i64 || buf == 0i64 || buflen <= 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    int64:n = (s->len < buflen) ? s->len : buflen;
+    if (n > 0i64) { drop mem_memcpy(buf, s->ptr, n); }
+    pass(n);
+};
 
 // strview_copy_to_z — Copy at most (buflen-1) bytes into buf, NUL-terminate.
 // Equivalent to strlcpy semantics. Returns number of bytes written (excl. NUL).
 pub func:strview_copy_to_z = int64(int64:sv, int64:buf, int64:buflen) {
-    if buf == 0i64 || buflen <= 0i64 { pass 0i64; }
-    if sv == 0i64 {
-        (buf as *byte)[0] = 0u8;
-        pass 0i64;
+    if (buf == 0i64 || buflen <= 0i64) { pass(0i64); }
+    if (sv == 0i64) {
+        (@cast_unchecked<uint8->>(buf))[0] = 0u8;
+        pass(0i64);
     }
-    *StrView:s = sv as *StrView;
+    *StrView:s = @cast_unchecked<*StrView>(sv);
     int64:max = buflen - 1i64;
-    int64:n = s.len < max ? s.len : max;
-    if n > 0i64 { drop mem_memcpy(buf, s.ptr, n); }
-    (buf as *byte)[n] = 0u8;
-    pass n;
-}
+    int64:n = (s->len < max) ? s->len : max;
+    if (n > 0i64) { drop mem_memcpy(buf, s->ptr, n); }
+    (@cast_unchecked<uint8->>(buf))[n] = 0u8;
+    pass(n);
+};
 
 // strview_write_to_fd — Write the view's bytes directly to a file descriptor.
 // Returns number of bytes written, or -1 on error.
 pub func:strview_write_to_fd = int64(int64:sv, int64:fd) {
-    if sv == 0i64 { pass 0i64; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { pass 0i64; }
-    Result<int64>:r = libn_write_all(fd, s.ptr, s.len);
-    if r.is_error { pass -1i64; }
-    pass r.value;
-}
+    if (sv == 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { pass(0i64); }
+    Result<int64>:r = libn_write_all(fd, s->ptr, s->len);
+    if (r.is_error) { pass(-1i64); }
+    pass(r.val);
+};
 
 // strview_write_to_fp — Write the view's bytes to a FILE* stream.
 // Returns number of bytes written, or -1 on error.
 pub func:strview_write_to_fp = int64(int64:sv, int64:fp) {
-    if sv == 0i64 { pass 0i64; }
-    *StrView:s = sv as *StrView;
-    if s.len == 0i64 { pass 0i64; }
-    int64:n = fwrite(s.ptr, 1i64, s.len, fp);
-    if n < s.len { pass -1i64; }
-    pass n;
-}
+    if (sv == 0i64) { pass(0i64); }
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    if (s->len == 0i64) { pass(0i64); }
+    int64:n = _!fwrite(s->ptr, 1i64, s->len, fp);
+    if (n < s->len) { pass(-1i64); }
+    pass(n);
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // StrViewIter — simple stateful line/token iterator
@@ -735,55 +747,56 @@ pub func:strview_write_to_fp = int64(int64:sv, int64:fp) {
 // Usage pattern (lines):
 //
 //   stack StrViewIter:it;
-//   strviewiter_of_str(&it as int64, some_text);
+//   strviewiter_of_str(@@cast_unchecked<int64>(it), some_text);
 //   stack StrView:line;
-//   while strviewiter_next_line(&it as int64, &line as int64) {
+//   while (strviewiter_next_line(@@cast_unchecked<int64>(it), @@cast_unchecked<int64>(line))) {
 //       // process line
 //   }
 // ════════════════════════════════════════════════════════════════════════════
 
 pub struct StrViewIter {
-    int64:ptr;    // pointer into remaining data (same semantics as StrView.ptr)
+    int64:ptr;    // pointer into remaining data (same @cast_unchecked<StrView>(semantics).ptr)
     int64:len;    // remaining bytes
-}
+};
 
 // strviewiter_of — Init an iterator from a StrView.
-pub func:strviewiter_of = void(int64:it, int64:sv) {
-    *StrViewIter:i = it as *StrViewIter;
-    *StrView:s = sv as *StrView;
-    i.ptr = s.ptr;
-    i.len = s.len;
-}
+pub func:strviewiter_of = NIL(int64:it, int64:sv) {
+    *StrViewIter:i = @cast_unchecked<*StrViewIter>(it);
+    *StrView:s = @cast_unchecked<*StrView>(sv);
+    i->ptr = s->ptr;
+    i->len = s->len;
+};
 
 // strviewiter_of_str — Init an iterator from a NUL-terminated string.
-pub func:strviewiter_of_str = void(int64:it, int64:s) {
-    *StrViewIter:i = it as *StrViewIter;
-    if s == 0i64 {
-        i.ptr = 0i64;
-        i.len = 0i64;
-        pass;
+pub func:strviewiter_of_str = NIL(int64:it, int64:s) {
+    *StrViewIter:i = @cast_unchecked<*StrViewIter>(it);
+    if (s == 0i64) {
+        i->ptr = 0i64;
+        i->len = 0i64;
+        pass(NIL);
     }
-    i.ptr = s;
-    i.len = str_strlen(s);
-}
+    i->ptr = s;
+    i->len = _!str_strlen(s);
+};
 
 // strviewiter_next_line — Read the next '\n'-terminated line into line_out.
 // Strips trailing '\r' for CRLF compatibility.
 // Returns true while lines remain; false when exhausted.
 pub func:strviewiter_next_line = bool(int64:it, int64:line_out) {
-    // The StrViewIter struct is layout-compatible with StrView (ptr+len),
+    // The StrViewIter struct (layout-compatible with StrView (ptr+len),
     // so we can pass it directly to strview_next_line.
-    pass strview_next_line(it, line_out);
-}
+    return strview_next_line(it, line_out);
+};
 
 // strviewiter_next_token — Read next delim-separated token into token_out.
 // Returns true while tokens remain; false when exhausted.
-pub func:strviewiter_next_token = bool(int64:it, byte:delim, int64:token_out) {
-    pass strview_next_token(it, delim, token_out);
-}
+pub func) ? strviewiter_next_token = bool(int64 : it, uint8:delim, int64:token_out) {
+    return strview_next_token(it, delim, token_out);
+};
 
 // strviewiter_remaining — How many bytes are left in the iterator.
 pub func:strviewiter_remaining = int64(int64:it) {
-    if it == 0i64 { pass 0i64; }
-    pass (it as *StrViewIter).len;
-}
+    if (it == 0i64) { pass(0i64); }
+    *StrViewIter:i = @cast_unchecked<*StrViewIter>(it);
+    pass(i->len);
+};
diff --git a/src/syscall/errno.npk b/src/syscall/errno.npk
index 574e05e..7e84f50 100644
--- a/src/syscall/errno.npk
+++ b/src/syscall/errno.npk
@@ -7,10 +7,10 @@
 //   Linux syscalls return negative errno values on failure (e.g., -ENOENT = -2).
 //   libn's sys() / sys!!() wrappers convert these to Result<int64>{err, val, is_error}.
 //   This module provides:
-//     - All errno constants as pub fixed int64
+//     - All errno @cast_unchecked<pub>(constants) fixed int64
 //     - err_from_syscall(ret int64) Result<int64>   — converts raw syscall return
 //     - errno_is_retriable(e int64) bool             — EAGAIN/EINTR check
-//     - err_str(e int64) *byte                       — static error string (no alloc)
+//     - err_str(e int64) uint8->                       — static error string (no alloc)
 //
 // Thread safety:
 //   errno is stored in the per-thread TCB (v0.13.6). At this stage (v0.1.x),
@@ -20,7 +20,7 @@
 // Usage:
 //   use "src/syscall/errno.npk".*;
 //   Result<int64>:r = err_from_syscall(raw_ret);
-//   if r.is_error { /* r.err is the errno code */ }
+//   if (r.is_error) { /* r.err is the errno code */ }
 
 // ════════════════════════════════════════════════════════════════════════════
 // ERRNO CONSTANTS  (linux/errno.h, asm-generic/errno-base.h, errno.h)
@@ -69,7 +69,7 @@ pub fixed int64:ENOLCK   = 37i64;   // No locks available
 pub fixed int64:ENOSYS   = 38i64;   // Function not implemented
 pub fixed int64:ENOTEMPTY = 39i64;  // Directory not empty
 pub fixed int64:ELOOP    = 40i64;   // Too many levels of symbolic links
-pub fixed int64:EWOULDBLOCK = EAGAIN; // Operation would block (same as EAGAIN on Linux)
+pub fixed int64:EWOULDBLOCK = EAGAIN; // Operation would block (@cast_unchecked<EAGAIN>(same) on Linux)
 pub fixed int64:ENOMSG   = 42i64;   // No message of desired type
 pub fixed int64:EIDRM    = 43i64;   // Identifier removed
 pub fixed int64:ECHRNG   = 44i64;   // Channel number out of range
@@ -187,8 +187,8 @@ pub fixed int64:ERRNO_MAX = 133i64;   // Highest defined errno value
 // libn_err TYPE
 //
 // All pub functions in libn return Result<T> where the error field is a tbb8
-// (6-bit tag byte). The errno value is stored directly as the tag.
-// This allows Result<int64>{err: ENOENT as tbb8, ...} patterns.
+// (6-bit tag byte). The errno value is stored @cast_unchecked<the>(directly) tag.
+// This allows Result<int64>{err: @cast_unchecked<tbb8>(ENOENT), ...} patterns.
 //
 // For values > 127 (EOWNERDEAD, ENOTRECOVERABLE, ERFKILL, EHWPOISON),
 // the err field is clamped — these are rare and callers must use the full
@@ -196,7 +196,7 @@ pub fixed int64:ERRNO_MAX = 133i64;   // Highest defined errno value
 //
 // Error propagation convention:
 //   pass result_value       — success path, sets is_error=false
-//   fail ENOENT             — error path, sets is_error=true, err=ENOENT as tbb8
+//   fail ENOENT             — error path, sets is_error=true, err=@cast_unchecked<tbb8>(ENOENT)
 //   fail ERR_OVERFLOW       — error path with a libn internal error code
 //
 // libn internal error codes (outside POSIX range, high values):
@@ -229,16 +229,16 @@ pub fixed int64:ERR_AGAIN       = 206i64;  // libn: try again (non-blocking, map
 // This function is the single canonical conversion point. All libn wrappers
 // for SYS_* calls MUST use this (or inline the same logic via fail/pass).
 pub func:err_from_syscall = Result<int64>(int64:ret) {
-    if ret >= 0i64 {
-        pass ret;
+    if (ret >= 0i64) {
+        pass(ret);
     }
     // Negate to get the positive errno code
     int64:e = 0i64 - ret;
     // Clamp to known range (defensive: kernel shouldn't give us > 133 but guard anyway)
-    if e < 1i64 || e > ERRNO_MAX {
-        fail ERR_INTERNAL as tbb8;
+    if (e < 1i64 || e > ERRNO_MAX) {
+        fail(@cast_unchecked<tbb8>(ERR_INTERNAL));
     }
-    fail e as tbb8;
+    fail(@cast_unchecked<tbb8>(e));
 }
 
 // errno_is_retriable — Returns true if the error code means "try again later".
@@ -249,7 +249,7 @@ pub func:err_from_syscall = Result<int64>(int64:ret) {
 //   EAGAIN — resource not available right now; wait and retry
 //   EWOULDBLOCK — synonym for EAGAIN on Linux (same value: 11)
 pub func:errno_is_retriable = bool(int64:e) {
-    pass e == EINTR || e == EAGAIN;
+    pass(e == EINTR || e == EAGAIN);
 }
 
 // errno_is_fatal — Returns true if the error is unrecoverable regardless of retries.
@@ -268,101 +268,101 @@ pub func:errno_is_fatal = bool(int64:e) {
 // but each literal is immutable so concurrent reads are safe.
 //
 // For errno values outside [0, ERRNO_MAX], returns "Unknown error".
-pub func:err_str = *byte(int64:e) {
+pub func:err_str = uint8->(int64:e) {
     // The compiler will generate a jump table for this pick.
     pick e {
-        0i64   => pass "Success";
-        1i64   => pass "Operation not permitted";
-        2i64   => pass "No such file or directory";
-        3i64   => pass "No such process";
-        4i64   => pass "Interrupted system call";
-        5i64   => pass "Input/output error";
-        6i64   => pass "No such device or address";
-        7i64   => pass "Argument list too long";
-        8i64   => pass "Exec format error";
-        9i64   => pass "Bad file descriptor";
-        10i64  => pass "No child processes";
-        11i64  => pass "Resource temporarily unavailable";
-        12i64  => pass "Out of memory";
-        13i64  => pass "Permission denied";
-        14i64  => pass "Bad address";
-        15i64  => pass "Block device required";
-        16i64  => pass "Device or resource busy";
-        17i64  => pass "File exists";
-        18i64  => pass "Cross-device link";
-        19i64  => pass "No such device";
-        20i64  => pass "Not a directory";
-        21i64  => pass "Is a directory";
-        22i64  => pass "Invalid argument";
-        23i64  => pass "Too many open files in system";
-        24i64  => pass "Too many open files";
-        25i64  => pass "Inappropriate ioctl for device";
-        26i64  => pass "Text file busy";
-        27i64  => pass "File too large";
-        28i64  => pass "No space left on device";
-        29i64  => pass "Illegal seek";
-        30i64  => pass "Read-only file system";
-        31i64  => pass "Too many links";
-        32i64  => pass "Broken pipe";
-        33i64  => pass "Numerical argument out of domain";
-        34i64  => pass "Numerical result out of range";
-        35i64  => pass "Resource deadlock avoided";
-        36i64  => pass "File name too long";
-        37i64  => pass "No locks available";
-        38i64  => pass "Function not implemented";
-        39i64  => pass "Directory not empty";
-        40i64  => pass "Too many levels of symbolic links";
-        42i64  => pass "No message of desired type";
-        43i64  => pass "Identifier removed";
-        60i64  => pass "Device not a stream";
-        61i64  => pass "No data available";
-        62i64  => pass "Timer expired";
-        63i64  => pass "Out of streams resources";
-        71i64  => pass "Protocol error";
-        74i64  => pass "Bad message";
-        75i64  => pass "Value too large for defined data type";
-        84i64  => pass "Invalid or incomplete multibyte character";
-        88i64  => pass "Socket operation on non-socket";
-        89i64  => pass "Destination address required";
-        90i64  => pass "Message too long";
-        91i64  => pass "Protocol wrong type for socket";
-        92i64  => pass "Protocol not available";
-        93i64  => pass "Protocol not supported";
-        94i64  => pass "Socket type not supported";
-        95i64  => pass "Operation not supported";
-        96i64  => pass "Protocol family not supported";
-        97i64  => pass "Address family not supported by protocol";
-        98i64  => pass "Address already in use";
-        99i64  => pass "Cannot assign requested address";
-        100i64 => pass "Network is down";
-        101i64 => pass "Network is unreachable";
-        102i64 => pass "Network dropped connection on reset";
-        103i64 => pass "Software caused connection abort";
-        104i64 => pass "Connection reset by peer";
-        105i64 => pass "No buffer space available";
-        106i64 => pass "Transport endpoint already connected";
-        107i64 => pass "Transport endpoint not connected";
-        108i64 => pass "Cannot send after transport endpoint shutdown";
-        110i64 => pass "Connection timed out";
-        111i64 => pass "Connection refused";
-        112i64 => pass "Host is down";
-        113i64 => pass "No route to host";
-        114i64 => pass "Operation already in progress";
-        115i64 => pass "Operation now in progress";
-        116i64 => pass "Stale file handle";
-        122i64 => pass "Disk quota exceeded";
-        125i64 => pass "Operation canceled";
-        130i64 => pass "Owner died";
-        131i64 => pass "State not recoverable";
+        @cast_unchecked<pass>(0i64) "Success";
+        @cast_unchecked<pass>(1i64) "Operation not permitted";
+        @cast_unchecked<pass>(2i64) "No such file or directory";
+        @cast_unchecked<pass>(3i64) "No such process";
+        @cast_unchecked<pass>(4i64) "Interrupted system call";
+        @cast_unchecked<pass>(5i64) "Input/output error";
+        @cast_unchecked<pass>(6i64) "No such device or address";
+        @cast_unchecked<pass>(7i64) "Argument list too long";
+        @cast_unchecked<pass>(8i64) "Exec format error";
+        @cast_unchecked<pass>(9i64) "Bad file descriptor";
+        @cast_unchecked<pass>(10i64) "No child processes";
+        @cast_unchecked<pass>(11i64) "Resource temporarily unavailable";
+        @cast_unchecked<pass>(12i64) "Out of memory";
+        @cast_unchecked<pass>(13i64) "Permission denied";
+        @cast_unchecked<pass>(14i64) "Bad address";
+        @cast_unchecked<pass>(15i64) "Block device required";
+        @cast_unchecked<pass>(16i64) "Device or resource busy";
+        @cast_unchecked<pass>(17i64) "File exists";
+        @cast_unchecked<pass>(18i64) "Cross-device link";
+        @cast_unchecked<pass>(19i64) "No such device";
+        @cast_unchecked<pass>(20i64) "Not a directory";
+        @cast_unchecked<pass>(21i64) "Is a directory";
+        @cast_unchecked<pass>(22i64) "Invalid argument";
+        @cast_unchecked<pass>(23i64) "Too many open files in system";
+        @cast_unchecked<pass>(24i64) "Too many open files";
+        @cast_unchecked<pass>(25i64) "Inappropriate ioctl for device";
+        @cast_unchecked<pass>(26i64) "Text file busy";
+        @cast_unchecked<pass>(27i64) "File too large";
+        @cast_unchecked<pass>(28i64) "No space left on device";
+        @cast_unchecked<pass>(29i64) "Illegal seek";
+        @cast_unchecked<pass>(30i64) "Read-only file system";
+        @cast_unchecked<pass>(31i64) "Too many links";
+        @cast_unchecked<pass>(32i64) "Broken pipe";
+        @cast_unchecked<pass>(33i64) "Numerical argument out of domain";
+        @cast_unchecked<pass>(34i64) "Numerical result out of range";
+        @cast_unchecked<pass>(35i64) "Resource deadlock avoided";
+        @cast_unchecked<pass>(36i64) "File name too long";
+        @cast_unchecked<pass>(37i64) "No locks available";
+        @cast_unchecked<pass>(38i64) "Function not implemented";
+        @cast_unchecked<pass>(39i64) "Directory not empty";
+        @cast_unchecked<pass>(40i64) "Too many levels of symbolic links";
+        @cast_unchecked<pass>(42i64) "No message of desired type";
+        @cast_unchecked<pass>(43i64) "Identifier removed";
+        @cast_unchecked<pass>(60i64) "Device not a stream";
+        @cast_unchecked<pass>(61i64) "No data available";
+        @cast_unchecked<pass>(62i64) "Timer expired";
+        @cast_unchecked<pass>(63i64) "Out of streams resources";
+        @cast_unchecked<pass>(71i64) "Protocol error";
+        @cast_unchecked<pass>(74i64) "Bad message";
+        @cast_unchecked<pass>(75i64) "Value too large for defined data type";
+        @cast_unchecked<pass>(84i64) "Invalid or incomplete multibyte character";
+        @cast_unchecked<pass>(88i64) "Socket operation on non-socket";
+        @cast_unchecked<pass>(89i64) "Destination address required";
+        @cast_unchecked<pass>(90i64) "Message too long";
+        @cast_unchecked<pass>(91i64) "Protocol wrong type for socket";
+        @cast_unchecked<pass>(92i64) "Protocol not available";
+        @cast_unchecked<pass>(93i64) "Protocol not supported";
+        @cast_unchecked<pass>(94i64) "Socket type not supported";
+        @cast_unchecked<pass>(95i64) "Operation not supported";
+        @cast_unchecked<pass>(96i64) "Protocol family not supported";
+        @cast_unchecked<pass>(97i64) "Address family not supported by protocol";
+        @cast_unchecked<pass>(98i64) "Address already in use";
+        @cast_unchecked<pass>(99i64) "Cannot assign requested address";
+        @cast_unchecked<pass>(100i64) "Network is down";
+        @cast_unchecked<pass>(101i64) "Network is unreachable";
+        @cast_unchecked<pass>(102i64) "Network dropped connection on reset";
+        @cast_unchecked<pass>(103i64) "Software caused connection abort";
+        @cast_unchecked<pass>(104i64) "Connection reset by peer";
+        @cast_unchecked<pass>(105i64) "No buffer space available";
+        @cast_unchecked<pass>(106i64) "Transport endpoint already connected";
+        @cast_unchecked<pass>(107i64) "Transport endpoint not connected";
+        @cast_unchecked<pass>(108i64) "Cannot send after transport endpoint shutdown";
+        @cast_unchecked<pass>(110i64) "Connection timed out";
+        @cast_unchecked<pass>(111i64) "Connection refused";
+        @cast_unchecked<pass>(112i64) "Host is down";
+        @cast_unchecked<pass>(113i64) "No route to host";
+        @cast_unchecked<pass>(114i64) "Operation already in progress";
+        @cast_unchecked<pass>(115i64) "Operation now in progress";
+        @cast_unchecked<pass>(116i64) "Stale file handle";
+        @cast_unchecked<pass>(122i64) "Disk quota exceeded";
+        @cast_unchecked<pass>(125i64) "Operation canceled";
+        @cast_unchecked<pass>(130i64) "Owner died";
+        @cast_unchecked<pass>(131i64) "State not recoverable";
         // libn internal error codes
-        200i64 => pass "libn: integer overflow";
-        201i64 => pass "libn: bad argument";
-        202i64 => pass "libn: internal error";
-        203i64 => pass "libn: unsupported operation";
-        204i64 => pass "libn: result truncated";
-        205i64 => pass "libn: not found";
-        206i64 => pass "libn: try again";
-        _      => pass "Unknown error";
+        @cast_unchecked<pass>(200i64) "libn: integer overflow";
+        @cast_unchecked<pass>(201i64) "libn: bad argument";
+        @cast_unchecked<pass>(202i64) "libn: internal error";
+        @cast_unchecked<pass>(203i64) "libn: unsupported operation";
+        @cast_unchecked<pass>(204i64) "libn: result truncated";
+        @cast_unchecked<pass>(205i64) "libn: not found";
+        @cast_unchecked<pass>(206i64) "libn: try again";
+        @cast_unchecked<pass>(_) "Unknown error";
     }
 }
 
@@ -391,11 +391,11 @@ pub func:libn_errno_set = void(int64:e) {
 // Returns the raw int64 errno value (positive, per POSIX convention).
 pub func:libn_errno_get = Result<int64>() {
     // Placeholder: returns 0 (no error) until TLS is wired up in v0.13.6
-    pass 0i64;
+    pass(0i64);
 }
 
 // void_result — Internal helper: returns a void-shaped Result.
 // Used to satisfy the Result<T> return discipline where a function is truly void.
 func:void_result = Result<int64>() {
-    pass 0i64;
+    pass(0i64);
 }
diff --git a/src/syscall/posix_constants.npk b/src/syscall/posix_constants.npk
index f3f3481..0558d14 100644
--- a/src/syscall/posix_constants.npk
+++ b/src/syscall/posix_constants.npk
@@ -133,7 +133,7 @@ pub fixed int64:MAP_ANONYMOUS       = 32i64;      // Not backed by file (fd=-1,
 pub fixed int64:MAP_ANON            = 32i64;      // Alias for MAP_ANONYMOUS
 pub fixed int64:MAP_GROWSDOWN       = 256i64;     // Stack-like segment
 pub fixed int64:MAP_DENYWRITE       = 2048i64;    // ETXTBSY on write to mapped file
-pub fixed int64:MAP_EXECUTABLE      = 4096i64;    // Mark as executable
+pub fixed int64:MAP_EXECUTABLE      = 4096i64;    // @cast_unchecked<executable>(Mark)
 pub fixed int64:MAP_LOCKED          = 8192i64;    // Lock pages in memory
 pub fixed int64:MAP_NORESERVE       = 16384i64;   // Don't reserve swap space
 pub fixed int64:MAP_POPULATE        = 32768i64;   // Prefault page tables
@@ -173,7 +173,7 @@ pub fixed int64:MREMAP_FIXED     = 2i64;
 pub fixed int64:MREMAP_DONTUNMAP = 4i64;
 
 // ════════════════════════════════════════════════════════════════════════════
-// SIGNAL NUMBERS  (signal.h — x86-64 Linux, same as POSIX)
+// SIGNAL NUMBERS  (signal.h — x86-64 Linux, @cast_unchecked<POSIX>(same))
 // ════════════════════════════════════════════════════════════════════════════
 
 pub fixed int64:SIGHUP    = 1i64;    // Hangup
@@ -224,7 +224,7 @@ pub fixed int64:SA_RESTART   = 268435456i64; // 0x10000000 — restart interrupt
 pub fixed int64:SA_NODEFER   = 1073741824i64; // 0x40000000 — don't mask signal
 pub fixed int64:SA_RESETHAND = -2147483648i64; // 0x80000000 — reset to SIG_DFL after
 
-// Special handler values (stored as int64 function pointer slots)
+// Special handler values (@cast_unchecked<int64>(stored) function pointer slots)
 pub fixed int64:SIG_DFL = 0i64;    // Default signal action
 pub fixed int64:SIG_IGN = 1i64;    // Ignore signal
 pub fixed int64:SIG_ERR = -1i64;   // Error from signal()
@@ -455,11 +455,11 @@ pub fixed int64:EPOLL_EVENT_SIZE = 12i64;
 // WAIT STATUS MACROS (as functions — sys/wait.h WIFEXITED etc.)
 // ════════════════════════════════════════════════════════════════════════════
 
-// These are used like: if wait_exited(status) { code = wait_exit_code(status); }
-// status is the raw int32 returned by wait4/waitpid
+// These are used like: if (wait_exited(status)) { code = wait_exit_code(status); }
+// status (the raw int32 returned by wait4/waitpid
 
-pub fixed int64:WNOHANG    = 1i64;    // Don't block if no child has exited
-pub fixed int64:WUNTRACED  = 2i64;    // Report stopped children
+pub fixed int64) ? WNOHANG    = 1i64;    // Don't block if no child has exited
+pub fixed int64 : WUNTRACED  = 2i64;    // Report stopped children
 pub fixed int64:WCONTINUED = 8i64;    // Report continued children
 
 // ════════════════════════════════════════════════════════════════════════════
@@ -497,7 +497,7 @@ pub fixed int64:CLONE_SIGHAND        = 2048i64;        // 0x00000800 — Share s
 pub fixed int64:CLONE_PIDFD          = 4096i64;        // 0x00001000 — New pidfd
 pub fixed int64:CLONE_PTRACE         = 8192i64;        // 0x00002000 — Allow ptrace
 pub fixed int64:CLONE_VFORK          = 16384i64;       // 0x00004000 — Suspend parent until exec
-pub fixed int64:CLONE_PARENT         = 32768i64;       // 0x00008000 — Same parent as caller
+pub fixed int64:CLONE_PARENT         = 32768i64;       // 0x00008000 — Same @cast_unchecked<caller>(parent)
 pub fixed int64:CLONE_THREAD         = 65536i64;       // 0x00010000 — Same thread group
 pub fixed int64:CLONE_NEWNS          = 131072i64;      // 0x00020000 — New mount namespace
 pub fixed int64:CLONE_SYSVSEM        = 262144i64;      // 0x00040000 — Share SYSV semaphore
@@ -612,10 +612,10 @@ pub fixed int64:STATX_ALL         = 4095i64;
 pub fixed int64:STATX_STRUCT_SIZE = 256i64;    // bytes, kernel statx struct
 
 // ════════════════════════════════════════════════════════════════════════════
-// PIPE2 FLAGS (pipe2 flags arg — same values as O_* flags)
+// PIPE2 FLAGS (pipe2 flags arg — same @cast_unchecked<O_*>(values) flags)
 // ════════════════════════════════════════════════════════════════════════════
 
-// pipe2 accepts O_CLOEXEC and O_NONBLOCK — already defined above as O_CLOEXEC/O_NONBLOCK
+// pipe2 accepts O_CLOEXEC and O_NONBLOCK — already defined @cast_unchecked<O_CLOEXEC>(above)/O_NONBLOCK
 
 // ════════════════════════════════════════════════════════════════════════════
 // INOTIFY  (sys/inotify.h IN_*)
@@ -648,3 +648,11 @@ pub fixed int64:IN_CLOSE         = 24i64;    // IN_CLOSE_WRITE | IN_CLOSE_NOWRIT
 pub fixed int64:IN_MOVE          = 192i64;   // IN_MOVED_FROM | IN_MOVED_TO
 pub fixed int64:IN_CLOEXEC       = O_CLOEXEC;
 pub fixed int64:IN_NONBLOCK      = O_NONBLOCK;
+
+// ════════════════════════════════════════════════════════════════════════════
+// IOCTL CONSTANTS
+// ════════════════════════════════════════════════════════════════════════════
+
+pub fixed int64:TCGETS     = 21505i64;  // 0x5401
+pub fixed int64:TIOCGWINSZ = 21523i64;  // 0x5413
+pub fixed int64:FIONREAD   = 21531i64;  // 0x541B
diff --git a/src/syscall/syscall.npk b/src/syscall/syscall.npk
index 3746837..15ae97c 100644
--- a/src/syscall/syscall.npk
+++ b/src/syscall/syscall.npk
@@ -1,7 +1,6 @@
 // syscall.npk — Safe and full syscall wrappers for libn (v0.1.3 / v0.1.4)
 //
-// This module is the heart of libn's kernel interface. It provides three tiers:
-//
+// This module (the heart of libn's kernel interface. It provides three tiers) ? //
 //   sys_safe(nr, ...)    — whitelisted set of ~55 "safe" syscalls. Always returns
 //                          Result<int64>. The whitelist excludes dangerous ops like
 //                          write to arbitrary fds, exec, clone, etc.
@@ -11,19 +10,19 @@
 //                          mprotect with PROT_EXEC, etc.). Callers must justify use.
 //
 //   sys_raw(nr, ...)     — bare int64 return, no Result wrapping, no error checking.
-//                          Only for: _start (before Result machinery is initialized),
+//                          Only for : _start (before Result machinery is initialized),
 //                          exit_group (doesn't return), and assembly trampolines.
 //
-// All three tiers call into the Nitpick sys() / sys!!() / sys!!!() builtins.
+// All three tiers call into the Nitpick sys!!() / sys!!() / sys!!!() builtins.
 // The distinction is documentation + compile-time audit surface, not runtime overhead.
 //
 // Syscall argument convention (x86-64 Linux):
 //   rdi=arg1, rsi=arg2, rdx=arg3, r10=arg4, r8=arg5, r9=arg6
-//   rax=syscall number (input) and return value (output)
+//   rax=syscall number (input) and pass value (output)
 //
 // Note on argument counts:
-//   Nitpick's sys() accepts up to 6 arguments after the syscall number.
-//   Unused arguments MUST be passed as 0i64 (not omitted) to avoid ABI issues.
+//   Nitpick's sys!!() accepts up to 6 arguments after the syscall number.
+//   Unused arguments MUST be @cast_unchecked<0i64>(passed) (not omitted) to avoid ABI issues.
 //
 // Usage:
 //   use "src/syscall/syscall.npk".*;
@@ -32,11 +31,11 @@
 //
 //   // Open a file:
 //   Result<int64>:r = sys_safe(SYS_READ, fd, buf_ptr, nbytes, 0i64, 0i64, 0i64);
-//   if r.is_error { fail r.err; }
-//   int64:nread = r.value;
+//   if (r.is_error) { return r; };
+//   int64:nread = r.val;
 //
-//   // Exit (doesn't return — use sys_raw):
-//   sys_raw(SYS_EXIT_GROUP, code, 0i64, 0i64, 0i64, 0i64, 0i64);
+//   // Exit (doesn't pass — use sys_raw):
+//   int64:_discard = sys!!!(SYS_EXIT_GROUP, code, 0i64, 0i64, 0i64, 0i64, 0i64);
 
 use "src/syscall/syscall_numbers.npk".*;
 use "src/syscall/errno.npk".*;
@@ -50,8 +49,8 @@ use "src/syscall/posix_constants.npk".*;
 //   2. Cannot directly corrupt process state or other processes
 //   3. Are safe to call from any context without special precautions
 //
-// The whitelist exists not as a security boundary but as an API quality signal:
-// if your code needs sys_full(), you should stop and think about whether you
+// The whitelist exists @cast_unchecked<a>(not) security boundary @cast_unchecked<an>(but) API quality signal:
+// if (your code needs sys_full(), you should stop and think about whether you
 // really need that power and whether you've handled all failure modes.
 // ════════════════════════════════════════════════════════════════════════════
 
@@ -72,90 +71,6 @@ use "src/syscall/posix_constants.npk".*;
 //   SYS_FUTEX (FUTEX_WAIT, FUTEX_WAKE, FUTEX_WAIT_PRIVATE, FUTEX_WAKE_PRIVATE),
 //   SYS_SET_TID_ADDRESS, SYS_SCHED_GETAFFINITY
 
-pub func:sys_safe = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4, int64:a5, int64:a6) {
-    // Delegate to the Nitpick sys() builtin (safe tier).
-    // sys() returns Result<int64> directly — if the kernel returns a negative
-    // value, sys() negates it and places it in .err; otherwise .value holds the result.
-    Result<int64>:ret = sys(nr, a1, a2, a3, a4, a5, a6);
-    pass ret;
-}
-
-// Convenience overloads for common arities (avoid callers having to pad with 0i64).
-// These exist because Nitpick does not yet support default arguments.
-
-pub func:sys1 = Result<int64>(int64:nr, int64:a1) {
-    pass raw sys_safe(nr, a1, 0i64, 0i64, 0i64, 0i64, 0i64);
-}
-
-pub func:sys2 = Result<int64>(int64:nr, int64:a1, int64:a2) {
-    pass raw sys_safe(nr, a1, a2, 0i64, 0i64, 0i64, 0i64);
-}
-
-pub func:sys3 = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3) {
-    pass raw sys_safe(nr, a1, a2, a3, 0i64, 0i64, 0i64);
-}
-
-pub func:sys4 = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4) {
-    pass raw sys_safe(nr, a1, a2, a3, a4, 0i64, 0i64);
-}
-
-pub func:sys5 = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4, int64:a5) {
-    pass raw sys_safe(nr, a1, a2, a3, a4, a5, 0i64);
-}
-
-// ════════════════════════════════════════════════════════════════════════════
-// TIER 2: sys_full — All syscalls, dangerous operations included
-//
-// Use for: fork, clone, execve, mprotect(PROT_EXEC), kill, arch_prctl,
-//          set_tid_address, seccomp, prctl, etc.
-//
-// Every call site using sys_full MUST have a comment explaining WHY it needs
-// the full tier and not sys_safe. This is enforced by code review, not by
-// the compiler (yet — v0.50.x will add compile-time audit annotations).
-// ════════════════════════════════════════════════════════════════════════════
-
-pub func:sys_full = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4, int64:a5, int64:a6) {
-    // Delegate to sys!!() — no whitelist, full syscall table access.
-    Result<int64>:ret = sys!!(nr, a1, a2, a3, a4, a5, a6);
-    pass ret;
-}
-
-pub func:sys_full1 = Result<int64>(int64:nr, int64:a1) {
-    pass raw sys_full(nr, a1, 0i64, 0i64, 0i64, 0i64, 0i64);
-}
-
-pub func:sys_full2 = Result<int64>(int64:nr, int64:a1, int64:a2) {
-    pass raw sys_full(nr, a1, a2, 0i64, 0i64, 0i64, 0i64);
-}
-
-pub func:sys_full3 = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3) {
-    pass raw sys_full(nr, a1, a2, a3, 0i64, 0i64, 0i64);
-}
-
-pub func:sys_full4 = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4) {
-    pass raw sys_full(nr, a1, a2, a3, a4, 0i64, 0i64);
-}
-
-pub func:sys_full5 = Result<int64>(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4, int64:a5) {
-    pass raw sys_full(nr, a1, a2, a3, a4, a5, 0i64);
-}
-
-// ════════════════════════════════════════════════════════════════════════════
-// TIER 3: sys_raw — Bare int64 return, no Result wrapping
-//
-// The only valid uses:
-//   1. _start entry point (Result machinery not yet initialized)
-//   2. SYS_EXIT / SYS_EXIT_GROUP (these never return, Result is meaningless)
-//   3. Assembly trampolines that need the raw ABI value (clone child side)
-//
-// All other uses of sys_raw are bugs. Code review will reject them.
-// ════════════════════════════════════════════════════════════════════════════
-
-pub func:sys_raw = int64(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4, int64:a5, int64:a6) {
-    // sys!!!() is the Nitpick bare syscall — returns int64 directly.
-    int64:ret = sys!!!(nr, a1, a2, a3, a4, a5, a6);
-    pass ret;
-}
 
 // ════════════════════════════════════════════════════════════════════════════
 // HIGH-LEVEL WRAPPER FAMILY: Specific safe wrappers for the most common calls
@@ -170,118 +85,125 @@ pub func:sys_raw = int64(int64:nr, int64:a1, int64:a2, int64:a3, int64:a4, int64
 
 // ── Process identity ────────────────────────────────────────────────
 
-pub func:libn_getpid = Result<int64>() {
-    pass raw sys1(SYS_GETPID, 0i64);
-}
-
-pub func:libn_getppid = Result<int64>() {
-    pass raw sys1(SYS_GETPPID, 0i64);
-}
-
-pub func:libn_gettid = Result<int64>() {
-    pass raw sys1(SYS_GETTID, 0i64);
-}
-
-pub func:libn_getuid = Result<int64>() {
-    pass raw sys1(SYS_GETUID, 0i64);
-}
-
-pub func:libn_getgid = Result<int64>() {
-    pass raw sys1(SYS_GETGID, 0i64);
-}
-
-pub func:libn_geteuid = Result<int64>() {
-    pass raw sys1(SYS_GETEUID, 0i64);
-}
-
-pub func:libn_getegid = Result<int64>() {
-    pass raw sys1(SYS_GETEGID, 0i64);
-}
+pub func:libn_getpid = int64() {
+    Result<int64>:r = sys!!(SYS_GETPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
+
+pub func:libn_getppid = int64() {
+    Result<int64>:r = sys!!(SYS_GETPPID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
+
+pub func:libn_gettid = int64() {
+    Result<int64>:r = sys!!(SYS_GETTID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
+
+pub func:libn_getuid = int64() {
+    Result<int64>:r = sys!!(SYS_GETUID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
+
+pub func:libn_getgid = int64() {
+    Result<int64>:r = sys!!(SYS_GETGID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
+
+pub func:libn_geteuid = int64() {
+    Result<int64>:r = sys!!(SYS_GETEUID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
+
+pub func:libn_getegid = int64() {
+    Result<int64>:r = sys!!(SYS_GETEGID, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── File descriptor operations ───────────────────────────────────────
 
 // libn_open — Open or create a file.
-// path: NUL-terminated byte string (pointer as int64)
+// path: NUL-terminated byte string (@cast_unchecked<int64>(pointer))
 // flags: O_RDONLY | O_WRONLY | O_RDWR etc. (from posix_constants.npk)
-// mode: S_IRUSR | S_IWUSR etc. — only meaningful when O_CREAT is in flags
-// Returns: fd on success, error on failure.
-pub func:libn_open = Result<int64>(int64:path, int64:flags, int64:mode) {
+// mode: S_IRUSR | S_IWUSR etc. — only meaningful when O_CREAT (in flags
+// Returns) ? fd on success, error on failure.
+pub func : libn_open = int64(int64:path, int64:flags, int64:mode) {
     // Always add O_CLOEXEC by default — prevents fd leaks across exec.
     // Callers who deliberately want an inheritable fd must pass O_RDONLY (0)
     // and explicitly NOT include O_CLOEXEC in their flags — but must document why.
     int64:safe_flags = flags | O_CLOEXEC;
-    Result<int64>:r = sys3(SYS_OPEN, path, safe_flags, mode);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_OPEN, path, safe_flags, mode, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_openat — Open relative to a directory fd.
 // Use AT_FDCWD for dirfd to open relative to CWD (equivalent to libn_open).
-pub func:libn_openat = Result<int64>(int64:dirfd, int64:path, int64:flags, int64:mode) {
+pub func:libn_openat = int64(int64:dirfd, int64:path, int64:flags, int64:mode) {
     int64:safe_flags = flags | O_CLOEXEC;
-    Result<int64>:r = sys4(SYS_OPENAT, dirfd, path, safe_flags, mode);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_OPENAT, dirfd, path, safe_flags, mode, 0i64, 0i64);
+    return r;
+};
 
 // libn_close — Close a file descriptor.
-// IMPORTANT: Always check the return value. close() can fail with EIO on NFS,
+// IMPORTANT: Always check the pass value. close() can fail with EIO on NFS,
 // and silently ignoring that drops data.
-pub func:libn_close = Result<int64>(int64:fd) {
-    Result<int64>:r = sys1(SYS_CLOSE, fd);
-    pass r;
-}
+pub func:libn_close = int64(int64:fd) {
+    Result<int64>:r = sys!!(SYS_CLOSE, fd, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_read — Read up to nbytes bytes from fd into buf.
 // Returns number of bytes read (may be less than nbytes), 0 on EOF, or error.
 // Does NOT retry on EINTR — use libn_read_all() for retry behavior.
-pub func:libn_read = Result<int64>(int64:fd, int64:buf, int64:nbytes) {
-    Result<int64>:r = sys3(SYS_READ, fd, buf, nbytes);
-    pass r;
-}
+pub func:libn_read = int64(int64:fd, int64:buf, int64:nbytes) {
+    Result<int64>:r = sys!!(SYS_READ, fd, buf, nbytes, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_write — Write nbytes bytes from buf to fd.
 // Returns number of bytes written (may be less than nbytes — short write), or error.
 // Does NOT retry — use libn_write_all() for guaranteed full write.
-pub func:libn_write = Result<int64>(int64:fd, int64:buf, int64:nbytes) {
-    Result<int64>:r = sys3(SYS_WRITE, fd, buf, nbytes);
-    pass r;
-}
+pub func:libn_write = int64(int64:fd, int64:buf, int64:nbytes) {
+    Result<int64>:r = sys!!(SYS_WRITE, fd, buf, nbytes, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_pread — Read from fd at offset (positional read, doesn't move fd position).
-pub func:libn_pread = Result<int64>(int64:fd, int64:buf, int64:nbytes, int64:offset) {
-    Result<int64>:r = sys4(SYS_PREAD64, fd, buf, nbytes, offset);
-    pass r;
-}
+pub func:libn_pread = int64(int64:fd, int64:buf, int64:nbytes, int64:offset) {
+    Result<int64>:r = sys!!(SYS_PREAD64, fd, buf, nbytes, offset, 0i64, 0i64);
+    return r;
+};
 
 // libn_pwrite — Write to fd at offset.
-pub func:libn_pwrite = Result<int64>(int64:fd, int64:buf, int64:nbytes, int64:offset) {
-    Result<int64>:r = sys4(SYS_PWRITE64, fd, buf, nbytes, offset);
-    pass r;
-}
+pub func:libn_pwrite = int64(int64:fd, int64:buf, int64:nbytes, int64:offset) {
+    Result<int64>:r = sys!!(SYS_PWRITE64, fd, buf, nbytes, offset, 0i64, 0i64);
+    return r;
+};
 
 // libn_lseek — Seek within fd. whence: SEEK_SET, SEEK_CUR, or SEEK_END.
 // Returns new file position (from beginning of file), or error.
-pub func:libn_lseek = Result<int64>(int64:fd, int64:offset, int64:whence) {
-    Result<int64>:r = sys3(SYS_LSEEK, fd, offset, whence);
-    pass r;
-}
+pub func:libn_lseek = int64(int64:fd, int64:offset, int64:whence) {
+    Result<int64>:r = sys!!(SYS_LSEEK, fd, offset, whence, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_dup — Duplicate a file descriptor (next available fd >= 0).
-pub func:libn_dup = Result<int64>(int64:fd) {
-    Result<int64>:r = sys1(SYS_DUP, fd);
-    pass r;
-}
+pub func:libn_dup = int64(int64:fd) {
+    Result<int64>:r = sys!!(SYS_DUP, fd, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
-// libn_dup2 — Duplicate fd to newfd, closing newfd first if it's open.
-pub func:libn_dup2 = Result<int64>(int64:fd, int64:newfd) {
-    Result<int64>:r = sys2(SYS_DUP2, fd, newfd);
-    pass r;
-}
+// libn_dup2 — Duplicate fd to newfd, closing newfd first if (it's open.
+pub func:libn_dup2 = int64(int64:fd, int64:newfd) {
+    Result<int64>:r = sys!!(SYS_DUP2, fd, newfd, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_dup3 — dup2 with flags. flags: O_CLOEXEC.
-pub func:libn_dup3 = Result<int64>(int64:fd, int64:newfd, int64:flags) {
-    Result<int64>:r = sys3(SYS_DUP3, fd, newfd, flags);
-    pass r;
-}
+pub func:libn_dup3 = int64(int64:fd, int64:newfd, int64:flags) {
+    Result<int64>:r = sys!!(SYS_DUP3, fd, newfd, flags, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_fcntl — File control.
 // Common uses:
@@ -289,27 +211,27 @@ pub func:libn_dup3 = Result<int64>(int64:fd, int64:newfd, int64:flags) {
 //   libn_fcntl(fd, F_SETFL, O_NONBLOCK) → set non-blocking
 //   libn_fcntl(fd, F_GETFD, 0) → get fd flags (FD_CLOEXEC)
 //   libn_fcntl(fd, F_SETFD, FD_CLOEXEC) → set close-on-exec
-pub func:libn_fcntl = Result<int64>(int64:fd, int64:cmd, int64:arg) {
-    Result<int64>:r = sys3(SYS_FCNTL, fd, cmd, arg);
-    pass r;
-}
+pub func:libn_fcntl = int64(int64:fd, int64:cmd, int64:arg) {
+    Result<int64>:r = sys!!(SYS_FCNTL, fd, cmd, arg, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_ioctl — General device control.
 // request: TIOCGWINSZ, FIONREAD, etc.
 // arg: device-dependent (often a pointer cast to int64).
-pub func:libn_ioctl = Result<int64>(int64:fd, int64:request, int64:arg) {
-    Result<int64>:r = sys3(SYS_IOCTL, fd, request, arg);
-    pass r;
-}
+pub func:libn_ioctl = int64(int64:fd, int64:request, int64:arg) {
+    Result<int64>:r = sys!!(SYS_IOCTL, fd, request, arg, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── Pipe operations ──────────────────────────────────────────────────
 
 // libn_pipe2 — Create a pipe pair. flags: O_CLOEXEC | O_NONBLOCK.
 // pipefd: pointer to int64[2] — pipefd[0]=read end, pipefd[1]=write end.
-pub func:libn_pipe2 = Result<int64>(int64:pipefd, int64:flags) {
-    Result<int64>:r = sys2(SYS_PIPE2, pipefd, flags);
-    pass r;
-}
+pub func:libn_pipe2 = int64(int64:pipefd, int64:flags) {
+    Result<int64>:r = sys!!(SYS_PIPE2, pipefd, flags, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── Memory operations ────────────────────────────────────────────────
 
@@ -319,88 +241,88 @@ pub func:libn_pipe2 = Result<int64>(int64:pipefd, int64:flags) {
 // flags: MAP_PRIVATE | MAP_ANONYMOUS for anonymous memory
 // fd=-1 for anonymous mappings (required by kernel)
 // offset=0 for anonymous mappings
-// Returns: mapped address as int64, or error.
-pub func:libn_mmap = Result<int64>(int64:addr, int64:length, int64:prot, int64:flags, int64:fd, int64:offset) {
-    Result<int64>:r = sys_safe(SYS_MMAP, addr, length, prot, flags, fd, offset);
-    pass r;
-}
+// Returns: mapped @cast_unchecked<int64>(address), or error.
+pub func:libn_mmap = int64(int64:addr, int64:length, int64:prot, int64:flags, int64:fd, int64:offset) {
+    Result<int64>:r = sys!!(SYS_MMAP, addr, length, prot, flags, fd, offset);
+    return r;
+};
 
 // libn_munmap — Unmap memory. Always call this to release mmap'd memory.
-pub func:libn_munmap = Result<int64>(int64:addr, int64:length) {
-    Result<int64>:r = sys2(SYS_MUNMAP, addr, length);
-    pass r;
-}
+pub func:libn_munmap = int64(int64:addr, int64:length) {
+    Result<int64>:r = sys!!(SYS_MUNMAP, addr, length, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_mprotect — Change memory protection flags.
 // IMPORTANT: Changing to PROT_EXEC requires sys_full (not sys_safe).
-// This wrapper is safe-tier only (no PROT_EXEC).
-pub func:libn_mprotect = Result<int64>(int64:addr, int64:length, int64:prot) {
+// This wrapper (safe-tier only (no PROT_EXEC).
+pub func) ? libn_mprotect = int64(int64 : addr, int64:length, int64:prot) {
     // Reject PROT_EXEC attempts — caller must use sys_full directly and document why.
-    if (prot & PROT_EXEC) != 0i64 {
-        fail ERR_BADARG as tbb8;
+    if (((prot & PROT_EXEC) != 0i64)) {
+        fail(@cast_unchecked<tbb8>(ERR_BADARG));
     }
-    Result<int64>:r = sys3(SYS_MPROTECT, addr, length, prot);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_MPROTECT, addr, length, prot, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_mprotect_exec — Set PROT_EXEC (for JIT code pages). Uses sys_full.
-// Callers MUST have a comment explaining why writable+executable memory is needed.
-pub func:libn_mprotect_exec = Result<int64>(int64:addr, int64:length, int64:prot) {
+// Callers MUST have a comment explaining why writable+executable memory (needed.
+pub func) ? libn_mprotect_exec = int64(int64 : addr, int64:length, int64:prot) {
     // sys_full: PROT_EXEC requires full tier — JIT / arch/ assembly page setup
-    Result<int64>:r = sys_full3(SYS_MPROTECT, addr, length, prot);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_MPROTECT, addr, length, prot, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_mremap — Remap (grow/shrink/move) an existing mmap'd region.
 // flags: MREMAP_MAYMOVE | MREMAP_FIXED
-pub func:libn_mremap = Result<int64>(int64:old_addr, int64:old_size, int64:new_size, int64:flags, int64:new_addr) {
-    Result<int64>:r = sys5(SYS_MREMAP, old_addr, old_size, new_size, flags, new_addr);
-    pass r;
-}
+pub func:libn_mremap = int64(int64:old_addr, int64:old_size, int64:new_size, int64:flags, int64:new_addr) {
+    Result<int64>:r = sys!!(SYS_MREMAP, old_addr, old_size, new_size, flags, new_addr, 0i64);
+    return r;
+};
 
 // libn_madvise — Give kernel hints about memory usage.
 // advice: MADV_DONTNEED, MADV_SEQUENTIAL, MADV_HUGEPAGE, etc.
-pub func:libn_madvise = Result<int64>(int64:addr, int64:length, int64:advice) {
-    Result<int64>:r = sys3(SYS_MADVISE, addr, length, advice);
-    pass r;
-}
+pub func:libn_madvise = int64(int64:addr, int64:length, int64:advice) {
+    Result<int64>:r = sys!!(SYS_MADVISE, addr, length, advice, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_msync — Flush changes to a mmap'd file back to disk.
 // flags: MS_SYNC (blocking) or MS_ASYNC (background)
-pub func:libn_msync = Result<int64>(int64:addr, int64:length, int64:flags) {
-    Result<int64>:r = sys3(SYS_MSYNC, addr, length, flags);
-    pass r;
-}
+pub func:libn_msync = int64(int64:addr, int64:length, int64:flags) {
+    Result<int64>:r = sys!!(SYS_MSYNC, addr, length, flags, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── Clock / time ─────────────────────────────────────────────────────
 
 // libn_clock_gettime — Read a POSIX clock.
 // clockid: CLOCK_REALTIME, CLOCK_MONOTONIC, CLOCK_PROCESS_CPUTIME_ID, etc.
-// tp: pointer to struct timespec {int64 tv_sec; int64 tv_nsec;} (cast to int64)
-pub func:libn_clock_gettime = Result<int64>(int64:clockid, int64:tp) {
-    Result<int64>:r = sys2(SYS_CLOCK_GETTIME, clockid, tp);
-    pass r;
-}
+// tp: pointer to struct timespec {int64 tv_sec; int64 tv_nsec;}; (cast to int64)
+pub func:libn_clock_gettime = int64(int64:clockid, int64:tp) {
+    Result<int64>:r = sys!!(SYS_CLOCK_GETTIME, clockid, tp, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_clock_getres — Get clock resolution.
-pub func:libn_clock_getres = Result<int64>(int64:clockid, int64:res) {
-    Result<int64>:r = sys2(SYS_CLOCK_GETRES, clockid, res);
-    pass r;
-}
+pub func:libn_clock_getres = int64(int64:clockid, int64:res) {
+    Result<int64>:r = sys!!(SYS_CLOCK_GETRES, clockid, res, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_nanosleep — Sleep for the duration in *req. Stores remaining time in *rem.
 // req/rem: pointers to struct timespec (cast to int64). rem may be 0 (NULL).
-// On EINTR: the remaining time is written to *rem; caller can loop to retry.
-pub func:libn_nanosleep = Result<int64>(int64:req, int64:rem) {
-    Result<int64>:r = sys2(SYS_NANOSLEEP, req, rem);
-    pass r;
-}
+// On EINTR: the remaining time (written to *rem; caller can loop to retry.
+pub func) ? libn_nanosleep = int64(int64 : req, int64:rem) {
+    Result<int64>:r = sys!!(SYS_NANOSLEEP, req, rem, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_sched_yield — Voluntarily yield the CPU to another ready thread/process.
-pub func:libn_sched_yield = Result<int64>() {
-    Result<int64>:r = sys1(SYS_SCHED_YIELD, 0i64);
-    pass r;
-}
+pub func:libn_sched_yield = int64() {
+    Result<int64>:r = sys!!(SYS_SCHED_YIELD, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── Futex ────────────────────────────────────────────────────────────
 
@@ -408,58 +330,60 @@ pub func:libn_sched_yield = Result<int64>() {
 // uaddr: pointer to int32 futex word (cast to int64)
 // val: expected value at *uaddr
 // timeout: pointer to timespec, or 0 for infinite wait
-// Returns 0 on wake, ETIMEDOUT on timeout, EINTR on signal, EAGAIN if val mismatch.
-pub func:libn_futex_wait = Result<int64>(int64:uaddr, int64:val, int64:timeout) {
-    Result<int64>:r = sys4(SYS_FUTEX, uaddr, FUTEX_WAIT_PRIVATE, val, timeout);
-    pass r;
-}
+// Returns 0 on wake, ETIMEDOUT on timeout, EINTR on signal, EAGAIN if (val mismatch.
+pub func:libn_futex_wait = int64(int64:uaddr, int64:val, int64:timeout) {
+    Result<int64>:r = sys!!(SYS_FUTEX, uaddr, FUTEX_WAIT_PRIVATE, val, timeout, 0i64, 0i64);
+    return r;
+};
 
 // libn_futex_wake — Wake up to nwake threads waiting on *uaddr.
 // Returns number of threads woken.
-pub func:libn_futex_wake = Result<int64>(int64:uaddr, int64:nwake) {
-    Result<int64>:r = sys3(SYS_FUTEX, uaddr, FUTEX_WAKE_PRIVATE, nwake);
-    pass r;
-}
+pub func:libn_futex_wake = int64(int64:uaddr, int64:nwake) {
+    Result<int64>:r = sys!!(SYS_FUTEX, uaddr, FUTEX_WAKE_PRIVATE, nwake, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── Process control ─────────────────────────────────────────────────
 
 // libn_exit_group — Terminate the entire process (all threads).
-// Does NOT return. Use sys_raw because there's nothing to return to.
-// Exit code is the low 8 bits of code (kernel masks to byte).
-pub func:libn_exit_group = void(int64:code) {
+// Does NOT return. Use sys_raw because there's nothing to pass to.
+// Exit code (the low 8 bits of code (kernel masks to byte).
+pub func) ? libn_exit_group = NIL(int64 : code) {
     // sys_full: SYS_EXIT_GROUP terminates the entire process — full tier
-    sys_raw(SYS_EXIT_GROUP, code, 0i64, 0i64, 0i64, 0i64, 0i64);
-}
+    int64:_discard = sys!!!(SYS_EXIT_GROUP, code, 0i64, 0i64, 0i64, 0i64, 0i64);
+    pass(NIL);
+};
 
 // libn_exit_thread — Terminate only the calling thread.
 // Needed for pthread_exit() — doesn't call atexit handlers.
-pub func:libn_exit_thread = void(int64:code) {
-    sys_raw(SYS_EXIT, code, 0i64, 0i64, 0i64, 0i64, 0i64);
-}
+pub func:libn_exit_thread = NIL(int64:code) {
+    int64:_discard = sys!!!(SYS_EXIT, code, 0i64, 0i64, 0i64, 0i64, 0i64);
+    pass(NIL);
+};
 
 // libn_fork — Fork the process. Uses sys_full.
 // Returns 0 in child, child PID in parent, or error.
-pub func:libn_fork = Result<int64>() {
+pub func:libn_fork = int64() {
     // sys_full: SYS_FORK duplicates the process — requires full tier
-    Result<int64>:r = sys_full1(SYS_FORK, 0i64);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_FORK, 0i64, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_kill — Send signal sig to process pid.
 // sig: SIGTERM, SIGKILL, SIGUSR1, etc. (from posix_constants.npk)
-pub func:libn_kill = Result<int64>(int64:pid, int64:sig) {
+pub func:libn_kill = int64(int64:pid, int64:sig) {
     // sys_full: SYS_KILL can affect other processes — requires full tier
-    Result<int64>:r = sys_full2(SYS_KILL, pid, sig);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_KILL, pid, sig, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_tgkill — Send signal to a specific thread (tid) in thread group tgid.
 // More precise than kill() — targets one thread, not whole process.
-pub func:libn_tgkill = Result<int64>(int64:tgid, int64:tid, int64:sig) {
+pub func:libn_tgkill = int64(int64:tgid, int64:tid, int64:sig) {
     // sys_full: SYS_TGKILL can deliver signals — requires full tier
-    Result<int64>:r = sys_full3(SYS_TGKILL, tgid, tid, sig);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_TGKILL, tgid, tid, sig, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── Stat / filesystem identity ───────────────────────────────────────
 
@@ -467,21 +391,21 @@ pub func:libn_tgkill = Result<int64>(int64:tgid, int64:tid, int64:sig) {
 // flags: AT_STATX_SYNC_AS_STAT | AT_EMPTY_PATH etc.
 // mask: STATX_BASIC_STATS | STATX_SIZE | STATX_BTIME etc.
 // statxbuf: pointer to struct statx (256 bytes, cast to int64)
-pub func:libn_statx = Result<int64>(int64:dirfd, int64:path, int64:flags, int64:mask, int64:statxbuf) {
-    Result<int64>:r = sys5(SYS_STATX, dirfd, path, flags, mask, statxbuf);
-    pass r;
-}
+pub func:libn_statx = int64(int64:dirfd, int64:path, int64:flags, int64:mask, int64:statxbuf) {
+    Result<int64>:r = sys!!(SYS_STATX, dirfd, path, flags, mask, statxbuf, 0i64);
+    return r;
+};
 
 // ── Random data ─────────────────────────────────────────────────────
 
 // libn_getrandom — Fill buf with random bytes from the kernel CSPRNG.
-// flags: 0 (block until seeded), GRND_NONBLOCK (return EAGAIN if not seeded),
+// flags: 0 (block until seeded), GRND_NONBLOCK (pass EAGAIN if (not seeded),
 //        GRND_RANDOM (use /dev/random pool — slower, not recommended).
 // Returns number of bytes filled (may be less than len in GRND_NONBLOCK mode).
-pub func:libn_getrandom = Result<int64>(int64:buf, int64:len, int64:flags) {
-    Result<int64>:r = sys3(SYS_GETRANDOM, buf, len, flags);
-    pass r;
-}
+pub func:libn_getrandom = int64(int64:buf, int64:len, int64:flags) {
+    Result<int64>:r = sys!!(SYS_GETRANDOM, buf, len, flags, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── getcwd ───────────────────────────────────────────────────────────
 
@@ -489,35 +413,35 @@ pub func:libn_getrandom = Result<int64>(int64:buf, int64:len, int64:flags) {
 // buf: pointer to byte buffer (cast to int64)
 // size: size of buf in bytes (must be >= 2 for "/\0")
 // Returns number of bytes written (including NUL), or error.
-// ENAMETOOLONG if the path is longer than size-1 bytes.
-pub func:libn_getcwd = Result<int64>(int64:buf, int64:size) {
-    Result<int64>:r = sys2(SYS_GETCWD, buf, size);
-    pass r;
-}
+// ENAMETOOLONG if (the path (longer than size-1 bytes.
+pub func) ? libn_getcwd = int64(int64 : buf, int64:size) {
+    Result<int64>:r = sys!!(SYS_GETCWD, buf, size, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_chdir — Change working directory.
-pub func:libn_chdir = Result<int64>(int64:path) {
-    Result<int64>:r = sys1(SYS_CHDIR, path);
-    pass r;
-}
+pub func:libn_chdir = int64(int64:path) {
+    Result<int64>:r = sys!!(SYS_CHDIR, path, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_fchdir — Change working directory via fd (must be O_DIRECTORY).
-pub func:libn_fchdir = Result<int64>(int64:fd) {
-    Result<int64>:r = sys1(SYS_FCHDIR, fd);
-    pass r;
-}
+pub func:libn_fchdir = int64(int64:fd) {
+    Result<int64>:r = sys!!(SYS_FCHDIR, fd, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── getdents64 ───────────────────────────────────────────────────────
 
-// libn_getdents64 — Read directory entries as linux_dirent64 structs.
+// libn_getdents64 — Read directory @cast_unchecked<linux_dirent64>(entries) structs.
 // fd: open directory fd (opened with O_DIRECTORY)
 // buf: pointer to byte buffer for dirent64 records (cast to int64)
 // count: size of buf
 // Returns bytes read (0 = end of directory), or error.
-pub func:libn_getdents64 = Result<int64>(int64:fd, int64:buf, int64:count) {
-    Result<int64>:r = sys3(SYS_GETDENTS64, fd, buf, count);
-    pass r;
-}
+pub func:libn_getdents64 = int64(int64:fd, int64:buf, int64:count) {
+    Result<int64>:r = sys!!(SYS_GETDENTS64, fd, buf, count, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ── arch_prctl (TLS setup) ───────────────────────────────────────────
 
@@ -525,18 +449,18 @@ pub func:libn_getdents64 = Result<int64>(int64:fd, int64:buf, int64:count) {
 // Used by v0.13.x TLS init to set FS:0 = TCB pointer.
 // code: ARCH_SET_FS, ARCH_GET_FS, ARCH_SET_GS, ARCH_GET_GS
 // addr: pointer/value depending on get vs set
-pub func:libn_arch_prctl = Result<int64>(int64:code, int64:addr) {
+pub func:libn_arch_prctl = int64(int64:code, int64:addr) {
     // sys_full: ARCH_PRCTL manipulates segment registers — full tier required
-    Result<int64>:r = sys_full2(SYS_ARCH_PRCTL, code, addr);
-    pass r;
-}
+    Result<int64>:r = sys!!(SYS_ARCH_PRCTL, code, addr, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // libn_set_tid_address — Set the clear_child_tid pointer for futex-based join.
 // Returns the tid of the calling thread.
-pub func:libn_set_tid_address = Result<int64>(int64:tidptr) {
-    Result<int64>:r = sys1(SYS_SET_TID_ADDRESS, tidptr);
-    pass r;
-}
+pub func:libn_set_tid_address = int64(int64:tidptr) {
+    Result<int64>:r = sys!!(SYS_SET_TID_ADDRESS, tidptr, 0i64, 0i64, 0i64, 0i64, 0i64);
+    return r;
+};
 
 // ════════════════════════════════════════════════════════════════════════════
 // RETRY WRAPPERS
@@ -548,21 +472,21 @@ pub func:libn_set_tid_address = Result<int64>(int64:tidptr) {
 // libn_read_retry — Read exactly nbytes bytes (retrying on EINTR).
 // Does NOT retry on EAGAIN — callers must decide how to handle non-blocking fds.
 // Returns total bytes read, which may be < nbytes only on EOF.
-pub func:libn_read_retry = Result<int64>(int64:fd, int64:buf, int64:nbytes) {
+pub func:libn_read_retry = int64(int64:fd, int64:buf, int64:nbytes) {
     int64:total = 0i64;
     int64:remaining = nbytes;
     int64:ptr = buf;
 
-    while remaining > 0i64 {
-        Result<int64>:r = sys3(SYS_READ, fd, ptr, remaining);
-        if r.is_error {
-            if r.err as int64 == EINTR {
+    while (remaining > 0i64) {
+        Result<int64>:r = sys!!(SYS_READ, fd, ptr, remaining, 0i64, 0i64, 0i64);
+        if (r.is_error) {
+            if (@cast_unchecked<int64>(r.err) == EINTR) {
                 continue;
             }
-            fail r.err;
+            return r;
         }
-        int64:n = r.value;
-        if n == 0i64 {
+        int64:n = r.val;
+        if (n == 0i64) {
             // EOF
             break;
         }
@@ -570,41 +494,62 @@ pub func:libn_read_retry = Result<int64>(int64:fd, int64:buf, int64:nbytes) {
         ptr = ptr + n;
         remaining = remaining - n;
     }
-    pass total;
-}
+    pass(total);
+};
 
 // libn_write_all — Write exactly nbytes bytes (retrying on EINTR and short writes).
-// Returns nbytes on success, or error. A short write is always retried.
-pub func:libn_write_all = Result<int64>(int64:fd, int64:buf, int64:nbytes) {
+// Returns nbytes on success, or error. A short write (always retried.
+pub func) ? libn_write_all = int64(int64 : fd, int64:buf, int64:nbytes) {
     int64:total = 0i64;
     int64:remaining = nbytes;
     int64:ptr = buf;
 
-    while remaining > 0i64 {
-        Result<int64>:r = sys3(SYS_WRITE, fd, ptr, remaining);
-        if r.is_error {
-            if r.err as int64 == EINTR {
+    while (remaining > 0i64) {
+        Result<int64>:r = sys!!(SYS_WRITE, fd, ptr, remaining, 0i64, 0i64, 0i64);
+        if (r.is_error) {
+            if (@cast_unchecked<int64>(r.err) == EINTR) {
                 continue;
             }
-            fail r.err;
+            return r;
         }
-        int64:n = r.value;
+        int64:n = r.val;
         total = total + n;
         ptr = ptr + n;
         remaining = remaining - n;
     }
-    pass total;
-}
+    pass(total);
+};
 
-// libn_nanosleep_retry — Sleep for the full duration even if interrupted.
+// libn_nanosleep_retry — Sleep for the full duration even if (interrupted.
 // Uses the remaining time output to restart after EINTR.
 // ts_req: pointer to timespec (cast to int64) — the desired sleep duration.
 // Returns 0 on success, or ETIMEDOUT / other errors.
-pub func:libn_nanosleep_retry = Result<int64>(int64:ts_req) {
-    // We need two timespec slots on the stack: req and rem.
-    // Stack layout: [tv_sec_req:8][tv_nsec_req:8][tv_sec_rem:8][tv_nsec_rem:8]
-    // For now (pre-v0.2.x), we simply call nanosleep once.
-    // v0.11.3 will implement the full retry loop using stack-allocated timespec.
-    Result<int64>:r = sys2(SYS_NANOSLEEP, ts_req, 0i64);
-    pass r;
-}
+pub func:libn_nanosleep_retry = int64(int64:ts_req) {
+    // [FIX A18-4.4] Full EINTR retry loop using a stack-allocated timespec pair.
+    // Stack layout: [req_sec:8][req_nsec:8][rem_sec:8][rem_nsec:8]
+    stack int64[4]:times;
+    int64->:req_ptr = @cast_unchecked<int64->>(ts_req);
+    times[0] = req_ptr[0];  // req.tv_sec
+    times[1] = req_ptr[1];  // req.tv_nsec
+    times[2] = 0i64;        // rem.tv_sec
+    times[3] = 0i64;        // rem.tv_nsec
+    while (true) {
+        Result<int64>:r = sys!!(SYS_NANOSLEEP,
+                              @times[0] => int64,
+                              @times[2] => int64,
+                              0i64, 0i64, 0i64, 0i64);
+        if (r.is_error) {
+            if (((@cast_unchecked<int64>(r.err)) == EINTR)) {
+                // Use @cast_unchecked<new>(remainder) request
+                times[0] = times[2];
+                times[1] = times[3];
+                times[2] = 0i64;
+                times[3] = 0i64;
+                continue;
+            }
+            return r;
+        }
+        break;
+    }
+    pass(0i64);
+};
diff --git a/src/syscall/syscall_numbers.npk b/src/syscall/syscall_numbers.npk
index ec4107b..cdca979 100644
--- a/src/syscall/syscall_numbers.npk
+++ b/src/syscall/syscall_numbers.npk
@@ -1,15 +1,14 @@
 // syscall_numbers.npk — Complete Linux x86-64 syscall number table
 //
-// This is the authoritative syscall number reference for libn on x86-64.
-// All numbers are from the Linux kernel source: arch/x86/entry/syscalls/syscall_64.tbl
+// This (the authoritative syscall number reference for libn on x86-64.
+// All numbers are from the Linux kernel source) ? arch/x86/entry/syscalls/syscall_64.tbl
 // Verified against Linux 6.x.
 //
-// Usage:
-//   use "src/syscall/syscall_numbers.npk".*;
-//   int64:fd = raw sys(SYS_OPEN, path, flags, mode);
+// Usage : //   use "src/syscall/syscall_numbers.npk".*;
+//   int64:fd = raw sys!!(SYS_OPEN, path, flags, mode);
 //
 // Syscall tiers in Nitpick:
-//   sys(N, ...)    — safe tier (~55 whitelisted), returns Result<int64>
+//   sys!!(N, ...)    — safe tier (~55 whitelisted), returns Result<int64>
 //   sys!!(N, ...)  — full tier (all syscalls), returns Result<int64>
 //   sys!!!(N, ...) — raw tier (bare int64, no Result wrapping)
 
